# 알려진 한계

**이 도구가 지금 못 하는 것과, 그것이 결과에 어떤 영향을 주는지** 적는
문서입니다. 보고서의 "분석 범위 한계"는 자동 생성되지만, 그 자동 생성이
잡아내지 못하는 층위의 한계가 여기 모입니다.

한계를 아는 것과 모르는 것의 차이가 이 프로젝트의 핵심입니다. 못 본 것을
못 봤다고 말할 수 없으면, "봤는데 없었다"와 구분되지 않습니다.

---

## 1. 증거 입력

### 디스크 이미지를 직접 열 수 있다 (**부분 해결**)

`evidence.py`의 `VolumeSource`가 이제 `dissect.target` 기반으로
구현돼 있습니다. `--evidence`에 추출된 폴더 대신 raw 이미지 **파일**을
주면 `open_source()`가 자동으로 이쪽으로 갑니다.

**실측(`evidence/test_image.001`, 60GB raw NTFS 볼륨)** — 파티션 테이블
없이 NTFS 볼륨 하나만 담은 형태로, 카탈로그의 7개 아티팩트
전부($MFT·$UsnJrnl·evtx 2종·registry 3종, Amcache 포함)를 04단계
파이프라인 전체로 관통시켜 575,992건을 확인했습니다. `dissect.util
.stream.RunlistStream`이 `seekable() == True`이고 재읽기 값이 원본과
일치함도 실측으로 확인했습니다(`mft.py`의 두 번 순회 패턴과 호환).

**아직 검증 못 한 것**:
- **파티션 테이블이 있는 전체 디스크 이미지**(MBR/GPT, EFI·복구
  파티션이 섞인 형태)도 이제 엽니다(**2026-08-24 해결**). `--volume N`으로
  **사람이 고릅니다.** 도구는 크기나 순서로 추측하지 않습니다 — 복구
  파티션과 시스템 볼륨이 둘 다 NTFS라 자동 판별이 그럴듯하게 틀릴 수
  있고, 잘못 고르면 "아티팩트가 없다"가 아니라 **다른 볼륨의 결과가
  조용히 나옵니다.** 지정하지 않으면 후보를 크기·이름과 함께 보여 주고
  멈춥니다.

  실측(`evidence/0824test.001`, 60GB VMware 물리 디스크): NTFS 2개
  (0.4GiB 복구 + 59.4GiB 시스템)가 나왔고 `--volume 1`로 카탈로그
  8종 전부를 찾았습니다. **일반적인 Win10/11 물리 디스크는 복구
  파티션도 NTFS라 거의 항상 이 분기를 탑니다** — 논리 드라이브만
  뜬 이미지가 예외입니다.
- **폴더 아티팩트(프리패치)도 볼륨에서 읽습니다**(**2026-08-24 해결**).
  그전까지 `VolumeSource.open_all`·`locate_all`이 `NotImplementedError`
  였습니다 — 이미지에서 돌리면 `$MFT`·하이브·evtx는 나오는데 **프리패치만
  통째로 빠졌습니다.** `FileSource._collect`와 같은 규약(이름순 고정,
  0바이트 제외)을 씁니다. 실측 이미지에서 `.pf` 137건을 잡았습니다.
- **E01은 컨테이너까지 확인했습니다** (**부분 해결**, 2026-09-01 실측).
  `evidence/week10.E01`(21,474,836,480바이트)을 `Target.open`에 주면
  `EwfContainer`로 열리고 파티션 3개가 나옵니다.

  ```
  volumes      0.00GiB / 0.50GiB(mkfs.fat = EFI) / 19.50GiB(ext4, magic 0xEF53)
  filesystems  0  →  open_source()가 "NTFS 파일시스템을 찾지 못했습니다"로 멈춥니다
  ```

  **그 이미지가 Windows가 아니라서 멈춘 것입니다** — EFI + ext4, 리눅스
  디스크입니다. 멈춘 것 자체는 결함이 아니라 정상 동작입니다. 이 이미지는
  Windows 아티팩트 검증에 쓸 수 없습니다.

  **남은 것은 Windows NTFS가 담긴 E01로 04단계를 관통시키는 것입니다.**
  EWF 디코딩과 파티션 해석까지는 봤지만, 그 위에서 NTFS를 열어 아티팩트를
  뽑는 경로는 raw 이미지로만 확인돼 있습니다. `RunlistStream` 재읽기처럼
  스트림의 성질에 기대는 자리가 있어(위 실측), EWF 위에서 다시 봐야 합니다.
- **vmdk는 열어 본 적이 없습니다.** `dissect.hypervisor` 3.21이 `.venv`에
  있고 `Target.open`이 같은 경로를 태운다고 문서화돼 있으나 확인한 적은
  없습니다. 스냅샷 델타(`-000001.vmdk`)와 분할 vmdk가 걸릴 수 있는
  자리입니다. **이것이 되면 버전별 증거 확보가 "VM을 끄고 vmdk를 가리킨다"
  한 줄로 줄어듭니다** — 왜 그 값이 있는지는 `work.md` 1-2절입니다.
- `$UsnJrnl`은 추출 폴더용 이름 후보(`FILE_LAYOUT`)와 별개로
  `evidence.py`의 `VOLUME_PATH_OVERRIDES`에 원본 ADS 경로
  (`$Extend/$UsnJrnl:$J`, 콜론 포함)를 따로 둡니다 — 추출 도구가
  콜론을 못 쓰는 것과 원본 볼륨이 콜론을 그대로 갖는 것의 차이입니다.

E01/dd 이미지를 추출된 파일 단위로 다루려면 여전히 FTK Imager 등으로
**먼저 뽑아 둘 수도 있습니다** — `FileSource`도 그대로 유지됩니다.
`evidence/[root]/` 형태가 그 결과물입니다.

**영향** — 단일 NTFS 볼륨 raw 이미지와 파티션 테이블이 있는 전체 디스크는
추출 단계 없이 바로 돌릴 수 있습니다. E01은 컨테이너까지만 확인됐고 그 위의
NTFS 관통이 남아 있으므로, 급한 사건에서는 사전 추출이 여전히 안전합니다.
vmdk는 미확인입니다.

#### `run_pipeline.sh` 도 볼륨을 고를 수 있습니다 (**해결됨**, 2026-08-26)

그전까지 `--volume` 은 `parse.py` 를 직접 부를 때만 줄 수 있었습니다.
관통 스크립트에는 통로가 없어서, **일반적인 Win10/11 물리 디스크
이미지에서는 04단계만 손으로 따로 돌려야 했습니다** — 거의 항상
해당한다는 점을 생각하면 관통 스크립트가 관통하지 못한 셈입니다.

```bash
VOLUME=1 PYTHON=.venv/Scripts/python.exe ./run_pipeline.sh K-ALERT evidence/0824test.001
```

위치 인자가 아니라 환경 변수인 것은 세 번째 자리를 `replay_dir` 이
이미 쓰고 있어서입니다. 위치로 받으면 replay 없이는 볼륨을 못 줍니다.

**주지 않았을 때 후보를 보여 주고 멈추는 04단계의 동작은 그대로입니다.**
스크립트가 대신 추측하지 않습니다.

### 한 실행은 한 볼륨

`$MFT`에는 드라이브 문자가 없어 증거 경로에서 유추합니다. 볼륨이
여럿이면 케이스를 나눠 각각 돌립니다(`C-001-C`, `C-001-D`).

**영향** — 볼륨을 넘나드는 흔적은 사람이 이어 붙여야 합니다.

---

## 2. 파서

### 지원 아티팩트

| 아티팩트 | 상태 |
|---|---|
| `$MFT` | 구현 — analyzeMFT(MIT) 기반 어댑터 |
| `$UsnJrnl:$J` | 구현 — 전부 자체 구현 |
| `evtx:Security` / `evtx:System` | 구현 — python-evtx 기반 어댑터 |
| `registry:SYSTEM` / `registry:SOFTWARE` | 구현 — python-registry 기반 어댑터 |
| `registry:Amcache` | 구현 — python-registry 기반 어댑터 (SYSTEM/SOFTWARE와 같은 클래스 재사용). **Windows 8 이상에만 존재** |
| `recentfilecache` | 구현 — 전부 자체 구현. **Windows 7 전용.** 실물 1건 대조 완료, 아래 단서 참조 |
| `prefetch` | 구현 — 전부 자체 구현 (압축 해제 포함). 아래 단서 참조 |
| `srum:NetworkUsage` / `srum:AppResourceUsage` / `srum:NetworkConnectivity` | 구현 — dissect.esedb 기반 어댑터. **Windows 8 이상에만 존재.** 아래 단서 참조 |
| `$LogFile` | 미지원 (카탈로그에서 제외 처리) |

**카탈로그의 아티팩트는 전부 파서가 있습니다.** 어긋나면
`tests/test_pipeline_e2e.py`가 양방향으로 잡습니다 — 카탈로그가 지원한다는데
파서가 없거나, 파서가 있는데 카탈로그에 없거나.

### Windows 버전 — 어디까지 갈라 보는가 (2026-08-25)

04단계가 SOFTWARE 하이브의 `Microsoft\Windows NT\CurrentVersion`에서
빌드 번호를 읽어 매니페스트의 `windows`에 싣습니다
(`src/stage04_parse/osinfo.py`). 03단계는 증거를 열지 않아 버전을 알 수
없으므로 판정은 증거를 처음 여는 04단계의 몫입니다.

**빌드 번호만 믿습니다.** `ProductName`은 Win11 초기 빌드가 "Windows 10
Pro"라고 적혀 있고, `CurrentVersion`은 Win8.1 이후 전부 "6.3"으로 굳어
변별력이 없습니다.

| 판정 | 빌드 | 확인 상태 |
|---|---|---|
| `win7` | 7600–7601 | **실측** — `evidence/windows7_testimage.001`, 빌드 7601 / Ultimate / Client |
| `win8` | 9200 | **미확인** |
| `win81` | 9600 | **미확인** |
| `win10` | 10240–21999 | **실측** — `evidence/0824test.001`, 빌드 15063 / 1703 / Client |
| `win11` | 22000 이상 | **미확인** |

Win7 실물에서 `CurrentVersion`이 `6.1`이고 `UBR`·`ReleaseId`·`DisplayVersion`이
**없습니다.** 빌드 번호 하나만 필수로 두고 나머지를 선택으로 둔 판단이
여기서 확인됐습니다 — 이 값들을 요구했다면 Win7에서 판정이 통째로 실패합니다.

`family`는 제품명이 아니라 **온디스크 구조 세대**입니다. 서버판은
클라이언트와 빌드를 공유하므로(2008 R2 = 7601, 2012 = 9200, 2016 = 14393)
`win10`이 클라이언트라는 뜻이 아닙니다. 제품 구분은
`windows.installation_type`(Client/Server)이 따로 싣습니다.

#### 버전에 따라 존재하지 않는 아티팩트

`Amcache.hve`(Win8 이상)와 `RecentFileCache.bcf`(Win7)는 **배타적인
짝**입니다. 매핑은 둘 다 요청하고(03단계는 버전을 모릅니다) 04단계가
해당 없는 쪽을 `version_not_applicable`로 뺍니다.

가르기 전에는 Win7 이미지의 Amcache가 `artifact_not_found` — 즉 **"수집
누락"** 으로 기록됐습니다. 보고서를 읽은 분석가는 있지도 않은 파일을
다시 뽑으러 갑니다. 07단계가 이제 두 사유를 다른 문장으로 싣습니다.

**판정에 실패하면 아무것도 거르지 않습니다.** 잘못 거르면 있는 증거를 안
읽지만, 안 거르면 최악이 지금과 같기 때문입니다. 사유는 stderr와
매니페스트 `windows.reason`에 남되 `errors.jsonl`에는 남기지 않습니다 —
저 로그는 아티팩트 단위 실패의 집계이고 버전 판정은 아티팩트가 아닙니다.

#### 아직 버전을 가르지 않는 곳

**이벤트 ID에 버전 조건이 없습니다.** 매핑의 `event_ids`는 전부 Vista 이후
번호(4xxx·7045 등)이고, 일부는 하위 버전에 존재하지 않습니다(`T1053.005`의
4698 등). 구버전 이미지에서 0건이 나와도 "그 버전엔 그 이벤트가 없다"와
"탐지 실패"가 구별되지 않습니다. 아티팩트 단위 가용성만 있고 **이벤트
단위 가용성은 없습니다.**

**XP/2003은 대상이 아닙니다.** `.evt`(evtx 이전 포맷)를 읽지 못하고,
채널 경로가 `Windows/System32/winevt/Logs`로 고정돼 있습니다. 프리패치
버전 17의 자리는 표에 있지만 그것만으로 XP를 지원한다고 할 수 없습니다.

**레지스트리 값 안의 버전별 구조를 해석하지 않습니다.** `AppCompatCache`
(Shimcache — XP/7/8/8.1/10이 전부 다름)·`UserAssist`·`BAM`·`SRUM`은
값이 바이너리 블롭이고 버전마다 포맷이 갈리는데, 파서는 키/값 일반
순회만 합니다. 애초에 매핑에도 없어 미착수 범위입니다.

**섹터 크기가 512로 박혀 있습니다.** `structs/mft_record.py`의
`SECTOR_SIZE`. `apply_fixups`가 인자로 받게 돼 있지만 부트섹터에서 읽어
넘기는 호출자가 없습니다. 4Kn 디스크에서 픽스업이 어긋납니다. Windows
버전 차이는 아니지만 같은 부류의 미완입니다.

### RecentFileCache — 실물 1건으로 확인했다 (2026-08-25)

`evidence/windows7_testimage.001`(Windows 7 Ultimate, 빌드 7601)의 실제
파일 6,742바이트 / 67항목으로 대조했습니다. 명세만 보고 쓴 구조가 그대로
맞았습니다.

| 확인한 것 | 결과 |
|---|---|
| 시그니처 `FE FF EE FF` | 일치 |
| 길이 필드가 **문자 수** | 67항목을 연쇄해 걸었을 때 마지막 항목 끝이 파일 크기와 **정확히 일치**(남은 바이트 0) |
| 독립 경로 대조 | `tools/scan_recentfilecache.py` — 길이 필드를 보지 않고 널로 쪼갠 결과 67건 전부 일치 |
| 04단계 관통 | 67건, `parse_errors` 0 |

**남은 바이트 0이 가장 강한 신호입니다.** 파일 크기는 Windows가 쓴 값이라
우리 해석과 독립인데, 길이 필드의 단위를 바이트로 잘못 잡았다면 첫 항목에서
어긋나 끝까지 갈 수 없습니다.

**아직 남은 것 둘.**

- **외부 도구 대조를 안 했습니다.** 위 둘은 우리가 만든 두 경로라 공통
  오해가 있으면 같은 방향으로 틀립니다. `RecentFileCacheParser`(Eric
  Zimmerman)와 맞춰 보는 것이 남았습니다.
- **이미지가 하나입니다.** 다른 빌드·언어·SP 조합에서 헤더가 다를 수
  있습니다. 헤더의 미상 4필드는 실측값을 구조체 docstring에 적어 뒀고,
  **항목 수(67)와 맞는 값이 없어 개수 필드는 없다**고 판단했습니다.

어긋났을 때 거부하는 검사들은 그대로 둡니다 — 시그니처 불일치는 파일
전체 거부(실제로 본 4바이트를 메시지에 담습니다), 길이 단위 오류는 종결자
검사, 중간 파손은 거기까지 유지하고 남은 바이트 집계.

### 프리패치에서 확인되지 않은 것

**버전 23(Windows 7)만 실물로 확인했습니다.** `evidence/[root]` 73건이
전부 그 버전입니다. 나머지는 아래와 같습니다.

| | 상태 |
|---|---|
| 버전 23 | 실측 73건 — 적재 경로 10,187건 독립 대조 일치. **다만 그 볼륨(`evidence/[root]`)은 지금 디스크에 없어 재확인 불가** |
| **버전 30 / 220바이트** | **실측 137건** — 적재 경로 10,195건 독립 대조 일치 (2026-08-24) |
| 버전 17 / 26 / 31, 버전 30의 216·224 | **명세만.** 실물로 확인한 적 없음 |
| `MAM` 압축 해제 | **실물 137건 해제 성공**(2026-08-24). `RtlCompressBuffer` 왕복 83건 일치 |

`(30, 220)`은 표에 없어서 **한 이미지의 프리패치가 통째로 0건이 됐던**
조합입니다. 아래 2번이 실제로 일어난 사례이고, 자리를 어떻게 확정했는지는
`docs/artifact-notes.md` "2026-08-24 · 프리패치 버전 30 / 220바이트" 참고.

**표의 Windows 이름표를 그대로 믿지 마십시오.** `(30, 224)`에 붙은
"Windows 10 (1809 이하)"와 `(30, 216)`의 "1903 이상"은 [LIBSCCA]를 옮긴
것인데, 실측한 `evidence/0824test.001`은 **빌드 15063(1703)인데 220바이트**
입니다. 1703은 1809 이하이므로 명세대로라면 224여야 합니다. 이름표가
틀렸는지 빌드마다 더 잘게 갈리는지는 확인하지 못했습니다.

**동작은 이 불일치에 영향받지 않습니다.** 파서가 블록 크기를 표에서
가져오지 않고 `메트릭 오프셋 - 헤더 크기`로 파일에서 읽기 때문입니다
(`read_file_information`). 버전 번호만으로 자리를 정했다면 이 이미지의
프리패치가 통째로 어긋났을 자리입니다.

버전별 자리는 `tests/test_prefetch_parser.py`가 합성 파일로 왕복 검증합니다
(17·23·26·30의 세 크기·31). **명세가 맞다는 증명은 아닙니다** — 테스트가
모듈의 표를 가져다 쓰지 않고 명세 값을 따로 적어 대조하므로, 고정되는 것은
"표를 말없이 바꾸지 못한다"입니다.

#### 장치 경로가 드라이브 문자로 안 바뀌었다 (**해결됨**, 2026-08-26)

`\DEVICE\HARDDISKVOLUME<n>` 만 살아 있는 볼륨으로 보고 있었습니다.
`win10_sysmon_testimage.001` 의 프리패치 **127건 전부**가 마운트 관리자의
영구 볼륨 이름(`\VOLUME{<생성시각>-<일련번호>}`)을 씁니다.

```
모델 주장 : C:\WINDOWS\SYSTEM32\SVCHOST.EXE
실제 레코드: \VOLUME{01d8e7bd02796420-a202ae01}\WINDOWS\SYSTEM32\SVCHOST.EXE
```

**조용히 틀리는 종류는 아닙니다** — 값이 그대로 남을 뿐입니다. 다만 그
결과가 06단계에서 **정상 문장의 기각**으로 나타납니다. 모델은 `C:\` 로
정규화해 쓰기 때문입니다.

정규식이 두 형태를 다 받습니다. 조치 후 127건 중 115건이 `C:\` 로
바뀝니다(나머지 12건은 `path_unresolved` — 별개 사안이고 조치 전후 같은
수). 대조 기록은 `docs/artifact-notes.md`.

**함정 둘은 그대로 지켰습니다.** `SHADOWCOPY` 는 여전히 걸리지 않고
(첫 형태는 숫자만, 둘째는 16진수만 받습니다), 살아 있는 볼륨이 둘 이상이면
바꾸지 않습니다. 형태가 섞여도 서로 다른 문자열이라 둘로 세어 포기합니다 —
같은 볼륨인지 우리는 모릅니다.

**다만 그 보수성이 선별로 넘어가던 것은 고쳤습니다 (2026-08-28).**
문자를 못 정하면 `_to_drive` 가 장치 경로를 그대로 두는데, 그러면
`path_prefix` 가 `C:\...` 로 적힌 스코프에서 프리패치가 **전량**
`out_of_scope` 로 빠졌습니다. `_in_scope` 가 원본과 변환본을 둘 다 보게
만든 이유("여기서 놓치면 아티팩트가 통째로 사라집니다")와 정반대 방향의
구멍이었습니다.

표기에서 보수적인 것은 옳습니다 — D: 의 실행 파일을 C: 로 보고하면 안
됩니다. 그런데 **필터에서는 그 보수성이 뒤집힙니다**: "문자를 모르겠다"가
"제외한다"가 됩니다. 이제 그때는 접두어 판정을 건너뛰고 넓게 냅니다.
확장자는 장치 경로에서도 판정되므로 계속 봅니다.

조용히 넘기지 않도록 `scope_undecidable` 로 셉니다. **이 값이 크면
프리패치가 사실상 범위 없이 05단계로 간 것**이므로 쿼터를 그만큼 먹습니다.

**실물로 확인하지 못했습니다.** 손에 있는 이미지의 프리패치는 137건 전부
살아 있는 볼륨이 하나라 이 경로가 한 번도 밟히지 않습니다. 합성 .pf 로만
고정했습니다.

**남은 것 — `fields.loaded_files` 는 여전히 장치 이름입니다.** 섀도 카피
경로를 살아 있는 볼륨의 것으로 둔갑시키지 않으려는 의도된 결정이고
(`artifact-notes.md`), 바뀌는 것은 `path` 하나뿐입니다. 둘이 걸립니다.

- 모델이 적재 파일을 인용하면서 `C:\` 로 정규화하면 그 문장은 같은 이유로
  기각됩니다. **아직 실측된 적은 없습니다** — K-ALERT 실측의 F2·F3 는 둘 다
  `path` 쪽이었습니다.
- **경로 기준으로 고를 수 없습니다.** 05단계가 목록을 20개로 자를 때
  "공격과 연계되는 것을 먼저 남긴다"를 하려면 경로를 비교해야 하는데,
  접두어가 `\VOLUME{...}` 이라 실측에서 `Windows 폴더 밖` 이 100%,
  `실행 파일과 같은 폴더` 가 0% 로 나옵니다 — 값이 아니라 **비교가
  성립하지 않는 것**입니다. 부분 문자열(`\windows\temp\`)만 됩니다.
  사이드로딩(T1574)의 가장 강한 신호 둘이 그래서 막혀 있습니다.

`_to_drive()` 를 목록에도 걸면 풀립니다. **그 함수가 이미 조건부입니다** —
살아 있는 볼륨이 정확히 하나일 때만 바꾸고 섀도 카피는 접두어가 안 맞아
그대로 남습니다. 다음 세션 작업으로 `work.md` 에 있습니다.

세 가지가 남습니다.

**1. Win10/11 실물을 읽어 본 적이 없습니다** (**2026-08-26 부분 해결**).
압축 해제는 Windows 자신의 압축기와 왕복이 맞지만 압축기와 복호기가 같은
구현 계열이라 **공통 오해는 잡지 못합니다.**
`win10_sysmon_testimage.001` 에서 129건 중 127건을 읽었습니다(버전 30).
못 읽은 둘은 시그니처 불일치 1건, `MAM` 압축 스트림 잘림 1건으로 둘 다
`parse_errors` 에 남습니다 — 구동 중 캡처라 쓰이는 중이던 파일로 보이지만
확인하지는 않았습니다. **외부 도구 대조는 여전히 안 했습니다.**

**2. 모르는 레이아웃은 건너뜁니다.** `(버전, 파일 정보 블록 크기)`가 표에
없으면 그 파일만 건너뛰고 `parse_errors`로 셉니다. 새 Windows 빌드가 블록
크기를 바꾸면 **그 시스템의 프리패치가 통째로 0건**이 됩니다. 조용하지는
않습니다 — 매니페스트의 `parse_errors`와 로그에 버전·크기가 함께 남고,
표에 한 줄 추가하면 되는 일임이 메시지에 적혀 있습니다.

**3. 적재 파일의 MFT 참조를 버립니다.** 메트릭 원소마다 파일 참조(엔트리
번호 + 시퀀스)가 있어 `$MFT` 레코드와 상호 참조할 수 있는데, 지금은
`fields.loaded_files`를 **문자열 리스트**로 냅니다. 06단계 `compare`가
리스트를 "원소 중 하나라도 일치"로 보므로 경로를 지목한 문장이 그대로
검증되기 때문입니다. 객체 리스트로 바꾸면 그 성질이 사라집니다.

**영향** — "이 DLL이 그때 그 파일이었는가"는 사람이 이어 붙여야 합니다.

### 레지스트리 — 라이브러리를 우회한 곳 (**해결됨**)

기록으로 남깁니다. **독립 디코더 대조로만 드러난 것들**이라, 대조를 하지
않았으면 그대로 갔습니다. 셋 다 조용히 틀리는 종류입니다.

대조 방법은 `nk`/`vk` 레코드를 명세대로 직접 디코딩해 파서 출력과
맞춰 본 것입니다. 결과(`SYSTEM\CurrentControlSet\Services` 1,754키 +
`SOFTWARE\...\CurrentVersion` 44,393키):

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 키 이름 | 46,147 / 46,147 | 46,147 / 46,147 |
| LastWrite | **565 / 1,754** (SYSTEM) | **46,147 / 46,147** |
| 값 전체 | 21,473 / 46,147 | **46,142 / 46,147** |

남은 5건은 big data(`db`) 레코드로, 값이 16,344바이트를 넘어 여러 셀로
쪼개진 것입니다(222KB·56KB 등). **python-registry가 맞고 대조 도구가
구현하지 않은 것**이라 파서 문제가 아닙니다.

#### ① 한글 문자열이 잘렸다

python-registry의 `decode_utf16le`가 종결자를 `s.index(b"\x00\x00")`로
찾습니다. **바이트 검색이라 문자 경계를 보지 않습니다.**

```
'볼륨 관리자 드라이버'
 fc bc │ 68 b9 │ 20 00 │ 00 ad │ ...
  볼      륨    공백    관
                └─ 00 00 ─┘   ← 오프셋 5(홀수)에서 끊긴다

라이브러리 : '볼륨 '
실제       : '볼륨 관리자 드라이버'
```

공백(`U+0020`, 고위 바이트 `0x00`) 다음에 `U+XX00` 형태 문자가 오면 두
문자에 걸쳐 `00 00`이 만들어집니다. **한글에 흔한 배치입니다** —
`U+AC00`(가), `U+AD00`(관)처럼 하위 바이트가 `0x00`인 음절이 많습니다.

실측 SYSTEM 하이브에서 **문자열 값 42,578건 중 56건(0.13%)** 이
잘렸습니다. 전부 한글이고, 서비스 표시명과 드라이버 설명이 주로
걸렸습니다.

**이 프로젝트는 한국어 환경이 대상입니다.** 비율은 작지만 걸리는 자리가
보고서에 그대로 실리는 필드입니다. 예외도 경고도 없고 잘린 문자열은 그
자체로 그럴듯해 보입니다.

**조치** — 문자열 타입은 `value.value()`를 쓰지 않고 `raw_data()`를 직접
UTF-16LE로 디코딩합니다. 정렬을 지키므로 가짜 종결자에 걸리지 않습니다.

#### ② `MULTI_SZ` 종결자가 값으로 남았다

`MULTI_SZ`는 각 문자열이 널로 끝나고 목록 전체가 널 하나로 더 끝납니다.
라이브러리가 널 기준으로 그냥 쪼개므로 빈 항목이 둘 남습니다.

```
raw_data : b'r\x00p\x00c\x00s\x00s\x00\x00\x00\x00\x00'
라이브러리 : ['rpcss', '', '']
실제 내용  : ['rpcss']
```

`Services` 1,754개 키 중 **249개**가 이 형태였습니다.

**06단계에 실제 영향이 있습니다.** `compare`가 리스트를 "원소 중 하나라도
일치"로 보므로, 빈 문자열이 남아 있으면 모델이 지어낸 `value: ""` 주장이
**검증을 통과합니다.** 환각 탐지에 구멍이 생깁니다.

**조치** — 끝의 빈 문자열을 뗍니다. 가운데 것은 건드리지 않습니다 —
구조적으로 있을 수 있고 위치가 의미를 가집니다.

#### ③ 타임스탬프가 반올림됐다

python-registry가 100ns를 마이크로초로 줄이면서 `ROUND_HALF_EVEN`으로
**반올림**합니다. 이 프로젝트의 나머지 파서는 전부 `filetime_to_datetime`
으로 **절삭**합니다.

```
raw FILETIME   128920208934228618   (끝자리 8)
라이브러리      .422862   ← 반올림
정수 절삭       .422861
```

1µs 차이라 허용 오차 안이지만, 문제는 값이 아니라 **표기가 사실과
달라지는 것**입니다. 우리 형식은 끝에 `0`을 붙여 7자리를 만듭니다 —
"100ns 자릿수는 버렸다"는 뜻인데, 반올림하면 그 자리가 6번째 자리에
섞여 들어가고 표기는 여전히 버렸다고 말합니다.

evtx가 python-evtx의 `timestamp()`를 우회한 것과 같은 결론입니다
(`artifact-notes.md`). 저쪽은 float 오차였고 이쪽은 반올림이라 원인은
다릅니다.

**조치** — `_nkrecord.unpack_qword(0x04)`로 raw FILETIME을 읽어
`filetime_to_datetime`에 넘깁니다.

회귀 테스트는 `tests/test_registry_parser.py`에 있습니다(한글 잘림,
`MULTI_SZ` 종결자, 가운데 빈 항목 보존, 홀수 길이).

#### `registry:Amcache` — 실물 하이브 구조는 확인, 값 의미는 대조 안 함 (**부분 해결**)

`Amcache.hve`도 regf 포맷이라 `RegistryParser`를 그대로 재사용했습니다
(`HIVE_OF`에 `"registry:Amcache": "Amcache"` 추가).
셀 구조 자체의 정확성은 위 세 가지 우회(한글 잘림·`MULTI_SZ` 종결자·
타임스탬프 반올림)와 `docs/artifact-notes.md`의 `scan_hive_cells.py`
대조가 이미 검증한 것을 그대로 물려받습니다 — SYSTEM/SOFTWARE와 셀
디코딩 경로가 완전히 같기 때문입니다.

**실물 이미지(`evidence/test_image.001`)로 1,140건을 뽑아 구조를
확인했습니다.** `Amcache\Root\Programs\...`, `Amcache\Root
\InventoryApplicationFile\...` 서브키가 예상대로 나오고,
`InventoryApplicationFile` 레코드의 `fields`에 `LowerCaseLongPath`
(전체 경로)·`FileId`(`0000`+SHA1로 보이는 41자리 값)·`BinFileVersion`·
`BinaryType`·`Size`·`ProgramId`가 실제로 담겨 있음을 확인했습니다 —
파서 확충 때 기대한 "실행 파일 경로·SHA1·타임스탬프"가 맞습니다.

**다만 두 가지는 여전히 안 됐습니다.**
- **필드 이름의 의미를 우리가 해석하지 않습니다** — 그럴 필요가
  없습니다. `registry.py`의 설계(가치가 경로·값 이름에 있지 레코드
  자체에 있지 않음, `docs/limitations.md` 6-7)를 그대로 따라 원시
  값만 냅니다. `Programs` 서브키의 필드는 `"0"`·`"1"`·`"17"` 같은
  숫자 이름이라("13": 0, "a": 1787353733 등) 사람이 읽으려면 외부
  문서 대조가 필요한데, 그건 05단계 해석의 몫입니다.
- **매핑(`T1547.001.yaml`)의 `path_prefix`를 아직 안 정했습니다** —
  구조는 확인했지만 "어느 서브키가 조사에 실제로 유용한가"(예:
  `InventoryApplicationFile`만 볼지, `Programs`도 볼지)는 검증하지
  않았습니다. 범위를 좁히면 놓칠 위험이 있어 보류합니다.
- ~~**외부 도구(AmcacheParser 등) 대조는 하지 않았습니다.**~~
  **2026-08-27 에 했습니다.** AmcacheParser 2026.5.0 과 대조해 다섯
  범주 **457건이 레코드 수·값 모두 일치**했습니다. `Root\File` 361건은
  저쪽이 "new format" 으로 보고 안 읽는 것이라 채점 대상이 아닙니다.
  대조기는 `tools/compare_amcache.py`, 기록은 `docs/artifact-notes.md`
  2026-08-27 "AmcacheParser 2026.5.0 대조" 절입니다.

### 레지스트리에서 못 보는 것

#### 값에는 타임스탬프가 없다

**이것이 이 아티팩트의 가장 큰 제약입니다.** 타임스탬프는 키(nk)에만
있고 그 키 아래 **모든 값이 하나를 공유합니다.** python-registry도 값에
시각을 물으면 거부합니다 — `ValueError: value does not have a timestamp`.

```
Dnscache LastWrite = 2009-07-14T04:54:53Z
  ├ ImagePath    시각 없음
  ├ Start        시각 없음
  └ ObjectName   시각 없음
```

**영향** — "ImagePath가 이 시각에 설정되었다"는 문장은 **원리적으로 검증할
수 없습니다.** 키 타임스탬프는 "그 키 아래 무언가가 바뀌었다"까지만
말합니다. 06단계 `value_match`는 통과시킵니다(키 타임스탬프와 대조하므로).
**즉 근거가 되지 않는 문장이 통과합니다.** `$MFT`·evtx는 레코드 하나에
시각 하나가 대응하는데 레지스트리만 N:1입니다.

07단계 타임라인도 같은 영향을 받습니다. 키 하나가 값 몇 개를 담고 있든
타임라인에는 한 점으로 찍힙니다.

#### 트랜잭션 로그를 재생하지 않는다

`.LOG1`/`.LOG2`를 읽지 않습니다. 더티 하이브(기본 블록의 시퀀스 번호 두
개가 다름)는 **정상적으로 열리고 정상적으로 파싱되는데 값이 낡았습니다.**
최신 내용이 로그에 있기 때문입니다.

`$UsnJrnl`의 0바이트 껍데기(4-0)와 같은 유형입니다 — 파일이 있고, 파서가
성공하고, 답이 틀립니다. 그래서 조용히 넘어가지 않고 경고를 찍고
`stats.dirty_hive`에 남깁니다.

**다만 매니페스트까지 가지 않습니다.** `parse.py`가 파서 `stats`에서
`parse_errors`와 `unreadable_bytes`만 옮기기 때문입니다. evtx의
`recovered_chunks`도 같은 처지입니다. 콘솔 경고를 놓치면 보고서만 보고는
더티 하이브였는지 알 수 없습니다.

실측(`evidence/[root]`)에서는 SYSTEM·SOFTWARE 둘 다 clean이었습니다.

#### 삭제된 키를 복구하지 않는다

미할당 셀에 남은 `nk`를 읽지 않습니다. python-registry가 할당된 트리만
걷기 때문입니다. 삭제된 Run 키나 서비스가 포렌식적으로 가치가 큰데
보지 못합니다.

`tools/scan_hive_cells.py`가 **개수는 셉니다.** 실측에서 SYSTEM 7건,
SOFTWARE 2건이었습니다. 이 값이 크면 그 증거에서는 복구가 의미 있다는
신호입니다.

#### SAM·SECURITY·NTUSER를 읽지 않는다

SYSTEM·SOFTWARE 둘뿐입니다. `T1136.001`(로컬 계정 생성)의 1차 아티팩트는
SAM 하이브인데 지원하지 않습니다. 사용자별 흔적(NTUSER.DAT의 `Run`,
`RecentDocs`, `TypedPaths`)도 전부 범위 밖입니다.

파서는 그대로 쓸 수 있습니다 — 하이브 형식이 같습니다. `refs.py`의
접두어와 `FILE_LAYOUT` 항목, 카탈로그 등재가 필요합니다.

#### CurrentControlSet은 해석하지만 경로는 실제 이름으로 나간다

`scope`의 `CurrentControlSet`은 `Select\Current`를 읽어 `ControlSet001`로
바꿉니다. 다만 **산출물의 `path`에는 실제 이름이 들어갑니다.**

**영향** — 모델이 `SYSTEM\CurrentControlSet\Services\...`라고 인용하면
06단계가 `ControlSet001`과 대조해 **기각합니다.** 문장은 맞는데
표기가 다른 경우이고, `comparators`가 흡수하지 못하는 종류입니다.
04단계 산출물이 원본에 충실해야 한다는 것과 충돌하므로 06단계에서
풀어야 할 문제로 남깁니다.

#### 장치 속성의 DEVPROP 타입을 못 읽는다 (2026-08-26 실측)

`K-ALERT` 실행에서 04단계가 경고 **90건**을 냈고, 90건 전부 같은 자리였습니다.

```
SYSTEM\ControlSet001\Enum\USB\...\Properties\{GUID}\NNNN
  → Unknown Type Exception (Unknown VK Record type 0x12)
```

`0x12`는 `DEVPROP_TYPE_STRING`입니다. 장치 속성 저장소는 표준 레지스트리
값 타입(`REG_SZ` 등)이 아니라 **DEVPROP 타입 체계**를 쓰는데,
python-registry가 이것을 모릅니다.

**Stage 1(USB 삽입) 증거에는 손실이 없습니다.** 정작 중요한 타임스탬프는
멀쩡히 나옵니다.

```
Properties\{83da6326-97a6-4088-9453-a1923f573b29}\0064  InstallDate       2022-10-24T15:29:48.8920000Z
                                                 \0065  FirstInstallDate  2022-10-24T15:29:48.8920000Z
                                                 \0066  LastArrivalDate   2022-10-24T15:45:41.6995900Z
```

못 읽는 90건은 문자열형 속성(장치 설명·제조사 등)이고, 같은 사실이
`DeviceDesc`·`Mfg`·`HardwareID`·`ContainerID` 같은 평범한 값으로 **이미
따로 들어옵니다.** 그래서 우선순위를 낮게 둡니다 — 다만 경고 90줄이
04단계 출력을 덮어 다른 경고를 가릴 수 있습니다.

### evtx에서 못 보는 것

- **메시지 문자열을 만들지 않습니다.** 이벤트 로그 뷰어가 보여 주는 문장은
  제공자 매니페스트(DLL 리소스)와 `<Data>` 값을 합성한 결과입니다. 우리는
  원시 값만 냅니다. `fields`에 재료가 다 들어 있어 해석은 되지만, 보고서에
  뷰어와 같은 문장이 실리지는 않습니다.
- **`EventID`만으로 이벤트를 특정하지 않습니다.** ID는 제공자 안에서만
  유일합니다. `fields.Provider`에 제공자를 함께 실어 두었으나,
  03단계의 `event_ids` 선별은 제공자를 구분하지 않습니다. 다른 제공자가
  같은 번호를 쓰면 함께 딸려 옵니다.

  **채널을 늘리면서 이 위험이 현실이 됐습니다**(2026-08-24). NetworkProfile의
  `10000`(네트워크 연결)은 `T1200` 매핑이 `evtx:System`에 요청하는 Kernel-PnP
  `10000`과 번호가 같습니다. 새 플래그 룰(`firewall_config_changed`·
  `bits_transfer`·`network_state_changed`)은 그래서 기존 룰과 달리
  `evtx:*`가 아니라 **정확한 아티팩트 이름**으로 겁니다.

  **기존 룰들은 아직 `evtx:*`입니다.** 지금 카탈로그에서는 번호가 겹치지
  않아 문제가 없지만, 채널을 더 늘리면 다시 확인해야 합니다 — 조용히
  틀리는 쪽입니다.

### 네트워크 흔적 — 절반만 볼 수 있다 (2026-08-24)

키오스크 사고에서 "외부 서버로 데이터를 보냈는가"를 물었더니 **카탈로그에
네트워크 아티팩트가 하나도 없었습니다.** 그래서 보고서의 "확인하지 못한
아티팩트"에도 나타나지 않았습니다 — 안 본 것을 안 봤다고 말하지도
못하는 상태였습니다.

**붙인 것** — `evtx:Firewall`·`evtx:BITS`·`evtx:NetworkProfile`. 파서는
새로 만들지 않았습니다(채널만 다르고 형식이 같습니다). 대조 기록은
`docs/artifact-notes.md` "2026-08-24 · evtx 채널 확장". 기법은 `T1562.004`
(방화벽 무력화)와 `T1197`(BITS Jobs)을 `KNOWN_TECHNIQUES`에 더하고 매핑을
새로 썼습니다.

**여전히 못 보는 것** — 여기가 중요합니다.

| 아티팩트 | 무엇을 담나 | 왜 못 읽나 |
|---|---|---|
| SRUM (`SRUDB.dat`) | **앱별 송수신 바이트** | ESE 포맷. 신규 라이브러리 필요 |
| WebCache | 브라우저 방문·다운로드 | ESE 포맷 |
| `qmgr.db` | BITS 작업 큐 원본 | ESE 포맷 |
| NTUSER.DAT | 사용자별 흔적 | 사용자 프로필마다 하나 (다중 매칭 미지원) |
| DNS 클라이언트 로그 | 조회한 도메인 | 기본적으로 비활성 |

**SRUM이 가장 아픕니다.** "어느 프로그램이 얼마를 보냈나"에 직접 답하는
유일한 아티팩트인데 못 읽습니다. 실측 이미지에서 640KB로 존재하고,
`dissect.database`의 `ESE`로 열리는 것까지는 확인했습니다 — 즉 트랙 B
후보 중 가장 값이 큰 자리입니다.

#### 그리고 증거 자체에 없는 것

도구를 고쳐도 안 되는 층이 있습니다. 실측 이미지 기준:

```
Security.evtx 912건 중
  5156 (연결 허용/WFP)  0건   ← 연결 감사가 꺼져 있었다
  5158 (바인드)          0건
  4688 (프로세스 생성)  41건 — 사고 시간창 안 0건
```

**연결 단위 기록이 아예 없습니다.** 방화벽 로그(WFP)는 연결이 아니라
**규칙 변경**만 남기므로 이것을 대신하지 못합니다. 어떤 도구를 붙여도
"어느 IP로 얼마를 보냈다"는 이 증거에서 나오지 않습니다.

**영향** — 네트워크 관련 결론은 두 가지를 나눠 적어야 합니다. "우리가 안
본 것"(SRUM 등)과 "아무도 못 보는 것"(연결 감사 미설정). 섞으면 도구의
한계가 증거의 한계로 둔갑하거나 그 반대가 됩니다.
- **손상 구간의 레코드 수를 모릅니다.** 라이브러리가 청크를 조기 종료하면
  못 읽은 바이트 수는 알아도 그 안에 몇 건이 있었는지는 알 수 없어,
  `parse_errors`에 "구간 1건"으로 셉니다. 실제 유실 건수는 그보다 많습니다.

### $MFT만으로는 알 수 없는 것

`[LIBFSNTFS]`가 "$MFT metadata file only parsing"으로 정리한 것과 같습니다.

- 속성 목록(`$ATTRIBUTE_LIST`)이 `$MFT` 밖에 저장된 경우
- `$I30` 인덱스가 `$MFT` 밖에 있는 경우
- 부모 MFT 엔트리가 재사용됐으면 경로 재구성이 어긋남

**영향** — 고아 레코드는 만들 수 있는 만큼만 경로를 냅니다. 경로가
없다고 레코드를 버리지는 않습니다(삭제 흔적을 놓치므로).

### $UsnJrnl에는 경로가 없다

USN 레코드는 파일 **이름**만 담습니다. 전체 경로를 얻으려면
`parent_entry`로 `$MFT`를 되짚어야 하는데, 파서가 그것을 하지 않습니다 —
한 아티팩트를 읽는 동안 다른 아티팩트에 의존하면 "무엇을 읽어 무엇이
나왔는가"가 흐려지기 때문입니다.

**영향** — `scope`의 `path_prefix`를 **적용할 수 없습니다.**
`extensions`만 이름에 적용됩니다. 03단계가 경로로 좁혀 달라고 해도
넓게 나옵니다. 좁히다 증거를 놓치는 것보다 낫다는 판단이지만, 아래
4-2의 원인이기도 합니다.

---

### K-001 설계서 대응 — 무엇을 열었고 무엇이 여전히 안 되나 (2026-08-25)

「K-001 공격 시나리오 설계서」의 Stage 1~7이 요구하는 아티팩트를 카탈로그와
대조하고, 열 수 있는 것을 열었습니다. **먼저 결론**: Tier 1 아티팩트 36개
중 24개가 이제 됩니다(이전 15개).

#### 연 것 — evtx 채널 아홉

전부 evtx 파서 재사용이라 새 파서가 없습니다.

| 아티팩트 | 채널 | Stage |
|---|---|---|
| `evtx:Sysmon` | `Microsoft-Windows-Sysmon/Operational` | 2·3·4b·6·7 |
| `evtx:DriverFrameworks` | `...DriverFrameworks-UserMode/Operational` | 1 |
| `evtx:KernelPnP` | `Microsoft-Windows-Kernel-PnP/Configuration` | 1 |
| `evtx:AssignedAccess` / `...Admin` / `...Broker` | Assigned Access 세 채널 | 3 |
| `evtx:RDPConnection` / `evtx:RDPSession` | TerminalServices 두 채널 | 4b |
| `evtx:Application` | `Application` | 3 |

**Sysmon 하나가 다섯 단계를 열었습니다.** 특히 Stage 3의 결정적 근거
("주문 UI가 셸을 자식으로 생성")는 Sysmon EID 1의 `ParentImage` 없이는
관측할 방법이 아예 없었습니다.

`evtx:Sysmon` 은 이 카탈로그에서 **유일하게 기본 탑재가 아닌** 로그입니다.
랩에 설치돼 있어야 하고, 없으면 `artifact_not_found` 로 빠집니다.

#### 검증하지 않은 채 채운 값

**이벤트 ID를 전부 추정으로 넣었습니다.** 공개 DFIR 자료와 설계서 §2의
서술에서 골랐고 실제 증거로 대조한 적이 없습니다. 신뢰도가 낮은 순:

- **`evtx:AssignedAccess` 세 채널의 이벤트 ID를 모릅니다.** 그래서
  `kiosk_restriction_event` 플래그가 `event_id` 로 좁히지 않고 **전
  레코드에 붙습니다.** "전 레코드에 붙는 플래그는 필터 역할을 못 한다"는
  원칙의 의도적 예외이고, 근거는 "세 채널 다 로그가 작다"입니다.
  **그 전제를 실측으로 확인하지 않았습니다** — 키오스크가 이 채널을 수만
  건 남기면 05단계 쿼터를 혼자 태웁니다.
- **`device_connected` 의 ID 목록**(DriverFrameworks 2003·2100·2102 계열,
  Kernel-PnP 400·410 계열) — 위 `T1200` 20001/10000 과 같은 부류입니다.
- **`remote_session` 의 ID 목록**(1149, 21·22·24·25) — RDP 관련 ID는
  공개 자료가 비교적 일치하지만 우리 증거로 확인하지는 않았습니다.
- **`shell_spawned` 의 실행 파일 목록** — 설계서가 든 cmd/explorer/taskmgr
  에 관례적으로 함께 보는 것들을 더했습니다. **키오스크 실물에서 정상
  운영 중 이 목록이 얼마나 뜨는지 모릅니다.**
- **`unexpected_parent_process` 가 `explorer.exe` 를 이상 징후로 봅니다** —
  잠긴 키오스크에서 셸이 주문 UI 로 대체된다는 전제입니다. 설계서 §1.3 의
  M1(현실 오구성)에서는 서드파티 키오스크 SW 가 explorer 위에서 도는 경우가
  있어 **정상 부모가 될 수 있습니다.** 그러면 이 룰이 전량을 잡습니다.
  랩 구성이 정해지면 다시 봐야 합니다.
- **`execution_from_unusual_path` 가 `C:\ProgramData` 를 뺐습니다** — Mock
  POS 가 거기 놓일 예정이라 넣으면 정상 POS 실행이 전량 걸립니다. POS 설치
  경로가 정해지면 다시 판단합니다. 같은 이유로 `pos_db_root`·`data_root`
  기본값도 임시입니다(아래).
- **`{pos_db_root}`·`{data_root}`·`{staging_root}` 기본값** — Mock POS 앱이
  개발되지 않아 거래 DB 위치를 모릅니다(설계서 §9 D1). 이 변수들은 시나리오
  `entities` 에서 채워지는 다섯에 없어 **항상 기본값이 쓰입니다.** 틀리면
  `T1565.001`·`T1005`·`T1657` 의 Tier 1 `$MFT` 요청이 0건을 내는데, 경로를
  잘못 짚은 것인지 흔적이 없는 것인지 산출물만 보고는 구별되지 않습니다.

`timestamp_truncated` 만 실측이 있습니다 — 아래. Sysmon 쪽은
2026-08-26 에 절반이 확인됐습니다 — 바로 아래.

#### 실측 — Sysmon 을 켠 실물 이미지 (2026-08-26, `win10_sysmon_testimage.001`)

Sysmon 을 설치해 구동한 Win10 을 FTK Imager 로 뜬 60GB 물리 디스크입니다
(MD5·SHA1 verified). **이 카탈로그에서 유일하게 기본 탑재가 아닌 로그라
그전까지 실물이 없었습니다.**

`0824test.001` 과 같은 레이아웃입니다 — NTFS 둘(0.4GiB 복구 + 59.4GiB
시스템)이라 `--volume 1` 이 필요합니다. 카탈로그 21종 중 15종을 찾았습니다.

**확인된 것 넷.**

| | |
|---|---|
| 파일 경로 | `Windows/System32/winevt/Logs/Microsoft-Windows-Sysmon%4Operational.evtx` — `FILE_LAYOUT` 의 첫 후보가 맞았습니다 |
| 로그 크기 | 1.1 MiB, 244건 / 5.5분. **05단계 쿼터를 태울 규모가 아닙니다** |
| 청크 복구 | 헤더가 `chunk_count=1` 인데 청크 4개를 복구했습니다. 구동 중 캡처라 헤더가 안 플러시된 것이고, **복구 로직이 실물에서 일한 첫 사례**입니다. 없었으면 244건 중 대부분을 놓쳤습니다 |
| EventID 분포 | 1(ProcessCreate) 125 · 5(ProcessTerminate) 114 · 16 2 · 4·10·11 각 1. 기본 설정(`ConfigurationFileHash "-"`) |

**정상 구동 중 플래그가 붙은 건수** (조치 전):

```
shell_spawned                 10건   ← EID 1: 5,  EID 5: 5
execution_from_unusual_path    6건   ← EID 1: 3,  EID 5: 3
unexpected_parent_process      0건
file_created                   1건
```

오탐의 정체는 전부 **Windows 서비싱**입니다 — `sedsvc.exe`(Remediation)·
`CompatTelRunner.exe`(텔레메트리)가 `cmd.exe`·`rundll32.exe` 를 띄우고,
`C:\Windows\Temp\{GUID}\DismHost.exe` 가 `execution_from_unusual_path` 6건
전부입니다. **다만 설치 직후 5.5분 구간이라 서비싱 활동이 평시보다
많습니다** — 시간당으로 환산하면 과대평가됩니다.

`unexpected_parent_process` 0건은 룰이 정확해서가 아닙니다. EID 5 에
`ParentImage` 가 없어 우연히 안 붙었고, **explorer.exe 전제는 이 이미지로
검증되지 않았습니다** — Assigned Access 를 안 켠 일반 Win10 이라 explorer 가
정상 셸입니다.

##### 세 룰이 EID 1 만 본다고 써 놓고 안 그랬다 (**해결됨**)

위 건수의 절반이 `ProcessTerminate` 였습니다. 세 룰 다 `condition:` 에
"Sysmon 1(프로세스 생성)" 이라고 적어 놓고 `when:` 절에 event_id 제약이
없었습니다. 바로 아래 `network_connection` 은 `match: event_id` 를 쓰고
있으니 관례는 이미 있었고, 이 셋만 안 지켰습니다.

같은 프로세스를 두 번 세는 것보다 나쁜 것이 있습니다. **EID 5 레코드에는
`ParentImage` 도 `CommandLine` 도 없습니다.**

```
SYSMON#212  EID=5  fields=[Image, ProcessGuid, ProcessId, RuleName, User, UtcTime]
```

`shell_spawned` 의 note 가 "부모가 무엇이냐가 그 다음 질문이다
(fields.ParentImage 는 레코드에 그대로 실려 모델이 본다)" 라고 적은 바로 그
필드가 없는 쪽입니다. **05단계 자리는 먹고 정보는 더 적습니다.**

2026-08-25 에 채널 단위로 좁혔던 "flag 사정거리" 와 같은 부류인데, 이번엔
채널 **안에서** EventID 사이입니다.

**조치** — `Clause` 에 `event_ids` 를 더했습니다. `artifact` 와 같은
자리이고, 둘 다 대상을 좁힌 뒤 `match` 가 판정합니다. `match: event_id`
(EventID 자체가 신호)와 뜻이 달라 함께 쓰면 거부됩니다.
작성 규칙은 `docs/mapping-guide.md`.

```
조치 전  17건 (EID 5 가 8건)
조치 후   9건 (EID 5 가 0건)
```

##### 이 이미지로 확인하지 못한 것

`evtx:AssignedAccess`·`AssignedAccessAdmin`·`AssignedAccessBroker`·
`DriverFrameworks`·`RDPConnection` — **파일 자체가 없습니다.** Assigned
Access 를 켜지 않았고, USB 를 꽂지 않았고, RDP 를 쓰지 않았습니다. 이
다섯의 경로 문자열과 event_id 추정값은 여전히 미검증이고, 위의
`kiosk_restriction_event` 볼륨 전제도 재지 못했습니다.

#### 실측한 것 — `timestamp_truncated` 오탐률 0%

설계서 Stage 5의 두 번째 탐지 포인트($SI 서브초 0 정렬)를 넣으면서
실물 두 이미지의 `$MFT` 전체를 훑었습니다.

| 이미지 | 레코드 | $SI 하나라도 서브초 0 | 둘 이상(적용 조건) |
|---|---|---|---|
| Win7 7601 | 72,795 | **0건** | **0건** |
| Win10 15063 | 98,151 | **0건** | **0건** |

정상적으로 만들어진 파일의 100ns 자리는 사실상 난수라, 두 이미지
170,946 레코드 어디에도 서브초가 0인 `$SI` 가 없었습니다. 느슨한
조건("하나라도")도 0%지만 엄격한 쪽을 씁니다 — FAT 에서 복사된 파일처럼
초 단위로 정렬되는 경우가 있는 이미지에서 여유를 두려는 것입니다.

**이 실측이 목업 픽스처의 오류를 드러냈습니다.** "정상 파일"을 표현하려던
목업 `$MFT` 레코드가 초 단위로 딱 떨어지는 `$SI` 를 쓰고 있어 조작된
파일로 판정됐습니다. 실물에는 그런 파일이 없으므로 픽스처를 고쳤습니다.

#### 여전히 안 되는 것

| 아티팩트 | Stage | 왜 |
|---|---|---|
| `setupapi.dev.log` | 1 | 텍스트 로그. 파서 없음 |
| MountPoints2 (NTUSER.DAT) | 1 | 사용자별 하이브 미지원 |
| ShimCache (`AppCompatCache`) | 2 | 값이 바이너리 블롭. 아래 |
| BAM/DAM | 2 | 같음 |
| SRUM (`SRUDB.dat`) | 2·7 | ESE DB. 파서 없음 |
| SAM 하이브 | 4 | 미지원 |
| RDP 접속 이력 (NTUSER.DAT) | 4b | 사용자별 하이브 미지원 |
| RDP 비트맵 캐시 | 4b | 미지원 |
| POS 앱 로그·거래 DB | 5·6 | 팀이 만드는 애플리케이션의 산출물 (담당 6 영역) |
| Mock 수신 서버 로그 | 7 | 같음 |
| PCAP | 7 | 명시적 비목표 |

**레지스트리 값 안의 바이너리 블롭이 Stage 2를 반쯤 비웁니다.**
`AppCompatCache` 는 캐시 전체가 값 하나이고, 파서는 그것을 16진 문자열로
냅니다(실측 — Win7·Win10 둘 다). 버전마다 포맷이 갈려(XP/7/8/8.1/10)
디코더를 따로 써야 합니다. BAM 도 8바이트 FILETIME 블롭이라 같은 문제입니다.

**이벤트 단위 가용성은 여전히 없습니다.** 아티팩트 단위 버전 판정만
있어서, 하위 버전에 없는 이벤트 ID가 0건일 때 "그 버전엔 없다"와 "탐지
실패"가 구별되지 않습니다.

#### 게이트 4를 자동으로 검사하게 했다 (2026-08-25, 자체 점검에서 발견)

매핑에 `event_ids` 를 적어도 **그것을 받는 flag 가 없으면 05단계에 한 건도
가지 않습니다.** 03단계는 "봤다"고 적고 04단계는 파싱까지 하며
`errors.jsonl` 에도 안 남습니다 — 보고서에는 그냥 소견이 없는 걸로 보입니다.

K-001 매핑을 쓰면서 이 구멍을 **13개 만들었습니다.** Sysmon 11·22 와
Security 4634·4647·4722·4724·4735·4737·4738·4778·4779 입니다. 같은 검사가
**예전부터 있던 구멍 넷**도 드러냈습니다.

| | 어떻게 메꿨나 |
|---|---|
| Sysmon 11 (FileCreate) | 기존 `file_created` 에 절을 더함 — 새 이름을 만들지 않았다 |
| Sysmon 22 (DnsQuery) | 기존 `network_connection` 에 값을 더함 |
| Security 4722·4723·4724·4725·4726·4735·4737·4738 | 새 `account_modified` |
| Security 4634·4647·4778·4779 | 새 `session_state_changed` |
| Security 4698·4699·4702 (**예전부터**, T1053.005) | 새 `scheduled_task_changed` |
| System 20001·10000 (**예전부터**, T1200) | `device_connected` 에 `evtx:System` 절을 더함 |
| BITS 4 (**예전부터**, T1197) | `bits_transfer` 값에 더함 |

`device_connected` 의 새 절은 `evtx:*` 가 아니라 **`evtx:System` 을 정확히**
씁니다. 10000 을 NetworkProfile 이 "네트워크 연결"로 쓰기 때문입니다.

**Security 4673·4674 는 메꾸는 대신 매핑에서 뺐습니다.** 특권 서비스 호출은
감사를 켜면 정상 동작에서도 초당 수십 건이 나와, flag 를 만들면 05단계
쿼터를 혼자 태웁니다. 4672(새 로그온에 특수권한 부여)가 같은 사실의
요약이고 `logon_success` 가 이미 받습니다.

`tests/test_flag_rules.py` 의 `test_every_mapped_event_id_has_a_flag_that_
receives_it` 가 이제 이것을 막습니다. 예외는 두 목록에만 적을 수 있고,
둘 다 **매핑에 실제로 남아 있는 조합인지** 함께 검사합니다.

#### flag 의 사정거리가 채널을 늘릴 때마다 넓어지고 있었다 (2026-08-25, 코드 리뷰)

`account_created`·`privileged_group_add`·`logon_success`·`logon_failed`·
`service_config_changed`·`service_installed` 여섯이 `artifact: evtx:*` 로
걸려 있었습니다. **채널이 5개에서 14개가 되면서 같은 룰이 새 채널 전부에
걸리게 됐고, 그 재확인을 하지 않았습니다.**

EventID 는 **제공자 안에서만** 유일합니다. 채널은 그 근사치입니다 — 실측하면
`System.evtx` 하나에 제공자가 22개입니다(Win7 7601). 다만 **같은 채널 안에서
두 제공자가 같은 ID 를 쓴 경우는 0건**이었고, 진짜 위험은 채널 사이입니다.

| | Win7 7601 실측 |
|---|---|
| `evtx:System` | 1,468건 / 제공자 **22개** / 채널 내 ID 충돌 0건 |
| `evtx:Security` | 8,013건 / 제공자 2개 / 채널 내 ID 충돌 0건 |

**실제로 뚫려 있던 통로는 AssignedAccess 세 채널입니다.** `T1059.003` 이
`scope_template: {}` 로 요청해 event_id 필터 없이 전량 파싱되는데, 그 채널의
ID 를 우리가 모르는 채로 여섯 룰에 노출돼 있었습니다. 4624·4720·7045·4732
중 하나라도 쓰면 그 레코드에 로그온·계정 생성·서비스 설치 플래그가 붙습니다.

여섯을 정확한 채널명으로 좁혔습니다. **각 룰의 `artifacts:` 선언은 처음부터
맞는 채널을 적고 있었습니다** — 룰이 그 선언을 안 지키고 있었을 뿐입니다.

#### `evtx:Firewall` 이 안 가진 것을 요청하고 있었다

`T1041`·`T1048` 이 `evtx:Firewall` 을 Tier 1 로 걸고 rationale 에 "아웃바운드
연결의 허용·차단 기록"이라고 적었는데, **그 채널에는 그런 기록이 없습니다.**
`Windows Firewall With Advanced Security/Firewall` 이 담는 것은 방화벽 규칙·
프로필의 **구성 변경**입니다(그쪽은 `T1562.004` 가 봅니다).

연결 단위 허용/차단 감사는 Security 의 5152·5156·5157(필터링 플랫폼)에
있습니다. **걸지 않았습니다** — 기본 감사가 꺼져 있어 대개 안 남고, 켜면
정상 트래픽으로 로그가 가득 차 05단계 쿼터를 혼자 태웁니다. 외부 통신의
근거는 Sysmon 3·22 가 Tier 1 로 들고 있습니다.

그 결과 K-001 에서 `evtx:Firewall` 은 **제외**로 나갑니다. 이 시나리오의
기법 중 그 채널을 요구하는 것이 없기 때문이고, 그것이 사실입니다.

#### 가드 넷이 이제 이것을 막는다

`tests/test_flag_rules.py`. 넷 다 **일부러 되돌려 실제로 잡는 것을 확인**
했습니다 — 통과만 보고 믿으면 가드가 죽어도 모릅니다.

| 검사 | 막는 것 |
|---|---|
| `test_every_mapped_event_id_has_a_flag_that_receives_it` | 매핑이 적은 event_id 를 받을 flag 가 없다 |
| `test_no_event_id_rule_uses_a_wildcard_artifact` | `match: event_id` 절이 와일드카드를 써 채널이 늘 때마다 번진다 |
| `test_a_whole_channel_request_has_a_flag_that_can_receive_it` | 채널 전체를 요구했는데 받을 절이 없다 |
| `test_the_known_gap_list_does_not_rot` | 예외 목록이 실제 매핑과 어긋난 채 남는다 |

**세 번째 검사가 아는 것은 기계적 증상뿐입니다.** "받아 줄 절이 없다"까지고,
rationale 이 틀렸는지는 사람이 채널 내용을 확인해야 나옵니다. 위 Firewall 건도
그 순서로 드러났습니다 — 가드가 걸림 → 왜지 → 채널 내용 확인 → rationale 이
틀렸음. 여기서 걸리면 "고쳐라"가 아니라 **"왜 그런지 보라"** 는 신호입니다.

커버리지 계산에서 전역 표식(`artifact: "*"` + `match` 없음)을 제외하는 것도
**이름이 아니라 성질로** 바꿨습니다. 예전에는 `outside_time_range` 라는 이름을
박아 두었는데, 그러면 전역 표식이 하나 더 생기는 순간 모든 조합이 "덮였다"로
계산돼 검사가 조용히 죽습니다. 실패하지 않으므로 죽었다는 사실도 안 드러납니다.

#### Sysmon EID 1 을 이름이 아니라 맥락으로 가른다 (2026-08-25)

처음에는 `shell_spawned` 하나였습니다 — `fields.Image` 가 셸·시스템
유틸리티인 것만 잡는 룰입니다. **그것만으로는 K-001 Stage 2 를 못 잡습니다.**
그 단계가 겨냥하는 것은 USB 안의 비승인 실행파일이고, **공격자가 무엇을
실행할지는 우리가 알 수 없습니다.**

이름 대신 **맥락**으로 거는 룰 둘을 더했습니다. 셋이 겹치지 않는 사실을 봅니다.

| flag | 무엇을 보나 | 겨냥하는 단계 |
|---|---|---|
| `shell_spawned` | **무엇이** 실행됐나 (`fields.Image` 가 셸) | Stage 3 (키오스크 이탈) |
| `execution_from_unusual_path` | **어디에** 있나 (비시스템 볼륨·쓰기 가능 경로) | Stage 2 |
| `unexpected_parent_process` | **누가** 실행시켰나 (`fields.ParentImage`) | Stage 2·3 |

실제로 갈리는지 확인했습니다.

```
USB 에서 직접 실행 (E:\banker.exe)   execution_from_unusual_path
Temp 로 복사 후 실행                 execution_from_unusual_path
explorer 가 띄운 것                  unexpected_parent_process
셸 획득 (cmd.exe)                    shell_spawned
정상 POS 앱 / svchost                (없음)   ← 걸러진다
```

**부정으로 쓰지 않은 것이 핵심입니다.** "`C:` 로 시작하지 않으면"이 한 줄로
끝나는데, 그렇게 쓰면 형식 가정이 틀렸을 때 **전량이 걸립니다** — Sysmon
설정에 따라 `Image` 가 `\Device\HarddiskVolume2\...` 로 올 수 있고 그러면
정상 프로세스 생성이 모두 신호가 되어 05단계 쿼터를 통째로 태웁니다.
긍정으로 열거하면 형식이 다를 때 아무것도 안 걸립니다 — 그것도 문제지만
조용히 넘어가는 쪽이지 필터를 죽이지는 않습니다.

**실물 Sysmon 로그를 한 번도 파싱해 본 적이 없어서 내린 판단입니다.**
두 이미지 다 Sysmon 이 설치돼 있지 않습니다(기본 탑재가 아닙니다).

#### 넣지 않은 것 — 화이트리스트 역탐지

가장 확실한 방법은 **"알려진 정상 프로세스 목록을 제외한 전부"** 입니다.
키오스크는 도는 프로세스가 극도로 제한적이라 이 방식이 잘 듣습니다.
넣지 않은 이유가 둘입니다.

**① 목록이 환경마다 다릅니다.** `_flags.yaml` 은 **전역 어휘**이고, 어느
키오스크에 무엇이 설치돼 있는지는 그 이미지의 사실입니다. 여기에 특정 랩의
프로세스 목록을 박으면 다른 환경에서 전량이 걸립니다. `privileged_groups`
처럼 런타임에 읽는 자리를 두는 선례가 있지만, 그 목록 자체를 어디서 받을지가
정해져 있지 않습니다.

**② 그 목록은 Stage 0 베이스라인에서 나옵니다.** 설계서가 "베이스라인 없이는
이상을 판단할 수 없다"며 D3 에서 먼저 수집하기로 한 데이터인데, **아직
수집되지 않았습니다.** 지금 목록을 지어내면 그것이 검증되지 않은 가정으로
파이프라인 한가운데 박힙니다.

**베이스라인이 들어오면 이것이 가장 강한 필터가 됩니다.** 그때 필요한 것은
부정 매처 하나와 목록을 04단계에 넘길 통로이고, 둘 다 지금 없습니다.

#### 남겨 둔 구멍 하나 — `evtx:System` 7034·7036

`T1543.003` 이 요청하는데 받을 flag 가 없습니다. **일부러 남겼습니다.**

7036(서비스 상태 변경)은 정상 부팅에서만 수백 건이 나옵니다. flag 를
만들면 필터가 죽고, 안 만들면 이 요청이 모델에 닿지 않습니다. 어느 쪽이
나은지는 **실제 키오스크 로그의 분포를 봐야** 정해지고 지금 그 데이터가
없습니다. 7034(비정상 종료)는 드물지만 같은 채널·같은 판단이라 함께 뒀습니다.

`_KNOWN_GAPS` 에 사유와 함께 적혀 있어, 목록에서 지우면 테스트가 막습니다.

#### 상하위 기법이 남남이다

설계서가 Stage 2에 `T1059`(상위), Stage 3에 `T1059.003`(하위)를 쓰고,
Stage 4에 `T1078`(상위)을 쓰는데 우리에겐 `T1078.003`(하위)도 있습니다.
**`attack.py` 에 상하위 관계를 아는 코드가 없어 둘은 완전히 다른
문자열입니다.** 매핑을 양쪽 다 써 두어 어느 쪽이 와도 선별되게 했지만,
02단계가 어느 쪽을 내느냐에 따라 **선별되는 아티팩트 집합이 달라집니다.**
정답 데이터의 `expected_techniques` 와 02단계 출력이 어긋나면 기법 식별
재현율이 실제보다 낮게 집계됩니다. D1에서 못 박아야 할 항목입니다.

### 매핑 — 검증되지 않은 채 채운 항목 (키오스크 시나리오, 2026-08-23)

키오스크 침해사고 시나리오([[kiosk-scenario-goal]] 참고 — 웹셸은 벤치마크
케이스일 뿐 목표가 아니다)가 05단계에 도달하지 못해 막혀 있던 세 지점을
메꾸려고 `T1091`·`T1200`·`T1547.001` 매핑을 새로 채웠습니다. **실제
증거 이미지로 대조하지 않은 "임의 채움"이며, 아래는 신뢰도가 낮은 순으로
적습니다.**

- **`T1200.yaml`의 이벤트 ID(20001, 10000)** — 공개 DFIR 자료에서 흔히
  인용되는 UserPnp/Kernel-PnP 이벤트 ID를 추정으로 넣었습니다. 이
  프로젝트의 실제 이미지로 대조한 적이 없습니다. 실증거에서 걸리지
  않으면 매핑이 아니라 모델 문제로 잘못 집계될 수 있습니다.
- **`T1547.001.yaml`이 HKLM Run/RunOnce만 봅니다** — HKCU 쪽(사용자별
  자동실행)은 `NTUSER.DAT`가 필요한데 지금 카탈로그는 볼륨 공용 하이브
  (`SYSTEM`·`SOFTWARE`)만 지원합니다 — 사용자별 하이브 다중 매칭이
  아직 없습니다. 단일 로컬 계정으로 상시 로그인되는 키오스크에서는 영향이
  작을 것으로 보고 우선 이렇게 두었으나 검증하지 않은 가정입니다.
- **`T1091.yaml`의 `$UsnJrnl` 요청에 `path_prefix`가 없습니다** — 이동식
  매체에서 옮겨진 파일이 어느 경로에 놓일지 미리 알 수 없어 확장자만
  걸었습니다. 바쁜 볼륨에서는 `file_created` 플래그가 붙는 레코드가
  많아질 수 있고, 05단계 `record_filter`의 `DEFAULT_LIMIT`(60건) 안에
  더 급한 신호가 밀릴 위험이 있습니다.

**영향** — 세 매핑 다 "선별이 이 아티팩트를 보게는 한다"는 배선 확인
수준입니다. 재현율·정확도는 실제 키오스크 증거로 돌려 보기 전까지
모릅니다. 대조하면 이 절을 갱신하십시오(`work-guide.md` 3.2와 같은
의무 — 다만 이번 건은 매핑 도메인 지식이라 파서처럼 자동화된 대조
도구는 없고, 사람이 직접 아티팩트를 열어 확인해야 합니다).

#### 2차 추가분 5종 (같은 날, `mapping_table_version` 0.7)

`T1547.004`·`T1546.008`·`T1078.003`·`T1112`·`T1562.001`을 더 채웠습니다.
**같은 성격의 임의 채움이며**, 신뢰도가 낮은 순으로 적습니다.

- **`T1112.yaml`의 `AssignedAccessConfiguration` 경로** — 다중 앱 키오스크
  설정이 이 키에 남는다는 것은 공개 자료 기준 추정입니다. 단일 앱 키오스크는
  다른 자리(LogonUI 계열)를 쓴다고 알려져 있는데 확인하지 않았습니다.
- **`T1112`는 기법 전체가 아니라 "정책·잠금 설정 변조"로 좁혀져 있습니다.**
  `path_prefix` 없이 SOFTWARE를 요청하면 156,716건이 나와 선별이 의미를
  잃기 때문입니다(`_artifacts.yaml` 주석). 랜섬웨어의 복구 설정 변조 같은
  다른 T1112 용례는 이 매핑으로 잡히지 않습니다. 매핑 이름과 실제 범위가
  어긋나 있다는 뜻이므로, 다른 시나리오를 붙일 때 반드시 넓혀야 합니다.
- **`T1546.008`의 접근성 프로그램 7종 목록** — `sethc`·`utilman`은 확실하나
  `AtBroker`·`DisplaySwitch`는 Windows 버전에 따라 존재하지 않을 수 있습니다.
  없는 경로를 걸면 조용히 0건이 되고, 그것이 "흔적 없음"과 구별되지 않습니다.
- **`T1562.001`의 서비스 이름 5종** — `WinDefend`·`wscsvc`·`EventLog`는
  표준이지만 `Sense`·`WdNisSvc`는 Defender ATP 구성에 따라 없을 수 있습니다.

**영향** — 위와 같습니다. 다만 이번 5종은 앞의 3종과 달리 **evtx·$MFT 경로가
섞여 있어 일부는 실제로 05단계까지 도달합니다.** 아래 절에 가른 결과가 있습니다.

#### 새 매핑이 05단계에 도달하는지 (실측)

관문 4를 기법별로 확인한 결과입니다. 대표 레코드를 만들어
`flagging.apply_all` → `allocation.allocate_records`(카탈로그의 실제
`signal_source`를 준 상태)를 태웠습니다.

| 기법 | Tier 1 아티팩트 | 플래그 | 05단계 전달 |
|---|---|---|---|
| T1078.003 | evtx:Security (4624·4625·4672) | 5/5 | **5/5** (`logon_success`/`logon_failed` 신설) |
| T1562.001 | evtx:System (7040·7045) | 5/5 | **5/5** (`service_config_changed` 신설) |
| T1546.008 | $MFT (접근성 바이너리) | 5/5 | **5/5** (`timestamp_mismatch`) |
| T1546.008 | registry:SOFTWARE (IFEO) | 0/5 | **5/5** (`signal_source: scope`) |
| T1547.004 | registry:SOFTWARE (Winlogon) | 0/5 | **5/5** (`signal_source: scope`) |
| T1112 | registry:SOFTWARE·SYSTEM | 0/6 | **6/6** (`signal_source: scope`) |
| T1547.001 | registry:Amcache | 0/5 | **5/5** (`signal_source: scope`) |

**레지스트리 쪽 플래그가 0인 것은 그대로입니다.** 달라진 것은 그것이
탈락 사유가 아니게 된 점입니다 — 05단계 배분이 아티팩트별로 자리를
나누므로 질문이 "신호냐"에서 "몇 자리를 줄 것이냐"로 바뀌었습니다(6-7).

이 매핑들을 처음 쓸 때는 `T1112`가 Tier 1이 레지스트리뿐이라 **매핑은
있으나 사실상 작동하지 않는 유일한 기법**이었습니다. `T1547.004`도
레지스트리가 본체이고 `$MFT`는 Tier 2(루프백 없음)라 같은 상태였습니다.
둘 다 배분이 들어오면서 풀렸습니다.

`flagging.py`에 "Winlogon 하위면 신호" 같은 룰을 넣어 때우지 않은 것은
잘한 선택으로 남았습니다. 그렇게 했으면 지금 배분이 들어온 뒤 룰을 도로
걷어내야 했을 것이고, 그 사이 나온 수치는 플래그가 부풀려진 값이었을
것입니다.

---

## 3. 파이프라인 구조

### Tier 2 루프백이 없다

03단계는 아티팩트를 Tier 1(즉시 읽음)과 Tier 2(조건부 유예)로 나누지만,
**그 조건을 평가하는 루프가 구현돼 있지 않습니다.** `deferred` 항목은
전부 미발동으로 처리됩니다.

**영향** — 예전에는 보고서에 `Tier 2 조건 미충족`이라고 적혔는데, 실제로는
**조건을 평가한 적이 없습니다.** C-100-C에서 `$MFT`가 `deleted` 69건을
냈는데도 `$UsnJrnl`이 "조건 미충족"으로 기록된 것이 그 예입니다 —
평가했는데 안 걸린 것처럼 읽혀 사실과 달랐습니다.

**표기는 고쳤습니다.** 이제 `Tier 2 루프백 미구현으로 미평가 (조건: …)`로
적힙니다. `trigger` 문장은 그대로 실으므로, 읽는 사람이 "무엇을 보면
이걸 봤어야 하는가"를 알 수 있습니다.

**루프백 자체는 여전히 미구현이며 명시적 비목표입니다**(`work-guide.md`
1.4). 표기가 사실과 맞아진 것이지 기능이 생긴 것은 아닙니다.

### 07단계는 LLM을 쓰지 않는다

스펙은 sLLM으로 적었으나 의도적으로 템플릿 렌더링만 합니다. 검증을
통과한 문장을 모델이 다시 쓰게 하면 마지막 단계에서 환각이 재유입되고,
앞의 모든 검증이 무의미해집니다.

**영향** — 한계가 아니라 설계 결정입니다. 다만 스펙과 다릅니다.

---

## 4. 결과 해석에 영향을 주는 것 — 미해결

### 4-1. 04단계의 스킵이 보고서에 도달하지 않았다 (**해결됨**)

기록으로 남깁니다. 고치기 전 보고서는 이랬습니다.

```markdown
## 분석 범위 한계

카탈로그의 모든 아티팩트를 확인했습니다.
```

같은 실행에서 04단계는 3개 중 1개만 읽었습니다. **누락이 아니라 거짓
진술이었습니다.** 침해사고 보고서에서 "보안 이벤트 로그까지 봤는데
깨끗하다"와 "보안 이벤트 로그를 못 구했다"는 완전히 다른 문서입니다.

원인은 `_limits()`가 `03_selection.json`의 `excluded`·`deferred`만 본 것
입니다. `selected`에 들어간 뒤 04단계에서 스킵된 것을 볼 경로가 없었고,
스킵은 `errors.jsonl`에만 남았습니다.

**조치 — 네 가지.**

1. **04가 스킵을 매니페스트에 남깁니다.** `_manifest.json`의 `skipped`에
   아티팩트·사유코드·메시지가 들어갑니다. 매니페스트는 04가 자기 일을
   적는 곳이므로 "안 한 일"도 여기가 맞습니다. `errors.jsonl`은 전 단계가
   공유하는 집계용이라 07이 그것을 파싱하면 에러 로그 형식에 묶입니다.
2. **07이 차집합으로 검산합니다.** `selected` − (`files` ∪ `skipped`)에
   남는 것이 있으면 04가 기록을 빠뜨린 것이므로 "사유를 남기지 않았습니다"
   로 표에 올립니다. 새 실패 유형이 생겨도 조용히 사라지지 않습니다.
3. **무엇을 봤는지도 적습니다.** "안 본 것"만 적으면 범위가 반쪽입니다.
   `### 확인한 아티팩트`에 레코드 수와 부분 판독 여부가 실립니다.
4. **"모든 아티팩트를 확인했습니다"를 없앴습니다.** 선별 자체가 놓친 것은
   이 도구가 알 수 없으므로 단정할 수 없습니다. 대신 선별 범위 안에서의
   진술임을 명시합니다.

부수적으로, 읽은 아티팩트가 `deferred`에도 있으면 "확인함"과 "확인 못 함"
양쪽에 실리던 문제도 막았습니다. 산출물이 있다는 사실을 더 강한 증거로
봅니다.

사유는 **분석가가 할 일이 다르므로** 나눠 적습니다.

| 사유 코드 | 보고서 문구 | 분석가의 조치 |
|---|---|---|
| `artifact_not_found` | 증거에 없음 (수집 누락) | 다시 수집 |
| `empty_artifact` | 파일이 0바이트 (추출 확인 필요) | 다시 추출 |
| `parser_missing` | 본 버전 미지원 (파서 없음) | 다른 도구로 확인 |

회귀 테스트는 `tests/test_report.py`("04가 읽지 못한 것이 보고서에
실리는가")에 있습니다.

### 4-1-1. 남은 것 — 선별이 놓친 것은 여전히 알 수 없다 (미해결)

4-1이 닫은 것은 **"선별했는데 못 읽은 것"**입니다. 그보다 한 겹 위는
그대로입니다 — **03단계가 애초에 선별하지 않은 것**은 보고서에 나타나지
않습니다.

`_artifacts.yaml` 카탈로그에 있는 아티팩트는 `excluded`로 실리지만,
**카탈로그에 없는 것은 존재하지 않는 것과 같습니다**(`mapping-guide.md`).
지금 카탈로그는 `$MFT`·`$UsnJrnl`·evtx 2종·registry 2종·`prefetch`·
`$LogFile`뿐입니다. ShimCache·Amcache·Jump List·브라우저 이력, 그리고
레지스트리 중에서도 SAM·SECURITY·NTUSER는 목록에 없으므로 "확인하지 못한
아티팩트"에도 오르지 않습니다.

**영향** — 보고서의 "확인하지 못한 아티팩트" 표는 **이 도구가 아는 범위
안에서만 완전합니다.** 그래서 템플릿이 "선별된 아티팩트를 모두 확인했습니다"
라고만 쓰고 "카탈로그의 모든 아티팩트"라고 쓰지 않습니다. 문구를 좁힌
것이지 문제를 푼 것은 아닙니다.

이것은 선별 방식 자체의 리스크이며 **재현율로 측정할 대상**입니다
(`work-guide.md` 1.2). 매핑을 채워 나가면서 재현율이 어떻게 변하는지
`mapping_table_version`으로 추적합니다.

### 4-0. 0바이트 껍데기가 진짜 저널을 가렸다 (**해결됨**)

기록으로 남깁니다. **증거가 있는데도 0건을 보고하던 문제**입니다.

실측(`evidence/[root]`, Windows 7 시스템 볼륨):

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 읽은 파일 | `$Extend\$UsnJrnl` (0바이트) | **`$J` (34,406,400바이트)** |
| `record_count` | **0** | **306,857** |

원인은 두 가지가 겹친 것이었습니다.

**① 탐색이 껍데기를 먼저 잡았다.** `_probe`가 `relative_paths`를 순서대로
훑다가 `$Extend/$UsnJrnl`에서 멈췄습니다. 파일이 **존재하되 0바이트**인데
존재만 보고 채택했고, `filenames`에 등록된 `$J`에는 도달하지 못했습니다.

`$UsnJrnl:$J`는 콜론이 파일명에 못 들어가 도구마다 다르게 저장되는데,
**추출본이 껍데기(이름 없는 `$DATA`)와 알맹이(`$J`)를 둘 다 내놓는 경우**가
이 조합입니다.

**② `$UsnJrnl` 파서가 빈 스트림에 침묵했다.** evtx는 같은 상황에서
`ValueError`로 중단하고 `$MFT`는 시그니처로 걸리는데 이것만 조용했습니다.

**조치** — 셋 다 했습니다.

1. `_probe`·`_search`가 **0바이트 후보를 건너뜁니다.** 크기 0은 내용이
   다른 스트림에 있다는 신호이므로 다음 후보로 넘어갑니다.
2. 건너뛴 껍데기를 **버리지 않고 남깁니다.** `Located.empty_candidates`에
   담겨 매니페스트의 `source_empty_skipped`로 나가고, 콘솔에 경고가
   찍힙니다. **추출이 잘못됐다는 진단이므로 조용히 넘어가면 안 됩니다.**
3. 후보가 **전부 0바이트면** `EmptyArtifact`로 중단합니다. `ArtifactNotFound`를
   상속해 04단계 처리는 같지만 메시지가 다릅니다 — "수집 안 됨"과
   "수집됐는데 알맹이가 없음"은 분석가가 취할 조치가 다르기 때문입니다.
   `$UsnJrnl` 파서에도 같은 취지의 빈 스트림 가드를 뒀습니다.

회귀 테스트는 `tests/test_parse_scaffold.py`("0바이트 껍데기")와
`tests/test_usn_parser.py`에 있습니다.

### 4-0-1. `parse_errors`가 스캔 횟수를 셌다 (**해결됨**)

기록으로 남깁니다. 같은 실측에서 `parse_errors: 62,969`가 나왔습니다.
숫자만 보면 저널이 심하게 깨진 것 같지만 실제로는 이랬습니다.

- 정상 레코드 **306,857건 / 스키마 위반 0**
- USN 증가분과 오프셋 증가분이 정확히 일치 (33,901,024 = 33,901,024)
- 재동기화 62,969회는 **전부 한 덩어리** — `@0x2055000`부터 503,752바이트

그 구간은 저널이 아니라 **phpMyAdmin 언어 파일 텍스트**입니다(이 볼륨의
`APM_Setup` 잔재). `$J`는 스파스 파일이라 추출 도구가 유효 데이터 끝
너머 클러스터까지 담았고, 거기 남아 있던 삭제 파일 내용이 딸려온 것입니다.

파서 동작 자체는 정확했습니다 — 가짜 레코드를 만들지 않고 8바이트씩
재동기화하며 지나갔습니다. 틀린 것은 **집계 단위**였습니다.
`503,752 / 8 = 62,969`로 나머지 없이 딱 떨어지는 것이, 저 숫자가
"오류 건수"가 아니라 **"걸어간 횟수"**라는 증거입니다.

**조치** — 연속된 실패를 한 구간으로 묶고 규모를 바이트로 분리했습니다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| `parse_errors` | 62,969 | **1** |
| `unreadable_bytes` | — | **503,752** |
| 로그 | `재동기화 62969회` | `@0x2055000 부터 503752바이트를 읽지 못했습니다` |

evtx 파서가 이미 쓰던 방식과 같습니다. 구간 수만으로는 8바이트가 깨진
것과 500KB가 통째로 저널이 아닌 것을 구별할 수 없으므로 **둘을 함께**
냅니다. 저널 중간에 진짜 손상이 여러 군데 생기면 구간 수가 실제로 여러
개로 나옵니다 — 그게 원래 알고 싶었던 값입니다.

회귀 테스트는 `tests/test_usn_parser.py`에 있습니다(구간 1곳이 1건으로,
떨어진 손상 둘은 2건으로, 규모는 `unreadable_bytes`로).

### 4-0-2. 시각 없는 신호가 05단계를 멈춰 세웠다 (**해결됨**)

기록으로 남깁니다. **타임스탬프가 조작된 증거에서 도구가 죽는** 문제였습니다.

`record_filter.select_records`가 시각을 못 읽은 신호 레코드에 tz-naive
`datetime.max`를 정렬 자리로 주고, 시각이 있는 신호(tz-aware)와 같은
리스트에서 정렬했습니다.

```
TypeError: can't compare offset-naive and offset-aware datetimes
```

**드문 경우가 아닙니다.** `$SI` 타임스탬프가 전부 0이면
`filetime_to_datetime`이 `None`을 주고 04단계가 `zero_timestamp`를
붙입니다 — 즉 **시각이 없다는 사실 자체가 그 레코드를 신호로 만듭니다.**
안티포렌식 흔적을 찾는 필터가 바로 그 흔적에서 멈추는 구조였습니다.
목업 데이터에는 타임스탬프가 전부 정상이라 드러나지 않았습니다.

**조치** — 정렬 자리를 `NO_TIME`(tz-aware `datetime.max`)로 상수화하고,
"앵커가 되지 못한다"는 판정을 `tzinfo` 검사가 아니라 그 상수와의 동일성
비교로 바꿨습니다. 시각 없는 신호는 **전달되되 주변 창은 열지 않습니다** —
"그 시각 주변"이라고 할 시각이 없기 때문입니다.

회귀 테스트는 `tests/test_interpret.py`에 있습니다(시각 없는 신호가
버려지지 않고 맨 뒤로 정렬되는지, 창을 열지 않는지).

### 4-3. 아티팩트가 19종이 되면서 아티팩트당 자리가 얇아졌다 (2026-08-25)

`DEFAULT_LIMIT`(60건)은 웹셸 케이스에서 정해진 값입니다. 그때는 선별되는
아티팩트가 대여섯이라 하나당 8~12건이 갔습니다.

K-001 은 **19종**이 선별됩니다. 같은 60건을 나누면 이렇게 됩니다(측정).

| priority | 아티팩트당 |
|---|---|
| 1 | **4건** |
| 2 | 2건 |
| 3 | 1건 |

**굶는 아티팩트는 없습니다** — 배분이 공평하게 작동하고, `priority` 도
의도대로 갈립니다. 문제는 절대량입니다. Tier 1 아티팩트 하나당 4건으로
7단계 침해 체인을 재구성해야 합니다.

**고치지 않았습니다.** 올릴지 말지는 모델의 컨텍스트 한계(qwen2.5-7b)와
맞물린 판단이고, 그 데이터가 없습니다. 후보는 셋입니다.

1. `--limit` 을 올린다 — 컨텍스트가 감당하는지 재 봐야 한다
2. 매핑의 `priority` 를 더 정교하게 매긴다 — 지금은 대부분 1 이라 변별력이 낮다
3. 아티팩트를 줄인다 — 시나리오가 요구하는 것이라 줄일 수 없다

실제 K-001 증거가 나오면 1번을 먼저 재 보는 것이 순서로 보입니다.

**2026-08-26 갱신 — 1번은 이제 재 볼 필요가 없습니다.** `--limit` 은
상한이고, 컨텍스트가 감당하는지는 토큰 예산이 실제 글자 수로 판정합니다
(아래 "05단계 프롬프트가 컨텍스트 창을 넘었다"). 올려도 창을 넘지는
않습니다 — 대신 **넘는 지점에서 예산이 도로 깎으므로 절대량 문제는 그대로**
입니다. 32,768 창에서는 44건이 천장이었습니다. 진짜 답은 창이 더 큰
모델이거나 레코드 다이어트입니다.

### 4-2. 신호 플래그가 필터 역할을 못 한다 (**배분으로 완화됨**)

**`$UsnJrnl`만의 문제가 아닙니다.** 시스템 볼륨을 처음 돌려 보니
`$MFT`도 같은 상태였습니다.

| 아티팩트 | 볼륨 | 신호 비율 |
|---|---|---|
| `$UsnJrnl` `deleted` | 개발 장비 저널 | 355,850건 중 37% |
| `$MFT` `timestamp_mismatch` | `evidence/test` (데이터) | 154건 중 **0%** |
| `$MFT` `timestamp_mismatch` | `evidence/[root]` (시스템) | 2,034건 중 **81%** |

`$MFT` 쪽 원인은 **OS 설치**입니다. Windows Setup이 `$SI`를 설치 미디어의
빌드 시각으로 보존하고 `$FN`은 설치 시각으로 쓰기 때문에
`si_btime < fn_btime`이 OS 파일 전체에서 정상적으로 성립합니다. 측정과
근거는 `artifact-notes.md`("시스템 볼륨에서는 좁힌 룰도 81%가 걸린다").

**즉 데이터 볼륨에서 0%였던 룰이 시스템 볼륨에서 81%입니다.** 실제 사고
조사 대상은 시스템 볼륨입니다.

측정치는 `artifact-notes.md`("USN 신호 플래그는 $MFT보다 훨씬 많이
걸린다")도 참조.

`evidence/$J` 355,850건 중 `deleted` 37%, `file_created` 23%.
확장자와 20분 시간창으로 좁혀도 2,042건 중 **1,281건(63%)이 신호**로
남습니다. `record_filter.DEFAULT_LIMIT`은 60입니다.

**룰이 틀린 것이 아닙니다.** 플래그는 정확합니다. 이름은 같지만
카디널리티가 다릅니다.

- `$MFT`의 `deleted` = **상태**("지금 미할당"). 볼륨 크기에 묶임
- `$UsnJrnl`의 `deleted` = **사건**("지워졌다"). 시간에 비례해 무한정 쌓임

**영향이 조용해서 위험합니다.** 신호 1,281건 중 60건만 05단계로
넘어가고 나머지는 모델이 아예 보지 못합니다. 그런데 **06단계가 이것을
잡지 못합니다** — 환각이 아니기 때문입니다. 모델이 없는 것을 지어낸
것이 아니라, 있는 것을 못 받은 것입니다. 결과는 "sLLM이 탐지에
실패했다"로 집계되는데 실제 원인은 필터가 증거를 안 넘긴 것입니다.

> **벤치마크 수치를 내기 전에 반드시 정해야 합니다.** 정하지 않고 돌리면
> 나온 환각률·재현율이 모델 성능이 아니라 필터 설정을 재게 됩니다.

검토 중인 방향 셋:

1. 04단계에서 `file_entry` → `$MFT` 상호 참조로 경로 복원 —
   가장 효과적이나 아티팩트 간 의존이 생김
2. 05단계 `record_filter`에서 상호 참조 — 04단계 순수성 유지
3. 이진 선택에서 순위 매기기로 — 고볼륨 아티팩트 일반의 문제로 다룸
   (**채택·구현됨**, 6장)

임계값만 조정하는 것은 답이 아닙니다. `$MFT`의 `MISMATCH_PAIRS`처럼
**물리적 근거가 있는 판별자**를 찾은 뒤에 좁혀야 합니다.

3번을 4-2-1과 함께 설계·구현한 결과가 **6장**입니다. 아티팩트별 쿼터가
생겼으므로 시끄러운 아티팩트가 조용한 아티팩트를 굶기는 일은 없어졌습니다.
**남은 것은 쿼터 안의 문제입니다** — `$UsnJrnl` 신호 1,281건 중 배분받은
자리만큼을 무엇으로 채울지는 여전히 시간창 근접성에 기댑니다(6-6).

### 4-2-1. 넘길 60건을 고르는 것이 파일명 순서였다 (**해결됨**)

4-2가 "1,281건 중 60건"이라고 쓴 그 60건이 **어떻게 뽑히는지**는 따로
봐야 했습니다. 예전 `select_records`는 신호를 정렬하지 않고 `[:limit]`으로
잘랐습니다. 그 순서는 `io.read_parsed_records`가 읽은 순서, 즉
`sorted(glob("*.jsonl"))`입니다.

```
evtx_security → evtx_system → mft → usnjrnl
```

**알파벳순입니다.** 앞의 파일이 60자리를 채우면 뒤의 아티팩트는 신호가
몇 건이든 모델에 한 건도 도달하지 않습니다. 4-2의 실측 규모(신호
1,281건)에서는 `$UsnJrnl`이 통째로 잘립니다 — 저널이 가장 많은 신호를
내는 아티팩트인데도 그렇습니다.

4-2와 원인이 다릅니다. 4-2는 **몇 건을 넘길지**의 문제이고 이것은
**어느 것을 넘길지**의 문제입니다. 상한을 올려도 순서 편향은 남습니다.

**영향** — 4-2와 같았습니다. 06단계가 못 잡고 "sLLM 탐지 실패"로
집계됩니다. 다만 이쪽은 **아티팩트 단위로 통째로** 빠지므로, 아티팩트별
재현율을 내면 값이 파일명 알파벳순과 상관됐습니다.

**해결** — 6장의 배분이 아티팩트마다 자리를 먼저 나누므로 앞 파일이 뒤
아티팩트를 통째로 굶기는 경로가 없어졌습니다. 파일을 읽는 순서는 그대로지만
배분 결과가 그 순서에 의존하지 않습니다(`test_allocation.py`의
`test_the_allocation_does_not_depend_on_insertion_order`).

---

## 5. 평가

### 정답 데이터가 없다

`ground_truth.json`은 스펙 예시에서 역산한 것이라 자기채점입니다.
`evaluate.py`가 그 사실을 경고로 띄웁니다.

### 실제 모델로 처음 돌렸다 (2026-08-24, `qwen2.5:7b`)

그전까지 케이스 산출물은 전부 `StubBackend` 리플레이였습니다. 키오스크
사고 이미지(`evidence/0824test.001`, 60GB)와 로컬 Ollama `qwen2.5:7b`로
**01→07 전 구간을 실제 추론으로 관통시켰습니다**(케이스 `K-002`).

관통 자체는 됐지만 **findings 0건**입니다. 아래가 그 사이에 드러난
것들이고, 셋은 고쳤고 셋은 남았습니다.

#### 고친 것

**① `findings.schema.json`의 `ref` 패턴에 `PF`가 없었다.**
`refs.py`와 `parsed_record.schema.json`에는 있는데 여기만 빠져 있었습니다
(프리패치 파서를 붙이며 한 군데를 놓친 것). `input_refs`는 **모델이 아니라
우리 코드가** 배분 결과에서 만드는 값이라, 프리패치가 05단계에 닿는 케이스는
모델이 무엇을 내든 **원리적으로 통과할 수 없었습니다.** 더 나쁜 것은 그
실패가 `schema_violation`으로 기록돼 **모델의 환각으로 집계된다**는
점입니다. 동결 대상 스키마를 고쳤으므로 전체 공지 대상입니다.
`tests/test_schemas.py`가 이제 `refs.py`의 접두어와 스키마 패턴을 양방향으로
대조합니다 — 이 결함이 테스트를 통과했던 이유가 두 스키마의 `ref`가 서로
다른 자리(`$defs` vs `properties`)에 적혀 있어서였습니다.

**② 05단계 배분이 실물 규모에서 멈췄다.** 앵커마다 거리를 재는 구현이라
연산량이 `비신호 레코드 × 앵커 시각`이었습니다. 이 이미지에서
`12,470 × 8 × 337,264 ≈ 3.4 × 10¹⁰`회, 약 **56분**입니다. 목업은 레코드가
수십 건이라 드러날 수 없었습니다. 정렬 + 이분 탐색(`AnchorIndex`)으로
바꿨고, **무차별 대입과 비트 단위로 같은 값**임을 표본 1,200건으로
확인했습니다. 처음 `timestamp()` float로 짰을 때는 1,200건 중 750건이
갈렸습니다 — 2020년대 epoch 초에서 마이크로초 자리가 float 유효숫자
한계에 걸립니다. 정수 마이크로초로 재고 마지막에 한 번만 나눕니다.

**③ 타임아웃을 올릴 방법이 없었다.** 120초 고정이라 05단계가 네 번 연속
`timeout`으로 죽었습니다. 02·05에 `--timeout`을 노출했습니다
(`src/common/llm.py`의 `DEFAULT_TIMEOUT`). 이 장비에서 05단계 한 번 호출이
120초를 넘습니다.

#### 남은 것

**④ `entities`에 없는 값이 들어오는데 스키마가 못 잡는다.** 프롬프트가
"입력에 명시된 것만 넣으십시오. 추측하지 마십시오"라고 적어 두었는데도
두 번 다 지어냈습니다 — `C:\Windows\System32`(K-001),
`C:\Program Files\BankingApp\`(K-002). 둘 다 입력 서술에 없는 경로입니다.
**`entities.paths`의 첫 항목은 03단계가 기준 경로로 씁니다.** 지어낸 경로가
선별 범위를 정하는 자리까지 갑니다. 06단계는 `findings`의 `claims`만
검증하므로 이 층은 아무도 안 봅니다.

**⑤ 재시도가 `temperature 0`에서 무의미하다.** 오류를 프롬프트에 되먹이는
구조는 있는데, 결정론적이라 모델이 같은 답을 반복합니다. K-001에서
`T1200.001`(존재하지 않는 하위기법)을 **다섯 번 연속** 냈습니다.
`--temperature 0.4`로 올리자 2회차에 통과했습니다. 재현성과 재시도가
상충하는 자리이고, 지금은 사람이 온도를 골라야 합니다.

**⑥ Ollama 기본 `num_ctx`가 4096이었다 (고침).** `OllamaBackend`가
`num_ctx`를 지정하지 않아 모델이 4,096 토큰 창으로 떴습니다(`/api/ps`의
`context_length`로 확인). `qwen2.5:7b` 자체는 32,768을 지원합니다.
05단계는 레코드 60건을 실어 보내므로 **프롬프트가 조용히 잘렸습니다** —
모델이 못 본 레코드에 대해 "흔적 없음"을 말해도 06단계는 검증할 수단이
없습니다. `DEFAULT_NUM_CTX = 32768`로 명시하고 `--num-ctx`로 노출했습니다.

**이것이 findings 0건의 원인이 맞았습니다.** 같은 입력으로 다시 돌리자
(케이스 `K-003`) findings가 1건 나왔습니다.

#### 실측 소요 시간 (`K-003`, 01→07 전 구간)

| 단계 | 초 |
|---|---|
| 02 정규화 (실제 추론) | 108.7 |
| 03 선별 | 0.5 |
| 04 파싱 (60GB 이미지, 570건) | 4.2 |
| 05 해석 (실제 추론, 60건 전달) | 215.4 |
| 06 검증 | 0.3 |
| 07 보고 | 0.4 |
| **합계** | **329.5 (5분 30초)** |

**04단계는 선별 범위에 좌우됩니다.** 이 실행은 evtx·레지스트리만 골라
4.2초였고, `$MFT` 전체가 걸린 실행(`K-002`, 98,151건)은 **약 4분**이었습니다.
시간을 지배하는 것은 이미지 크기가 아니라 **모델 호출 두 번**입니다 —
전체의 98%입니다.

### ⑤ 에 대한 조치 — 출력을 디코딩 단계에서 묶었습니다 (**실측함**, 2026-08-31)

위 ⑤ 는 재시도가 `temperature 0` 에서 무의미하다는 것이었습니다. 형식은
맞고 실재하지 않는 `T1200.001` 을 다섯 번 연속 냈고, `attack.is_known` 이
매번 잡아냈지만 잡아 봐야 같은 답이 다시 왔습니다. **잡는 것과 못 만들게
하는 것은 다릅니다.**

Ollama 의 `format` 에 JSON Schema 를 주면 문법으로 변환해 디코딩 단계에서
강제합니다. `src/common/llm.py` 가 이 필드를 보내지 않고 있었습니다. 열었고,
02 는 기법 ID 를 `attack.KNOWN_TECHNIQUES` 의 38개 enum 으로, 05 는
`$defs.ref` 를 **이번 배치에 실제로 보낸 ref** 의 enum 으로 묶습니다.

05 쪽이 더 큽니다. 프롬프트가 "이 목록에 없는 ref 를 쓰면 기각됩니다"라고
부탁해 왔고 06단계가 사후에 `ref_not_in_input` 으로 걸러 왔는데, 우리는
무엇을 보냈는지 **이미 알고 있었습니다**. 같은 목록을 enum 으로 주면 그
ref 가 나올 토큰 경로가 없습니다.

출력 스키마는 손으로 쓰지 않고 `llm.output_schema` 가 동결 스키마에서
파생시킵니다. 손으로 쓰면 진실이 둘이 되고, 갈라지는 순간 **모델은 시킨
대로 냈는데 검증에서 떨어져 환각률에 집계됩니다** — 이 절 ① 에서 이미 한 번
겪은 실패입니다. `tests/test_constrained_output.py` 가 부분집합인지,
헤더를 요구하지 않는지, enum 이 아는 목록과 같은지, 그리고 배치별 enum 이
캐시된 동결 스키마를 오염시키지 않는지를 봅니다.

#### 실측 (`qwen2.5:7b`, Ollama 0.32.14, RTX 3060 Laptop 6GB)

**강제는 진짜입니다.** 답이 명백히 `T1059.004` 쪽인 질문에 enum 을
`["T1505.003"]` 하나로 좁히자 그것만 나왔습니다. 같은 질문을 제약 없이
물으면 `T1059.004` 가 옵니다. 부탁과 강제의 차이가 실물로 갈립니다.

| 구간 | 제약 있음 | 제약 없음 |
|---|---|---|
| 02 정규화 | 66~76초 | (기준선 108.7초) |
| 05 해석 (레코드 2건) | **84초** | 157초 |

**제약이 더 빨랐습니다.** 문법이 탐색을 잘라 내고 출력이 짧아집니다.
느려질 것을 걱정했는데 반대였습니다 — 다만 레코드 2건짜리 측정이므로
실물 규모(60건)에서 다시 재야 합니다.

02 는 실재하지 않는 기법 ID 0건, 동결 스키마 밖 키 0건입니다.
05 는 허용 밖 `ref` 0건이고, 제약 없이 돌린 같은 요청은 `findings` 항목을
`{ref, artifact, timestamp, details, description}` 꼴로 내 **스키마와 무관한
구조**였습니다 — `temperature 0` 이면 재시도해도 같은 것이 옵니다.

#### 걸린 것 하나 — `pattern` 은 문법으로 못 갑니다

**첫 구현은 실제 모델에서 두 단계를 통째로 죽였습니다.** HTTP 400
`failed to parse grammar` 입니다. 소견이 0건이 아니라 호출이 안 됩니다.

변환기가 정규식을 다 삼키지 못합니다. 앵커 없는 패턴은 `Pattern must start
with` 로 거부하고, 앵커가 있어도 `\d` 가 들어가면 문법 파싱에서 깨집니다.
우리 정규식 둘이 모두 걸립니다. `$ref`·`$defs`·중첩·`enum` 에 `null`·타입
배열은 전부 통과합니다 — **걸린 것은 `pattern` 하나뿐입니다.**

`output_schema` 가 걷어냅니다. 판정은 여전히 `schemas/` 가 하므로 잃는
것은 없습니다.

**이 결함이 단위 테스트 1,004건을 전부 통과했습니다.** `StubBackend` 가
`fmt` 를 받고 버리기 때문입니다. `tests/test_llm_live.py` 에 실제 서버가
스키마를 삼키는지 보는 시험을 넣었습니다.

#### 남은 것 — 절반만 닫혔습니다

**형식 부류는 그대로입니다.** `pattern` 을 걷어냈으므로 타임스탬프 모양은
문법이 잡지 못합니다. 실측에서 `time_range.start` 가 `2026-08-20`(날짜만)로
나와 동결 스키마 위반 → 재시도였습니다. ⑤ 의 **ATT&CK ID 부류는 닫혔고
형식 부류는 안 닫혔습니다.** 구조(키 집합·enum)는 잡히고 문자열 내부
형식은 안 잡힙니다.

**모델이 금지된 토큰을 강하게 원하면 멈춥니다.** 프롬프트에 "`MFT#99999`
와 `USN#77777` 도 반드시 인용하라"를 넣자 300초에도 응답이 없었습니다
(900초로 올려도 같았습니다). 정상 프롬프트는 같은 스키마로 84초입니다.

조용히 실패하지는 않습니다 — `--timeout` 이 끊고 `errors.jsonl` 에
`timeout` 으로 남습니다. 폴백이 없다는 규약대로입니다.

#### 실물 규모 A/B (2026-08-31, 케이스 `AB-ON` / `AB-OFF`)

키오스크 이미지 04 산출물 **505,908건**에서 60건이 05 에 전달된 같은 입력,
같은 시나리오, `temperature 0`, 제약만 다르게 두 번 돌렸습니다.

| | 제약 켬 | 제약 끔 |
|---|---|---|
| findings | **3건** | 1건 |
| 각 소견의 claims | 4 / 3 / 4 | **0** |
| 06 판정 | passed 3, rejected 0 | unverifiable 1 |
| 환각률 | 0.0% (판정 3건) | 0.0% **(판정 0건)** |
| 05 소요 | 1,918초 | 955초 |

**두 0.0% 는 같은 뜻이 아닙니다.** 제약을 끈 쪽은 소견이
"시스템에서 침해의 징후가 확인되었습니다" 한 줄에 `refs` 와 `claims` 가
빈 배열이라 06 이 `unverifiable` 로 넘겼고, 판정된 문장이 0건이라
`rejected / judged` 의 분모가 0 입니다.

**이것은 지표의 결함입니다.** `hallucination_rate` 는 판정 대상이 없을 때
0.0 을 냅니다 — **"아무것도 말하지 않음"과 "전부 맞음"이 같은 숫자로
나옵니다.** 발표에 이 수치를 실을 때 판정 건수를 함께 적지 않으면
읽는 사람이 구별할 수 없습니다. `verify.py` 의 `stats` 에 `total_findings`
와 `passed` 가 이미 있으니 07 이 함께 싣도록 하는 것이 남은 일입니다.

**제약이 검증 가능한 소견을 만들었습니다.** 켠 쪽은 3건 모두 근거가
달렸고 06 을 통과했습니다. 다만 `findings.schema.json` 의 `refs`·`claims`
에는 `minItems` 가 없으므로 **문법이 빈 배열을 막은 것은 아닙니다** —
구조를 정확히 요구하니 모델이 채운 것이고, 관찰된 행동 차이이지 보장이
아닙니다.

**시간은 뒤집혔습니다.** 레코드 2건에서는 제약이 더 빨랐는데(84초 대
157초) 실물에서는 총 1,918초 대 955초로 두 배입니다. 다만 켠 쪽은 1회차가
**1,200초 타임아웃으로 끊겼고** 2회차가 성공했습니다 — 성공한 호출만 보면
약 718초로 오히려 빠릅니다. 붙는 비용은 속도가 아니라 **멈출 확률**입니다.

그리고 그 멈춤이 인위적 프롬프트 없이 실물에서 났습니다. 위 문단의
"인위적으로 만든 조건"이라는 진단은 실물 규모에서 유지되지 않습니다.
재시도가 받아 냈고 `errors.jsonl` 에 `timeout` 으로 남았지만, `--timeout`
을 넉넉히 두지 않으면 이 단계가 통째로 실패합니다.

**문법 크기는 병목이 아닙니다.** `ref` 60개짜리 05 스키마가 2,807바이트로
02 의 고정 스키마(3,052바이트)보다 작습니다. 120개로 늘려도 3,647바이트입니다.

#### 통과한 3건은 얇습니다 — 검증 가능한 것이지 통찰이 아닙니다

산출물을 읽어야 보입니다. `AB-ON` 이 06 을 통과한 세 문장은 이렇습니다.

- "사용되지 않는 파일이 생성되었으며, 이는 침해의 징후일 수 있습니다"
  — claims 는 `file_entry` 값 넷(86551, 86552…)
- "네트워크 프로필 상태가 변경되었으며…" — claims 는 `Name` 값
  ("네트워크", "식별 중...")
- "Windows Error Reporting 로그가 생성되었으며…" — claims 는 `Provider` 값 넷

셋 다 `technique` 이 `null` 입니다. 값이 레코드와 일치하니 통과가 맞지만,
**`passed 3/3` 을 성능으로 읽으면 안 됩니다.** 함의 타당성은 검증의 3층위
이고 이번 범위의 명시적 비목표인데(`work-guide.md` 2.3), 이 산출물이 바로
그 층이 비어 있다는 증거입니다. 제약이 만든 것은 **검증 가능한** 문장이지
**통찰 있는** 문장이 아닙니다.

#### timeline 은 오히려 끈 쪽이 낫습니다

| | 제약 켬 | 제약 끔 |
|---|---|---|
| findings (06 이 검증) | 3건, 전부 통과 | 1건, 검증 불가 |
| timeline (06 이 안 봄) | 14건, 뭉뚱그림 | **30건, 더 구체적** |

끈 쪽 timeline 에는 ACPI·PCI 드라이버 로드, 시스템 로그온,
**"Remote Desktop Users 계정이 수정되었습니다"**, 방화벽 설정 변경이
들어 있습니다 — 키오스크 시나리오에서 실제로 눈여겨볼 자리입니다.
켠 쪽은 같은 구간을 "네트워크 프로필 상태가 변경되었습니다" 로 뭉쳤습니다.

**06 이 `timeline` 을 검증하지 않으므로 이 차이는 수치에 잡히지 않습니다.**
환각률·통과 건수만 보면 제약이 이겼는데, 사람이 읽을 산출물의 절반은
반대입니다.

원인은 모릅니다. 문법이 `findings` 를 채우도록 압박한 만큼 `timeline` 에
쓸 여지가 줄었을 수 있지만, **한 번씩 돌린 결과라 단정할 수 없습니다.**
`timeline` 을 06 의 검증 대상에 넣을지, 아니면 이 차이를 재는 실험을 따로
할지가 남은 판단입니다.

#### A/B 조건에 대해

양쪽 `input_refs` 60건이 **완전히 같습니다.** 배분이 결정론적이라 비교
조건은 깨끗합니다. 다만 timeline 에 `2022-10-24` 가 섞여 있습니다 —
시나리오 시간창(2026-08-23~28) 밖인데 05 에 전달됐습니다. 이 변경과 무관한
기존 동작이지만(`outside_time_range` 는 flag 로만 붙습니다), 배분이 시간창을
좁히는 데 쓰이지 않는다는 뜻입니다.

이 비교는 `--no-constrain` 없이는 불가능했습니다. 측정용 스위치를 남겨 둔
값이 여기서 났습니다.

### 06단계가 표기 오류를 환각으로 셌다 (**해결됨**, 2026-08-24 실측)

`K-003`의 유일한 findings가 기각돼 **환각률 100%**로 집계됐습니다.
그런데 내용을 보면 모델이 맞았습니다.

```
모델 주장 : REG-SW#9604700 / DisableRealtimeMonitoring / "1"
실제 레코드: SOFTWARE\Microsoft\Windows Defender\Real-Time Protection
            fields = {"DisableRealtimeMonitoring": 1, ...}
기각 사유 : field_not_found
```

**ref도 필드명도 값도 맞습니다.** 틀린 것은 `fields.` 접두어 하나입니다 —
`get_field`가 점 표기를 요구하는데 모델이 맨 이름을 썼습니다. 프롬프트가
점 표기를 명시했으니 모델의 잘못이 맞지만, **이것을 환각으로 세면
환각률이 표기 오류를 세게 됩니다.**

`CurrentControlSet` 표기 문제(2장)와 같은 부류입니다 — 문장은 맞는데
표기가 달라 `comparators`가 흡수하지 못합니다.

**`benchmark/validator_check.py`는 이것을 못 잡습니다.** 33건 전부 통과로
나옵니다. 사례에 점 표기를 쓰는 것이 4건 있으나, **맨 이름이 `fields` 아래
있는 경우**는 하나도 없습니다. 과엄격 감시에 이 부류가 비어 있습니다.

#### 조치 — `comparators`의 네 번째 관대함으로 흡수한다

`comparators.py`는 이미 셋을 관대하게 보고 있었습니다. 타임스탬프 절삭,
경로 대소문자, 타입(`4821` 대 `"4821"`). 전부 **"주장하는 사실은 같은데
표기가 다른"** 경우이고, `fields.` 접두어는 그 네 번째입니다. 그래서
새 예외 처리를 만들지 않고 `get_field`가 흡수합니다. **양방향입니다** —
맨 이름을 `fields` 아래에서 찾고, 반대로 `fields.path`를 최상위에서도
찾습니다(`$MFT`는 `path`가 최상위입니다).

**검증이 무르지 않는 이유**가 핵심입니다.

- 이 함수가 하는 일은 **모델이 가리킨 필드를 찾아 주는 것까지**입니다.
  찾은 뒤에 값을 그대로 대조하므로, 값이 틀리면 `value_mismatch`로
  기각됩니다. 없는 필드를 지어낸 경우는 대안 표기로도 못 찾으므로
  `field_not_found` 그대로입니다.
- **모호해질 수 없습니다.** 원래 표기를 먼저 시도하고 성공하면 대안을
  보지 않으므로, 최상위와 `fields`에 같은 이름이 있어도 최상위가 이깁니다.
- 기각될 때 **끊긴 지점은 모델이 쓴 표기 기준**으로 남깁니다. 대안 표기의
  끊긴 지점을 보고하면 사유가 모델이 쓰지도 않은 경로를 가리킵니다.

`tests/test_verify_checkers.py`가 이 성질들을 각각 고정합니다 — 특히
"지어낸 필드는 두 표기 모두에서 여전히 못 찾는다"와 "표기를 흡수해도
값이 틀리면 `value_mismatch`".

**과엄격 감시의 구멍도 메웠습니다.** `benchmark/validator_cases.json`에
V34(맨 이름)·V35(반대 방향)를 더해 33건 → 35건입니다. 이 부류가 통째로
비어 있어서 감시가 100% 통과를 보고하고 있었습니다.

**결과** — `K-003`을 다시 검증하니 환각률 **100% → 0%**, 보고서에 첫
"확인된 사항"이 실렸습니다(Defender 실시간 보호 비활성화, 06:48:31,
`REG-SW#9604700`).

#### 같은 부류인데 아직 남은 것

**`CurrentControlSet` 표기**(2장)는 그대로입니다. 04단계가 `ControlSet001`로
풀어 쓰고 모델은 `CurrentControlSet`이라고 인용하므로 기각됩니다. 이번
것과 달리 **필드를 찾는 문제가 아니라 값 문자열이 다른 문제**라
`get_field`가 아니라 `compare`/`normalize_path` 쪽에서 풀어야 합니다.
레지스트리 경로에만 적용되는 치환이라 경로 비교 규칙 전체를 건드리게
되고, 그만큼 오탐 위험이 커서 따로 둡니다.

**영향** — "소형 모델의 환각을 검증 계층으로 잡는다"는 논지는 이제
**한 건이지만 실물 수치가 있습니다**(통과 1 / 기각 0). 다만 표본이
findings 1건이라 비율로 말할 수 없고, `priority`가 비어 있는 동안의
배분으로 얻은 결과라는 점도 그대로입니다(6-5).

### 검증기가 경로 표기 차이를 환각으로 센다 (**부분 해결**, 2026-08-26 실측)

**같은 일이 다른 표기 부류로 또 일어났습니다.** 위 절이 `fields.` 접두어를
메꾸고 감시 사례를 35건으로 늘린 지 이틀 만입니다.

`K-ALERT`(실물 이미지 + 알럿 입력, 기법 8개)에서 06단계가 소견 4건을
**전부 기각해 환각률 100%**를 냈습니다. 열어 보니 셋이 오탐입니다.

| | 모델 주장 | 실제 레코드 | 판정 |
|---|---|---|---|
| F4 | `C:\WINDOWS\SYSTEM32\sppsvc.exe` | `c:\windows\system32\sppsvc.exe` | 대소문자만 다름 — **오탐** |
| F2 | `C:\WINDOWS\SYSTEM32\SVCHOST.EXE` | `\VOLUME{01d8e7bd02796420-a202ae01}\WINDOWS\...` | 볼륨 GUID 표기 — **오탐** |
| F3 | `C:\WINDOWS\SYSTEM32\WERMGR.EXE` | 같음 | **오탐** |
| F1 | `SubjectUserName` = `MINWINPC$` | `-` (레코드에 그 문자열 없음) | **진짜 환각** |

**실제 환각률은 100%가 아니라 25%입니다.**

#### 원인 — 경로 비교 규칙이 필드 **이름**으로 켜진다

```python
# comparators.py:48
PATH_FIELDS = frozenset({"path", "target_path", "source_path", "image_path"})
```

`is_path_field()`가 이 넷(과 `*_path`)일 때만 `normalize_path`를 태웁니다.
그 밖의 이름은 `_scalar_equal`의 정확 문자열 비교로 갑니다. 실측:

```
compare("15",   'C:\WINDOWS\...\sppsvc.exe', 'c:\windows\...\sppsvc.exe')  →  False
compare("path", 같은 값)                                                    →  True
```

F4의 필드명이 `"15"`인 것은 Amcache 값 이름이 숫자 문자열이기 때문입니다.

**K-001 랩에서 이것이 훨씬 크게 터집니다.** Sysmon 이 실린 순간
Stage 2·3 이 통째로 기대는 필드들이 전부 경로로 인식되지 않습니다.

```
is_path_field('Image')          = False   ← Sysmon EID 1
is_path_field('ParentImage')    = False   ← Stage 3 의 결정적 근거
is_path_field('TargetFilename') = False   ← Sysmon EID 11
```

F2·F3 은 별개 원인입니다 — 프리패치의 `_to_drive()` 가
`\DEVICE\HARDDISKVOLUME*` 만 드라이브 문자로 바꾸고, 실물에서 나온
`\VOLUME{serial-serial}` 형태는 그대로 둡니다("바꿀 수 없으면 그대로
둔다"는 주석대로의 동작). **04단계에서 풀 문제이지 06단계가 흡수할
것이 아닙니다.**

#### 과엄격 감시가 또 못 잡았다

같은 시점에 `benchmark/validator_check.py` 는 **35건 중 35건 통과,
"오탐 없음"**을 보고했습니다. 볼륨 GUID 표기도 숫자 필드명도 사례에
없는 부류입니다.

**위 절이 예고한 그대로입니다** — 감시는 사례에 있는 부류만 봅니다.
새 표기 부류가 생기면 100% 통과를 보고하면서 정상 문장을 기각합니다.
**`PATH_FIELDS` 를 손대기 전에 사례부터 추가해야 합니다.**

#### 영향

**이 상태에서 나온 환각률 수치는 쓸 수 없습니다.** 발표 수치가 모델의
환각이 아니라 우리 비교기의 이름 화이트리스트를 재고 있습니다. 위 절이
"실물 수치가 있습니다(통과 1 / 기각 0)"라고 적은 것도 findings 1건짜리
표본이라 이 오탐 부류를 만날 기회가 없었을 뿐입니다.

#### 조치 — 이름 목록을 넓혔다. 셋 중 하나만 풀렸다

**순서를 지켰습니다** — `benchmark/validator_cases.json` 에 사례 V36~V39 를
먼저 넣어 넷 다 기각되는 것을 확인하고, 그다음 `PATH_FIELDS` 를 고쳤습니다.
사례 없이 비교기만 고치면 감시가 계속 "오탐 없음"을 보고합니다.

사례용 레코드는 `benchmark/validator_records/` 에 따로 뒀습니다. C-001
목업은 웹셸 벤치마크의 입력이라, 표기 시험용 레코드를 섞으면 파싱 건수와
05단계 배분이 함께 흔들립니다.

| | 상태 |
|---|---|
| Sysmon `Image`·`ParentImage`·`TargetFilename`·`ImageLoaded` | **해결** — `PATH_FIELDS` 에 추가 |
| Security `ProcessName`·`NewProcessName`·`ParentProcessName` | **해결** — 같음 |
| `filename` 으로 끝나는 새 이름 | **해결** — `PATH_FIELD_SUFFIXES` 가 받음 |
| Amcache 숫자 값 이름 (`"15"`) | **해결** (2026-08-27) — 아래 |
| 프리패치 볼륨 GUID 경로 | **미해결** — 06단계 문제가 아니라 04단계 `_to_drive()` 몫 |

**`CommandLine` 은 일부러 넣지 않았습니다.** 앞머리는 경로지만 뒤는
인자입니다. 경로 규칙으로 대소문자를 지우면 인자의 실제 차이까지 같이
지워져 검증이 물러집니다.

#### Amcache 숫자 값 이름 — 04단계에서 풀었다 (2026-08-27)

`"15"` 는 전체 경로지만 **이름이 아무 뜻도 담고 있지 않았습니다.**
`PATH_FIELDS` 에 `"15"` 를 넣는 것은 답이 아닙니다 — 다른 서브키의 `15`
까지 경로로 보게 되고, 값의 생김새로 판단하는 것은 이 모듈이 처음부터
거부한 방식입니다. 그래서 04단계가 이름을 바꿔 내보내는 쪽으로 풀었습니다.

**대응표는 공개 자료가 아니라 같은 이미지 안의 대조에서 얻었습니다.**
Amcache 는 같은 사실을 두 레이아웃으로 적는데, 실측
(`evidence/0824test.001`, Win10 15063)에 **둘이 함께 있었습니다** —
`Root\File` 360건(숫자 이름)과 `Root\InventoryApplicationFile` 29건
(이름 있음). `101` 을 `FileId` 로 보고 1:1 로 조인하면 29건이 맞물리고:

```
15  == LowerCaseLongPath   29 / 29
6   == Size (16진수)       29 / 29
100 == ProgramId           28 / 29
```

`100` 의 한 건이 어긋납니다. 두 저장소가 같은 파일을 다른 프로그램에
귀속시킨 것으로 보이나 **확인하지 못했습니다.** 28/29 를 근거로 매핑에
넣었으므로, 이 한 건은 우리가 아는 오차입니다.

**확인하지 못한 이름은 넣지 않았습니다.** `17`(FILETIME, 360건)·`b`·
`10`·`16` 은 이 이미지 안에 대조할 상대가 없습니다. 공개 자료에는
대응표가 있지만 추정으로 채우면 틀렸을 때 **조용히** 틀립니다. 숫자
이름 그대로 나가면 최소한 "우리가 모른다"가 산출물에 보입니다.

`17` 은 값이 FILETIME 이고 360건 전부에 있어 가치가 커 보이는데, 아직
근거가 없습니다. **$MFT 의 같은 경로 타임스탬프와 대조하면 이 이미지
안에서 확정할 수 있습니다** — 하지 않았습니다.

**이름이 한 군데서 사실과 어긋납니다.** `LowerCaseLongPath` 는
Microsoft 가 `InventoryApplicationFile` 에 붙인 이름이고 그쪽 값은 실제로
전부 소문자인데, `Root\File` 의 `15` 는 대소문자가 살아 있습니다. 위
29/29 는 대소문자를 무시한 비교입니다. 그래도 같은 이름을 쓴 이유는
`parsers/registry.AMCACHE_FILE_VALUE_NAMES` 에 있습니다.

**06단계도 함께 고쳐야 했습니다.** `PATH_FIELDS` 에
`lowercaselongpath` 가 없었습니다. 04 가 이름을 바꿔도 06 이 그 이름을
경로로 안 받으면 아무것도 안 풀립니다. 그리고 이것은 V36 하나의 문제가
아니었습니다 — **`InventoryApplicationFile` 의 29건은 원래부터 이름이
있었는데도 같은 구멍에 빠져 있었습니다.** Amcache 값은 소문자로
저장되고 모델은 `C:\Program Files\...` 라고 쓰므로, Amcache 를 인용한
정상 문장은 지금까지 전량 기각되고 있었습니다.

`V36` 의 `expect` 를 `passed` 로 되돌리고 `gap` 을 지웠습니다.
`benchmark/validator_check.py` 가 **40건 중 40건 기대대로**로, 아는
구멍 0건입니다.

#### 이 조치로 K-ALERT 수치는 움직이지 않았습니다

**중요합니다.** 다시 검증해도 환각률은 100% 그대로입니다.

```
F1  value_mismatch  SubjectUserName   ← 진짜 환각 (올바른 기각)
F2  value_mismatch  path              ← 프리패치 볼륨 GUID (04단계 몫)
F3  value_mismatch  path              ← 같음
F4  value_mismatch  "15"              ← Amcache (아는 구멍)
```

> 2026-08-27 — F4 의 원인은 위에 적은 대로 04·06 양쪽에서 닫혔습니다.
> 위 블록은 그 시점의 기록 그대로 둡니다. **수치를 다시 재지는
> 않았습니다** — K-ALERT 케이스가 `cases/` 에 남아 있지 않습니다.

그 이미지에 Sysmon 이 없어 이번에 고친 필드들이 등장하지 않기 때문입니다.
**이번 조치는 K-001 랩에서 터질 것을 미리 막은 것이지, 이 케이스의 수치를
고친 것이 아닙니다.** 수치가 정상으로 돌아오려면 위 표의 미해결 둘이
남아 있습니다.

### 05단계 프롬프트가 컨텍스트 창을 넘었다 (**해결됨**, 2026-08-26 실측)

`K-ALERT`(기법 8개 → 아티팩트 10종 1,693건)에서 05단계가 **3회 재시도
끝에 중단**됐습니다. 약 25분 소모, 소견 0건.

```
attempt 1  malformed_output   응답에서 JSON 객체를 찾지 못함
attempt 2  timeout            qwen2.5:7b: 900.0초 내 응답 없음
attempt 3  malformed_output   응답에서 JSON 객체를 찾지 못함
           abort              → SystemExit(1)
```

프롬프트를 그대로 재현해 재 봤습니다.

| | |
|---|---|
| 전달 레코드 60건의 JSON | 71,476자 (**추정 28,600 토큰** — 글자수÷2.5, 토크나이저를 돌린 값이 아님) |
| 시스템 프롬프트 | 3,002자 |
| `--num-ctx` 기본값 | 32,768 |
| `qwen2.5:7b` 의 `context_length` | 32,768 (**모델 상한이라 더 열 수 없음**) |

출력에 쓸 자리가 2천 토큰도 남지 않습니다.

**`--limit 15` 로 낮추니 첫 시도에 통과했습니다**(소견 4건, 재시도 0회,
`errors.jsonl` 0건). 원인이 프롬프트 크기임이 확인됐고, attempt 2의
타임아웃도 같은 원인입니다 — 3만 토큰 프롬프트의 처리 시간입니다.

**5장이 "넘는지는 아무도 검사하지 않습니다"라고 적어 둔 자리이고,
이번이 실제로 넘긴 첫 실측입니다.** 웹셸 목업은 레코드가 5건이라 닿을
수 없던 지점입니다.

#### 왜 60건이 이렇게 커졌나

4-3이 적은 것과 같은 구조입니다 — 아티팩트가 늘면 자리를 나눠 갖지만
**총량 60건은 그대로**라, 레코드 하나당 평균 1,190자가 그대로 실립니다.

```
evtx:Security  후보 482 → 9자리     registry:Amcache  후보 392 → 8자리
evtx:KernelPnP 후보 385 → 9자리     registry:SYSTEM   후보 258 → 8자리
prefetch       후보 137 → 9자리     evtx:RDPSession   후보  12 → 9자리
```

`--limit` 기본값 60은 카탈로그가 11종이던 시절 값입니다.

#### 조치 — 배분기에 토큰 예산을 넣었습니다

`--limit` 를 낮추는 것은 우회지 수정이 아니었습니다. 기본값 60은 카탈로그가
11종이던 시절 값이고, 22종이 된 지금 **60건이 무슨 크기가 되는지는 그
숫자가 말해 주지 못합니다.** 레코드 하나의 크기가 아티팩트마다 열 배씩
차이 나기 때문입니다 — 적재 파일 103개를 들고 있는 프리패치 레코드와
`$MFT` 레코드는 같은 "한 자리"가 아닙니다.

같은 날 `win10_sysmon_testimage.001` 로 다시 쟀습니다. **자릿수는 그대로인데
크기는 40% 더 컸습니다.**

| | 60건의 크기 | 추정 토큰 |
|---|---|---|
| `K-ALERT` (2026-08-26, 아티팩트 10종) | 71,476자 | 28,600 |
| `win10_sysmon_testimage` (같은 날, 5종 2,422건) | **100,068자** | **40,027** |

둘 다 `--num-ctx` 기본값 32,768을 넘습니다. 뒤엣것이 더 큰 것은 프리패치가
자리를 받았기 때문입니다.

**`allocate_records` 에 `char_budget` 을 주면 예산에 맞을 때까지 자릿수를
낮춰 다시 배분합니다.**

```
num_ctx=32,768   예산 69,803자   →  60자리를 44자리로 줄임 (65,312자 ≈ 26,124토큰)
num_ctx=131,072  예산 315,563자  →  60자리 그대로 (100,068자)
```

**꼬리를 자르지 않습니다.** 자르면 시간순으로 마지막인 아티팩트가 통째로
사라져 4-2-1이 고친 문제가 그대로 되살아납니다. `limit` 을 낮춰 동트 배분을
다시 돌리므로 `priority` 가중치와 바닥 한 자리가 그대로 살아 있습니다.

**예산이 어디서 나오나.**

```
(--num-ctx − --reserve-output-tokens) × CHARS_PER_TOKEN − 프롬프트 고정분
```

- 출력 자리를 **먼저** 뗍니다(기본 4,096토큰). 프롬프트가 창을 꽉 채우면
  응답이 잘리고, 잘린 응답은 `malformed_output` 으로 옵니다 — 위 attempt 1·3이
  정확히 그 모양입니다.
- 프롬프트 고정분(시스템 프롬프트 + 시나리오 머리말)은 **추정하지 않고
  실제로 조립해 잽니다**(`InterpretClient.prompt_overhead_chars`). 상수로
  적어 두면 프롬프트를 고치는 순간 어긋납니다.
- 레코드 크기도 `llm_client` 가 내보내는 **바로 그 문자열**을 잽니다
  (줄당 하나의 JSONL, `ensure_ascii=False`).

**`CHARS_PER_TOKEN = 2.5` 는 추정치입니다.** 토크나이저를 돌린 값이
아닙니다. 순수 ASCII 는 토큰당 3~4자, 한글은 1~1.5자라 그 사이를 잡았고,
2026-08-26 환산에 쓴 값과 같습니다. **틀리는 방향이 중요합니다** — 크게
잡으면 예산이 헐거워져 창을 넘고, 작게 잡으면 자리를 덜 쓸 뿐입니다.

**깎였으면 말합니다.** `넘는데도 조용히 도는 것`이 이 자리에서 가장 나쁜
성질이었습니다.

```
  토큰 예산: 4건 → 2건으로 줄임 (861자 ≈ 344토큰 / 예산 1,133자).
             --num-ctx 5300 에서 출력 4096토큰을 뺀 값이다
```

한 건도 안 들어가면 `empty_result` 로 중단하되, **"후보가 0건" 과 사유를
구분합니다.** 창을 키우거나 모델을 바꿔야 풀리는 문제이지 flags 룰을
들여다볼 일이 아니기 때문입니다.

#### 레코드 다이어트 — 한 필드가 프롬프트의 54%였습니다

예산은 "몇 건을 보낼까"를 정할 뿐 레코드 하나를 줄이지 않습니다. 그래서
어디가 무거운지 쟀습니다(`win10_sysmon_testimage`, 60건 100,068자).

| | |
|---|---|
| `fields.loaded_files` | **54,577자 — 전체의 54%** |
| 나머지 전부 | 45,491자 |

아티팩트로 보면 프리패치 12건이 61,423자(61%)로, 건당 5,118자입니다.
한 레코드가 `loaded_files` **1,017건**을 들고 있었습니다.

**`fields` 안의 목록을 앞에서 20개까지만 싣습니다**(`--max-list-items`,
0이면 안 자름). `04_parsed/` 의 원본은 그대로입니다.

```
num_ctx 32,768 · 예산 69,741자

  예산만        44건   65,312자
  예산 + 상한20  60건   63,041자   ← 더 많이 보내면서 더 작다
```

**왜 20인가.** 상한별 실측입니다.

```
안 자름  100,068자  →  44건       40      78,026자  →  54건
20        63,041자  →  60건 전부  10      54,395자  →  60건
```

20에서 천장을 넘고, **그 아래로는 수익이 급감합니다** — 20→3이
14,731자를 더 아낄 뿐인데 모델이 볼 근거는 계속 줄어듭니다. 잘리지
않아야 할 것과도 안 부딪힙니다: 실측에서 `run_times`는 최대 8(포맷 상한),
`volumes`는 1이라 10 이상이면 건드리지 않습니다.

**세 가지를 지켰습니다.**

- **`fields` 안만 자릅니다.** 최상위는 스키마가 정한 자리이고 무엇보다
  `flags`가 거기 있습니다 — 신호 판정 자체를 자르면 안 됩니다.
- **앞에서부터 자릅니다.** 어느 항목이 중요한지 고르는 물리적 근거가
  없습니다. "시스템 DLL은 덜 중요하다" 같은 추측으로 순위를 매기면 그
  추측이 틀렸을 때 조용히 증거를 버립니다. 파일 순서는 근거는 아니지만
  재현은 됩니다(6-6의 "임시"와 같은 자리).
- **잘렸다고 프롬프트가 말합니다.** 말하지 않으면 모델이 "적재 파일은
  20개였다"고 쓸 수 있고 그것은 우리가 **유발한** 환각입니다. 전체 개수는
  `loaded_file_count`에 원본 그대로 있습니다. 그런 문장이 나와도 06단계가
  잡습니다 — 검증은 `04_parsed/` 를 읽으므로 1,017과 맞지 않아 기각됩니다.

**예산과 프롬프트가 같은 `for_prompt` 을 씁니다.** 둘이 어긋나면 예산이
맞아도 프롬프트가 넘칩니다. 실제로 처음 구현에서 `allocate_records` 가
상한을 `record_chars` 에 안 넘겨 크기를 잘못 쟀고, 테스트가 그것을
잡았습니다.

**남은 것** — 목록 말고도 반복되는 자리가 있습니다. `Provider`(2,004자)·
`channel`(1,728자)·`artifact`(1,524자)는 레코드마다 같은 문자열이 실립니다.
묶어서 머리말로 빼면 더 줄지만, 레코드 하나가 자족적이지 않게 되어 모델이
헷갈릴 위험과의 거래라 재 보지 않았습니다.

#### 실패한 모델 원문은 이제 남깁니다 (**해결됨**, 2026-08-26)

위 진단이 "프롬프트가 잘렸다"를 **확인한 것이 아니라 크기를 재고
`--limit` 를 낮춰 재현한 것**이었습니다. 모델이 실제로 무엇을 뱉었는지
아무도 보지 못한 채 원인을 단정했습니다.

02·05단계가 재시도할 때마다 그 시도의 응답 원문을 `errors.jsonl` 옆에
떨굽니다.

```
cases/<id>/02_normalize_raw_attempt1.txt
cases/<id>/05_interpret_raw_attempt1.txt
```

`errors.jsonl` 항목의 `detail.raw` 가 그 파일 이름을 가리키고, 재시도를
모두 소진해 중단할 때의 stderr 도 이 경로를 알려 줍니다. 시도마다 파일이
따로인 것은 **모델이 지적을 받고 달라졌는지**를 보기 위해서입니다 —
한 파일에 덮어쓰면 마지막 시도만 남습니다.

**`timeout` 은 떨구지 않습니다.** 응답 자체가 없었으므로 남길 원문이
없고, 직전 시도의 것을 쓰면 이번 실패의 원문으로 오해됩니다.

### 외부 도구 대조가 부분적이다

| 아티팩트 | 대조 상대 | 상태 |
|---|---|---|
| `$MFT` | MFTECmd | **미실시** — 추출 파일 크기 대조로 대신함 |
| `$MFT` | `dissect.ntfs` | **실시**(2026-08-28) — 두 이미지에서 설명 안 되는 차이 0건. `tools/compare_mft_dissect.py`. 계보가 달라 공통 오해는 안 생기지만 DFIR 표준 도구는 아니다 |
| `$UsnJrnl` | MFTECmd (`-f $J`) | **미실시** — 독립 순회 코드로 대신함 |
| `evtx` | `wevtutil` | **실시** — 8,257건 전량 일치. s4a-challenge4 에서 4채널 2,144건 재실시, 레코드 ID 집합까지 일치(2026-08-28) |
| `registry` (커버리지) | 원시 셀 스캔 | **실시** — SYSTEM 34,855 / SOFTWARE 156,716건 일치 |
| `registry` (값) | 독립 `nk`/`vk` 디코더 | **실시** — 46,147키 중 46,142건 일치 (나머지는 대조 도구의 big data 미구현) |
| `registry:Amcache` | AmcacheParser 2026.5.0 | **실시** — 457건 전량 일치. 단 `Root\File` 361건은 저쪽이 안 읽어 대조가 없다 |
| `registry` (값) | `reg load` | **미실시** |
| `prefetch` · `srum` | 외부 도구 | **미실시** — 자체 독립 대조와 내부 정합성뿐 |

레지스트리는 대조를 둘로 나눴습니다. 위험의 성격이 `$MFT`와 다르기
때문입니다 — 값을 틀리게 읽는 것이 아니라 **서브트리를 통째로 못 보고
지나가는 것**이 위험합니다. python-registry는 서브키 목록(`lf`/`lh`/`li`/
`ri`)을 따라 내려가는데, 그 목록 하나가 손상되면 아래 전부가 예외도
경고도 없이 사라집니다.

`tools/scan_hive_cells.py`가 **서브키 목록을 전혀 따라가지 않고** hbin의
셀을 직접 걸어 `nk`를 셉니다. 트리 구조와 무관하므로 커버리지 정답지가
됩니다. 두 하이브 모두 정확히 일치했습니다.

**값 대조는 독립 디코더로 한 번 했습니다.** `nk`/`vk`를 명세대로 직접
읽어 파서 출력과 맞춰 본 것이며, **이 대조에서만 드러난 버그가 셋**
있었습니다(위 「라이브러리를 우회한 곳」). 다만 같은 사람이 명세를 읽고
짠 두 구현이라 공통 오해는 잡지 못합니다.

**그래서 `reg load` 대조는 여전히 필요합니다** — 모든 Windows에
들어 있고 마이크로소프트 자신의 구현이라 `wevtutil`과 같은 조건입니다.
관리자 권한이 필요하고 원본이 아닌 복사본에 걸어야 합니다(마운트가
하이브를 더티로 만들 수 있음). 절차는 `tools/scan_hive_cells.py`
docstring에 있습니다.

`regipy`나 `python-registry`로 대조하면 의미가 줄어듭니다. 아래
`evtx_dump.py` 함정과 같습니다.

독립 순회 정답지는 같은 사람이 짠 두 구현이라 **공통 오해는 잡지
못합니다.** 순회 로직 버그는 잡히지만 명세 이해가 틀렸다면 둘 다 같이
틀립니다. `$MFT`·`$UsnJrnl`은 외부 도구 대조가 남아 있습니다.

evtx만 실시된 이유는 준비물 때문입니다. `wevtutil`은 모든 Windows에 들어
있어 받을 것이 없고, 마이크로소프트 자신의 구현이라 공통 오해가 생기지
않습니다. `$MFT`·`$UsnJrnl`은 MFTECmd를 따로 받아야 합니다. 기록은
`artifact-notes.md`에 있습니다.

**`evtx_dump.py`(python-evtx 동봉)로 대조하면 의미가 없습니다.** 우리가 쓰는
바로 그 라이브러리라 자기 대조입니다. 위의 "공통 오해" 함정과 같습니다.

---

## 6. 05단계 레코드 배분 (**구현됨**)

4-2("몇 건을 넘길지")와 4-2-1("어느 것을 넘길지")을 함께 푸는 설계입니다.
구현은 `src/stage05_interpret/allocation.py`이고, 아래는 설계 논의와 그
근거를 그대로 둔 것입니다. **채우지 않은 것이 하나 남아 있습니다** —
6-5의 `priority` 값입니다.

### 6-1. 왜 정해야 했는가

예전에는 `record_filter`가 신호를 이진 판정한 뒤 앞에서 60개를 잘랐습니다.
자르는 기준이 파일명 알파벳순이라 **어느 증거가 모델에 도달하는지가
파일 이름 철자에 달려 있었습니다.**

파이프라인 전체에서 **05단계의 자르기만 시나리오의 영향을 받지 않았습니다.**
03단계가 "이 기법 때문에 이 아티팩트를 본다"고 정해 놓은 것을 05단계가
통째로 무시했습니다. 이 도구의 논지가 "시나리오 기반 선별"인데 마지막
관문이 시나리오와 무관했습니다.

### 6-2. 채택한 구조 — 2단 배분

```
매핑 priority   →  이 케이스에서 어느 아티팩트가 중요한가   [가중치]
신호 수 정규화  →  어느 아티팩트가 시끄러운가              [보정]
                          ↓
                아티팩트별 자릿수 배분 (쿼터)
                          ↓
                쿼터 안에서 레코드 선택 (시간창 중심성)
```

가중치만 있으면 시끄러운 아티팩트가 자리를 쓸어가고, 보정만 있으면 모든
아티팩트를 똑같이 취급합니다. 둘 다 있어야 "웹셸 케이스에서 `$MFT`를
우선하되, `$UsnJrnl`이 30만 건이라고 `$MFT`를 밀어내지는 못한다"가 됩니다.

**가중치는 (기법, 아티팩트) 쌍의 속성입니다.** 기법의 속성이 아닙니다.
같은 `$MFT`라도 `T1070.006`(Timestomp)에서는 판정의 근거 그 자체이고
`T1053.005`(Scheduled Task)에서는 이벤트 로그를 보조하는 자리입니다.

매핑 YAML이 이미 그 모양이라 스키마도 코드도 바꿀 것이 없습니다.

```yaml
artifacts:
  - name: $MFT
    tier: 1
    priority: 1        # ← 이 한 줄
    scope_template: {}
    rationale: $SI와 $FN 타임스탬프 불일치
```

`work-guide.md` 5.1의 "`mappings/`는 코드가 아닌 데이터" 원칙에 맞습니다 —
여러 명이 동시에 채우고, 개수를 세고, 파이썬을 몰라도 씁니다.

**보정값은 아티팩트별 후보 수입니다.** 배분이 어차피 전 레코드를 훑으며
`is_signal()`을 부르므로 새 입력 없이 셀 수 있습니다.

구현에서 정한 것 둘을 함께 적습니다.

- **눈금은 셋입니다**(1·2·3, 작을수록 강함 — `tier`와 같은 방향). 가중치는
  4·2·1로 한 눈금마다 두 배입니다. 배수를 더 키우지 않은 것은 약한
  아티팩트가 굶으면 안 되기 때문입니다 — 자리를 적게 받는 것과 한 건도 못
  받는 것은 보고서에서 전혀 다른 결과입니다.
- **후보가 있는 아티팩트는 최소 한 자리를 받습니다.** 03단계가 보라고 한
  아티팩트가 보고서에서 통째로 사라지지 않는다는 보장이며, 6-7이 여기서
  풀립니다.

남은 자리는 동트(D'Hondt)식으로 하나씩 나눕니다. 매번 `가중치 / (이미 받은
자리 + 1)`이 가장 큰 아티팩트가 다음 자리를 가져갑니다. 최대잔여법과 달리
소수점 잔여를 들고 다니지 않아 **자리마다 누가 왜 가져갔는지 손으로 따라갈
수 있습니다.** 동점은 이름순으로 가릅니다 — 근거가 있어서가 아니라 같은
입력에 같은 배분이 나와야 재현율이 모델 성능의 지표가 되기 때문입니다.

### 6-3. 배선

05단계에 `--selection`이 생겼습니다. `priority`는 03단계가 매핑에서 읽어
`selected[].priority`로 산출물에 실어 보내고, 05단계는 그것만 읽습니다.
05단계가 `mappings/`를 다시 읽지 않는 이유는 **그 사이 매핑이 바뀌면
보고서와 대조가 되지 않기 때문입니다** — "왜 이 60건입니까"에 답할 근거가
케이스 산출물 안에 남아 있어야 합니다.

`signal_source`는 반대로 카탈로그에서 읽습니다(`--mappings`, 기본
`mappings`). 케이스마다 달라지는 판단이 아니라 아티팩트의 고정된
성질이기 때문입니다.

한 아티팩트를 여러 기법이 요청할 수 있습니다(04단계가 `scope`를 합치는
이유와 같음). 아티팩트 가중치는 **요청한 기법들 중 최대값**으로 봅니다 —
합치면 여러 기법이 스치듯 요청한 아티팩트가 한 기법이 강하게 요청한
아티팩트를 이깁니다.

### 6-4. 검토했으나 채택하지 않은 것

기록으로 남깁니다. 셋 다 그럴듯해 보이는데 근거가 없습니다.

**① ATT&CK 기법 번호순** — 번호는 **카탈로그 등재 순서**입니다. 심각도도
킬체인 순서도 아니며, 하위 번호(`.001`)도 부모 기법 안에서의 등재
순번입니다. 매핑 6건을 ID순으로 세우면 이렇게 됩니다.

```
T1053.005  Scheduled Task
T1070.004  File Deletion
T1070.006  Timestomp
T1136.001  Create Account
T1505.003  Web Shell          ← 5번째
T1543.003  Windows Service
```

대표 데이터셋이 `C-001-webshell`인데 **웹셸이 5번째**입니다. `1505 > 1053`
이라는 이유뿐입니다. 킬체인으로 봐도 거꾸로입니다 — Web Shell(초기 침투)이
File Deletion(사후 방어 회피)보다 뒤에 옵니다.

알파벳순을 번호순으로 바꾸는 것이라 근거 없기는 같은데, **겉보기에
원칙이 있어 보여서 더 위험합니다.** "왜 이 60건입니까"에 "ATT&CK
번호순입니다"라고 답하면 되묻는 사람이 없습니다.

**② 파일 크기 반비례 배분** — 볼륨 편차를 보정한다는 방향은 맞지만,
파일 크기는 증거의 양이 아니라 **그릇의 크기**를 잽니다. `evidence/[root]`
실측:

| 아티팩트 | 파일 크기 | 실제 레코드 | 바이트/레코드 |
|---|---|---|---|
| `$MFT` | **103.5 MB** | 101,120 슬롯 (대부분 미사용) | 1,024 |
| `$J` | 34.4 MB | **306,857** | 112 |
| `Application.evtx` | 1.1 MB | 8,257 | 135 |
| `HardwareEvents.evtx` | 68 KB | **0** | — |

**크기 순서가 레코드 수 순서와 정반대입니다.**

- `$MFT`가 가장 큰데(103MB) 가장 적게 받습니다. 그런데 범위를 좁히면
  실제로 154건(C-100-C)~2,034건뿐입니다. 넘칠 걱정이 제일 적은 것이
  제일 깎입니다.
- `$J`는 `$MFT`의 3분의 1 크기인데 레코드는 3배입니다. 4-2가 지적한
  "무한정 쌓이는" 바로 그 아티팩트가 오히려 후하게 받습니다.
- `HardwareEvents.evtx`는 68KB에 레코드 0건입니다. 작다는 이유로
  빈 로그에 자리가 배분됩니다.

셋의 그릇 성격이 달라서입니다 — `$MFT`는 안 쓰는 슬롯도 1,024바이트를
차지하는 고정 테이블, evtx는 채널마다 상한이 있는 링 버퍼, `$J`는
스파스 파일입니다. 특히 `$J`는 34MB 중 503,752바이트가 저널이 아니라
phpMyAdmin 텍스트입니다(4-0-1). 즉 파일 크기는 **추출 도구의 동작**을
재고 있습니다.

**③ `scenario.techniques[].confidence`** — 이미 스키마에 있는 0.0–1.0
값이라 쓰고 싶어지지만, 쓰면 순환이 생깁니다. **LLM이 매긴 확신도가 그
LLM이 볼 증거의 양을 정하게** 됩니다. 02단계가 맞는 기법을 낮은
확신도로 뽑으면 05단계가 바로 그 기법의 증거를 굶기고, 결과는 "sLLM
탐지 실패"로 집계됩니다 — 4-2가 경고한 조용한 실패를 우리가 직접
만드는 것입니다.

`work-guide.md` 2.2 원칙 1("LLM 구간과 결정론적 구간을 번갈아 배치")에도
어긋납니다. 03·05의 결정론적 구간에 LLM 판단을 되돌려 넣는 셈입니다.

### 6-5. 남는 수작업 (**미완 — 팀이 나중에 채우기로 함, 2026-08-24**)

**배분 기계는 돌아가지만 아직 아무 매핑도 `priority`를 적지 않았습니다.**
전부 중립(2)이라 지금 배분을 가르는 것은 후보 수뿐입니다. 시나리오가
자릿수에 반영되려면 이 값이 채워져야 합니다.

> **누락이 아니라 미룬 것입니다.** 배분을 구현하면서 함께 정하지 않고
> 나중에 채우기로 했습니다. 그때까지 나오는 배분 수치는 **가중치가 빠진
> 상태의 값**이므로, 벤치마크에 쓰기 전에 이 절을 먼저 확인하십시오.

매핑 9건의 아티팩트 항목 약 22개에 `priority`를 사람이 채워야 합니다.
**이 값은 어떤 자동 규칙에서도 나오지 않습니다.** 기법 번호에서도, 파일
크기에서도, 모델 출력에서도 나오지 않습니다. 분석가가 판단해 적고 다른
사람이 검토해야 나옵니다 — 그래서 코드가 아니라 `mappings/`에 둡니다.

일부러 채우지 않고 두었습니다. 자동으로 채울 수 있었다면 애초에 코드에
두었을 것이고, 그럴듯한 값을 넣어 두면 **검토되지 않은 값이 검토된 값처럼
보입니다.** 매핑 도메인 지식은 파서처럼 자동 대조 도구가 없어 사람이 직접
아티팩트를 열어 확인해야 합니다(2장 "매핑 — 검증되지 않은 채 채운 항목"과
같은 이유).

논의 중 `T1070.006`(Timestomp)에 이벤트 로그를 우선하자는 안이 나왔는데,
실제 매핑에는 이벤트 로그가 아예 없습니다. 타임스톰핑은 이벤트 로그에
기록되지 않고 `$SI`/`$FN` 불일치로만 잡히기 때문입니다. **6개 기법 중
가장 `$MFT` 의존적인 기법입니다.** 값 하나하나가 이 정도의 도메인 판단을
요구한다는 예로 남깁니다.

### 6-6. 이것으로 풀리지 않는 것

기법 우선순위는 **아티팩트 단위**입니다. `$MFT` 신호 1,648건 중 어느
60건이냐는 여전히 쿼터 안의 문제로 남습니다. 지금 설계는 시간창
중심성으로 채우기로 했으나 근거가 약합니다 — `MISMATCH_PAIRS`처럼
**물리적 근거가 있는 판별자**를 찾기 전까지는 임시입니다(4-2 말미).

### 6-7. 레지스트리가 05단계에 도달하지 못했다 (**해결됨**)

파서를 붙이면서 드러난 것이라 6장에 함께 적습니다. **04단계까지는
정상인데 모델에 한 건도 가지 않았습니다.**

`registry:Amcache`도 같은 클래스(`RegistryParser`)를 쓰므로 이 절이
그대로 적용됩니다 — Amcache 레코드도 `flags`가 붙지 않습니다. 카탈로그에
세 하이브 모두 `signal_source: scope`를 적어 두었습니다.

실측(`evidence/[root]`, `SYSTEM\CurrentControlSet\Services`):

```
registry:SYSTEM: 1754건 (플래그 0건) → registry_system.jsonl
```

`flagged_count`가 **0**입니다. 오류가 아니라 구조입니다.

기존 파서 셋은 **"전부 훑고 재미있는 것에 플래그"** 모델입니다.
레지스트리는 반대로 **가치가 레코드가 아니라 경로에 있습니다.**
`$MFT` 레코드 12345는 맥락 없이도 의미가 있지만
`Services\XYZ\ImagePath`는 그 경로가 무엇을 뜻하는지 알아야 의미가
생깁니다. 그래서 신호가 `flagging.py`가 아니라 **선별에서** 나옵니다.

그런데 05단계는 `is_signal()`로 `flags`가 빈 레코드를 버렸습니다. 결과적으로 **선별이 정확히 골라 온 1,754건이 전부 탈락합니다.**

임계값 문제가 아니라 필터 모델이 이 아티팩트에 맞지 않는 것입니다.
`flagging.py`에 억지로 룰을 넣는 것은 답이 아닙니다 — "Services 하위면
신호"라고 쓰면 선별이 이미 한 일을 한 번 더 하는 것이고, 그러면 플래그가
"볼 만하다"는 뜻이기를 그만둡니다.

6-2의 2단 배분에서 함께 풀렸습니다. 쿼터가 아티팩트별로 자리를 배분하므로
**"신호냐"가 아니라 "몇 자리를 줄 것이냐"가 질문이 되고**, 플래그 없는
아티팩트도 자기 몫을 받습니다.

**고친 방법.** 카탈로그에 `signal_source`를 두었습니다. 기존 셋은
`flags`(기본값)이고 레지스트리 두 하이브가 `scope`입니다. `scope`
아티팩트에서는 모든 레코드가 후보이며, 시간창으로 다시 거르지 않습니다 —
`Services` 하위 키의 LastWrite는 대개 OS 설치 시각이라 사건 시간창에
걸리지 않기 때문입니다. 쿼터 안에서는 사건 앵커에 가까운 키부터 들어갑니다.

이 예외를 `flagging.py`의 룰로 넣지 않은 이유는 그대로입니다. "Services
하위면 신호"라고 쓰면 선별이 이미 한 일을 한 번 더 하는 것이고, 그러면
플래그가 "볼 만하다"는 뜻이기를 그만둡니다. 카탈로그에 두면 **그 아티팩트의
신호가 어디서 나오는가**라는 사실 하나만 적는 것으로 끝납니다.

**앵커는 아티팩트를 가리지 않습니다.** evtx의 로그온 시각이 레지스트리
키를 끌어올 수 있습니다. 증거가 여러 계층에 흩어지는 사건에서 이 교차가
상관분석의 실질입니다.

재현 시나리오(1,754건 전부 플래그 0, `$UsnJrnl` 신호 500건, 상한 60):

| 배분 | `registry:SYSTEM` | `$UsnJrnl` | `evtx:Security` |
|---|---|---|---|
| 예전(플래그로 거름) | **0~3** | 55 | 2 |
| `signal_source: scope`, 중립 | **29** | 29 | 2 |
| 매핑이 레지스트리를 `priority: 1`로 지목 | **47** | 11 | 2 |

세 번째 줄은 아직 **가정입니다.** 6-5대로 `priority`가 비어 있어 실제
매핑으로는 두 번째 줄이 나옵니다.

### 6-8. 프리패치도 같은 자리에 있었다 (**해결됨**)

파서를 붙이면서 같은 일이 반복됐습니다. 실측(`evidence/[root]`):

```
prefetch: 73건 (플래그 0건) → prefetch.jsonl
```

레지스트리와 원인이 같습니다 — **가치가 레코드가 아니라 그 안의 경로에
있습니다.** "CMD.EXE가 5회 실행됐다"는 그 자체로 신호가 아니고,
`C:\inetpub\wwwroot\shell.aspx`를 적재했다는 사실이 신호입니다. 그 판단은
선별(`path_prefix`)이 이미 했으므로 `flagging.py`가 더 할 일이 없습니다.

`program_executed` 같은 플래그를 새로 만드는 안을 검토했다가 **접었습니다.**
프리패치 레코드는 전부 "무언가 실행됐다"이므로 모든 레코드에 붙고, 그러면
플래그가 "볼 만하다"는 뜻이기를 그만둡니다. 레지스트리에 "Services 하위면
신호"를 넣지 않은 것과 같은 이유입니다.

`outside_time_range`는 붙습니다(실측: 1시간 범위로 좁히면 73건 중 47건).
다만 `record_filter.NON_SIGNAL_FLAGS`에 들어 있어 신호로 치지 않으므로,
**결론도 같았습니다 — 프리패치도 05단계에 도달하지 못했습니다.**

**해결도 레지스트리와 같습니다.** 카탈로그에 `signal_source: scope`를
적었습니다. 배분이 그것을 보고 프리패치에도 자리를 줍니다.

재현(프리패치 73건 전부 플래그 0, 최근 실행 시각은 몇 주에 걸쳐 흩어져
있고 사건 근처는 2건, 상한 60):

| | 후보 | 전달 |
|---|---|---|
| `flags`로 볼 때 | **2건** (다른 아티팩트 신호의 시간창에 걸린 것만) | 2건 |
| `signal_source: scope` | **73건** (선별이 골라 온 전부) | 59건 |

두 경우 다 사건 근처 2건은 들어갑니다. 달라지는 것은 **나머지 71건을
후보로 볼 것이냐**입니다 — 프리패치에서는 그 71건이 "사건 시각에 실행되지
않았다"가 아니라 "그 프로그램이 이 시스템에서 실행된 적이 있다"이므로,
경로를 보고 판단할 거리가 남아 있습니다.

---

## 2026-08-27 · SRUM 파서가 못 하는 것

파서 자체의 대조 기록은 `docs/artifact-notes.md` 2026-08-27 절에 있습니다.
여기 적는 것은 **알면서 안 한 것과 못 하는 것**입니다.

### 트랜잭션 로그를 재생하지 않는다

실물 이미지의 `SRUDB.dat` 옆에는 `SRU.log`·`SRU0000N.log`·`SRUDB.jfm`
이 함께 있었습니다. 클린 셧다운이 아닌 상태이고, **아직 DB 에 반영되지
않은 기록이 그 로그 안에 있습니다.**

`dissect.esedb` 는 로그를 재생하지 않습니다. 우리도 하지 않습니다.

**영향** — 사고 직전 몇 분~한 시간의 사용량이 빠져 있을 수 있습니다.
SRUM 은 원래 시간 단위로 집계해 주기적으로 써 내려가는 구조라 **가장
최근 구간이 가장 취약합니다.** 유출이 사고 마지막에 일어났다면 그것이
바로 빠지는 구간입니다.

되짚을 방법은 있습니다 — 레지스트리
`HKLM\SYSTEM\CurrentControlSet\Services\DPS\Parameters\...` 아래에
반영 대기 중인 항목이 남기도 합니다. **하지 않았습니다.**

### 시간 해상도가 시간 단위다

SRUM 은 개별 전송을 적지 않고 **한 시간 치를 합쳐** 한 줄로 씁니다.
"14시대에 이 앱이 10MB 를 보냈다"까지고, 그 안에서 언제 몇 번 보냈는지는
없습니다. 목적지 IP·포트·도메인도 없습니다.

그래서 매핑(`T1041`·`T1048`)에서 Sysmon 3·22 를 `priority: 1` 로,
SRUM 을 `priority: 2` 로 두었습니다. **둘은 대체재가 아니라 보완재**
입니다 — 저쪽은 "누가 어디로 연결했나", 이쪽은 "얼마나 나갔나"입니다.

### 앱 경로가 장치 이름이라 06단계가 기각한다

`AppName` 이 `\device\harddiskvolume4\program files\...` 형태로 나갑니다.
프리패치와 같은 부류인데 **프리패치와 달리 바꿀 근거가 없습니다** —
SRUM 에는 볼륨 표가 없어 `HarddiskVolume4` 가 우리가 분석 중인 볼륨인지
알 방법이 없습니다. 틀린 드라이브 문자를 다는 것보다 안 바꾸는 편이
낫다는 판단(프리패치 파서와 같은 자리)이지만, **대가가 있습니다.**

모델이 `C:\Program Files\...` 라고 쓰면 그 문장은 기각됩니다. Amcache 에서
방금 닫은 것과 **같은 부류의 구멍**이고, 이쪽은 이름이 아니라 값의 형태가
문제라 `PATH_FIELDS` 로는 안 풀립니다.

풀 방법 둘 — (a) `$MFT` 나 레지스트리 `MountedDevices` 에서 볼륨 번호와
드라이브 문자의 대응을 읽어 04단계에 넘긴다 (b) 06단계가 장치 경로와
드라이브 경로를 같은 것으로 보는 비교기를 갖는다. **둘 다 안 했습니다.**
(a)가 프리패치·SRUM 을 한꺼번에 푸는 자리로 보이지만 범위가 큽니다.

### 공급자 셋만 읽는다

에너지 사용량과 푸시 알림 테이블은 **일부러 뺐습니다.** 조사 가치가
낮은 데 비해 등록 지점이 일곱 군데씩 늘어납니다. 늘리는 절차는
`.claude/skills/add-parser/SKILL.md` 에 있습니다.

### 임계치가 없어 `signal_source: scope` 다

SRUM 의 모든 행은 "무언가가 자원을 썼다"라 그 사실 자체는 변별력이
없습니다. 신호는 **얼마나** 인데, "몇 바이트부터 이상인가"를 정할 근거가
아직 없습니다. 임계치를 지어내면 그 추측이 틀렸을 때 조용히 증거를
버립니다.

그래서 레지스트리·프리패치와 같이 `scope` 로 두었습니다 — 03단계가 시간
범위로 걸러 온 것을 전부 후보로 올립니다. 실측 규모가 셋 합쳐 423건이라
05단계 쿼터를 태우지 않습니다. **큰 이미지에서 이 가정이 깨지는지는 아직
안 쟀습니다.**

---

## 2026-08-27 · AmcacheParser 대조가 남긴 것

대조 결과 자체는 `docs/artifact-notes.md` 에 있습니다(457건 전부 일치).
여기 적는 것은 **그 대조가 드러낸, 아직 안 고친 것**입니다.

### `InstallDate` 가 우리 표기 규약 밖에 있다

`InventoryApplication` 의 `InstallDate` 는 하이브에 **문자열로** 적혀
있습니다 — `RegSZ`, `'03/20/2017 03:53:52'`. 우리는 원본 그대로 냅니다.
`registry.py` 가 문자열 값을 생김새로 재해석하지 않기 때문이고, 그것은
`comparators.py` 의 "값의 생김새로 판단하지 않는다"와 같은 원칙입니다.

**파서의 오류가 아닙니다.** 그러나 하류에 위험이 남습니다.

- 이 프로젝트의 다른 시각은 전부 ISO 8601 (`2017-03-20T03:53:52.0000000Z`)
  입니다. `fields.InstallDate` 만 `MM/dd/yyyy` 입니다.
- 모델이 "설치일 2017-03-20" 이라고 쓰면 06단계의 정확 문자열 비교가
  `03/20/2017` 과 어긋나 **기각합니다.**

**Amcache 경로 대소문자에서 방금 닫은 것과 같은 부류**인데, 이쪽은
이름이 아니라 **값의 표기**가 문제라 `PATH_FIELDS` 로는 안 풀립니다.

아직 안 고쳤습니다. 고칠 자리 둘로 보입니다 — (a) 06단계가 날짜로 보이는
값을 비교할 때 표기를 흡수한다(그러면 "생김새로 판단하지 않는다"를
어깁니다) (b) 04단계가 이 값 이름 하나를 알고 변환한다(`registry.py` 가
Amcache 값 이름을 이미 아는 자리가 있으므로 그 옆입니다).
**(b) 가 맞아 보이지만 정하지 않았습니다.**

측정된 적은 없습니다. 실측 이미지에 `InventoryApplication` 이 78건 있고
전부 이 표기입니다.

### 로그 재생 차이는 이미지 하나에서만 0이었다

`Amcache.hve.LOG1/LOG2` 를 재생시켜 다시 돌린 결과가 재생 안 한 것과
**행 하나도 다르지 않았습니다.** 우리가 로그를 재생하지 않는 것이 이
이미지에서는 비용이 없었다는 뜻입니다.

**일반화하지 마십시오.** 이미지 하나의 관측이고, `LOG1` 은 0바이트였습니다.
사고 직후 수집한 dirty 하이브에서는 다를 수 있습니다 — SRUM 쪽에 같은
한계를 적어 둔 것과 같은 자리입니다.

### `Root\File` 은 외부 대조가 없는 상태로 남는다

AmcacheParser 는 이 하이브를 "new format" 으로 보고 `Root\File` 361건을
읽지 않습니다. 그래서 그 361건에는 **외부 대조 상대가 없습니다.**

전부 근거가 없는 것은 아닙니다 — 값 이름 대응은 같은 하이브의
`InventoryApplicationFile` 29건에 조인해 얻었고, 그 29건이 이번에 외부
도구로 확인됐습니다(정답지 쪽이 검증됨). 그래도 **`Root\File` 자체의
레코드 수와 값을 독립적으로 확인한 것은 아닙니다.**

옛 포맷을 읽는 다른 도구(예: 옛 버전 AmcacheParser)를 구하면 닫힙니다.
하지 않았습니다.

---

## 2026-08-27 · Amcache 숫자 값 이름 — 채운 뒤에 남은 것

채운 경위와 근거는 `docs/artifact-notes.md` 두 절에 있습니다
(`Root\File` 99.8% 이름 붙음). 여기 적는 것은 **여전히 못 하는 것**입니다.

### 절반 이상이 "문서를 믿은 것"이다

`AMCACHE_FILE_VALUE_PROVENANCE` 의 분포:

```
image  5   이 이미지 안에서 대조해 확인 (15·6·100·101·17)
shape  5   공개 출처 둘이 일치 + 값 생김새가 뒷받침 (16·10·11·12·b)
docs   7   공개 출처 둘이 일치하나 이 이미지로 확인 못 함
```

**`docs` 일곱(`0`·`1`·`3`·`5`·`c`·`f`·`a`)은 틀릴 수 있습니다.** 출처가
둘이고 서로 독립이라 확률은 낮지만, 둘 다 같은 원 출처를 물려받았을
가능성은 배제하지 못합니다 — Zimmerman 과 Plaso 모두 초기 Amcache 역공학
결과를 참조했을 수 있습니다. **독립인 것은 구현이지 원 출처가 아닙니다.**

틀나면 조용히 틀립니다. 잘못된 이름이 붙은 값이 05단계 프롬프트에 들어가고
모델이 그것을 그대로 인용하면, 06단계는 레코드와 문장이 일치하므로
**통과시킵니다.** 그래서 근거 표를 남겼습니다.

### `17` 의 정체가 절반만 밝혀졌다

`$MFT` 대조로 **파일의 타임스탬프라는 것**까지는 확인했습니다(335건 중
329건이 `si_mtime` 과 2초 이내). 그런데 `si_btime` 도 328건으로 거의 같이
맞습니다 — 이 이미지의 해당 파일들이 설치 시각과 수정 시각이 같아서
**둘을 가르지 못했습니다.**

생성 시각과 수정 시각이 다른 파일이 많은 이미지가 있으면 갈립니다.
하지 않았습니다.

### 여전히 숫자로 남는 것

```
Root\File      6개 인스턴스 (레코드 1건) — 8·9·106 등, 이 이미지에 한 건씩만
Root\Programs  7종 — 5·13·14·15·16·17·18
Root\Orphan    c (26건)
```

`Root\Programs` 의 일곱은 **AmcacheParser 자신도 모릅니다** — 그쪽 코드에서도
이름이 `Dword5`·`UnknownBytes` 같은 자리 표시입니다. 뜻 없는 이름으로
바꾸면 숫자로 두는 것보다 나을 게 없어 그대로 뒀습니다.

`Root\Orphan` 은 손대지 않았습니다. AmcacheParser 도 Plaso 도 이 서브키를
다루지 않아 대응표 자체가 없습니다.

### `Root\File` 은 여전히 외부 대조가 없다

앞 절(AmcacheParser 대조)에 적은 그대로입니다. AmcacheParser 는 이
하이브를 "new format" 으로 보고 `Root\File` 361건을 읽지 않으므로,
**우리가 이름을 붙인 그 값들을 외부 도구와 맞춰 본 적이 없습니다.**

`17` 만 `$MFT` 라는 제3의 증거로 확인됐습니다. 나머지는 공개 출처의
문서적 근거와 값 생김새뿐입니다.

---

## 2026-08-27 · 프리패치 적재 경로와 프롬프트 keep 목록 — 남은 것

바꾼 경위와 대조는 `docs/artifact-notes.md` 의 "프리패치 적재 경로" 절에
있습니다. 여기 적는 것은 **여전히 못 하는 것**입니다.

### "실행 파일과 같은 폴더" 를 keep 기준으로 쓰지 못한다

변환이 끝나면 이 비교가 성립합니다. 그래서 재 봤더니 **기준으로 쓸 수
없다는 것이 드러났습니다**(win10_sysmon_testimage, 적재 경로 10,109건):

```
실행 파일과 같은 폴더  4,716건 (46.65%)  적중 레코드 115/127, 중앙값 34
```

당연합니다 — `C:\Windows\System32\svchost.exe` 의 "같은 폴더" 는
System32 이고, 거기 DLL 이 있는 것은 정상입니다. **이 신호가 뜻을 가지려면
"실행 파일이 정상 설치 자리가 아닌 곳에 있다" 와 함께 봐야 합니다.**
둘을 엮는 것은 keep 목록이 아니라 판정 룰의 일이고, 그 룰은 아직
없습니다(`_flags.yaml` 의 `execution_from_unusual_path` 는 Sysmon 만 봅니다).

work.md 가 이것을 "가장 강한 신호" 로 적어 둔 것은 맞지만, **단독으로는
아닙니다.**

### "Windows 폴더 밖" 도 안 넣었다

부정 조건이라 안 넣었습니다. 04단계의 장치 경로 변환이 실패하면
"C:\Windows\ 로 시작하지 않는 것" 이 **전량을 잡아** keep 목록이 조용히
무의미해집니다 — 지금 고친 그 증상이 그대로 되살아납니다. 열거로 쓰면
같은 상황에서 0건이 되고, 0건은 `stats["loaded_paths_converted"]` 에
드러납니다(`flagging.field_startswith` 의 부정 금지와 같은 근거).

### keep 어휘를 침해 이미지로 대조한 적이 없다

`prompt_keep_paths.contains` 는 드롭 자리 어휘와 사용자 폴더입니다. 정상
운영 이미지에서 적중 992건(9.81%) · 적중 레코드 47/127 · 중앙값 5 라는
것까지만 봤습니다. **실제로 악성 DLL 이 그 자리에 있는 이미지로는 확인하지
못했습니다** — 키오스크 스냅샷과 같은 대기 항목입니다.

`max_items: 12` 도 이 분포에서 고른 값이라 다른 환경에서 다시 재야 합니다.

### 프롬프트가 조금 커진다

같은 20자리인데 글자 수가 늘어납니다. 사용자 프로필 경로가 System32
경로보다 길기 때문입니다(실측 프리패치 127건, 20자리 기준):

```
프롬프트에 도달하는 keep 적중 항목   22건 → 313건
그 127건의 글자 수                182,658 → 196,654 (+7.7%)
```

**예산이 알아서 흡수합니다** — `char_budget` 이 같으면 레코드가 몇 건 덜
들어갑니다. 자리를 잃는 쪽과 얻는 쪽 중 어느 쪽이 나은지는 실측한 적이
없습니다. 지금 근거는 "적중 항목이 14배 더 도달한다" 하나뿐입니다.

---

## 2026-08-28 · 04단계 성능 — 무엇을 고쳤고 무엇이 남았나

**의미를 바꾼 것은 없습니다.** 세 변경 모두 "안 쓰는 계산을 안 한다"이고,
두 이미지 전량(646,879건)에서 jsonl 이 바이트 단위로 같습니다. 그래서
한계가 아니라 **기록**입니다 — 다만 남의 코드를 고친 것이 하나 있어
CLAUDE.md 규약대로 여기 적습니다.

### `analyzeMFT` 의 `WindowsTime` 을 고쳤다 (우회가 아니라 수정)

`third_party/analyzeMFT/windows_time.py` 의 `dt`·`dtstr`·`unixtime` 을
생성자 계산에서 property 로 늦췄습니다. 파일 상단에 무엇을 왜 고쳤는지
적어 뒀습니다(`third_party/README.md` 2번).

**우리는 그 셋을 한 번도 읽지 않습니다.** 시각은 `parsers/mft.py` 의
`_times()` 가 `low`/`high` 에서 정수 연산으로 다시 만듭니다 —
`get_unix_time()` 의 float 나눗셈이 마이크로초를 틀어뜨리기 때문이고,
그 근거는 같은 문서의 앞 절에 있습니다. 즉 생성자가 만들던 값은 **전량
폐기물**이었습니다.

실측(98,151 레코드): 생성자 호출 **4,034,184회 = 레코드당 41회**.
`$MFT` 파서가 경로 재구성 때문에 두 번 도는데 1회차는 이름과 부모 번호만
쓰면서도 타임스탬프를 전부 변환하고 버렸습니다.

**남는 위험** — 원본이 갱신되면 이 수정을 다시 얹어야 합니다. 원본에서
`.dtstr` 을 읽는 곳은 `mft_record.to_csv()` 하나이고 우리는 그것을 부르지
않으므로, 갱신 시 확인할 곳도 그 하나입니다.

### 아직 남은 왕복 — 시각을 문자열로 만들고 다시 파싱한다

```
filetime → datetime → strftime 문자열 → parse_timestamp → datetime
             (_times, 78만 회)          (flagging, 126만 회)
```

`_times()` 가 ISO 문자열을 만들고, flagging 의 `timestamp_mismatch`·
`timestamp_truncated` 핸들러가 그것을 **다시 파싱합니다.** 레코드당 12.9회
파싱하는데 고유 타임스탬프는 8개입니다.

**안 고쳤습니다.** 04단계 산출물이 문자열이어야 하는 것은 스키마가
정한 것이라 바꿀 수 없고, 고칠 자리는 flagging 안의 레코드 단위 캐시인데
범위를 따로 잡는 편이 낫다고 판단했습니다.

### `$MFT` 1회차가 여전히 전체 파싱을 한다

`_build_index` 는 `filename` 과 부모 번호만 쓰는데 `MftRecord()` 전체를
만듭니다. `WindowsTime` 을 늦추면서 그 비용의 큰 몫이 사라졌지만
속성 파싱 자체는 남아 있습니다. **더 줄이려면 `$FN` 만 읽는 경로를 따로
내야 하는데, 그것은 남의 코드의 파싱 순서에 손을 대는 일이라 하지
않았습니다.**

### evtx 와 프리패치는 성격이 다르다

둘은 flagging 개선의 영향을 거의 받지 않았습니다. 각각 python-evtx 의
노드 순회와 LZXPRESS Huffman 복호가 시간을 먹기 때문입니다. evtx 는 아래
"넣지 않은 것 — 네이티브 evtx 엔진" 절에 따로 적었습니다.

---

## 2026-08-28 · 오프셋을 되짚는 도구가 없었다 (**해결됨**, 2026-08-30)

`parsed_record` 스키마의 `offset` 은 이렇게 설명돼 있습니다 — *"원본 바이트
위치. 기존 파서를 쓰지 않고 직접 구현하는 이유가 이 필드다.
`tools/hexdump_record.py` 가 이 값으로 원본을 되짚는다."*

**그 파일은 0바이트였습니다.** 2026-08-14 에 자리만 만들어 두고 채우지
않았습니다. 즉 `offset` 을 넣고 다니지만 **그것으로 원본을 실제로 되짚어
본 적은 없었습니다.** 값이 틀렸다는 뜻은 아니었습니다 — `$MFT` 는
`레코드번호 × 1024` 로 검산했고(`artifact-notes.md` 2026-08-14), `$UsnJrnl`
은 오프셋 증가분과 USN 증가분이 일치하는 것으로 확인했습니다(2026-08-19).
다만 **분석가가 보고서를 들고 원본 바이트로 내려가는 경로**가 비어
있었습니다.

이것이 드러난 계기는 evtx 엔진 교체 검토였습니다. `pyevtx-rs` 가 오프셋을
주지 않아 후보에서 빠졌는데(아래 "넣지 않은 것 — 네이티브 evtx 엔진"),
그 근거를 확인하다가 스키마가
가리키는 도구가 비어 있는 것을 발견했습니다.

**2026-08-30 에 채웠습니다.** 실물 이미지에서 10종 357건을 되짚었고 어긋남
0건입니다. 대조는 파서로 다시 파싱해 비교하는 것이 아니라 **레코드가 자기
안에 들고 있는 식별자**로 합니다 — `$MFT` 헤더의 레코드 번호와 업데이트
시퀀스, USN 레코드의 USN, evtx 의 EventRecordID. 전체 기록은
`docs/artifact-notes.md` 2026-08-30 절에 있습니다.

### SRUM 은 페이지까지만 대조된다 (실물 확인함)

`srum:*` 의 `offset` 은 레코드가 아니라 **레코드가 실린 ESE 페이지**입니다
(`parsers/srum.py` 의 `_file_offset`). ESE 레코드는 페이지 안에서 태그로
가리켜지고 압축된 키 접두어를 공유해서, 레코드 하나의 시작 바이트를 파일
좌표로 말할 수 없습니다. **이것은 도구의 한계가 아니라 형식의 성질이며,
없어지지 않습니다.** 도구는 그 사실을 출력에 싣습니다.

같은 페이지에 여러 레코드가 실린 것도 실물로 봤습니다 —
`SRUM-CONN#1·2·3` 의 오프셋이 셋 다 `0x60000` 입니다. 그 안에서
레코드를 가르는 것은 `AutoIncId` 입니다.

페이지 단위 대조 자체는 2026-08-30 에 실물로 확인했습니다(3종 238건 중
77건, 어긋남 0건). 페이지 크기는 추측하지 않고 ESE 헤더 `0xEC` 에서
읽습니다 — 4096의 배수인지만 보면 8KB DB 에서 엉뚱한 오프셋의 절반이
통과합니다.

### 남은 것 — 프리패치는 오프셋으로 가릴 것이 없다

`offset` 이 항상 `0x0` 이라(레코드가 곧 파일) 되짚기가 `.pf` 파일명 뒤
8자리 해시 대조로 대체됩니다. 그 값도 Windows 가 쓴 것이라 독립적이지만,
**다른 아티팩트와 같은 종류의 확인은 아닙니다.**

## 2026-08-28 · 4Kn 디스크 — 고친 것과 남은 것

`apply_fixups` 가 섹터 크기를 512로 고정하고 있었습니다. 인자는 있었지만
유일한 호출부(`parsers/mft.py`)가 넘기지 않았고, 부트섹터에서 읽는 코드는
레포 어디에도 없었습니다.

**4Kn 디스크(섹터 4096)에서 어떻게 되는지 합성 레코드로 쟀습니다.**

```
4Kn (4096바이트 레코드)
   기본값(512)   → FixupError: 섹터 1 끝의 USN 불일치
   유도값(4096)  → 성공
```

**그리고 그 실패가 조용했습니다.** `FixupError` 는 `StructError` 의
하위이고 호출부가 `except StructError: continue` 로 삼켰습니다. 카운터도
경고도 `errors.jsonl` 도 없었으므로, **4Kn 이미지를 넣으면 `$MFT: 0건` 이
나오고 이유가 아무 데도 안 남습니다.** 손상도 아니고 미지원도 아닌
"아무것도 없음"이 산출물이 됩니다.

### 부트섹터 대신 레코드에서 유도한다

업데이트 시퀀스 배열은 항목이 `섹터 수 + 1`개이므로 레코드 크기를 섹터
수로 나누면 나옵니다(`sector_size_of`). **부트섹터를 읽는 것보다 낫습니다**
— `evidence/` 가 폴더이고 추출된 `$MFT` 만 있는 경우에는 부트섹터가 아예
없어서 "부트섹터에서 읽어 넘긴다"가 성립하지 않습니다.

건너뛴 레코드는 이제 `stats` 에 셉니다(`parse_errors` / `fixup_errors`).
둘을 나눠 세는 이유는 원인이 다르기 때문입니다 — **`fixup_errors` 가 전체와
비슷하면 손상이 아니라 섹터 크기 판정이 틀린 것**을 의심해야 합니다.

### 남은 것 — 4Kn 실물 이미지가 없다

**합성 레코드로만 확인했습니다.** 손에 있는 이미지 셋은 전부 512n 입니다.
유도 공식이 맞다는 것과 4Kn 실물이 실제로 읽힌다는 것은 다른 주장입니다.

`$MFT` 외에는 손대지 않았습니다. NTFS 에서 업데이트 시퀀스를 쓰는 구조는
`$MFT` 말고도 있지만(인덱스 버퍼 등) 본 버전은 그것들을 읽지 않습니다.

---

## 2026-08-30 · 모델 호출 실패가 규약 밖이었다 (**해결됨**)

`--llm ollama`로 부를 때 **타임아웃이 아닌** 실패 — 모델명 오타, 서버
미기동, 잘못된 호스트 — 를 아무도 잡지 않았습니다. `llm.LLMError`가 파이썬
트레이스백으로 그대로 올라왔습니다.

멈추기는 했으므로 조용한 실패는 아니었지만, **`errors.jsonl`에 남지
않았습니다.** 07단계가 그것을 볼 수 없고, `errors.tally()`가 세는 통계에도
안 들어갑니다. "폴백을 만들지 않는다 — 실패는 `errors.jsonl`에 기록하고
사유를 출력하며 중단한다"는 규약의 밖이었습니다.

**02·05 양쪽에서 `llm_error`로 기록하고 즉시 중단합니다. 재시도하지
않습니다** — 모델명 오타는 세 번 불러도 같은 답이라 시간만 씁니다(같은
논리가 5장 ⑤에 있습니다).

### `ERROR_TYPES`에 `llm_error`를 추가했다 (**전체 공지 대상**)

`src/common/errors.py`의 고정 어휘이고 공용 모듈입니다. `timeout`으로
합치지 않은 이유는 **조치가 다르기 때문**입니다 — 기다릴 것인가, 설정을
고칠 것인가. 07단계 통계가 이 어휘로 직접 산출되므로 합치면 "모델이
느리다"와 "모델명을 잘못 적었다"가 한 칸에 들어갑니다.

### 이것이 드러난 방식 — 스텁으로는 나올 수 없었다

`tests/test_llm_live.py`를 만들면서 나왔습니다. 실제 모델을 부르는
테스트를 켜자 **네 개 중 하나가 실패했고**, 그 하나가 이 결함이었습니다.
`StubBackend`는 네트워크를 타지 않으므로 이 경로를 지나갈 수 없습니다.

같은 이유로 드러난 것이 앞서 넷 더 있습니다(5장 — 스키마의 `ref` 패턴
누락, 배분의 실물 규모, 120초 고정 타임아웃, `num_ctx` 4096). **실제
모델을 부르는 테스트가 없던 동안 이 부류는 실측 때마다 사람이 발견해
왔습니다.**

---

## 2026-08-31 · HID/BadUSB 매핑이 카탈로그와 어긋나 있었다 (**해결됨**)

PR #35 가 매핑 YAML 아홉과 아티팩트 다섯을 더하면서 등록 지점 몇 곳을
빠뜨렸고, `main` 이 테스트 11건 실패 상태로 있었습니다. 고치면서 드러난
것을 적습니다. **셋은 테스트가 잡았고 셋은 테스트도 못 잡았습니다.**

### 테스트가 잡은 것

**관문 1이 비어 있었다.** 매핑 YAML 이 여덟 기법을 들고 왔는데
`KNOWN_TECHNIQUES` 에 없었습니다(`T1016`·`T1018`·`T1021.002`·`T1071.001`·
`T1082`·`T1083`·`T1105`·`T1569.002`). 이 상태면 02단계가 그 기법을 낼 수
없어 **매핑이 도달 불가**입니다. 관문 1이 관문 2보다 앞이라는 것이 이
자리에서 드러났습니다.

지금은 이것이 더 세게 막힙니다 — 02단계 출력 제약의 enum 이
`KNOWN_TECHNIQUES` 에서 나오므로(같은 날 작업), 목록에 없는 기법은 모델이
**생성 자체를 못 합니다.** 예전에는 내면 `is_known` 이 기각했고 지금은
나오지 않습니다. 어느 쪽이든 못 쓰는 것은 같지만, 이 목록의 결손이 곧
기능 결손이 되는 정도가 커졌습니다.

**관문 3이 반쯤 열려 있었다.** `evtx:Defender`·`evtx:PowerShell` 이
`supported: true` 인데 등록 지점 넷(`CHANNEL_FALLBACK`·`FILE_LAYOUT`·
출력 파일명·`ref` 접두어)이 비어 있었습니다. **`ref` 접두어를 더하려면
동결 스키마 둘의 패턴을 손으로 고쳐야 합니다** — `parsed_record` 와
`findings` 가 접두어를 열거하고 있고 생성기가 없습니다. `flags` enum 은
`sync_flag_enum.py` 가 맞춰 주는데 `ref` 접두어는 그런 것이 없습니다.

**관문 4가 비어 있었다.** `T1021.002` 의 5140/5145 와 `T1059.001` 의
4103/4104 를 받을 flag 가 없었습니다. `share_accessed`·`powershell_logged`
를 만들었습니다.

### 테스트도 못 잡은 것 — `scope:` 오타

`T1059.001.yaml` 과 `T1105.yaml` 이 `scope_template:` 대신 **`scope:`** 를
썼습니다. 로더가 모르는 키를 조용히 버리므로 `scope_template` 이 `{}` 가
되고, **적어 둔 `event_ids` 가 통째로 사라집니다.**

증상이 고약합니다. `T1059.001` 은 `evtx:Sysmon` 에 `[1, 3]` 을 적었는데
그것이 사라져 **Sysmon 채널 전량 요청**이 됩니다 — EID 1(프로세스 생성)이
전부 후보가 되어 05단계 쿼터를 혼자 태웁니다. 좁히려고 적은 것이 넓히는
결과가 됩니다.

로더가 모르는 키를 거부하면 잡혔을 자리입니다. **아직 안 고쳤습니다** —
다른 매핑 38개가 전부 정상이라 급하지 않고, 거부로 바꾸면 기존 파일의
주석성 키까지 걸릴 수 있어 별도로 봐야 합니다.

### 테스트도 못 잡은 것 — 카탈로그 주석이 사실과 달랐다

`_artifacts.yaml` 이 `evtx:Defender` 를 "T1562.001·T1059.001 매핑이
참조한다"고 적었는데 **어느 매핑도 요청하지 않았습니다.** 파서를 붙여도
아무도 안 쓰는 채널이 됩니다. `T1562.001` 에 이어 붙이고
`defender_disabled` flag 를 만들었습니다.

이 부류는 자동으로 잡기 어렵습니다. "카탈로그에 있는데 아무 매핑도
요청하지 않는 아티팩트"를 세는 검사는 가능하지만, 그것이 정상인 경우도
있습니다(`$LogFile` 처럼 미지원으로 두는 것).

### 실물로 확인하지 못한 값

이번에 넣은 event_id 는 **전부 미확인입니다.** 손에 있는 두 이미지에
Defender·PowerShell 채널이 없고, Security 5140/5145 는 개체 액세스 감사가
켜져 있어야 남는데 그 기록이 없습니다.

| flag | event_id | 근거 |
|---|---|---|
| `share_accessed` | 5140, 5145 | 공개 자료. 감사 정책 의존 |
| `powershell_logged` | 4103, 4104 | 공개 자료. **둘 다 기본 꺼짐** |
| `defender_disabled` | 5001, 5004, 5007 | 카탈로그 설명의 값을 그대로 옮김 |

`powershell_logged` 는 특히 주의해야 합니다. 스크립트블록 로깅은 그룹
정책으로 켜야 남으므로, **이 채널이 비어 있다고 PowerShell 이 안 돌았다는
뜻이 아닙니다.** 보고서가 그렇게 읽히지 않아야 합니다.

### 함께 움직인 것

`mapping_table_version` 을 1.1 → 1.2 로 올렸고 세 곳(`_artifacts.yaml`,
`test_mapping_loader.py`, 목업 `03_selection.json`)을 맞췄습니다. 목업
`07_report.md` 도 제외 목록이 늘어 다시 만들었습니다. `C-001` 의
`selected`·`deferred` 는 2/2 로 그대로입니다 — 늘어난 것은 `excluded` 뿐입니다.

`tests/test_evtx_parser.py` 의 "모르는 아티팩트를 거부하는가"가 두 번째로
깨졌습니다. 미지원 예시로 실재 채널 이름을 쓰다가 그 채널이 지원 대상이
되면서입니다(`evtx:Application` 에 이어 `evtx:PowerShell`). 채널이 될 수
없는 이름으로 고정했습니다.

---

## 2026-09-01 · 프리패치가 이 이미지에서 통째로 죽어 있었다 (**해결됨**)

키오스크 스냅샷의 프리패치 192건이 전부 `UnknownLayout` 이었습니다.
Windows 10 **19045.6466** 의 `FILE_INFORMATION` 이 212바이트인데 표에 버전
30 의 216·220·224 만 있었습니다. `(30, 212)` 를 **192건 전량 실측으로**
채웠습니다 — 자리를 정한 방법과 대조 기록은 `docs/artifact-notes.md`
2026-09-01 절에 있습니다.

`0건 → 192건`, 그중 153건이 사고 당일 실행분입니다.

### 이 실패가 두 번 숨었다

**① `parse_errors` 가 `errors.jsonl` 에 남지 않았습니다** (**해결됨** —
아래 "파서 실패가 `errors.jsonl` 밖에 있었다" 절). 매니페스트에는
`parse_errors: 192` 가 정확히 적혔고 04 로그도 찍었는데, `errors.jsonl` 이
만들어지지 않아 `tools/live_check.py` 가 **"재시도·실패 0건 — 모든 단계가
첫 시도에 통과"** 로 결산했습니다.

"폴백을 만들지 않는다 — 실패는 `errors.jsonl` 에 기록한다"가 규약인데,
**파서 단위 실패만 그 규약 밖에 있었습니다.**

**② 좋은 경고가 나쁜 경고를 덮었습니다.** 같은 실행에서 evtx 청크 복구
경고가 215줄 생략될 만큼 쏟아졌습니다. 그건 python-evtx 가 놓친 것을
우리가 되살린 **좋은 신호**인데, 화면에서 프리패치 실패가 그 사이에
묻혔습니다.

### 함께 드러난 것 — `name_pattern` 은 아무도 모르는 키다 (**해결됨** — 아래 "매핑이 모르는 키를 써도" 절)

`mappings/windows/T1105.yaml` 이 프리패치를 `name_pattern: [CERTUTIL.EXE,
POWERSHELL.EXE]` 로 좁히는데, **`src/` 어디에도 그런 키가 없습니다.**

```
Scope 가 아는 필드 : path_prefix, extensions, event_ids, start, end
merge_scopes 의 UNION_KEYS : path_prefix, extensions, event_ids
```

`merge_scopes` 가 조용히 버립니다. 2026-08-31 에 고친 `scope:` 오타와
**같은 부류**입니다 — 매핑이 모르는 키를 써도 로더도 병합기도 아무 말
하지 않습니다. 이번에는 버려진 결과가 넓어지는 쪽이라 손해가 없었지만
(프리패치 전량 요청이 됐습니다), 반대 방향이면 증거를 놓칩니다.

셋 다 이 브랜치 밖이었고, 아래 두 절에서 각각 닫혔습니다.

---

## 2026-09-01 · 파서 실패가 `errors.jsonl` 밖에 있었다 (**해결됨**)

아티팩트 하나가 **100% 실패해도 단계가 정상 종료하고 `errors.jsonl` 이
아예 만들어지지 않았습니다.** `parse_errors` 는 `_manifest.json` 에만
남았습니다.

실측에서 물렸습니다. 키오스크 스냅샷의 프리패치 192건이 전부
`UnknownLayout` 이었는데 `tools/live_check.py` 가 11개 관문 전부 PASS 를
주고 이렇게 결산했습니다.

```
재시도·실패  0건 (errors.jsonl 없음 — 모든 단계가 첫 시도에 통과)
```

**같은 리포트가 위쪽에서는 "파싱 오류 192건"을 찍고 있었습니다.** 산출물이
아니라 결산이 틀린 것이었고, 그 이유는 `live_check` 가 `errlog.tally` 를
읽는데 04단계가 거기에 쓰지 않았기 때문입니다.

"폴백을 만들지 않는다 — 실패는 `errors.jsonl` 에 기록한다"가 규약인데
**파서 단위 실패만 그 규약 밖에 있었습니다.** `parse_error` 는 예전부터
`ERROR_TYPES` 어휘에 있었고, 04단계가 그것을 치명적 중단(`abort`)에만
쓰고 있었습니다.

### 고친 방식

`note_skip` 이 스킵을 두 곳에 남기는 것과 같은 규약으로 맞췄습니다 —
`errors.jsonl` 은 전 단계가 공유하는 집계용이고 매니페스트는 이 단계의
산출물입니다.

**아티팩트당 한 줄만 씁니다.** 프리패치 192건이면 192줄이 되어 다른
단계의 실패가 묻힙니다. 어느 파일이 왜 실패했는지는 파서가 이미 stderr 로
말합니다.

**전량 실패와 일부 실패를 `detail.total_failure` 로 가릅니다.** 9,606건을
내면서 192구간을 건너뛴 것(evtx)과 192건을 읽어 한 건도 못 낸 것
(프리패치)은 조치가 다릅니다. 앞은 로그가 더러운 것이고 뒤는 그 아티팩트가
죽은 것입니다. 전량 실패는 stderr 에도 따로 한 줄 냅니다 — 실측에서 이
실패가 evtx 청크 복구 경고 215줄 사이에 묻혔습니다.

04단계 요약 줄에도 실패 건수를 넣었습니다. 예전에는
`prefetch: 0건 (플래그 0건)` 이라 192건이 실패했다는 사실이 그 줄에
없었습니다.

### 남은 것

**`errors.jsonl` 이 생겨도 단계는 여전히 성공으로 끝납니다.** 읽을 파일이
있었고 파서가 돌았으므로 그것이 맞다고 봤습니다 — 전량 실패를 실패로
처리하면 "이 이미지에 그 아티팩트가 원래 이상하다"는 정상 상황까지 파이프
라인을 멈춥니다. 대신 결산에 드러나게 했습니다. 이 판단이 틀렸다면
`live_check` 가 전량 실패를 FAIL 로 올리는 쪽이 다음 후보입니다.

---

## 2026-09-01 · 미지원 레코드 버전이 결산 밖에 있었다 (**해결됨**)

`$UsnJrnl` v3/v4 를 만나면 파서가 **손상이 아니라 지원 범위 밖**으로
알아보고 레코드 하나를 통째로 건너뜁니다(8바이트씩 걸어 들어가면 본문을
레코드로 오해해 가짜 손상이 잡힙니다). 그 집계가
`stats["unsupported_version"]` 인데, **매니페스트에도 `errors.jsonl` 에도
실리지 않았습니다.** 04단계가 매니페스트에 싣던 것은 `parse_errors` 와
`unreadable_bytes` 둘뿐이었습니다.

v3 를 쓰는 볼륨이면 저널이 통째로 비는데 화면이 이렇게 말합니다.

```
$UsnJrnl: 0건 (플래그 0건) → usnjrnl.jsonl
```

남는 것은 레코드마다 찍히는 stderr 경고뿐이고, 그 경고가 무엇에 묻히는지는
바로 위 절에 있습니다 — 같은 실행에서 evtx 청크 복구 경고 215줄이 프리패치
전량 실패를 덮었습니다.

**같은 부류가 다른 파서에 남아 있던 것입니다.** 위 "파서 실패가
`errors.jsonl` 밖에 있었다" 를 고칠 때 본 것이 `parse_errors` 뿐이었고,
지원 범위 밖은 그 밖에 있었습니다.

### 실물에서 물린 것이 아니라 코드에서 찾았습니다

**이것은 실측 사고가 아닙니다.** 지금까지 연 저널은 전부 v2.0 이었습니다
(2026-08-18 · 355,850건, V3/V4 0건). 버전 축 작업(`work.md` 1번)에서
"어긋나면 시끄러운가 조용한가"를 가르다 나왔습니다.

즉 **아직 아무도 v3 저널을 이 파서에 넣어 본 적이 없습니다.** 이 수정은
그때 무슨 일이 벌어지는지를 보이게 만든 것이지, v3 를 읽게 만든 것이
아닙니다.

### 고친 방식 — 두 수를 합치지 않습니다

- `_manifest.json` — `unsupported_version`. 0이면 키를 만들지 않습니다
  (`unreadable_bytes` 와 같은 규약입니다)
- `errors.jsonl` — 아티팩트당 한 줄. 손상과 미지원이 함께 있어도 줄을
  나누지 않고 한 줄에 두 수를 담습니다
- 04단계 요약 줄 — `, 미지원 버전 N건`

**`parse_errors` 에 합치지 않았습니다.** 조치가 다릅니다 — 앞은 저널이
더럽거나 잘린 것이고, 이것은 **파서를 넓혀야 하는 것**입니다. 단위도
다릅니다(구간 수 / 레코드 수).

**어휘는 `parse_error` 를 그대로 씁니다.** `ERROR_TYPES` 는 `src/common/`
이라 새 유형을 만드는 것이 전체 공지 대상이고, 07단계 통계가 그 어휘로
직접 산출됩니다. "읽지 못한 레코드가 있다"는 사실은 같으므로, 손상과 지원
범위 밖의 구분은 `detail` 의 두 수와 문장이 집니다.

```
"$UsnJrnl: 레코드가 한 건도 나오지 않았습니다 — 지원하지 않는 레코드 버전
 3건을 건너뛰었습니다 (손상이 아니라 지원 범위 밖입니다)"
```

되돌려 확인했습니다. 새 시험 둘을 고치기 전 `parse.py` 로 돌리면 둘 다
`KeyError: 'unsupported_version'` 으로 실패하고, 그때 화면은
`$UsnJrnl: 1건 (플래그 1건)` — 건너뛴 사실이 어디에도 없습니다.

### 남은 것 둘

**① v3/v4 를 읽지는 못합니다.** 이 수정은 못 읽는다는 사실이 보이게 된
것뿐입니다. 읽게 만들려면 `file_entry`·`file_sequence` 를 무엇으로 채울지
먼저 정해야 합니다 — v3 의 파일 참조는 128비트 파일 ID 라 NTFS 의
(엔트리, 시퀀스) 로 쪼개지지 않는데, 그 두 필드가 USN 레코드를 `$MFT` 와
잇는 유일한 통로입니다. `ref` 는 영향이 없습니다(`record_num` 이 USN 값
자체입니다).

**② 경고가 레코드마다 찍히던 것** (**해결됨**, 같은 날 이어서). v3 볼륨이면
레코드 수만큼 쏟아져 방금 보이게 만든 결산 한 줄을 도로 덮었습니다.

`parse_errors` 가 손상을 **구간 단위**로 세는 것과 같은 규약으로 맞췄습니다 —
연속된 미지원 레코드는 한 줄로 나오고, 그 줄에 시작 오프셋·바이트 수·버전·
건수가 함께 들어갑니다.

```
$UsnJrnl: @0x0 부터 400바이트에서 지원하지 않는 USN 레코드 버전: v3.0
          — 레코드 5건을 건너뛰었습니다
```

**집계는 레코드 단위 그대로입니다.** 매니페스트와 `errors.jsonl` 이 싣는 수는
바뀌지 않았습니다. 묶은 것은 로그뿐입니다.

**버전이 달라지면 다른 구간으로 봅니다.** v3 와 v4 를 한 줄에 묶으면 어느
쪽이 몇 건인지 말할 수 없습니다. 정상 레코드나 손상 구간을 만나도 끊깁니다.

그러다 **오래된 구멍 하나가 함께 드러났습니다.** 스트림이 끝나는 경로
(`if not buf: return`)가 열린 구간을 닫지 않고 나갔습니다. 저널의 **마지막**
부분이 손상이면 `parse_errors` 는 올라가는데 "@0x… 부터 N바이트를 읽지
못했습니다" 한 줄이 통째로 사라졌습니다. 미지원 구간 때문에 눈에 띄었고,
같은 자리에서 둘 다 닫습니다.

---

## 2026-09-01 · 매핑이 모르는 키를 써도 아무도 말하지 않았다 (**해결됨**)

로더가 모르는 키를 조용히 버렸습니다. 매핑은 데이터라 오타가 문법 오류가
아니라 **뜻 없는 키**로 나타나고, YAML 파서도 스키마도 그것을 잡지
못합니다. 두 번 물렸습니다.

**① `scope:` 오타 (2026-08-31).** `T1059.001` 과 `T1105` 가
`scope_template` 대신 `scope:` 를 썼습니다. 적어 둔 `event_ids` 가 통째로
사라져 `evtx:Sysmon` 이 **채널 전량 요청**이 됐습니다 — EID 1(프로세스
생성)이 전부 후보가 되어 05단계 쿼터를 혼자 태웁니다. **좁히려고 적은 것이
넓히는 결과가 됐습니다.**

**② `name_pattern` 유령 키 (2026-09-01).** `T1105` 가 프리패치를
`name_pattern: [CERTUTIL.EXE, POWERSHELL.EXE]` 로 좁히려 했는데 `Scope`
에도 `merge_scopes` 의 `UNION_KEYS` 에도 없는 이름입니다. 매핑에는 좁혔다고
적혀 있고 실제로는 폴더 전체가 요청되고 있었습니다.

셋째가 하나 더 있었습니다 — `T1105` 의 `$MFT` 가 `time_range: true` 를
적었는데, 그것은 무시되는 것이 아니라 `scope_resolver` 가 시나리오의 값으로
**덮어씁니다.** 뜻이 있어 보이는데 아무 일도 하지 않습니다.

### 고친 방식

세 층 전부에서 모르는 키를 로드 시점에 거부합니다 — 최상위,
`artifacts[]`/`followups[]`, `scope_template`. 어휘는 `mapping_loader` 에
`MAPPING_KEYS`·`REQUEST_KEYS`·`FOLLOWUP_KEYS`·`SCOPE_KEYS` 로 있고,
`docs/mapping-guide.md` 에 표로 옮겼습니다.

`followups[]` 는 `technique`·`artifact` 를 직접 적으므로 어휘가 다릅니다.
거부가 과하면 정상 매핑이 안 들어오므로 양쪽을 시험으로 함께 고정했습니다.

기존 매핑 40개는 전부 통과합니다. 걸린 것은 `T1105` 의 죽은 키 둘뿐이고
지워서 실제 동작과 맞췄습니다 — **동작은 바뀌지 않습니다.** 둘 다 이미
버려지고 있었습니다.

### 남은 것 — 이름으로 좁히는 축이 없다

`T1105` 의 원래 의도(certutil·powershell 실행만 보기)를 지금 표현할 수단이
없습니다. `path_prefix` 에 `C:\Windows\System32\certutil.exe` 를 못박을 수는
있지만, 그러면 **다른 자리에 복사해 둔 실행 파일을 놓칩니다** — T1105 가
겨냥하는 것이 바로 그런 실행입니다.

`Scope` 에 이름 매처를 더하는 것이 후보인데, `Scope` 는 모든 파서가
공유하므로 각 파서가 "이름"을 무엇으로 볼지(프리패치는 실행 파일,
`$MFT` 는 파일명, evtx 는 없음)를 먼저 정해야 합니다. 그전까지 `T1105` 의
프리패치 요청은 폴더 전체이고, 그 사실이 이제 YAML 에 보입니다.

---

## 2026-08-28 · 넣지 않은 것 — 네이티브 evtx 엔진 (`pyevtx-rs`)

**여기 적힌 수치는 전부 잰 것이고, 추정은 추정이라고 적었다.** `work.md` 6번에
있던 것을 옮겨 왔다 — 큐가 아니라 닫힌 결정이라 이 문서가 자리다.

### 결론 먼저 — 네이티브 엔진 교체는 후보에서 빠진다

`pyevtx-rs` 는 **레코드의 원본 바이트 오프셋을 주지 않는다.** 타입 스텁
전체(`_native.pyi`)와 실제 반환값을 확인했다 — `records()` 도
`records_json()` 도 키가 셋뿐이다.

```
event_record_id / timestamp / data
```

API 어디에도 레코드 오프셋이 없다(`Event.template_offset` 은 WEVT
매니페스트 개념이라 무관하다).

**그런데 `offset` 은 동결된 `parsed_record` 스키마의 필수 필드다.** 설명이
이렇게 적혀 있다 — *"원본 바이트 위치. 기존 파서를 쓰지 않고 직접 구현하는
이유가 이 필드다."* `work-guide.md` 3.1 도 같은 말을 한다:

> 직접 구현의 이유는 성능이 아니라 **설계 요구**다. 기존 파서는 파싱된
> 값만 반환하고 원본 오프셋을 주지 않아, 본 프로젝트의 근거 추적 구조가
> 성립하지 않는다.

그리고 `work-guide.md` 원칙 4가 *"모든 분석 문장은 원본 오프셋을
참조한다"* 이다. 07단계도 `report.py` 에서 `offset` 을 읽는다.

**즉 이 교체는 발표에서 내세우는 논지를 정면으로 깬다.** 그리고 python-evtx
와 공존이 안 되므로(아래) 오프셋만 따로 얻어 올 우회로도 없다.

**후보를 새로 찾을 때의 첫 관문도 이것이다** — 빠르기보다 먼저,
레코드마다 원본 바이트 오프셋을 주는가.

### 아래는 그 결론에 이르기까지 잰 것 — 다시 재지 않아도 된다

#### 병목은 문자열이 아니라 python-evtx 의 순회다

04단계에서 evtx 는 **199 rec/s** 로 나머지 파서의 1/50 이다(레지스트리
10,760 · USN 12,598 · MFT 3,507). 카탈로그 25개 중 14개가 evtx 라
채널을 늘리는 방향이 곧 이 비용을 늘리는 방향이다.

처음에는 `record.xml()` → `ET.fromstring` 의 **이중 파싱**이 원인으로
보였다. 쪼개 보니 아니었다.

| | 몫 |
|---|---|
| `ET.fromstring` 재파싱 | **1%** |
| `record.xml()` 안의 노드 순회 | 36% |
| `record.xml()` 안의 문자열 조립 | 64% |

**`lxml` 교체는 상한이 1.01배다.** 그리고 전제도 틀렸다 — CPython 의
`xml.etree.ElementTree` 는 순수 파이썬이 아니라 `_elementtree.pyd`(expat)
가 받친다. 이미 C 다.

**문자열을 안 만들고 딕셔너리로 바로 받아도 상한이 2.80배다.** 같은
노드를 밟되 문자열만 안 만드는 함수를 짜서 쟀다. 순회 자체가 남는데,
그것이 python-evtx 의 구조다 — 필드마다 memoize 데코레이터를 타고
`struct.unpack` 을 하고 노드 길이를 재귀로 구한다.

#### `pyevtx-rs` 는 정합성 관문은 통과했다

오프셋만 아니었으면 갈 만했다는 뜻이라 함께 남긴다.

| 채널 | pyevtx-rs | 우리 | wevtutil | 레코드 ID 집합 | rec/s |
|---|---|---|---|---|---|
| System | 1,268 | 1,268 | 1,268 | 일치 | 59,946 |
| Security | 636 | 636 | 636 | 일치 | 86,617 |
| Application | 233 | 233 | 233 | 일치 | 64,232 |
| BITS | 7 | 7 | 7 | 일치 | 6,619 |

**처리량보다 중요했던 것은 청크 복구다.** System.evtx 는 헤더가 선언한 것
밖에 청크가 하나 더 있고, 우리 파서는 그것을 살려 1,268건을 낸다
(python-evtx 를 그대로 쓰면 1,238건). pyevtx-rs 도 **1,268건**이다.

흠도 하나 있었다 — Security·Application 에서 마지막 유효 레코드 **뒤에**
`RuntimeError: Failed to parse chunk header` 를 던진다. 레코드 ID 집합이
우리·wevtutil 과 같으니 손실은 없지만, 이것을 "끝"으로 처리하면 진짜
손상과 구분이 안 된다.

#### 끝단 이득은 215배가 아니라 2.75배였다

04단계에서 Security 636건이 3.58초인데 그 안이 이렇다.

| | 초 | 몫 |
|---|---|---|
| python-evtx `record.xml()` | 2.27 | 63% |
| `ET.fromstring` | 0.02 | 1% |
| **우리 코드** (필드 추출·flagging·스키마 검증·jsonl 쓰기) | **1.29** | **36%** |

파싱이 0 이 되어도 1.30초가 남는다.

#### 두 패키지는 Windows 에서 공존할 수 없다

`pip install evtx` 를 하는 순간 **python-evtx 가 깨졌다.** python-evtx 는
`Evtx`, pyevtx-rs 는 `evtx` 로 import 하는데 Windows 는 둘을 같은
디렉터리로 본다. pyevtx-rs 파일이 `site-packages/Evtx/` 안에 덮어써져
`from Evtx.Evtx import FileHeader` 가 `ImportError` 를 냈다.

재현이 필요하면 **격리해서** 한다. 프로젝트 `.venv` 에 깔면 안 된다.

```bash
.venv/Scripts/python.exe -m pip install --target <임시디렉터리> evtx
# 그 디렉터리를 sys.path 맨 앞에 놓고, 같은 프로세스에서 Evtx 를 import 하지 않는다
```

### 그래서 남은 선택지 셋

| | 이득 | 대가 |
|---|---|---|
| **A. 파이썬 딕셔너리 워커** | 1.9배 (채널당 3.05s → 1.6s) | `render_root_node` 상당분 재구현 약 200줄. 오프셋·청크 진단은 전부 유지된다 |
| B. python-evtx 를 `third_party/` 로 들여와 직접 고친다 | **미측정** (memoize 래퍼·`declare_field` 가 비용 상위) | 벤더 코드를 떠안는다 |
| C. 안 한다 | — | evtx 는 199 rec/s 로 남는다 |

A 는 되돌리기 쉽고(의존성 무변경) 대조 하네스도 이미 있다. 다만 템플릿
치환 의미를 다시 구현하는 일이라 evtx 파서에서 가장 조심할 자리다.
**1.9배를 위해 검증된 경로를 다시 쓰는 값이 있는지가 판단할 몫이고,
2026-08-28 시점에는 집지 않기로 했다.**

---
