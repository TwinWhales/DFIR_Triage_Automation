# limitations-log.md — 해결된 것의 기록

`docs/limitations.md` 에서 옮겨 온, **이미 해결된** 항목의 기록입니다.
지금 상태를 알고 싶으면 이 문서가 아니라 `docs/limitations.md` 를 봅니다.

이 문서가 따로 있는 이유는 `work.md` 규약 때문입니다 — *"끝낸 항목은 지우고,
그 사실은 남긴다."* work.md 에서 지운 항목이 무엇이었고 어떻게 닫혔는지가
여기 남습니다. **고친 뒤에도 남은 제약은 옮겨 오지 않았습니다** —
그것은 지금도 참이므로 `docs/limitations.md` 에 있습니다.


---

## 레지스트리 — 라이브러리를 우회한 곳 (**해결됨**)

기록으로 남깁니다. **독립 디코더 대조로만 드러난 것들**이라, 대조를 하지
않았으면 그대로 갔습니다. 셋 다 조용히 틀리는 종류입니다.

대조 방법은 `nk`/`vk` 레코드를 명세대로 직접 디코딩해 파서 출력과
맞춰 본 것입니다. 결과(`SYSTEM\CurrentControlSet\Services` 1,754키 +
`SOFTWARE\...\CurrentVersion` 44,393키):

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 키 이름 | 46,147 / 46,147 | 46,147 / 46,147 |
| LastWrite | **565 / 1,754** (SYSTEM) | **46,147 / 46,147** |
| 값 전체 | 21,473 / 46,147 | **46,142 / 46,147** |

남은 5건은 big data(`db`) 레코드로, 값이 16,344바이트를 넘어 여러 셀로
쪼개진 것입니다(222KB·56KB 등). **python-registry가 맞고 대조 도구가
구현하지 않은 것**이라 파서 문제가 아닙니다.

### ① 한글 문자열이 잘렸다

python-registry의 `decode_utf16le`가 종결자를 `s.index(b"\x00\x00")`로
찾습니다. **바이트 검색이라 문자 경계를 보지 않습니다.**

```
'볼륨 관리자 드라이버'
 fc bc │ 68 b9 │ 20 00 │ 00 ad │ ...
  볼      륨    공백    관
                └─ 00 00 ─┘   ← 오프셋 5(홀수)에서 끊긴다

라이브러리 : '볼륨 '
실제       : '볼륨 관리자 드라이버'
```

공백(`U+0020`, 고위 바이트 `0x00`) 다음에 `U+XX00` 형태 문자가 오면 두
문자에 걸쳐 `00 00`이 만들어집니다. **한글에 흔한 배치입니다** —
`U+AC00`(가), `U+AD00`(관)처럼 하위 바이트가 `0x00`인 음절이 많습니다.

실측 SYSTEM 하이브에서 **문자열 값 42,578건 중 56건(0.13%)** 이
잘렸습니다. 전부 한글이고, 서비스 표시명과 드라이버 설명이 주로
걸렸습니다.

**이 프로젝트는 한국어 환경이 대상입니다.** 비율은 작지만 걸리는 자리가
보고서에 그대로 실리는 필드입니다. 예외도 경고도 없고 잘린 문자열은 그
자체로 그럴듯해 보입니다.

**조치** — 문자열 타입은 `value.value()`를 쓰지 않고 `raw_data()`를 직접
UTF-16LE로 디코딩합니다. 정렬을 지키므로 가짜 종결자에 걸리지 않습니다.

### ② `MULTI_SZ` 종결자가 값으로 남았다

`MULTI_SZ`는 각 문자열이 널로 끝나고 목록 전체가 널 하나로 더 끝납니다.
라이브러리가 널 기준으로 그냥 쪼개므로 빈 항목이 둘 남습니다.

```
raw_data : b'r\x00p\x00c\x00s\x00s\x00\x00\x00\x00\x00'
라이브러리 : ['rpcss', '', '']
실제 내용  : ['rpcss']
```

`Services` 1,754개 키 중 **249개**가 이 형태였습니다.

**06단계에 실제 영향이 있습니다.** `compare`가 리스트를 "원소 중 하나라도
일치"로 보므로, 빈 문자열이 남아 있으면 모델이 지어낸 `value: ""` 주장이
**검증을 통과합니다.** 환각 탐지에 구멍이 생깁니다.

**조치** — 끝의 빈 문자열을 뗍니다. 가운데 것은 건드리지 않습니다 —
구조적으로 있을 수 있고 위치가 의미를 가집니다.

### ③ 타임스탬프가 반올림됐다

python-registry가 100ns를 마이크로초로 줄이면서 `ROUND_HALF_EVEN`으로
**반올림**합니다. 이 프로젝트의 나머지 파서는 전부 `filetime_to_datetime`
으로 **절삭**합니다.

```
raw FILETIME   128920208934228618   (끝자리 8)
라이브러리      .422862   ← 반올림
정수 절삭       .422861
```

1µs 차이라 허용 오차 안이지만, 문제는 값이 아니라 **표기가 사실과
달라지는 것**입니다. 우리 형식은 끝에 `0`을 붙여 7자리를 만듭니다 —
"100ns 자릿수는 버렸다"는 뜻인데, 반올림하면 그 자리가 6번째 자리에
섞여 들어가고 표기는 여전히 버렸다고 말합니다.

evtx가 python-evtx의 `timestamp()`를 우회한 것과 같은 결론입니다
(`artifact-notes.md`). 저쪽은 float 오차였고 이쪽은 반올림이라 원인은
다릅니다.

**조치** — `_nkrecord.unpack_qword(0x04)`로 raw FILETIME을 읽어
`filetime_to_datetime`에 넘깁니다.

회귀 테스트는 `tests/test_registry_parser.py`에 있습니다(한글 잘림,
`MULTI_SZ` 종결자, 가운데 빈 항목 보존, 홀수 길이).

### `registry:Amcache` — 실물 하이브 구조는 확인, 값 의미는 대조 안 함 (**부분 해결**)

`Amcache.hve`도 regf 포맷이라 `RegistryParser`를 그대로 재사용했습니다
(`HIVE_OF`에 `"registry:Amcache": "Amcache"` 추가).
셀 구조 자체의 정확성은 위 세 가지 우회(한글 잘림·`MULTI_SZ` 종결자·
타임스탬프 반올림)와 `docs/artifact-notes.md`의 `scan_hive_cells.py`
대조가 이미 검증한 것을 그대로 물려받습니다 — SYSTEM/SOFTWARE와 셀
디코딩 경로가 완전히 같기 때문입니다.

**실물 이미지(`evidence/test_image.001`)로 1,140건을 뽑아 구조를
확인했습니다.** `Amcache\Root\Programs\...`, `Amcache\Root
\InventoryApplicationFile\...` 서브키가 예상대로 나오고,
`InventoryApplicationFile` 레코드의 `fields`에 `LowerCaseLongPath`
(전체 경로)·`FileId`(`0000`+SHA1로 보이는 41자리 값)·`BinFileVersion`·
`BinaryType`·`Size`·`ProgramId`가 실제로 담겨 있음을 확인했습니다 —
파서 확충 때 기대한 "실행 파일 경로·SHA1·타임스탬프"가 맞습니다.

**다만 두 가지는 여전히 안 됐습니다.**
- **필드 이름의 의미를 우리가 해석하지 않습니다** — 그럴 필요가
  없습니다. `registry.py`의 설계(가치가 경로·값 이름에 있지 레코드
  자체에 있지 않음, `docs/limitations.md` 6-7)를 그대로 따라 원시
  값만 냅니다. `Programs` 서브키의 필드는 `"0"`·`"1"`·`"17"` 같은
  숫자 이름이라("13": 0, "a": 1787353733 등) 사람이 읽으려면 외부
  문서 대조가 필요한데, 그건 05단계 해석의 몫입니다.
- **매핑(`T1547.001.yaml`)의 `path_prefix`를 아직 안 정했습니다** —
  구조는 확인했지만 "어느 서브키가 조사에 실제로 유용한가"(예:
  `InventoryApplicationFile`만 볼지, `Programs`도 볼지)는 검증하지
  않았습니다. 범위를 좁히면 놓칠 위험이 있어 보류합니다.
- ~~**외부 도구(AmcacheParser 등) 대조는 하지 않았습니다.**~~
  **2026-08-27 에 했습니다.** AmcacheParser 2026.5.0 과 대조해 다섯
  범주 **457건이 레코드 수·값 모두 일치**했습니다. `Root\File` 361건은
  저쪽이 "new format" 으로 보고 안 읽는 것이라 채점 대상이 아닙니다.
  대조기는 `tools/compare_amcache.py`, 기록은 `docs/artifact-notes.md`
  2026-08-27 "AmcacheParser 2026.5.0 대조" 절입니다.

---

## 4-1. 04단계의 스킵이 보고서에 도달하지 않았다 (**해결됨**)

기록으로 남깁니다. 고치기 전 보고서는 이랬습니다.

```markdown

---

## 4-0. 0바이트 껍데기가 진짜 저널을 가렸다 (**해결됨**)

기록으로 남깁니다. **증거가 있는데도 0건을 보고하던 문제**입니다.

실측(`evidence/[root]`, Windows 7 시스템 볼륨):

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 읽은 파일 | `$Extend\$UsnJrnl` (0바이트) | **`$J` (34,406,400바이트)** |
| `record_count` | **0** | **306,857** |

원인은 두 가지가 겹친 것이었습니다.

**① 탐색이 껍데기를 먼저 잡았다.** `_probe`가 `relative_paths`를 순서대로
훑다가 `$Extend/$UsnJrnl`에서 멈췄습니다. 파일이 **존재하되 0바이트**인데
존재만 보고 채택했고, `filenames`에 등록된 `$J`에는 도달하지 못했습니다.

`$UsnJrnl:$J`는 콜론이 파일명에 못 들어가 도구마다 다르게 저장되는데,
**추출본이 껍데기(이름 없는 `$DATA`)와 알맹이(`$J`)를 둘 다 내놓는 경우**가
이 조합입니다.

**② `$UsnJrnl` 파서가 빈 스트림에 침묵했다.** evtx는 같은 상황에서
`ValueError`로 중단하고 `$MFT`는 시그니처로 걸리는데 이것만 조용했습니다.

**조치** — 셋 다 했습니다.

1. `_probe`·`_search`가 **0바이트 후보를 건너뜁니다.** 크기 0은 내용이
   다른 스트림에 있다는 신호이므로 다음 후보로 넘어갑니다.
2. 건너뛴 껍데기를 **버리지 않고 남깁니다.** `Located.empty_candidates`에
   담겨 매니페스트의 `source_empty_skipped`로 나가고, 콘솔에 경고가
   찍힙니다. **추출이 잘못됐다는 진단이므로 조용히 넘어가면 안 됩니다.**
3. 후보가 **전부 0바이트면** `EmptyArtifact`로 중단합니다. `ArtifactNotFound`를
   상속해 04단계 처리는 같지만 메시지가 다릅니다 — "수집 안 됨"과
   "수집됐는데 알맹이가 없음"은 분석가가 취할 조치가 다르기 때문입니다.
   `$UsnJrnl` 파서에도 같은 취지의 빈 스트림 가드를 뒀습니다.

회귀 테스트는 `tests/test_parse_scaffold.py`("0바이트 껍데기")와
`tests/test_usn_parser.py`에 있습니다.

---

## 4-0-1. `parse_errors`가 스캔 횟수를 셌다 (**해결됨**)

기록으로 남깁니다. 같은 실측에서 `parse_errors: 62,969`가 나왔습니다.
숫자만 보면 저널이 심하게 깨진 것 같지만 실제로는 이랬습니다.

- 정상 레코드 **306,857건 / 스키마 위반 0**
- USN 증가분과 오프셋 증가분이 정확히 일치 (33,901,024 = 33,901,024)
- 재동기화 62,969회는 **전부 한 덩어리** — `@0x2055000`부터 503,752바이트

그 구간은 저널이 아니라 **phpMyAdmin 언어 파일 텍스트**입니다(이 볼륨의
`APM_Setup` 잔재). `$J`는 스파스 파일이라 추출 도구가 유효 데이터 끝
너머 클러스터까지 담았고, 거기 남아 있던 삭제 파일 내용이 딸려온 것입니다.

파서 동작 자체는 정확했습니다 — 가짜 레코드를 만들지 않고 8바이트씩
재동기화하며 지나갔습니다. 틀린 것은 **집계 단위**였습니다.
`503,752 / 8 = 62,969`로 나머지 없이 딱 떨어지는 것이, 저 숫자가
"오류 건수"가 아니라 **"걸어간 횟수"**라는 증거입니다.

**조치** — 연속된 실패를 한 구간으로 묶고 규모를 바이트로 분리했습니다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| `parse_errors` | 62,969 | **1** |
| `unreadable_bytes` | — | **503,752** |
| 로그 | `재동기화 62969회` | `@0x2055000 부터 503752바이트를 읽지 못했습니다` |

evtx 파서가 이미 쓰던 방식과 같습니다. 구간 수만으로는 8바이트가 깨진
것과 500KB가 통째로 저널이 아닌 것을 구별할 수 없으므로 **둘을 함께**
냅니다. 저널 중간에 진짜 손상이 여러 군데 생기면 구간 수가 실제로 여러
개로 나옵니다 — 그게 원래 알고 싶었던 값입니다.

회귀 테스트는 `tests/test_usn_parser.py`에 있습니다(구간 1곳이 1건으로,
떨어진 손상 둘은 2건으로, 규모는 `unreadable_bytes`로).

---

## 4-0-2. 시각 없는 신호가 05단계를 멈춰 세웠다 (**해결됨**)

기록으로 남깁니다. **타임스탬프가 조작된 증거에서 도구가 죽는** 문제였습니다.

`record_filter.select_records`가 시각을 못 읽은 신호 레코드에 tz-naive
`datetime.max`를 정렬 자리로 주고, 시각이 있는 신호(tz-aware)와 같은
리스트에서 정렬했습니다.

```
TypeError: can't compare offset-naive and offset-aware datetimes
```

**드문 경우가 아닙니다.** `$SI` 타임스탬프가 전부 0이면
`filetime_to_datetime`이 `None`을 주고 04단계가 `zero_timestamp`를
붙입니다 — 즉 **시각이 없다는 사실 자체가 그 레코드를 신호로 만듭니다.**
안티포렌식 흔적을 찾는 필터가 바로 그 흔적에서 멈추는 구조였습니다.
목업 데이터에는 타임스탬프가 전부 정상이라 드러나지 않았습니다.

**조치** — 정렬 자리를 `NO_TIME`(tz-aware `datetime.max`)로 상수화하고,
"앵커가 되지 못한다"는 판정을 `tzinfo` 검사가 아니라 그 상수와의 동일성
비교로 바꿨습니다. 시각 없는 신호는 **전달되되 주변 창은 열지 않습니다** —
"그 시각 주변"이라고 할 시각이 없기 때문입니다.

회귀 테스트는 `tests/test_interpret.py`에 있습니다(시각 없는 신호가
버려지지 않고 맨 뒤로 정렬되는지, 창을 열지 않는지).

---

## 4-2-1. 넘길 60건을 고르는 것이 파일명 순서였다 (**해결됨**)

4-2가 "1,281건 중 60건"이라고 쓴 그 60건이 **어떻게 뽑히는지**는 따로
봐야 했습니다. 예전 `select_records`는 신호를 정렬하지 않고 `[:limit]`으로
잘랐습니다. 그 순서는 `io.read_parsed_records`가 읽은 순서, 즉
`sorted(glob("*.jsonl"))`입니다.

```
evtx_security → evtx_system → mft → usnjrnl
```

**알파벳순입니다.** 앞의 파일이 60자리를 채우면 뒤의 아티팩트는 신호가
몇 건이든 모델에 한 건도 도달하지 않습니다. 4-2의 실측 규모(신호
1,281건)에서는 `$UsnJrnl`이 통째로 잘립니다 — 저널이 가장 많은 신호를
내는 아티팩트인데도 그렇습니다.

4-2와 원인이 다릅니다. 4-2는 **몇 건을 넘길지**의 문제이고 이것은
**어느 것을 넘길지**의 문제입니다. 상한을 올려도 순서 편향은 남습니다.

**영향** — 4-2와 같았습니다. 06단계가 못 잡고 "sLLM 탐지 실패"로
집계됩니다. 다만 이쪽은 **아티팩트 단위로 통째로** 빠지므로, 아티팩트별
재현율을 내면 값이 파일명 알파벳순과 상관됐습니다.

**해결** — 6장의 배분이 아티팩트마다 자리를 먼저 나누므로 앞 파일이 뒤
아티팩트를 통째로 굶기는 경로가 없어졌습니다. 파일을 읽는 순서는 그대로지만
배분 결과가 그 순서에 의존하지 않습니다(`test_allocation.py`의
`test_the_allocation_does_not_depend_on_insertion_order`).

---

---

## 06단계가 표기 오류를 환각으로 셌다 (**해결됨**, 2026-08-24 실측)

`K-003`의 유일한 findings가 기각돼 **환각률 100%**로 집계됐습니다.
그런데 내용을 보면 모델이 맞았습니다.

```
모델 주장 : REG-SW#9604700 / DisableRealtimeMonitoring / "1"
실제 레코드: SOFTWARE\Microsoft\Windows Defender\Real-Time Protection
            fields = {"DisableRealtimeMonitoring": 1, ...}
기각 사유 : field_not_found
```

**ref도 필드명도 값도 맞습니다.** 틀린 것은 `fields.` 접두어 하나입니다 —
`get_field`가 점 표기를 요구하는데 모델이 맨 이름을 썼습니다. 프롬프트가
점 표기를 명시했으니 모델의 잘못이 맞지만, **이것을 환각으로 세면
환각률이 표기 오류를 세게 됩니다.**

`CurrentControlSet` 표기 문제(2장)와 같은 부류입니다 — 문장은 맞는데
표기가 달라 `comparators`가 흡수하지 못합니다.

**`benchmark/validator_check.py`는 이것을 못 잡습니다.** 33건 전부 통과로
나옵니다. 사례에 점 표기를 쓰는 것이 4건 있으나, **맨 이름이 `fields` 아래
있는 경우**는 하나도 없습니다. 과엄격 감시에 이 부류가 비어 있습니다.

### 조치 — `comparators`의 네 번째 관대함으로 흡수한다

`comparators.py`는 이미 셋을 관대하게 보고 있었습니다. 타임스탬프 절삭,
경로 대소문자, 타입(`4821` 대 `"4821"`). 전부 **"주장하는 사실은 같은데
표기가 다른"** 경우이고, `fields.` 접두어는 그 네 번째입니다. 그래서
새 예외 처리를 만들지 않고 `get_field`가 흡수합니다. **양방향입니다** —
맨 이름을 `fields` 아래에서 찾고, 반대로 `fields.path`를 최상위에서도
찾습니다(`$MFT`는 `path`가 최상위입니다).

**검증이 무르지 않는 이유**가 핵심입니다.

- 이 함수가 하는 일은 **모델이 가리킨 필드를 찾아 주는 것까지**입니다.
  찾은 뒤에 값을 그대로 대조하므로, 값이 틀리면 `value_mismatch`로
  기각됩니다. 없는 필드를 지어낸 경우는 대안 표기로도 못 찾으므로
  `field_not_found` 그대로입니다.
- **모호해질 수 없습니다.** 원래 표기를 먼저 시도하고 성공하면 대안을
  보지 않으므로, 최상위와 `fields`에 같은 이름이 있어도 최상위가 이깁니다.
- 기각될 때 **끊긴 지점은 모델이 쓴 표기 기준**으로 남깁니다. 대안 표기의
  끊긴 지점을 보고하면 사유가 모델이 쓰지도 않은 경로를 가리킵니다.

`tests/test_verify_checkers.py`가 이 성질들을 각각 고정합니다 — 특히
"지어낸 필드는 두 표기 모두에서 여전히 못 찾는다"와 "표기를 흡수해도
값이 틀리면 `value_mismatch`".

**과엄격 감시의 구멍도 메웠습니다.** `benchmark/validator_cases.json`에
V34(맨 이름)·V35(반대 방향)를 더해 33건 → 35건입니다. 이 부류가 통째로
비어 있어서 감시가 100% 통과를 보고하고 있었습니다.

**결과** — `K-003`을 다시 검증하니 환각률 **100% → 0%**, 보고서에 첫
"확인된 사항"이 실렸습니다(Defender 실시간 보호 비활성화, 06:48:31,
`REG-SW#9604700`).

### 같은 부류인데 아직 남은 것

**`CurrentControlSet` 표기**(2장)는 그대로입니다. 04단계가 `ControlSet001`로
풀어 쓰고 모델은 `CurrentControlSet`이라고 인용하므로 기각됩니다. 이번
것과 달리 **필드를 찾는 문제가 아니라 값 문자열이 다른 문제**라
`get_field`가 아니라 `compare`/`normalize_path` 쪽에서 풀어야 합니다.
레지스트리 경로에만 적용되는 치환이라 경로 비교 규칙 전체를 건드리게
되고, 그만큼 오탐 위험이 커서 따로 둡니다.

**영향** — "소형 모델의 환각을 검증 계층으로 잡는다"는 논지는 이제
**한 건이지만 실물 수치가 있습니다**(통과 1 / 기각 0). 다만 표본이
findings 1건이라 비율로 말할 수 없고, `priority`가 비어 있는 동안의
배분으로 얻은 결과라는 점도 그대로입니다(6-5).

---

## 05단계 프롬프트가 컨텍스트 창을 넘었다 (**해결됨**, 2026-08-26 실측)

`K-ALERT`(기법 8개 → 아티팩트 10종 1,693건)에서 05단계가 **3회 재시도
끝에 중단**됐습니다. 약 25분 소모, 소견 0건.

```
attempt 1  malformed_output   응답에서 JSON 객체를 찾지 못함
attempt 2  timeout            qwen2.5:7b: 900.0초 내 응답 없음
attempt 3  malformed_output   응답에서 JSON 객체를 찾지 못함
           abort              → SystemExit(1)
```

프롬프트를 그대로 재현해 재 봤습니다.

| | |
|---|---|
| 전달 레코드 60건의 JSON | 71,476자 (**추정 28,600 토큰** — 글자수÷2.5, 토크나이저를 돌린 값이 아님) |
| 시스템 프롬프트 | 3,002자 |
| `--num-ctx` 기본값 | 32,768 |
| `qwen2.5:7b` 의 `context_length` | 32,768 (**모델 상한이라 더 열 수 없음**) |

출력에 쓸 자리가 2천 토큰도 남지 않습니다.

**`--limit 15` 로 낮추니 첫 시도에 통과했습니다**(소견 4건, 재시도 0회,
`errors.jsonl` 0건). 원인이 프롬프트 크기임이 확인됐고, attempt 2의
타임아웃도 같은 원인입니다 — 3만 토큰 프롬프트의 처리 시간입니다.

**5장이 "넘는지는 아무도 검사하지 않습니다"라고 적어 둔 자리이고,
이번이 실제로 넘긴 첫 실측입니다.** 웹셸 목업은 레코드가 5건이라 닿을
수 없던 지점입니다.

### 왜 60건이 이렇게 커졌나

4-3이 적은 것과 같은 구조입니다 — 아티팩트가 늘면 자리를 나눠 갖지만
**총량 60건은 그대로**라, 레코드 하나당 평균 1,190자가 그대로 실립니다.

```
evtx:Security  후보 482 → 9자리     registry:Amcache  후보 392 → 8자리
evtx:KernelPnP 후보 385 → 9자리     registry:SYSTEM   후보 258 → 8자리
prefetch       후보 137 → 9자리     evtx:RDPSession   후보  12 → 9자리
```

`--limit` 기본값 60은 카탈로그가 11종이던 시절 값입니다.

### 조치 — 배분기에 토큰 예산을 넣었습니다

`--limit` 를 낮추는 것은 우회지 수정이 아니었습니다. 기본값 60은 카탈로그가
11종이던 시절 값이고, 22종이 된 지금 **60건이 무슨 크기가 되는지는 그
숫자가 말해 주지 못합니다.** 레코드 하나의 크기가 아티팩트마다 열 배씩
차이 나기 때문입니다 — 적재 파일 103개를 들고 있는 프리패치 레코드와
`$MFT` 레코드는 같은 "한 자리"가 아닙니다.

같은 날 `win10_sysmon_testimage.001` 로 다시 쟀습니다. **자릿수는 그대로인데
크기는 40% 더 컸습니다.**

| | 60건의 크기 | 추정 토큰 |
|---|---|---|
| `K-ALERT` (2026-08-26, 아티팩트 10종) | 71,476자 | 28,600 |
| `win10_sysmon_testimage` (같은 날, 5종 2,422건) | **100,068자** | **40,027** |

둘 다 `--num-ctx` 기본값 32,768을 넘습니다. 뒤엣것이 더 큰 것은 프리패치가
자리를 받았기 때문입니다.

**`allocate_records` 에 `char_budget` 을 주면 예산에 맞을 때까지 자릿수를
낮춰 다시 배분합니다.**

```
num_ctx=32,768   예산 69,803자   →  60자리를 44자리로 줄임 (65,312자 ≈ 26,124토큰)
num_ctx=131,072  예산 315,563자  →  60자리 그대로 (100,068자)
```

**꼬리를 자르지 않습니다.** 자르면 시간순으로 마지막인 아티팩트가 통째로
사라져 4-2-1이 고친 문제가 그대로 되살아납니다. `limit` 을 낮춰 동트 배분을
다시 돌리므로 `priority` 가중치와 바닥 한 자리가 그대로 살아 있습니다.

**예산이 어디서 나오나.**

```
(--num-ctx − --reserve-output-tokens) × CHARS_PER_TOKEN − 프롬프트 고정분
```

- 출력 자리를 **먼저** 뗍니다(기본 4,096토큰). 프롬프트가 창을 꽉 채우면
  응답이 잘리고, 잘린 응답은 `malformed_output` 으로 옵니다 — 위 attempt 1·3이
  정확히 그 모양입니다.
- 프롬프트 고정분(시스템 프롬프트 + 시나리오 머리말)은 **추정하지 않고
  실제로 조립해 잽니다**(`InterpretClient.prompt_overhead_chars`). 상수로
  적어 두면 프롬프트를 고치는 순간 어긋납니다.
- 레코드 크기도 `llm_client` 가 내보내는 **바로 그 문자열**을 잽니다
  (줄당 하나의 JSONL, `ensure_ascii=False`).

**`CHARS_PER_TOKEN` 은 추정치였습니다.** 토크나이저를 돌린 값이 아니라
"순수 ASCII 는 토큰당 3~4자, 한글은 1~1.5자" 사이를 잡은 2.5 였습니다.
**그 값이 틀린 방향이었습니다** — 아래 절에서 실측으로 2.0 이 됐습니다.

#### 그 추정치가 창을 넘겼습니다 (**해결됨**, 2026-09-03)

**예산이 맞다고 했는데 프롬프트가 잘리고 있었습니다.** `qwen2.5:7b` 에
실제 05 프롬프트를 넣고 Ollama 의 `prompt_eval_count` 로 쟀습니다
(`K-LIVE-0902-wide`, 프롬프트마다 난스를 앞에 붙여 KV 캐시 재사용을 깼습니다).

```
10건  10,122자 →  4,997토큰   2.03자/토큰
20건  17,744자 →  8,679토큰   2.04
40건  37,863자 → 17,978토큰   2.11
55건  62,713자 → 29,910토큰   2.10
60건  69,875자 → 16,386토큰   4.26   ← 잘림
```

**60건에서 평가된 토큰이 40건보다 적습니다.** 글자는 1.8배인데 토큰은
0.9배입니다. 2.1자/토큰이면 69,875자는 약 33,300토큰이라 32,768 창을 넘고,
넘은 프롬프트를 Ollama 는 **거부하지 않고 앞을 잘라서 받습니다.** 16,386 은
창의 절반입니다 — 모델이 받은 것은 우리가 보낸 것의 절반이었습니다.

**우리 데이터는 토큰당 2자입니다.** 경로 문자열이 대부분이라 ASCII 라도
잘게 쪼개집니다 — 역슬래시·확장자·레지스트리 키마다 경계가 생깁니다.
"ASCII 는 토큰당 3~4자"라는 일반론이 맞지 않는 자리입니다. 2.03~2.11 로
구간 전체에서 안정적이었습니다.

**2.5 는 안전한 쪽으로 틀리지 않았습니다.** 같은 글자를 **적은** 토큰으로
세므로 예산이 실제보다 약 19% 헐거웠고, 그만큼 프롬프트가 창을 넘었습니다.
상수 옆에 적혀 있던 "작게 잡으면 자리를 덜 쓸 뿐"이 거꾸로였습니다.

**이것은 ⑥ 과 같은 증상이고 다른 원인입니다.** ⑥ 은 `num_ctx` 를 지정하지
않아 4,096 창으로 떴던 것이고(고쳐서 32,768 로 명시), 이번 것은 창은
제대로 열렸는데 **우리 예산이 그 창을 넘도록 허락한 것**입니다. 둘 다
오류 없이 끝나고 findings 만 얇아집니다.

**고친 것.** 상수를 관측 최솟값(2.03)보다 낮은 **2.0** 으로 내렸습니다.
같은 케이스가 60건 → 54건으로 줄고, 그 54건을 실제로 재니 26,482토큰
(창의 80%, 출력 예약을 더해도 93%)으로 **잘리지 않습니다.**

그리고 **창을 얼마나 쓰고 있는지를 늘 출력합니다.** 예산이 깎였을 때만
말하던 자리인데, 넘었는지는 사후에 알 방법이 없기 때문입니다.

```
  프롬프트 추정 27,972토큰 (쓸 수 있는 28,672토큰의 97%, 창 32,768 − 출력 예약 4,096)
```

`tests/test_allocation.py` 가 상수를 실측 비율(`62,713 / 29,910`)과 대조해,
누가 다시 올리면 실패합니다.

**깎였으면 말합니다.** `넘는데도 조용히 도는 것`이 이 자리에서 가장 나쁜
성질이었습니다.

```
  토큰 예산: 4건 → 2건으로 줄임 (861자 ≈ 344토큰 / 예산 1,133자).
             --num-ctx 5300 에서 출력 4096토큰을 뺀 값이다
```

한 건도 안 들어가면 `empty_result` 로 중단하되, **"후보가 0건" 과 사유를
구분합니다.** 창을 키우거나 모델을 바꿔야 풀리는 문제이지 flags 룰을
들여다볼 일이 아니기 때문입니다.

### 레코드 다이어트 — 한 필드가 프롬프트의 54%였습니다

예산은 "몇 건을 보낼까"를 정할 뿐 레코드 하나를 줄이지 않습니다. 그래서
어디가 무거운지 쟀습니다(`win10_sysmon_testimage`, 60건 100,068자).

| | |
|---|---|
| `fields.loaded_files` | **54,577자 — 전체의 54%** |
| 나머지 전부 | 45,491자 |

아티팩트로 보면 프리패치 12건이 61,423자(61%)로, 건당 5,118자입니다.
한 레코드가 `loaded_files` **1,017건**을 들고 있었습니다.

**`fields` 안의 목록을 앞에서 20개까지만 싣습니다**(`--max-list-items`,
0이면 안 자름). `04_parsed/` 의 원본은 그대로입니다.

```
num_ctx 32,768 · 예산 69,741자

  예산만        44건   65,312자
  예산 + 상한20  60건   63,041자   ← 더 많이 보내면서 더 작다
```

**왜 20인가.** 상한별 실측입니다.

```
안 자름  100,068자  →  44건       40      78,026자  →  54건
20        63,041자  →  60건 전부  10      54,395자  →  60건
```

20에서 천장을 넘고, **그 아래로는 수익이 급감합니다** — 20→3이
14,731자를 더 아낄 뿐인데 모델이 볼 근거는 계속 줄어듭니다. 잘리지
않아야 할 것과도 안 부딪힙니다: 실측에서 `run_times`는 최대 8(포맷 상한),
`volumes`는 1이라 10 이상이면 건드리지 않습니다.

**세 가지를 지켰습니다.**

- **`fields` 안만 자릅니다.** 최상위는 스키마가 정한 자리이고 무엇보다
  `flags`가 거기 있습니다 — 신호 판정 자체를 자르면 안 됩니다.
- **앞에서부터 자릅니다.** 어느 항목이 중요한지 고르는 물리적 근거가
  없습니다. "시스템 DLL은 덜 중요하다" 같은 추측으로 순위를 매기면 그
  추측이 틀렸을 때 조용히 증거를 버립니다. 파일 순서는 근거는 아니지만
  재현은 됩니다(6-6의 "임시"와 같은 자리).
- **잘렸다고 프롬프트가 말합니다.** 말하지 않으면 모델이 "적재 파일은
  20개였다"고 쓸 수 있고 그것은 우리가 **유발한** 환각입니다. 전체 개수는
  `loaded_file_count`에 원본 그대로 있습니다. 그런 문장이 나와도 06단계가
  잡습니다 — 검증은 `04_parsed/` 를 읽으므로 1,017과 맞지 않아 기각됩니다.

**예산과 프롬프트가 같은 `for_prompt` 을 씁니다.** 둘이 어긋나면 예산이
맞아도 프롬프트가 넘칩니다. 실제로 처음 구현에서 `allocate_records` 가
상한을 `record_chars` 에 안 넘겨 크기를 잘못 쟀고, 테스트가 그것을
잡았습니다.

**남은 것** — 목록 말고도 반복되는 자리가 있습니다. `Provider`(2,004자)·
`channel`(1,728자)·`artifact`(1,524자)는 레코드마다 같은 문자열이 실립니다.
묶어서 머리말로 빼면 더 줄지만, 레코드 하나가 자족적이지 않게 되어 모델이
헷갈릴 위험과의 거래라 재 보지 않았습니다.

### 실패한 모델 원문은 이제 남깁니다 (**해결됨**, 2026-08-26)

위 진단이 "프롬프트가 잘렸다"를 **확인한 것이 아니라 크기를 재고
`--limit` 를 낮춰 재현한 것**이었습니다. 모델이 실제로 무엇을 뱉었는지
아무도 보지 못한 채 원인을 단정했습니다.

02·05단계가 재시도할 때마다 그 시도의 응답 원문을 `errors.jsonl` 옆에
떨굽니다.

```
cases/<id>/02_normalize_raw_attempt1.txt
cases/<id>/05_interpret_raw_attempt1.txt
```

`errors.jsonl` 항목의 `detail.raw` 가 그 파일 이름을 가리키고, 재시도를
모두 소진해 중단할 때의 stderr 도 이 경로를 알려 줍니다. 시도마다 파일이
따로인 것은 **모델이 지적을 받고 달라졌는지**를 보기 위해서입니다 —
한 파일에 덮어쓰면 마지막 시도만 남습니다.

**`timeout` 은 떨구지 않습니다.** 응답 자체가 없었으므로 남길 원문이
없고, 직전 시도의 것을 쓰면 이번 실패의 원문으로 오해됩니다.

---

## 6-7. 레지스트리가 05단계에 도달하지 못했다 (**해결됨**)

파서를 붙이면서 드러난 것이라 6장에 함께 적습니다. **04단계까지는
정상인데 모델에 한 건도 가지 않았습니다.**

`registry:Amcache`도 같은 클래스(`RegistryParser`)를 쓰므로 이 절이
그대로 적용됩니다 — Amcache 레코드도 `flags`가 붙지 않습니다. 카탈로그에
세 하이브 모두 `signal_source: scope`를 적어 두었습니다.

실측(`evidence/[root]`, `SYSTEM\CurrentControlSet\Services`):

```
registry:SYSTEM: 1754건 (플래그 0건) → registry_system.jsonl
```

`flagged_count`가 **0**입니다. 오류가 아니라 구조입니다.

기존 파서 셋은 **"전부 훑고 재미있는 것에 플래그"** 모델입니다.
레지스트리는 반대로 **가치가 레코드가 아니라 경로에 있습니다.**
`$MFT` 레코드 12345는 맥락 없이도 의미가 있지만
`Services\XYZ\ImagePath`는 그 경로가 무엇을 뜻하는지 알아야 의미가
생깁니다. 그래서 신호가 `flagging.py`가 아니라 **선별에서** 나옵니다.

그런데 05단계는 `is_signal()`로 `flags`가 빈 레코드를 버렸습니다. 결과적으로 **선별이 정확히 골라 온 1,754건이 전부 탈락합니다.**

임계값 문제가 아니라 필터 모델이 이 아티팩트에 맞지 않는 것입니다.
`flagging.py`에 억지로 룰을 넣는 것은 답이 아닙니다 — "Services 하위면
신호"라고 쓰면 선별이 이미 한 일을 한 번 더 하는 것이고, 그러면 플래그가
"볼 만하다"는 뜻이기를 그만둡니다.

6-2의 2단 배분에서 함께 풀렸습니다. 쿼터가 아티팩트별로 자리를 배분하므로
**"신호냐"가 아니라 "몇 자리를 줄 것이냐"가 질문이 되고**, 플래그 없는
아티팩트도 자기 몫을 받습니다.

**고친 방법.** 카탈로그에 `signal_source`를 두었습니다. 기존 셋은
`flags`(기본값)이고 레지스트리 두 하이브가 `scope`입니다. `scope`
아티팩트에서는 모든 레코드가 후보이며, 시간창으로 다시 거르지 않습니다 —
`Services` 하위 키의 LastWrite는 대개 OS 설치 시각이라 사건 시간창에
걸리지 않기 때문입니다. 쿼터 안에서는 사건 앵커에 가까운 키부터 들어갑니다.

이 예외를 `flagging.py`의 룰로 넣지 않은 이유는 그대로입니다. "Services
하위면 신호"라고 쓰면 선별이 이미 한 일을 한 번 더 하는 것이고, 그러면
플래그가 "볼 만하다"는 뜻이기를 그만둡니다. 카탈로그에 두면 **그 아티팩트의
신호가 어디서 나오는가**라는 사실 하나만 적는 것으로 끝납니다.

**앵커는 아티팩트를 가리지 않습니다.** evtx의 로그온 시각이 레지스트리
키를 끌어올 수 있습니다. 증거가 여러 계층에 흩어지는 사건에서 이 교차가
상관분석의 실질입니다.

재현 시나리오(1,754건 전부 플래그 0, `$UsnJrnl` 신호 500건, 상한 60):

| 배분 | `registry:SYSTEM` | `$UsnJrnl` | `evtx:Security` |
|---|---|---|---|
| 예전(플래그로 거름) | **0~3** | 55 | 2 |
| `signal_source: scope`, 중립 | **29** | 29 | 2 |
| 매핑이 레지스트리를 `priority: 1`로 지목 | **47** | 11 | 2 |

세 번째 줄은 아직 **가정입니다.** 6-5대로 `priority`가 비어 있어 실제
매핑으로는 두 번째 줄이 나옵니다.

---

## 6-8. 프리패치도 같은 자리에 있었다 (**해결됨**)

파서를 붙이면서 같은 일이 반복됐습니다. 실측(`evidence/[root]`):

```
prefetch: 73건 (플래그 0건) → prefetch.jsonl
```

레지스트리와 원인이 같습니다 — **가치가 레코드가 아니라 그 안의 경로에
있습니다.** "CMD.EXE가 5회 실행됐다"는 그 자체로 신호가 아니고,
`C:\inetpub\wwwroot\shell.aspx`를 적재했다는 사실이 신호입니다. 그 판단은
선별(`path_prefix`)이 이미 했으므로 `flagging.py`가 더 할 일이 없습니다.

`program_executed` 같은 플래그를 새로 만드는 안을 검토했다가 **접었습니다.**
프리패치 레코드는 전부 "무언가 실행됐다"이므로 모든 레코드에 붙고, 그러면
플래그가 "볼 만하다"는 뜻이기를 그만둡니다. 레지스트리에 "Services 하위면
신호"를 넣지 않은 것과 같은 이유입니다.

`outside_time_range`는 붙습니다(실측: 1시간 범위로 좁히면 73건 중 47건).
다만 `record_filter.NON_SIGNAL_FLAGS`에 들어 있어 신호로 치지 않으므로,
**결론도 같았습니다 — 프리패치도 05단계에 도달하지 못했습니다.**

**해결도 레지스트리와 같습니다.** 카탈로그에 `signal_source: scope`를
적었습니다. 배분이 그것을 보고 프리패치에도 자리를 줍니다.

재현(프리패치 73건 전부 플래그 0, 최근 실행 시각은 몇 주에 걸쳐 흩어져
있고 사건 근처는 2건, 상한 60):

| | 후보 | 전달 |
|---|---|---|
| `flags`로 볼 때 | **2건** (다른 아티팩트 신호의 시간창에 걸린 것만) | 2건 |
| `signal_source: scope` | **73건** (선별이 골라 온 전부) | 59건 |

두 경우 다 사건 근처 2건은 들어갑니다. 달라지는 것은 **나머지 71건을
후보로 볼 것이냐**입니다 — 프리패치에서는 그 71건이 "사건 시각에 실행되지
않았다"가 아니라 "그 프로그램이 이 시스템에서 실행된 적이 있다"이므로,
경로를 보고 판단할 거리가 남아 있습니다.

---

---

## 2026-08-28 · 오프셋을 되짚는 도구가 없었다 (**해결됨**, 2026-08-30)

`parsed_record` 스키마의 `offset` 은 이렇게 설명돼 있습니다 — *"원본 바이트
위치. 기존 파서를 쓰지 않고 직접 구현하는 이유가 이 필드다.
`tools/hexdump_record.py` 가 이 값으로 원본을 되짚는다."*

**그 파일은 0바이트였습니다.** 2026-08-14 에 자리만 만들어 두고 채우지
않았습니다. 즉 `offset` 을 넣고 다니지만 **그것으로 원본을 실제로 되짚어
본 적은 없었습니다.** 값이 틀렸다는 뜻은 아니었습니다 — `$MFT` 는
`레코드번호 × 1024` 로 검산했고(`artifact-notes.md` 2026-08-14), `$UsnJrnl`
은 오프셋 증가분과 USN 증가분이 일치하는 것으로 확인했습니다(2026-08-19).
다만 **분석가가 보고서를 들고 원본 바이트로 내려가는 경로**가 비어
있었습니다.

이것이 드러난 계기는 evtx 엔진 교체 검토였습니다. `pyevtx-rs` 가 오프셋을
주지 않아 후보에서 빠졌는데(`docs/limitations.md` 의 "넣지 않은 것 —
네이티브 evtx 엔진"), 그 근거를 확인하다가 스키마가
가리키는 도구가 비어 있는 것을 발견했습니다.

**2026-08-30 에 채웠습니다.** 실물 이미지에서 10종 357건을 되짚었고 어긋남
0건입니다. 대조는 파서로 다시 파싱해 비교하는 것이 아니라 **레코드가 자기
안에 들고 있는 식별자**로 합니다 — `$MFT` 헤더의 레코드 번호와 업데이트
시퀀스, USN 레코드의 USN, evtx 의 EventRecordID. 전체 기록은
`docs/artifact-notes.md` 2026-08-30 절에 있습니다.

---

## 2026-08-30 · 모델 호출 실패가 규약 밖이었다 (**해결됨**)

`--llm ollama`로 부를 때 **타임아웃이 아닌** 실패 — 모델명 오타, 서버
미기동, 잘못된 호스트 — 를 아무도 잡지 않았습니다. `llm.LLMError`가 파이썬
트레이스백으로 그대로 올라왔습니다.

멈추기는 했으므로 조용한 실패는 아니었지만, **`errors.jsonl`에 남지
않았습니다.** 07단계가 그것을 볼 수 없고, `errors.tally()`가 세는 통계에도
안 들어갑니다. "폴백을 만들지 않는다 — 실패는 `errors.jsonl`에 기록하고
사유를 출력하며 중단한다"는 규약의 밖이었습니다.

**02·05 양쪽에서 `llm_error`로 기록하고 즉시 중단합니다. 재시도하지
않습니다** — 모델명 오타는 세 번 불러도 같은 답이라 시간만 씁니다(같은
논리가 5장 ⑤에 있습니다).

### `ERROR_TYPES`에 `llm_error`를 추가했다 (**전체 공지 대상**)

`src/common/errors.py`의 고정 어휘이고 공용 모듈입니다. `timeout`으로
합치지 않은 이유는 **조치가 다르기 때문**입니다 — 기다릴 것인가, 설정을
고칠 것인가. 07단계 통계가 이 어휘로 직접 산출되므로 합치면 "모델이
느리다"와 "모델명을 잘못 적었다"가 한 칸에 들어갑니다.

### 이것이 드러난 방식 — 스텁으로는 나올 수 없었다

`tests/test_llm_live.py`를 만들면서 나왔습니다. 실제 모델을 부르는
테스트를 켜자 **네 개 중 하나가 실패했고**, 그 하나가 이 결함이었습니다.
`StubBackend`는 네트워크를 타지 않으므로 이 경로를 지나갈 수 없습니다.

같은 이유로 드러난 것이 앞서 넷 더 있습니다(5장 — 스키마의 `ref` 패턴
누락, 배분의 실물 규모, 120초 고정 타임아웃, `num_ctx` 4096). **실제
모델을 부르는 테스트가 없던 동안 이 부류는 실측 때마다 사람이 발견해
왔습니다.**

---

---

## 2026-08-31 · HID/BadUSB 매핑이 카탈로그와 어긋나 있었다 (**해결됨**)

PR #35 가 매핑 YAML 아홉과 아티팩트 다섯을 더하면서 등록 지점 몇 곳을
빠뜨렸고, `main` 이 테스트 11건 실패 상태로 있었습니다. 고치면서 드러난
것을 적습니다. **셋은 테스트가 잡았고 셋은 테스트도 못 잡았습니다.**

### 테스트가 잡은 것

**관문 1이 비어 있었다.** 매핑 YAML 이 여덟 기법을 들고 왔는데
`KNOWN_TECHNIQUES` 에 없었습니다(`T1016`·`T1018`·`T1021.002`·`T1071.001`·
`T1082`·`T1083`·`T1105`·`T1569.002`). 이 상태면 02단계가 그 기법을 낼 수
없어 **매핑이 도달 불가**입니다. 관문 1이 관문 2보다 앞이라는 것이 이
자리에서 드러났습니다.

지금은 이것이 더 세게 막힙니다 — 02단계 출력 제약의 enum 이
`KNOWN_TECHNIQUES` 에서 나오므로(같은 날 작업), 목록에 없는 기법은 모델이
**생성 자체를 못 합니다.** 예전에는 내면 `is_known` 이 기각했고 지금은
나오지 않습니다. 어느 쪽이든 못 쓰는 것은 같지만, 이 목록의 결손이 곧
기능 결손이 되는 정도가 커졌습니다.

**관문 3이 반쯤 열려 있었다.** `evtx:Defender`·`evtx:PowerShell` 이
`supported: true` 인데 등록 지점 넷(`CHANNEL_FALLBACK`·`FILE_LAYOUT`·
출력 파일명·`ref` 접두어)이 비어 있었습니다. **`ref` 접두어를 더하려면
동결 스키마 둘의 패턴을 손으로 고쳐야 합니다** — `parsed_record` 와
`findings` 가 접두어를 열거하고 있고 생성기가 없습니다. `flags` enum 은
`sync_flag_enum.py` 가 맞춰 주는데 `ref` 접두어는 그런 것이 없습니다.

**관문 4가 비어 있었다.** `T1021.002` 의 5140/5145 와 `T1059.001` 의
4103/4104 를 받을 flag 가 없었습니다. `share_accessed`·`powershell_logged`
를 만들었습니다.

### 테스트도 못 잡은 것 — `scope:` 오타

`T1059.001.yaml` 과 `T1105.yaml` 이 `scope_template:` 대신 **`scope:`** 를
썼습니다. 로더가 모르는 키를 조용히 버리므로 `scope_template` 이 `{}` 가
되고, **적어 둔 `event_ids` 가 통째로 사라집니다.**

증상이 고약합니다. `T1059.001` 은 `evtx:Sysmon` 에 `[1, 3]` 을 적었는데
그것이 사라져 **Sysmon 채널 전량 요청**이 됩니다 — EID 1(프로세스 생성)이
전부 후보가 되어 05단계 쿼터를 혼자 태웁니다. 좁히려고 적은 것이 넓히는
결과가 됩니다.

로더가 모르는 키를 거부하면 잡혔을 자리입니다. **아직 안 고쳤습니다** —
다른 매핑 38개가 전부 정상이라 급하지 않고, 거부로 바꾸면 기존 파일의
주석성 키까지 걸릴 수 있어 별도로 봐야 합니다.

### 테스트도 못 잡은 것 — 카탈로그 주석이 사실과 달랐다

`_artifacts.yaml` 이 `evtx:Defender` 를 "T1562.001·T1059.001 매핑이
참조한다"고 적었는데 **어느 매핑도 요청하지 않았습니다.** 파서를 붙여도
아무도 안 쓰는 채널이 됩니다. `T1562.001` 에 이어 붙이고
`defender_disabled` flag 를 만들었습니다.

이 부류는 자동으로 잡기 어렵습니다. "카탈로그에 있는데 아무 매핑도
요청하지 않는 아티팩트"를 세는 검사는 가능하지만, 그것이 정상인 경우도
있습니다(`$LogFile` 처럼 미지원으로 두는 것).

### 실물로 확인하지 못한 값

이번에 넣은 event_id 는 **전부 미확인입니다.** 손에 있는 두 이미지에
Defender·PowerShell 채널이 없고, Security 5140/5145 는 개체 액세스 감사가
켜져 있어야 남는데 그 기록이 없습니다.

| flag | event_id | 근거 |
|---|---|---|
| `share_accessed` | 5140, 5145 | 공개 자료. 감사 정책 의존 |
| `powershell_logged` | 4103, 4104 | 공개 자료. **둘 다 기본 꺼짐** |
| `defender_disabled` | 5001, 5004, 5007 | 카탈로그 설명의 값을 그대로 옮김 |

`powershell_logged` 는 특히 주의해야 합니다. 스크립트블록 로깅은 그룹
정책으로 켜야 남으므로, **이 채널이 비어 있다고 PowerShell 이 안 돌았다는
뜻이 아닙니다.** 보고서가 그렇게 읽히지 않아야 합니다.

### 함께 움직인 것

`mapping_table_version` 을 1.1 → 1.2 로 올렸고 세 곳(`_artifacts.yaml`,
`test_mapping_loader.py`, 목업 `03_selection.json`)을 맞췄습니다. 목업
`07_report.md` 도 제외 목록이 늘어 다시 만들었습니다. `C-001` 의
`selected`·`deferred` 는 2/2 로 그대로입니다 — 늘어난 것은 `excluded` 뿐입니다.

`tests/test_evtx_parser.py` 의 "모르는 아티팩트를 거부하는가"가 두 번째로
깨졌습니다. 미지원 예시로 실재 채널 이름을 쓰다가 그 채널이 지원 대상이
되면서입니다(`evtx:Application` 에 이어 `evtx:PowerShell`). 채널이 될 수
없는 이름으로 고정했습니다.

---

---

## 2026-09-01 · 프리패치가 이 이미지에서 통째로 죽어 있었다 (**해결됨**)

키오스크 스냅샷의 프리패치 192건이 전부 `UnknownLayout` 이었습니다.
Windows 10 **19045.6466** 의 `FILE_INFORMATION` 이 212바이트인데 표에 버전
30 의 216·220·224 만 있었습니다. `(30, 212)` 를 **192건 전량 실측으로**
채웠습니다 — 자리를 정한 방법과 대조 기록은 `docs/artifact-notes.md`
2026-09-01 절에 있습니다.

`0건 → 192건`, 그중 153건이 사고 당일 실행분입니다.

### 이 실패가 두 번 숨었다

**① `parse_errors` 가 `errors.jsonl` 에 남지 않았습니다** (**해결됨** —
아래 "파서 실패가 `errors.jsonl` 밖에 있었다" 절). 매니페스트에는
`parse_errors: 192` 가 정확히 적혔고 04 로그도 찍었는데, `errors.jsonl` 이
만들어지지 않아 `tools/live_check.py` 가 **"재시도·실패 0건 — 모든 단계가
첫 시도에 통과"** 로 결산했습니다.

"폴백을 만들지 않는다 — 실패는 `errors.jsonl` 에 기록한다"가 규약인데,
**파서 단위 실패만 그 규약 밖에 있었습니다.**

**② 좋은 경고가 나쁜 경고를 덮었습니다.** 같은 실행에서 evtx 청크 복구
경고가 215줄 생략될 만큼 쏟아졌습니다. 그건 python-evtx 가 놓친 것을
우리가 되살린 **좋은 신호**인데, 화면에서 프리패치 실패가 그 사이에
묻혔습니다.

### 함께 드러난 것 — `name_pattern` 은 아무도 모르는 키다 (**해결됨** — 아래 "매핑이 모르는 키를 써도" 절)

`mappings/windows/T1105.yaml` 이 프리패치를 `name_pattern: [CERTUTIL.EXE,
POWERSHELL.EXE]` 로 좁히는데, **`src/` 어디에도 그런 키가 없습니다.**

```
Scope 가 아는 필드 : path_prefix, extensions, event_ids, start, end
merge_scopes 의 UNION_KEYS : path_prefix, extensions, event_ids
```

`merge_scopes` 가 조용히 버립니다. 2026-08-31 에 고친 `scope:` 오타와
**같은 부류**입니다 — 매핑이 모르는 키를 써도 로더도 병합기도 아무 말
하지 않습니다. 이번에는 버려진 결과가 넓어지는 쪽이라 손해가 없었지만
(프리패치 전량 요청이 됐습니다), 반대 방향이면 증거를 놓칩니다.

셋 다 이 브랜치 밖이었고, 아래 두 절에서 각각 닫혔습니다.

---

---

## 2026-09-01 · 파서 실패가 `errors.jsonl` 밖에 있었다 (**해결됨**)

아티팩트 하나가 **100% 실패해도 단계가 정상 종료하고 `errors.jsonl` 이
아예 만들어지지 않았습니다.** `parse_errors` 는 `_manifest.json` 에만
남았습니다.

실측에서 물렸습니다. 키오스크 스냅샷의 프리패치 192건이 전부
`UnknownLayout` 이었는데 `tools/live_check.py` 가 11개 관문 전부 PASS 를
주고 이렇게 결산했습니다.

```
재시도·실패  0건 (errors.jsonl 없음 — 모든 단계가 첫 시도에 통과)
```

**같은 리포트가 위쪽에서는 "파싱 오류 192건"을 찍고 있었습니다.** 산출물이
아니라 결산이 틀린 것이었고, 그 이유는 `live_check` 가 `errlog.tally` 를
읽는데 04단계가 거기에 쓰지 않았기 때문입니다.

"폴백을 만들지 않는다 — 실패는 `errors.jsonl` 에 기록한다"가 규약인데
**파서 단위 실패만 그 규약 밖에 있었습니다.** `parse_error` 는 예전부터
`ERROR_TYPES` 어휘에 있었고, 04단계가 그것을 치명적 중단(`abort`)에만
쓰고 있었습니다.

### 고친 방식

`note_skip` 이 스킵을 두 곳에 남기는 것과 같은 규약으로 맞췄습니다 —
`errors.jsonl` 은 전 단계가 공유하는 집계용이고 매니페스트는 이 단계의
산출물입니다.

**아티팩트당 한 줄만 씁니다.** 프리패치 192건이면 192줄이 되어 다른
단계의 실패가 묻힙니다. 어느 파일이 왜 실패했는지는 파서가 이미 stderr 로
말합니다.

**전량 실패와 일부 실패를 `detail.total_failure` 로 가릅니다.** 9,606건을
내면서 192구간을 건너뛴 것(evtx)과 192건을 읽어 한 건도 못 낸 것
(프리패치)은 조치가 다릅니다. 앞은 로그가 더러운 것이고 뒤는 그 아티팩트가
죽은 것입니다. 전량 실패는 stderr 에도 따로 한 줄 냅니다 — 실측에서 이
실패가 evtx 청크 복구 경고 215줄 사이에 묻혔습니다.

04단계 요약 줄에도 실패 건수를 넣었습니다. 예전에는
`prefetch: 0건 (플래그 0건)` 이라 192건이 실패했다는 사실이 그 줄에
없었습니다.

---

## 2026-09-01 · 미지원 레코드 버전이 결산 밖에 있었다 (**해결됨**)

`$UsnJrnl` v3/v4 를 만나면 파서가 **손상이 아니라 지원 범위 밖**으로
알아보고 레코드 하나를 통째로 건너뜁니다(8바이트씩 걸어 들어가면 본문을
레코드로 오해해 가짜 손상이 잡힙니다). 그 집계가
`stats["unsupported_version"]` 인데, **매니페스트에도 `errors.jsonl` 에도
실리지 않았습니다.** 04단계가 매니페스트에 싣던 것은 `parse_errors` 와
`unreadable_bytes` 둘뿐이었습니다.

v3 를 쓰는 볼륨이면 저널이 통째로 비는데 화면이 이렇게 말합니다.

```
$UsnJrnl: 0건 (플래그 0건) → usnjrnl.jsonl
```

남는 것은 레코드마다 찍히는 stderr 경고뿐이고, 그 경고가 무엇에 묻히는지는
바로 위 절에 있습니다 — 같은 실행에서 evtx 청크 복구 경고 215줄이 프리패치
전량 실패를 덮었습니다.

**같은 부류가 다른 파서에 남아 있던 것입니다.** 위 "파서 실패가
`errors.jsonl` 밖에 있었다" 를 고칠 때 본 것이 `parse_errors` 뿐이었고,
지원 범위 밖은 그 밖에 있었습니다.

### 실물에서 물린 것이 아니라 코드에서 찾았습니다

**이것은 실측 사고가 아닙니다.** 지금까지 연 저널은 전부 v2.0 이었습니다
(2026-08-18 · 355,850건, V3/V4 0건). 버전 축 작업(`work.md` 1번)에서
"어긋나면 시끄러운가 조용한가"를 가르다 나왔습니다.

즉 **아직 아무도 v3 저널을 이 파서에 넣어 본 적이 없습니다.** 이 수정은
그때 무슨 일이 벌어지는지를 보이게 만든 것이지, v3 를 읽게 만든 것이
아닙니다.

### 고친 방식 — 두 수를 합치지 않습니다

- `_manifest.json` — `unsupported_version`. 0이면 키를 만들지 않습니다
  (`unreadable_bytes` 와 같은 규약입니다)
- `errors.jsonl` — 아티팩트당 한 줄. 손상과 미지원이 함께 있어도 줄을
  나누지 않고 한 줄에 두 수를 담습니다
- 04단계 요약 줄 — `, 미지원 버전 N건`

**`parse_errors` 에 합치지 않았습니다.** 조치가 다릅니다 — 앞은 저널이
더럽거나 잘린 것이고, 이것은 **파서를 넓혀야 하는 것**입니다. 단위도
다릅니다(구간 수 / 레코드 수).

**어휘는 `parse_error` 를 그대로 씁니다.** `ERROR_TYPES` 는 `src/common/`
이라 새 유형을 만드는 것이 전체 공지 대상이고, 07단계 통계가 그 어휘로
직접 산출됩니다. "읽지 못한 레코드가 있다"는 사실은 같으므로, 손상과 지원
범위 밖의 구분은 `detail` 의 두 수와 문장이 집니다.

```
"$UsnJrnl: 레코드가 한 건도 나오지 않았습니다 — 지원하지 않는 레코드 버전
 3건을 건너뛰었습니다 (손상이 아니라 지원 범위 밖입니다)"
```

되돌려 확인했습니다. 새 시험 둘을 고치기 전 `parse.py` 로 돌리면 둘 다
`KeyError: 'unsupported_version'` 으로 실패하고, 그때 화면은
`$UsnJrnl: 1건 (플래그 1건)` — 건너뛴 사실이 어디에도 없습니다.

---

## 2026-09-01 · 미지원 경고가 레코드마다 찍혔다 (**해결됨**)

`$UsnJrnl` 이 못 내는 레코드를 만날 때마다 경고를 한 줄씩 찍었습니다. 저널이
통째로 미지원이면 레코드 수만큼 쏟아져 **바로 위 절에서 방금 보이게 만든
결산 한 줄을 도로 덮습니다.**

`parse_errors` 가 손상을 **구간 단위**로 세는 것과 같은 규약으로 맞췄습니다 —
연속된 미지원 레코드는 한 줄로 나오고, 그 줄에 시작 오프셋·바이트 수·사유·
건수가 함께 들어갑니다.

```
$UsnJrnl: @0x0 부터 400바이트에서 지원하지 않는 USN 레코드 버전: v4.0
          — 레코드 5건을 건너뛰었습니다
```

**집계는 레코드 단위 그대로입니다.** 매니페스트와 `errors.jsonl` 이 싣는 수는
바뀌지 않았습니다. 묶은 것은 로그뿐입니다.

**사유가 달라지면 다른 구간으로 봅니다.** 한 줄에 묶으면 어느 쪽이 몇 건인지
말할 수 없습니다. 정상 레코드나 손상 구간을 만나도 끊깁니다. (2026-09-07 에
v3 를 읽게 되면서 사유가 둘로 늘었습니다 — v4 와 "파일 ID 가 NTFS 참조가
아님". 조치가 다르므로 갈라 둡니다.)

그러다 **오래된 구멍 하나가 함께 드러났습니다.** 스트림이 끝나는 경로
(`if not buf: return`)가 열린 구간을 닫지 않고 나갔습니다. 저널의 **마지막**
부분이 손상이면 `parse_errors` 는 올라가는데 "@0x… 부터 N바이트를 읽지
못했습니다" 한 줄이 통째로 사라졌습니다. 미지원 구간 때문에 눈에 띄었고,
같은 자리에서 둘 다 닫았습니다.

---

## 2026-09-01 · 매핑이 모르는 키를 써도 아무도 말하지 않았다 (**해결됨**)

로더가 모르는 키를 조용히 버렸습니다. 매핑은 데이터라 오타가 문법 오류가
아니라 **뜻 없는 키**로 나타나고, YAML 파서도 스키마도 그것을 잡지
못합니다. 두 번 물렸습니다.

**① `scope:` 오타 (2026-08-31).** `T1059.001` 과 `T1105` 가
`scope_template` 대신 `scope:` 를 썼습니다. 적어 둔 `event_ids` 가 통째로
사라져 `evtx:Sysmon` 이 **채널 전량 요청**이 됐습니다 — EID 1(프로세스
생성)이 전부 후보가 되어 05단계 쿼터를 혼자 태웁니다. **좁히려고 적은 것이
넓히는 결과가 됐습니다.**

**② `name_pattern` 유령 키 (2026-09-01).** `T1105` 가 프리패치를
`name_pattern: [CERTUTIL.EXE, POWERSHELL.EXE]` 로 좁히려 했는데 `Scope`
에도 `merge_scopes` 의 `UNION_KEYS` 에도 없는 이름입니다. 매핑에는 좁혔다고
적혀 있고 실제로는 폴더 전체가 요청되고 있었습니다.

셋째가 하나 더 있었습니다 — `T1105` 의 `$MFT` 가 `time_range: true` 를
적었는데, 그것은 무시되는 것이 아니라 `scope_resolver` 가 시나리오의 값으로
**덮어씁니다.** 뜻이 있어 보이는데 아무 일도 하지 않습니다.

### 고친 방식

세 층 전부에서 모르는 키를 로드 시점에 거부합니다 — 최상위,
`artifacts[]`/`followups[]`, `scope_template`. 어휘는 `mapping_loader` 에
`MAPPING_KEYS`·`REQUEST_KEYS`·`FOLLOWUP_KEYS`·`SCOPE_KEYS` 로 있고,
`docs/mapping-guide.md` 에 표로 옮겼습니다.

`followups[]` 는 `technique`·`artifact` 를 직접 적으므로 어휘가 다릅니다.
거부가 과하면 정상 매핑이 안 들어오므로 양쪽을 시험으로 함께 고정했습니다.

기존 매핑 40개는 전부 통과합니다. 걸린 것은 `T1105` 의 죽은 키 둘뿐이고
지워서 실제 동작과 맞췄습니다 — **동작은 바뀌지 않습니다.** 둘 다 이미
버려지고 있었습니다.

---

## 2026-09-03 · 파서가 센 것이 04단계 밖으로 안 나갔다 (**해결됨**)

`parse.py` 가 파서 `stats` 에서 `parse_errors`·`unreadable_bytes`·
`unsupported_version` 셋만 매니페스트로 옮겼습니다. 나머지는 파서가 세고
콘솔에 경고까지 찍는데 **04단계 밖으로 나가지 않았습니다.**

| 집계 | 무엇을 못 보고 있었나 |
|---|---|
| `dirty_hive` | 파일이 있고, 파서가 성공하고, **값이 낡았다** |
| `recovered_chunks` | 헤더가 선언하지 않은 청크에서 나온 레코드가 섞여 있다 |
| `bad_chunks` | 체크섬이 안 맞아 통째로 건너뛴 청크. **`parse_errors` 에 합산되지도 않았다** |
| `fixup_errors` | 섹터 크기 판정이 틀린 것인지 손상인지 |
| `scope_undecidable` | 드라이브 문자를 못 정해 범위 없이 05단계로 간 레코드 |
| `missing_tables`·`unsupported_tables` | SQLite 프로파일이 기대한 테이블이 없거나 못 읽는 형태다 |

**기준을 코드에 적었습니다** — `parse.RELIABILITY_STATS` 의 판정은 하나입니다.
*산출물을 액면 그대로 읽으면 안 되는 이유가 되는가.* `record_count` 와
`parse_errors` 가 멀쩡해도 값이 낡았거나, 정상 경로 밖에서 나왔거나, 선별이
사실상 안 걸린 경우가 있습니다. 정보성 집계(`filtered_out`·
`amcache_values_renamed` 등)는 넣지 않았습니다 — 다 넣으면 그 자리가 다시
아무도 안 읽는 표가 됩니다.

**보고서 문구는 07단계가 따로 듭니다**(`report.RELIABILITY_NOTES`). 04는
"무엇을 셌나"까지고 "분석가에게 무엇으로 읽히나"는 저쪽 몫입니다. 매니페스트
키가 곧 보고서 문장이면 키 이름을 바꾸는 순간 보고서가 조용히 바뀝니다.
04가 보내는데 07이 문구를 모르는 조합은 테스트가 막습니다.

부수적으로 「확인한 아티팩트」의 `note` 를 문자열 하나에서 목록으로 바꿨습니다.
전에는 사유가 여럿이면 나중 것이 앞엣것을 덮었습니다.

가드 다섯을 일부러 되돌려 실제로 잡는 것을 확인했습니다.

## 2026-09-03 · 환각률이 판정 0건일 때 0.0% 를 냈다 (**해결됨**)

`hallucination_rate` 는 `rejected / (passed + rejected)` 인데 분모가 0이면
0.0 을 냈습니다. **"아무것도 말하지 않음"과 "전부 맞음"이 같은 숫자로
나왔습니다.**

실물 규모 A/B 에서 실제로 겪은 자리입니다 — 제약을 끈 쪽이 소견 1건을
`claims` 빈 배열로 내 06이 `unverifiable` 로 넘겼고, 판정된 문장이 0건이라
환각률이 0.0% 로 찍혔습니다. 제약을 켠 쪽의 **진짜** 0.0%(판정 3건 전부
통과)와 표에서 구별되지 않았습니다.

**대가가 가장 큰 자리는 평균이었습니다.** 잴 것이 없던 케이스가 다른
케이스의 환각률을 끌어내려 발표 수치를 좋게 만듭니다. 케이스 둘 중 하나가
0.4 이고 하나가 "판정 0건" 이면 종합이 0.2 로 나왔습니다.

**동결 스키마를 건드리지 않았습니다.** `verified.schema.json` 이
`hallucination_rate` 를 `number` 로 묶고 `additionalProperties: false` 라
`null` 도 `judged` 도 실을 수 없습니다. 그래서 파일의 값은 그대로 두고
**사람이 읽는 자리에서만** 갈랐습니다.

- `verify.judged_rate` — 분모가 0이면 `None`. `benchmark/evaluate.py` 의
  `_ratio` 가 이미 *"분모가 0이면 None, 0.0으로 두면 완벽히 실패로 잘못
  읽힌다"* 고 적어 둔 것과 같은 규약입니다
- `verify.format_rate` — 사람이 읽는 한 토막. 판정 0건이면 수치 대신 그
  사실을 적습니다
- 환각률은 어디서 인쇄되든 **판정 건수를 달고 나갑니다** (06 stderr,
  `evaluate.py`, `collect.py` 표, `live_check`)
- 평균에서 빠진 케이스 수를 `cases_with_nothing_judged` 로 함께 적습니다.
  빠졌다는 사실 자체가 수치이기 때문입니다

`collect.py` 표에 "판정" 이 둘이 되어 앞엣것(live_check 단계 판정 수)을
"관문"으로 갈랐습니다.

## 2026-09-03 · Amcache `InstallDate` 표기가 정상 문장을 기각시켰다 (**해결됨**)

`InventoryApplication` 의 `InstallDate` 는 하이브에 `RegSZ` 문자열로,
`03/20/2017 03:53:52` 형태로 적혀 있습니다. 이 프로젝트의 다른 시각은 전부
ISO 8601 이고 모델도 그렇게 씁니다. 그러면 `parse_timestamp` 가 모델 쪽만
파싱하고 레코드 쪽은 못 해 "같은 종류의 값이 아니다"로 떨어집니다. 실측
이미지의 `InventoryApplication` 78건이 전부 이 표기라, 설치 시각을 인용한
정상 문장이 **전량** 기각되고 있었습니다.

원래 적어 둔 선택지는 둘이었고 **어느 쪽도 아닌 셋째로 갔습니다.**

- (a) 06이 **날짜로 보이는 값**을 흡수한다 → 값의 생김새로 판단하게 되고,
  이 모듈이 처음부터 거부한 방식입니다
- (b) 04가 값을 변환한다 → 04단계 산출물은 하이브에 적힌 것에 충실해야
  합니다
- **(c) 06이 필드 이름으로 켜지는 흡수를 갖는다** — `PATH_FIELDS` 가 이미
  같은 방식으로 서 있어 새 개념을 만들지 않았습니다 (`DATE_FIELDS`)

**순서를 지켰습니다** — 사례 `V41` 을 먼저 넣어 `value_mismatch` 로 기각되는
것을 확인하고 그다음 비교기를 고쳤습니다. 사례 없이 비교기만 고치면 감시가
계속 "오탐 없음"을 보고합니다. `DATE_FIELDS` 를 비워 되돌리면
`validator_check` 가 다시 "검증기가 과엄격합니다"로 잡습니다.

**흡수한 것은 표기이지 값이 아닙니다.** 다른 날짜, 하루 안의 다른 시각,
이름이 허락하지 않는 필드는 전부 그대로 기각됩니다.

## 2026-09-03 · 문서에만 남아 있던 것 여섯 (**해결됨 — 진작에**)

`docs/limitations.md` 가 "지금도 참인 것만 둔다"는 규약을 갖고 있는데,
**이미 거짓이 된 서술 여섯이 남아 있었습니다.** 고친 사람이 그 절을
안 지웠거나, 같은 문서의 다른 절에만 적었기 때문입니다. 코드로 대조해
갈라냈습니다.

| 문서가 주장한 것 | 실제 | 언제 |
|---|---|---|
| 섹터 크기가 512로 박혀 있다 | `sector_size_of` 로 레코드에서 유도한다 | 2026-08-28 |
| SRUM — ESE 포맷, 신규 라이브러리 필요 | `parsers/srum.py`, 카탈로그 3종 등재 | 2026-08-27 |
| SRUM — ESE DB, 파서 없음 (K-001 표) | 같음 | 2026-08-27 |
| 카탈로그는 `$MFT`·`$UsnJrnl`·evtx 2종·registry 2종·`prefetch`·`$LogFile` 뿐 | 29종. Amcache·SRUM·SQLite 를 "없다"고 적고 있었다 | — |
| `fields.loaded_files` 는 여전히 장치 이름 | `_to_drive` 가 목록에도 걸린다 | 2026-08-27 |
| 기존 flag 룰들은 아직 `artifact: evtx:*` | 와일드카드 0건, 테스트가 되살아나는 것을 막는다 | 2026-08-25 |

**넷은 같은 문서 안에서 자기모순이었습니다.** 아래쪽 절이 고쳤다고 적고
있는데 위쪽 절이 안 고쳤다고 적고 있었습니다.

카탈로그 목록은 다시 적지 않고 `mappings/_artifacts.yaml` 을 가리키게
바꿨습니다 — 목록을 문서에 옮겨 적었기 때문에 낡은 자리입니다.

## 2026-09-03 · 같은 경고 90줄이 다른 경고를 덮었다 (**해결됨**)

실측 `K-ALERT` 에서 04단계가 경고 90건을 냈고 **90건 전부 같은 자리·같은
사유**였습니다 — 장치 속성 저장소의 `Unknown VK Record type 0x12`
(`DEVPROP_TYPE_STRING`). 그 90줄이 04단계 출력을 덮어 다른 경고를 가립니다.

`$MFT` 의 `fixup_errors`(첫 건만 사유를 보이고 나머지는 수로)와
`$UsnJrnl` 의 미지원 구간(연속된 것을 한 줄로)이 이미 같은 규약이었는데,
레지스트리의 값 읽기 실패만 그것을 안 지키고 있었습니다.

**사유별로 나눕니다.** 묶으면 어느 쪽이 몇 건인지 말할 수 없습니다 —
미지원 USN 레코드를 버전별로 나눈 것과 같은 근거입니다.

**집계는 레코드 단위 그대로입니다.** `stats["value_errors"]` 와
`parse_errors` 가 싣는 수는 바뀌지 않았습니다. 묶은 것은 로그뿐입니다.

DEVPROP 타입을 해석하게 된 것은 아닙니다 — 그쪽은 `docs/limitations.md`
에 남아 있습니다.

## 2026-08-26 · `run_pipeline.sh` 에 볼륨을 줄 통로가 없었다 (**해결됨**)

그전까지 `--volume` 은 `parse.py` 를 직접 부를 때만 줄 수 있었습니다. 관통
스크립트에는 통로가 없어서, **일반적인 Win10/11 물리 디스크 이미지에서는
04단계만 손으로 따로 돌려야 했습니다** — 거의 항상 해당한다는 점을
생각하면 관통 스크립트가 관통하지 못한 셈입니다.

```bash
VOLUME=1 PYTHON=.venv/Scripts/python.exe ./run_pipeline.sh K-ALERT evidence/0824test.001
```

위치 인자가 아니라 환경 변수인 것은 세 번째 자리를 `replay_dir` 이 이미
쓰고 있어서입니다. 위치로 받으면 replay 없이는 볼륨을 못 줍니다.

**주지 않았을 때 후보를 보여 주고 멈추는 04단계의 동작은 그대로입니다.**
스크립트가 대신 추측하지 않습니다 — 그것은 한계가 아니라 의도된 동작이라
`docs/limitations.md` 의 "디스크 이미지를 직접 열 수 있다" 절에 있습니다.

## 2026-08-26 · 세 룰이 EID 1 만 본다고 써 놓고 안 그랬다 (**해결됨**)

`win10_sysmon_testimage.001` 실측에서 붙은 플래그의 **절반이
`ProcessTerminate`(EID 5)** 였습니다. `shell_spawned`·
`execution_from_unusual_path`·`unexpected_parent_process` 셋 다
`condition:` 에 "Sysmon 1(프로세스 생성)" 이라고 적어 놓고 `when:` 절에
event_id 제약이 없었습니다. 바로 아래 `network_connection` 은
`match: event_id` 를 쓰고 있으니 관례는 이미 있었고, 이 셋만 안 지켰습니다.

같은 프로세스를 두 번 세는 것보다 나쁜 것이 있습니다. **EID 5 레코드에는
`ParentImage` 도 `CommandLine` 도 없습니다.**

```
SYSMON#212  EID=5  fields=[Image, ProcessGuid, ProcessId, RuleName, User, UtcTime]
```

`shell_spawned` 의 note 가 "부모가 무엇이냐가 그 다음 질문이다
(fields.ParentImage 는 레코드에 그대로 실려 모델이 본다)" 라고 적은 바로 그
필드가 없는 쪽입니다. **05단계 자리는 먹고 정보는 더 적습니다.**

2026-08-25 에 채널 단위로 좁혔던 "flag 사정거리" 와 같은 부류인데, 이번엔
채널 **안에서** EventID 사이입니다.

**조치** — `Clause` 에 `event_ids` 를 더했습니다. `artifact` 와 같은
자리이고, 둘 다 대상을 좁힌 뒤 `match` 가 판정합니다. `match: event_id`
(EventID 자체가 신호)와 뜻이 달라 함께 쓰면 거부됩니다. 작성 규칙은
`docs/mapping-guide.md`.

```
조치 전  17건 (EID 5 가 8건)
조치 후   9건 (EID 5 가 0건)
```

## 2026-09-03 · 일곱 번째 자기모순 — 프리패치 볼륨 GUID (**해결됨 — 진작에**)

검증기 오탐 절의 조치 표가 `프리패치 볼륨 GUID 경로 | **미해결**` 이라고
적고 있었는데, **같은 문서 위쪽의 "장치 경로가 드라이브 문자로 안 바뀌었다"
절이 같은 날(2026-08-26) 고쳤다고 적고 있었습니다.** 정규식이 두 형태를
다 받고, 검증 사례 `V40` 이 그것을 못 박고 있습니다.

그 표를 근거로 "수치가 정상으로 돌아오려면 미해결 둘이 남아 있습니다"라고
적힌 문단도 함께 틀려 있었습니다. K-ALERT 의 기각 넷 중 셋(F2·F3·F4)은
전부 닫혔으므로 지금 같은 케이스를 돌리면 F1 하나만 기각돼 환각률이 25%
여야 합니다.

**다만 재지 않았습니다** — K-ALERT 케이스가 `cases/` 에 남아 있지
않습니다. 그래서 "발표에 실을 실물 환각률 수치가 없다"는 쪽으로 고쳐
적었고, 그것은 지금도 참이라 `docs/limitations.md` 에 남습니다.

---

## 2026-09-04 · 근거 추적이 접두어 28종 중 3종만 알았다 (**해결됨**)

보고서의 ref 를 누르면 04단계 원본 레코드를 보여 주는 기능이, **04단계가
정상적으로 만들어 둔 레코드를 못 찾았습니다.**

### 증상은 같이 배포되는 케이스에서 이미 드러나 있었다

`cases/C-001/07_report.md` 에 ref 가 3개 있는데 눌리는 것이 1개였습니다.

| ref | 프론트엔드 정규식 | 백엔드 |
|---|---|---|
| `MFT#12345` | 잡힘 | 200 |
| `EVTX-SEC#40912` | 못 잡음 | **400** |
| `EVTX-SEC#40915` | 못 잡음 | **400** |

`{"detail":"지원하지 않는 Evidence Ref입니다: EVTX-SEC#40912"}`

`04_parsed/` 에는 `mft.jsonl` 과 `evtx_security.jsonl` 딱 둘이 있었으므로,
**아티팩트 2개 중 1개에서 근거 추적이 안 되는 상태**였습니다.

### 원인 — 두 곳이 각자 손으로 적었다

`src/common/refs.py` 는 접두어 **28종**을 압니다. 그런데

- `ui/routes/results.py` 의 `EVIDENCE_FILES` — 3줄짜리 손수 만든 사전
- `ui/static/js/report.js` 의 `EVIDENCE_REF_PATTERN` —
  `/\b(?:REG-SYS|USN|MFT)#.../g`

이고, `ui/` 는 `src/common/refs.py` 를 **한 번도 import 하지 않았습니다.**
CLAUDE.md 가 *"`ref` 문자열은 `src/common/refs.py`를 경유한다"* 로 못 박은
자리입니다.

빠진 25종에 `EVTX-AAOP`·`EVTX-AAADM`·`EVTX-AABRK`·`EVTX-DRV`·
`EVTX-RDPCM` 이 들어 있었습니다 — **키오스크 축 다섯 채널이 전부**입니다.
목표 시나리오를 돌리면 근거 추적이 그 채널에 대해 통째로 안 되는
상태였습니다.

### 고친 방식 — 목록을 없애고 유도한다 (손으로 적지 않는다)

`ui/evidence_refs.py` 를 새로 두고, 두 원본에서 사슬로 유도합니다.

```
접두어 → refs.PREFIX_ARTIFACT → 아티팩트
      → stage04_parse.parse.OUTPUT_FILENAMES → 파일명
```

두 맵이 28 ↔ 28 로 정합했고 양쪽 어디에도 빠짐이 없었습니다. 한쪽만 아는
아티팩트가 생기면 **기동 시점에 사유를 내며 멈춥니다** — 조용히 빼면 그
ref 만 추적이 안 되는, 방금 고친 그 상태로 돌아갑니다.

프론트엔드는 접두어를 `index.html` 이 실어 보냅니다(`window.
EVIDENCE_REF_PREFIXES`). 따로 받아 오게 하면 보고서가 먼저 그려질 수 있고,
그때는 ref 가 눌리지 않는 채로 남습니다. 정규식은 **긴 접두어부터** 교대에
넣습니다 — 지금 목록에는 겹치는 짝이 없지만, 늘어날 때 순서가 없으면
짧은 쪽이 먼저 걸려 뒤가 잘립니다.

뒤쪽 `\b` 는 쓰지 않습니다. ref 식별자는 `.` 이나 `:` 로 끝날 수 있어
`\b` 를 붙이면 그런 ref 가 통째로 빠집니다.

### 확인

`EVTX-SEC#40912` 가 200 이고, 응답이 `evtx_security.jsonl` 1번 줄의
`event_id: 4720`·`TargetUserName: svc_backup` 레코드입니다. 브라우저에서
C-001 을 불러오면 ref 3개가 **전부** 버튼이 되고, 눌렀을 때 근거 상세
패널에 원본 JSON 까지 나옵니다.

`tests/test_ui_evidence_refs.py` 8건으로 못 박았습니다 — 정합·순서·
`EVTX-SEC` 회귀·키오스크 다섯 채널. `ui/` 에 테스트가 없어서 이 결함이
조용했으므로, 그 자리를 함께 막았습니다. 다만 라우트와 JS 는 여전히
테스트가 없습니다(`limitations.md`).

---

## 2026-09-04 · venv 에서 폴더 선택창이 500 을 냈다 (**해결됨**)

`/api/evidence/browse` 와 `/browse-image` 가 둘 다 500 이었습니다.

```
Can't find a usable init.tcl in the following directories:
    .../Python313/lib/tcl8.6 ...
```

파일은 전부 있었습니다 — 실제 경로가 `<base_prefix>/tcl/tcl8.6` 인데
venv 안의 tkinter 가 `<base_prefix>/lib/tcl8.6` 을 찾습니다. Tcl 이 없는
기계가 아니라 **찾는 자리만 어긋난** 것이었습니다.

`ui/tcl_paths.py` 가 `sys.base_prefix` 에서 유도해 `TCL_LIBRARY`·
`TK_LIBRARY` 를 채웁니다. `start.bat` 에 환경변수를 박지 않은 이유가
둘입니다 — uvicorn 을 직접 띄우는 경우에도 같아야 하고(`start.bat` 이
기동 실패 때 안내하는 명령이 바로 그것입니다), 경로에 파이썬 버전이
박히면 3.14 로 올릴 때 조용히 되돌아옵니다.

디렉터리 이름만 보지 않고 표지 파일(`init.tcl`·`tk.tcl`)까지 봅니다 —
`<base_prefix>/tcl` 에는 `tcl8` 처럼 라이브러리가 아닌 디렉터리도 함께
있습니다. **찾지 못하면 아무것도 하지 않습니다.** 짐작한 경로를 넣어 두면
tkinter 의 원래 오류가 가려져서, Tcl 이 정말 없는 기계와 경로만 어긋난
기계를 구별할 수 없게 됩니다.

확인: `tkinter OK — Tk 8.6.14`, 그리고 `/api/evidence/browse` 가 500 대신
**대화상자를 실제로 열고 응답을 기다립니다**(로그에 완료 줄이 없는 것으로
확인). 그 대기 자체의 성질은 닫히지 않았으므로 `limitations.md` 에 있습니다.

---

## 2026-09-04 · `path_prefix` 의 `*` 가 확장되지 않았다 (**해결됨**)

`Scope.matches_prefix` 는 리터럴 `startswith` 비교였습니다. 매핑이
`C:\Users\*\AppData\Local\Temp` 라고 적으면 정규화된 접두어
`c:/users/*/appdata/local/temp` 가 그대로 비교되므로, 별표가 **문자 그대로**
든 경로만 맞았습니다. 그런 경로는 없으니 결과는 언제나 0건입니다.

와일드카드를 쓰던 매핑 셋이 전부 그랬습니다.

| 매핑 | 아티팩트 | 노린 것 |
|---|---|---|
| `T1105.yaml` | `$MFT` | 사용자 Temp 에 떨어진 `.ps1` |
| `T1105.yaml` | `cryptnet_urlcache` | `certutil -urlcache` 캐시 사본 |
| `T1059.001.yaml` | `psreadline_history` | 대화형 PS 입력 이력 |

**드러나기 어려운 결함이었습니다.** `errors.jsonl` 에 남지 않고,
`live_check.py` 의 04단계 판정은 "아티팩트 ≥ 1 / 총 레코드 ≥ 1" 이라
다른 아티팩트가 레코드를 내면 통과합니다. 그리고 07 은 0건을 이렇게
인쇄합니다 — *"레코드 0건은 해당 범위에서 흔적이 확인되지 않았다는 뜻이며,
아티팩트를 읽지 못한 것과 다릅니다."* **못 읽은 것이 "없었다"로 보고되고
있었습니다.** 실측은 `K-LIVE-0902-wide` (2026-09-04, KAPE `snapshot1/C`)
에서 `$MFT` 0건입니다.

고친 것은 둘입니다.

- **`*` 를 지원합니다.** `parsers/base.py` 에 `path_in_prefix` ·
  `path_leads_to_prefix` 를 두고 세그먼트 단위로 `fnmatch` 합니다. 별표는
  **구분자를 넘지 않습니다** — 한 자리만 채웁니다. 와일드카드가 없는
  접두어는 예전 문자열 비교 그대로 갑니다(49MB 하이브를 걷는 동안 키마다
  부르는 자리라 이 분기가 곧 시간입니다).
- **지원하지 않는 문법은 로드에서 멈춥니다.** `mapping_loader` 가 `?` ·
  `[` · `]` · `**` 를 거부합니다. 이유가 성능이나 취향이 아니라 **실패
  방식**입니다 — 04 가 글자 그대로 읽으면 결과가 조용한 0건이고, 0건은
  "흔적 없음"과 구별되지 않습니다.

`registry.py` 의 `_in_scope` · `_prune` 도 같은 함수를 쓰게 모았습니다.
전에는 같은 규칙을 두 번 적어 두고 주석으로 "같은 규칙입니다"라고만
해 두었는데, 이번 변경이 정확히 그렇게 갈라질 수 있는 종류였습니다.
가지치기가 뒤처지면 **범위 안에 있는 키를 걸어가 보지도 않고 버립니다.**

회귀 테스트는 `tests/test_parse_scaffold.py` 와
`tests/test_mapping_loader.py` 에 있습니다. 그중
`test_every_mapping_wildcard_can_actually_match_something` 은 실제
`mappings/` 를 돌면서 `*` 가 든 접두어가 무언가를 맞히는지 봅니다 —
로더를 통과하는 새 문법이 생겨도 여기서 걸립니다.

## 2026-09-04 · 05 프롬프트의 예시 문장이 소견으로 새어 나왔다 (**해결됨**)

`reduce_system.txt` 의 "좋은 reason" 예시가 완성된 서술문이었습니다 —
*"임시 폴더에 떨어진 실행 파일이 3분 뒤 실행되고 외부로 접속했다."*
`K-LIVE-0902-wide` 는 USB 시나리오인데, 소견 F4 의 문장이 이렇게 끝납니다:
*"… 이는 임시 폴더에 떨어진 실행 파일이 3분 뒤 실행되고 외부로 접속했다는
시나리오와 일치."* 그 문장이 **보고서 본문에 실렸습니다.**

06 은 잡지 못합니다. 대조 대상은 `claims` 이고, 인용된 값들
(`PF#198379905` 의 `run_count` 29 등)은 전부 진짜입니다. 틀린 것은
값이 아니라 **그 값에 붙은 이야기**입니다.

`select_system.txt` 의 예시도 같은 형태였으므로 함께 바꿨습니다. 둘 다
빈칸이 있는 틀(`"<Image>가 <CommandLine>으로 실행됐다"`)로 두고, 예시
문장을 옮겨 적지 말라고 명시했습니다. 빈칸이 남은 채로 나오면 그것은
눈에 띄지만, 그럴듯한 남의 시나리오는 눈에 띄지 않습니다.

`tests/test_interpret.py::test_prompt_examples_are_templates_not_sentences`
가 `prompts/*.txt` 의 모든 `- 좋음:` 줄에 빈칸이 있는지 봅니다.

## 2026-09-04 · 02단계가 "계정 관련 변경"을 물어도 계정 기법을 내지 않았다 (**해결됨**)

`K-LIVE-0902-wide` 의 입력은 넷을 물었는데 그중 **계정 축이 통째로
사라졌다.** 02 가 `T1136`·`T1098`·`T1078` 을 하나도 내지 않았고,
`entities.accounts` 도 비었다. 03 은 목록에 없는 기법의 아티팩트를 만들어
내지 않으므로 `evtx:Security` 가 요청되지 않았고, 07 은 그것을
**"식별된 기법에 매핑된 아티팩트가 아님"** 으로 인쇄했다. 그 파일은 같은
수집 안에 15.8MB 로 있었다.

**매핑은 처음부터 준비돼 있었다.** `T1136.001`·`T1098`·`T1078` 셋 다
`evtx:Security` 를 **tier 1** 로, 각각 맞는 `event_ids` 까지 붙여 요청한다.
빠진 것은 02 의 기법 하나뿐이었다.

### 원인은 few-shot 예시였다

프롬프트 본문은 `unmapped_text` 를 비워 두지 말라고 적어 두었는데,
**예시 둘이 모두 `unmapped_text: []` 였다.** 게다가 예시 1의 입력에는 어떤
`evidence_text` 에도 들어가지 않는 구간이 있다. 모델은 "부분만 덮고
unmapped 는 비우는 것이 정답"으로 배우고 있었다.

세 가지를 고쳤다.

- `evidence_text` 규칙에 **실패 예시**를 붙였다. 모델이 실제로 저지른
  형태다 — `실행됐고` 를 `실행됐습니다` 로 다듬는다.
- **복합절 규칙**을 새로 넣었다. `A와 B` 로 이어진 서술은 각각 옮기고,
  흘린 쪽은 조사에서 통째로 빠진다는 결과까지 적었다.
- **세 번째 few-shot 예시**를 넣었다. 복합절 분리(USB + 계정)와 매핑 불가
  서술을 함께 보여 주고, `unmapped_text` 가 실제로 채워진 유일한 예시다.

### 실측

같은 입력을 `qwen2.5:latest` 로 8회씩 돌렸다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 계정 기법이 나온 실행 | **0/8** | **8/8** |
| `evidence_text` 가 원문 그대로 | 0/8 | 0/8 (인용당 3건 → 1건) |
| 02단계 호출 시간(중앙값) | 13.4초 | **11.6초** |

`evtx:Security` 가 `event_ids: [4720, 4728, 4732]` 로 선별되는 것까지
03단계로 확인했다. 회귀 테스트는
`tests/test_llm_live.py::test_an_account_clause_reaches_an_account_technique`
이고 **기법 ID 를 못박지 않는다** — 계정 축에 닿기만 하면 된다.

### 재시도를 붙이지 않은 이유 — 실측이 말렸다

처음 설계는 "덮지 못한 구간이 있으면 재질의"였다. 재기 전에는 그럴듯했는데,
검사가 **8/8 걸렸다.** 붙였다면 매 실행이 `MAX_ATTEMPTS` 를 전부 소진해
02단계가 13.8초에서 41.4초가 되고(관통 실행의 +17%), 그러고도 통과하지
못한다 — 모델이 매번 같은 방식으로 다듬기 때문이다. `normalize.py` 의
`MAX_ATTEMPTS` 주석이 이미 적어 둔 것과 같은 이유다.

**검사 자체는 문자열 비교라 사실상 공짜다.** 그래서 재시도 대신
`src/stage02_normalize/coverage.py` 가 세기만 하고, 07단계가 인쇄한다.
`errors.jsonl` 에는 새 조치 `record` 로 남는다 — 흐름을 바꾸지 않았다는
뜻이고, `live_check` 의 "재시도·실패" 수에서도 빠진다.

### 검사를 두 번 고쳤다 — 둘 다 실측이 시켰다

- **버려질 응답까지 세고 있었다.** 스키마 검증 **전에** 불러서, 재시도로
  폐기된 응답의 커버리지가 남았다. 나중에 "얼마나 자주 놓치는가"를 셀 때
  분모가 실행 수가 아니라 시도 수가 된다. 검증 뒤로 옮겼다.
- **골든 픽스처가 걸렸다.** 사람이 손으로 쓴 시나리오에서 미커버 3건이
  나왔다 — `웹서버 WEB01에서`, `비슷한 시기에`, `7월 20일 전후로 보입니다`.
  호스트는 `entities` 가, 날짜는 `time_range.basis` 가 받는 몫인데 기법만
  보고 있었다. **완벽한 입력에서 늑대를 외치면 검사가 죽는다.** 두 필드도
  커버로 치고 최소 구간을 3낱말로 올려 0건이 됐다.

### 못 잡는 것

**절을 옮기긴 했는데 엉뚱한 기법에 붙인 것은 못 잡는다.** 인용이 있으므로
덮인 것으로 보인다. 실측(같은 케이스 3차 실행)에서 `계정 관련 변경이` 가
`T1543.003`(Windows Service)에 붙었고, 이 검사는 침묵했다. 그 한계를
`tests/test_coverage.py::test_a_clause_mapped_to_the_wrong_technique_is_not_caught`
에 굳혀 뒀다.

`src/common/errors.py` 의 어휘가 늘었다(전체 공지 대상, 사용자 승인 2026-09-04)
— 유형 `uncovered_input`·`nonverbatim_evidence`, 조치 `record`.

### 오배정 가드를 02단계에 두지 않기로 한 합의 (2026-09-05)

"그렇다면 오배정(엉뚱한 기법 매핑)도 검사기로 막아야 하는가?"에 대한 아키텍처 검토를 진행했다.

결론은 **02단계 오배정 가드는 만들지 않는다.**
- 검사기(Checker)는 자기가 발동한 곳에서만 참이면 되지만, 오배정을 가리려면 "이 서술은 원래 어느 기법이어야 하는가"를 코드가 알아야 하므로 검사기가 아니라 **분류기(Classifier)**가 된다.
- 이를 규칙 표로 만드는 순간 자연어 처리를 정적 키워드 매핑으로 되돌리는 꼴이 되어 LLM 도입 의미를 스스로 무너뜨린다.
- **설계 원칙**: *"조용하고 되돌릴 수 없는 실패(Silent & Irreversible)에만 앞단에 가드를 건다."* 오배정은 비가역적 파괴가 아니므로 02를 조이지 않고, 07단계 보고서에 `기법 ← "인용문"`을 나란히 찍어 인간에게 넘기며, 03단계 2-Tier 선별을 넓혀 흡수한다.
- 상세 내용은 `docs/proposals/stage02_coverage_and_misassignment.md` 에 기록했다.

## 2026-09-04 · 매핑에 검증 전용 축(`corroborates`)이 생겼다 (**기계만 해결됨**)

03단계와 06단계가 **같은 목록을 정반대 방향으로** 쓰고 있었다.
`artifacts:` 는 "어디를 수집할까"라 좁아야 하는데, 06 의
`technique_supported` 가 그것을 뒤집어 "이 증거로 그 기법을 말할 수 있나"로
읽는다. 한 목록이라 넓히면 04 파싱 시간과 05 토큰 예산이 커지고, 좁히면
06 이 정탐을 기각한다.

최상위에 `corroborates:` 를 더했다. **06단계만 읽는다** — 03 의 수집
목록은 그대로다. 카탈로그에 없는 이름은 로드 시점에 거부하고, 안 적으면
06 은 예전과 똑같이 동작하므로 호환성 폴백이 필요 없다.

**기존 매핑 40개를 하나도 고치지 않았다.** `work.md` 의 원래 스펙은
`artifacts:` 를 `collect_artifacts:` 로 개명하는 것이었는데, 03단계 동작을
위협하면서 얻는 것이 이름뿐이라 더하기만 했다.

### 채우지 않은 이유 — 여기가 이 항목의 핵심이다

**아직 아무 매핑도 이 키를 쓰지 않는다.** (2026-09-07에 `T1204.002`에 처음 사용되었습니다. 아래 2026-09-07 절 참조). 당시에는 넓힐 근거가 없었습니다.

지금까지 나온 모든 소견의 (기법, 인용 아티팩트) 쌍은 여섯이고 기각은
하나인데, **그 하나가 정탐이었다.** 소견 F3 은 *"cmd.exe 가 실행됐고 이는
VMware Tools 스크립트를 실행한 T1091"* 이라고 했지만 실제 레코드는 Wazuh
에이전트의 자기 재시작이었다(`restart-wazuh.exe` → `cmd.exe /c net stop
Wazuh`). USB 와 무관하다.

`--mode assemble` 에서 `value_match` 는 항등식이고 `ref` 는 출력 문법이
막으므로 **`technique_supported` 가 06단계의 유일한 판정자**다. 관측된
오기각 없이 넓히면 통과율 100% 가 성과가 아니라 측정의 소멸이 된다.
`CLAUDE.md` 가 적어 둔 유혹이 정확히 이것이다 — *"수치를 낮추려고 검증기를
무르게 만들고 싶어진다."*

`tests/test_mapping_loader.py::test_no_shipped_mapping_declares_corroborates_yet`
가 이 상태를 붙들고 있다. 넓히는 것은 기각 기록을 사람이 가른 뒤의 일이다.

## 2026-09-04 · 기각을 누적하는 자리가 생겼다 (**모으는 곳만. 데이터는 아직**)

`work.md` 10번의 3단계는 *"`also_supports` 로 누적된 매핑 결손 빈도 데이터를
기반으로"* 넓히라고 하는데, **그 데이터를 모으는 자리가 없었다.**

기각 상세는 `06_verified.json` 에만 있었고 그 파일은 같은 case-id 를 다시
돌리면 덮인다(`--force`). 세 번째 실행이 첫 번째의 기각을 지운다. 근거는
여러 실행에 걸쳐 쌓여야 하는데 쌓이는 곳이 없었다.

- `live_check` 가 기각 상세를 실행 기록(`benchmark/results/`)에 싣는다.
  이 파일은 실행마다 새로 쓰이므로 덮이지 않는다.
- `benchmark/collect.py --rejections` 가 **(기법, 인용 아티팩트)** 조합별로
  세고, 아직 안 가른 것을 맨 위에 올린다.
- 사람이 가른 것은 `benchmark/rejections.yaml` 에 남는다. 이 파일은
  gitignore 대상이 아니다 — **판단은 지식이고 실행 기록은 산출물**이다.

`mapping_narrow` 라고 적어 놓고 매핑을 안 고치는 것을 테스트가 막는다.
그 상태는 기각을 표에서 지우면서 검사기는 그대로 두므로, **판단했다는
기록이 판단하지 않은 것을 덮는** 가장 나쁜 모양이 된다.

### 관측 — 두 실행에서 같은 오배정

같은 조합이 두 번 나왔고 둘 다 정탐이었다. 모델이 **정상 소프트웨어의 자기
관리 동작을 USB 복제(T1091)로** 읽는다.

| 실행 | 인용한 것 |
|---|---|
| `K-LIVE-0902-wide` F3 | Wazuh 에이전트 자기 재시작 (`restart-wazuh.exe` → `net stop Wazuh`) |
| `K-LEDGER-0905` F2 | 위 + VMware Tools `resume-vm-default.bat` |

두 번째는 **레코드를 제대로 읽고 기법만 틀렸다.** 오배정 부류이고 06 이
잡았다 — 02단계에 분류기를 만들지 않기로 한 판단(같은 날)이 여기서 검증됐다.
기각이 두 원인을 섞는다는 것은 여전히 참이지만, **지금까지 관측된 것은
전부 모델 오류 쪽**이다.

### 이 대장이 못 하는 것

판단이 소견이 아니라 **조합**에 붙으므로, 나중에 정당한
`T1091 ← evtx:Sysmon` 소견이 나와도 "이미 가름"으로 묶여 표에서 사라진다.
소견 단위로 붙이면 이 문제는 없지만 대장이 실행마다 자라고 같은 원인이
매번 새 항목으로 올라온다. 매핑을 넓힐 때 적는 값이 조합이므로 조합을
골랐고, 대신 `note` 에 근거를 남기는 것을 규칙으로 뒀다.

## 2026-09-05 · `work.md` 가 964줄이 됐다 (**정리함**)

큐 문서가 **끝난 것과 계획 상세를 함께 이고 다니면서** 964줄이 됐다.
`work.md` 의 일은 "다음에 무엇을 할 차례인가"인데, 그 답을 찾으려면 아홉
화면을 넘겨야 했다. **255줄로 줄였다.**

무엇을 어디로 옮겼나.

| 옮긴 것 | 왜 | 어디로 |
|---|---|---|
| 1번의 목록·수집 방법·외부 도구 | 계획 자료라 분량이 컸다 | `docs/proposals/windows-version-axis.md` |
| 3번의 배선 설계 | 같은 이유 | `docs/proposals/wazuh-alert-intake.md` |
| 6번 evtx 엔진 | **닫힌 결정**이라 큐가 아니었다 | 이미 `limitations.md` 에 전문이 있었다 |
| 9번 05단계 컨텍스트 | 2026-09-03 에 끝났다 | 이미 `limitations.md` "05단계 레코드 배분" |
| 2-3 SQLite | 2026-09-02 에 끝났다 | 이미 `limitations.md` SQLite 절 둘 |
| 11번 | 1차 처리됐고 남은 절은 7번과 한 자리였다 | 7-2 로 합쳤다 |
| 5번 GUI 의 형태·호출·위치 결정 | 정했고 만들었다 | `ui/` 코드가 기록이다 |

**옮기지 않고 지운 것도 있다.** 6번·9번·SQLite 는 `limitations.md` 에 이미
전문이 있었다 — 큐에 있던 것은 그 요약이었다. 요약을 다른 문서로 옮기면
진실이 둘이 되므로 **가리키게만** 했다.

`work.md` 의 "참고 문서" 표도 뺐다. `CLAUDE.md` 의 같은 표와 겹쳤다.

### 번호를 다시 매기지 않았다

6·9·11 이 빠졌지만 나머지 번호는 그대로다. 다른 문서 일곱 곳이 `work.md`
번호로 가리키고 있어서, 다시 매기면 그 참조가 전부 조용히 틀려진다.
사라진 셋을 가리키던 참조는 **가리키는 곳을 바꿨다** — 6번은
`limitations.md`, 9번도 `limitations.md`, 11번은 7-2 다.

### 문서 세 개를 가르는 기준 (다시 적어 둔다)

- `work.md` — **무엇을, 어디서, 어떻게 확인하며.** 끝나면 지운다.
- `docs/limitations.md` — **지금도 참인 제약.** 고친 뒤에 남은 것도 여기다.
- `docs/limitations-log.md` — **닫힌 것의 기록.** 큐에서 무엇이 왜 내려갔나.
- `docs/proposals/` — **왜 그렇게 설계했나**, 그리고 분량이 큰 계획 자료.

## 2026-09-05 · 02단계가 지어낸 경로가 매핑 기본값을 덮고 있었다 (**해결됨**)

`entities.paths` 의 첫 항목이 03단계의 `{web_root}` 가 된다
(`scope_resolver.ENTITY_VARIABLES`). 그런데 `build_context` 는
`dict(defaults)` 로 시작해 entity 값이 있으면 **덮는다.** 즉 모델이 지어낸
경로가 **사람이 기법별로 써 둔 기본값을 이긴다.**

### 문서에 적혀 있던 전제가 틀렸다

`work.md` 7번은 이렇게 적어 두었다 — *"'입력에 없으면 환각' 으로 막으면
쓸모 있는 추론까지 잃는다"*, 근거는 골든 픽스처의 `C:\inetpub\wwwroot` 가
입력 서술에 없는 **도메인 추론**이라는 것이었다.

**그 값은 `T1505.003.yaml` 의 `defaults.web_root` 와 글자 그대로 같다.**
도메인 추론은 이미 매핑에 있었고, 그것도 기법별로 사람이 써서. 모델이 그것을
다시 추측할 이유가 없다.

`entities.paths` 를 비우고 03단계를 돌려 골든 산출물과 대조했다 —
**바이트 단위로 같다.** 떨궈도 잃는 것이 없다.

### 실측 — 모델은 경로를 뽑아낸 적이 없다

`qwen2.5:latest` 로 두 입력을 8회씩 돌렸다.

| | `hosts` 지어냄 | `paths` 지어냄 |
|---|---|---|
| 키오스크 | **0/8** (`키오스크 단말`) | **8/8** (`C:\Users\Public\Documents`) |
| 웹셸 | **0/8** (`WEB01`) | **8/8** (`C:\inetpub\wwwroot`) |

**`hosts` 환각은 이미 닫혀 있었다.** 2026-09-04 의 few-shot 수정(예시 3이
`entities` 를 전부 빈 배열로 둔 것)이 같이 닫은 것으로 보인다 — 그전에는
`キオスク`(일본어)가 나와 보고서 첫 줄 "대상 호스트"에 실렸다.

`paths` 는 반대로 **한 번도 입력에서 오지 않았다.** 키오스크가 낸
`C:\Users\Public\Documents` 는 그 실행에 `{web_root}` 를 쓰는 기법이 없어
피해가 없었을 뿐이다. 웹셸 기법이 함께 걸렸다면 **엉뚱한 폴더를 훑었다.**

### 고친 것

`coverage.ungrounded_entities` 가 `entities` 값이 입력 원문에 있는지 본다.
**부분문자열로, 대소문자를 무시하고** — 입력 `웹서버 WEB01에서` 에 대해
`WEB01` 은 정규화이지 창작이 아니다.

없는 값은 02단계가 **떨구고** `errors.jsonl` 에 `ungrounded_entity` 로
적는다. 떨구면 그 자리는 매핑 `defaults` 로 돌아간다.

`src/common/errors.py` 어휘가 늘었다(전체 공지 대상, 사용자 승인 2026-09-05)
— 유형 `ungrounded_entity`. 조치는 기존 `record` 다. **실패가 아니라 측정**
이므로 `live_check` 의 "재시도·실패" 수에서 빠진다.

### 사정거리를 먼저 쟀다

고치기 전에 `entities` 가 실제로 어디까지 가는지 셌다.

- 매핑이 쓰는 변수 중 **`{web_root}` 하나만** `entities` 에서 온다.
  `{system_root}`·`{pos_db_root}`·`{staging_root}` 등 나머지는 전부 매핑
  `defaults` 소관이다.
- `{host}`·`{account}`·`{process}`·`{ip}` 는 `ENTITY_VARIABLES` 에 선언돼
  있지만 **어느 매핑도 쓰지 않는다.**
- `entities.hosts` 는 07단계가 보고서 첫 줄에 그대로 인쇄한다.

즉 실제 피해 경로는 둘뿐이었고, 그중 하나(`hosts`)는 이미 닫혀 있었다.

### 남은 것

**입력이 진짜로 경로를 말하면 어떻게 되는가는 아직 못 쟀다.** 실측 두 입력
모두 경로를 언급하지 않았다. 언급하는 입력에서 모델이 그것을 옮기는지,
옮긴다면 이 검사가 통과시키는지는 확인이 필요하다 — 통과시켜야 맞다.

## 2026-09-05 · 기각 대장이 입구 하나만 보고 있었다 (**해결됨**)

2026-09-04 에 만든 기각 누적은 `tools/live_check.py` 가 실행 기록의
`measures` 에 기각 상세를 싣는 방식이었다. **입구가 셋인데 하나만
기록하고 있었다.**

`run_pipeline.sh` 도 웹 UI(`ui/`)도 `benchmark/results/` 를 참조조차 하지
않는다. 그 두 경로로 돌린 실행의 기각은 `cases/<id>/06_verified.json` 에만
남고, 같은 case-id 를 다시 돌리면 `--force` 로 지워진다. **매핑을 넓힐
근거가 새고 있었고, 새는 것이 드러나지도 않았다** — `CLAUDE.md` 가
`run_pipeline.sh` 를 관통 실행 명령으로 문서화해 두었고 UI 가 발표 경로다.

우연히 안 드러났던 이유는 지금까지의 케이스 셋을 전부 `live_check` 로
돌렸기 때문이다.

**기록하는 쪽을 기각을 만드는 단계 자신으로 내렸다.** 06단계가
`06_verified.json` 을 쓴 직후 `benchmark/results/rejections.jsonl` 에
덧붙인다(`src/stage06_verify/runlog.py`). 입구가 몇 개든 상관없어진다.

- **append 만 한다.** 실행 하나가 앞 실행의 줄을 고치지 않는다. 같은 줄이
  두 번 붙어도 `(case_id, verified_at, 소견 id)` 로 걸러 한 번만 센다 —
  재실행이 빈도를 부풀리면 그 빈도가 매핑을 넓히는 근거라 곧장 오염된다.
- **기각이 없으면 아무것도 안 쓴다.** 빈 줄이 쌓이면 "기각이 없었다"와
  "이 실행이 기록되지 않았다"가 구별되지 않는다.
- **대장을 못 써도 실행을 실패시키지 않는다.** 기록하려다 파이프라인을
  죽이는 것은 앞뒤가 바뀐 것이다.
- `--run-log` 로 자리를 바꾸거나 빈 문자열로 끌 수 있다. `None`(기본,
  기본 자리에 쓴다)과 `""`(쓰지 않는다)를 `or` 로 합치지 않는다 — 합치면
  끄려던 것이 기본값으로 되살아난다.

`live_check` 는 이제 기각 상세를 싣지 않는다. 06 이 대장에 쓰므로 같이
실으면 한 실행이 두 곳에 잡혀 `collect.py --rejections` 가 두 번 센다.
`collect.py` 는 대장과 **옛 실행 기록을 함께 읽는다** — 이 변경 전의 기록도
계속 보이게 두려는 것이고, 옛 실행에는 대장 줄이 없고 새 실행에는 `measures`
항목이 없어 겹치지 않는다.

**실증** — 06단계를 단독으로 돌려(= `run_pipeline.sh` 가 하는 것과 같은
호출) 대장에 줄이 붙고 `collect.py --rejections` 가 그것을 세는 것을
확인했다. 그전에는 이 호출이 아무것도 남기지 않았다.


---

## 2026-09-06 · 문장이 증거 밖의 것을 말해도 아무도 보지 않았다 (**해결됨**)

`--mode assemble` 에서 claims 는 파이썬이 원본에서 1:1 로 복사한다. 그러면
`value_match` 는 항등식이고, 06 이 대조하는 것은 claims 뿐이라 **문장이
무슨 말을 하든 관문이 없었다.** 2026-09-04 실물 실행에서 이렇게 통과한
문장이 있다.

    "컴파티텔런너.exe 가 여러 번 실행되며 …"

`CompatTelRunner.exe` 를 한글로 음차한 이름이고, 그런 파일은 증거 어디에도
없다. claims 는 타임스탬프 둘뿐이라 이름은 대조 대상이 아니었고, 보고서에는
존재하지 않는 파일명이 그대로 실렸다.

**06 에 체커 하나를 더 달았다** — `checkers/statement_grounded.py`. 문장에서
경로·파일명·큰 수를 뽑아 인용한 증거 안에 있는지 본다. 없으면 그 소견을
`unverifiable` 로 **강등**한다.

### 기각이 아니라 강등인 이유

기각 사유의 분포가 발표 수치이고 그것이 세는 것은 "모델이 지어냈다"이다.
강등이 말하는 것은 "우리가 대조하지 않았다"라 다른 사실이다. 한 어휘에
섞으면 환각률이 모델의 잘못과 검증 범위의 좁음을 함께 센다. 동결 스키마의
`reason` enum 을 건드리지 않아도 되는 것은 부수적인 이득이다
(`schemas/README.md` 4-3).

### claims 만 대조 상대로 두는 규칙은 실측에 반증됐다

`work.md` 12번의 (가)는 "문장이 언급한 고유명사·수치가 **claims 에** 있는지
본다"였다. 그대로 만들어 돌리니 **손으로 옳다고 판단해 둔 41건 중 8건이
강등**됐다(`benchmark/validator/cases.json` V1·V2·V5·V6·V11·V12·V30·V38).
걸린 것이 전부 같은 모양이었다.

    "shell.aspx 의 크기는 4821바이트입니다."
      claims: MFT#12345.size = 4821

`shell.aspx` 는 **인용한 레코드 자신의 이름**이다. `ref` 가 이미 그 레코드를
지목했고 `ref_exists`·`ref_in_input` 이 그것을 확인했는데, 문장이 그 레코드를
이름으로 부른 것을 근거 없음으로 세고 있었다. 문장의 **주어**를 환각으로 센
셈이다.

그래서 대조 상대를 셋으로 넓혔다 — claims 의 값, 인용한 `ref` 문자열, 그리고
**인용한 레코드의 값 전부**(중첩 필드와 배열 안까지). 이 규칙으로 45건 중
강등은 기대한 둘뿐이다.

**그 대신 (가)의 절반은 열린 채 남는다** — "claims 가 문장을 다 덮는가"는
여전히 아무도 보지 않는다. `docs/limitations.md` 에 남겼다.

### 기법 번호가 4자리 수로 잡혔다

손에 있는 실물 케이스 넷(`K-ENTITY-0905`·`K-LEDGER-0905`·`K-LIVE-0902-wide`·
`K-LIVE-0906-wide`)에 대 보니 **넷 전부**가 같은 문장에서 강등됐다.

    "…이는 T1059 기법에 해당한다"  →  증거에 없는 표현: 1059

점이 있는 `T1505.003` 은 파일명 규칙이 걸러 내지만 점 없는 `T1059` 는 그냥
4자리 수다. 기법 번호는 문장이 인용한 값이 아니라 분류 이름이고, 그것이
맞는지는 `technique_supported` 가 본다. 수를 뽑기 전에 지우게 고쳤고, 그
뒤 넷 전부 강등 0건이다.

**이것이 실물에 대 보지 않았으면 그냥 나갔을 자리다.** 픽스처 45건은 전부
통과했었다.

### 안 보는 것

셋은 일부러 지나간다. 강등은 환각률의 분모에서 빠져 수치를 **올리는 쪽**
이므로, 애매한 것을 잡으면 검증기가 자기 무지를 세게 된다.

- **시각 표현** — 문장은 부분·상대 표기를 쓴다("03:22:15", "약 8분 후").
- **3자리 이하의 수** — "4초 후" 같은 파생 수치와 식별자를 가를 수 없다.
- **일반 고유명사** — 계정명·그룹명·서비스명. 한국어 문장에서 어디까지가
  이름인지 기계가 가르지 못한다.

### 함께 바뀐 것

- 07 이 미검증 항목에 **사유를 함께 싣는다**. 검증불가로 오는 길이 둘이
  됐고("근거를 달지 않았다" / "증거에 없는 것을 말했다") 읽는 사람이 할 일이
  다르다. 골든 보고서를 재생성했다.
- `live_check.py` 가 강등 건수를 `statement_ungrounded` 로 재고, "검증 불가율"
  옆의 괄호를 고쳤다 — 더는 "claims 가 빈 문장"만이 아니다.


---

## 2026-09-06 · 경고를 묶는 장치가 한 번도 걸리지 않았다 (**해결됨**)

2026-09-03 에 registry 파서의 값 읽기 실패 경고를 **사유당 한 줄**로 묶고
`docs/limitations.md` 에 "닫았다" 고 적었다. **실제로는 한 번도 안 걸렸다.**

`K-GROUNDED-0906` 관통에서 드러났다. `registry:SYSTEM` 의 경고가 109줄
그대로 찍혔고, 전부 같은 사유(`Unknown VK Record type 0x12`)였다.

묶는 키가 `type(e).__name__ + ": " + str(e)` 인데 python-registry 의 예외
문자열이 **셀 오프셋을 품는다**.

```
Unknown VK Record type 0x12 at 0xc6785c
                               ^^^^^^^^ 건마다 다르다
```

그래서 매번 새 사유가 되고 `seen == 0` 이 언제나 참이었다.

**덮는 것이 04 출력만이 아니었다.** `tools/live_check.py` 는 스트림당
`MAX_ECHO_LINES = 40` 줄만 찍는다. 이 109줄이 예산을 태워 실행 화면에는
`! … (135줄 생략)` 과 "파싱 실패 109건" 이라는 수만 남았다 — **묶음이
막으려던 바로 그 상황이다.** 사유를 보려면 파서를 따로 돌려야 했다.

**주소만 지운다.** 타입 번호(`0x12`)는 남긴다 — 번호까지 지우면 서로 다른
미지원 타입이 한 줄로 뭉쳐 "무엇이 몇 건인지" 를 잃고, 사유별로 나눈 애초의
이유가 사라진다.

같은 수집본에서 **경고 110줄 → 2줄**, `parse_errors` 는 그대로 **109** 다.
줄이는 것은 줄이지 수가 아니다.

### 시험이 통과하고 있었다

`tests/test_registry_parser.py` 의 `test_the_same_failure_is_logged_once_and_counted`
가 이미 있었고 통과했다. **목업 예외에 주소가 없었기 때문이다** —
`Exception("Unknown VK Record type 0x12")` 로 90건을 만들면 묶인다.

실물 메시지 모양(`... at 0x{주소}`)으로 시험 둘을 더했다. 하나는 주소가
달라도 한 줄로 묶이는지, 하나는 타입 번호가 다르면 여전히 나뉘는지 본다.

**목업이 실물보다 깨끗하면 시험이 통과하면서 결함이 남는다.** 이 파서에서
같은 부류를 또 만나면 이 절을 먼저 본다.


---

## 2026-09-06 · 장치 속성을 DEVPROP 체계로 읽는다 (**해결됨**)

`ControlSet001\Enum\…\Properties\{GUID}\NNNN` 아래의 값은 표준
레지스트리 타입이 아니라 DEVPROP 타입 체계를 쓴다. 2026-08-26 부터 알고
있었지만 `work.md` 큐에 항목이 없었다 — 우선순위를 낮게 둔 판단이
`K-ALERT` 의 90건 위에 서 있었고, 목표 환경(키오스크 + USB)에서는 3,873건에
**조용히 틀리는 값**까지 붙는다.

두 가지가 함께 일어나고 있었다.

- **예외** — 겹치지 않는 번호. 값이 통째로 사라진다. 실측 수집본에서 109건.
- **조용히 틀리는 값** — 겹치는 번호. 표준 타입으로 해석되어 그럴듯한 값이
  실린다. `0x05` 가 `RegBigEndian` 으로 읽혀 `83951615` 로 나갔다.

### 경계를 실물로 그었다

2026-09-03 절이 "번호가 아니라 경로로 켜라" 고 적어 두었는데 **어느 경로
인가**가 안 적혀 있었다. 같은 하이브에 모양이 둘이다.

| | 형태 | 값 | 타입 체계 |
|---|---|---:|---|
| A | `…\Properties\{GUID}\NNNN` 의 `(default)` | 6,226 | DEVPROP |
| B | `…\Properties\<이름>` | 71 | 표준 `REG_*` |

B 는 `Control\Class\{…}\Properties\Security` 같은 것들이고 지금
옳게 읽힌다. "경로에 `Properties` 가 있으면" 으로 켜면 이 71건을 깨뜨린다.
`{GUID}\NNNN` 까지 맞아야 켠다.

### 확인한 것만 읽는다

명세를 옮겨 적지 않았다. 이미지 안에서 뜻을 확인한 여섯만 해석하고 나머지는
16진으로 낸다 — `0x12` STRING · `0x19` STRING_INDIRECT · `0x0D` GUID ·
`0x10` FILETIME · `0x08`/`0x09` 64비트 정수.

`0x0D` 는 "길이가 전부 16바이트" 라는 생김새만이 근거라 약한 축이었는데,
리틀엔디언으로 조립하니 표본이 **실재하는 장치 GUID** 로 떨어졌다
(`{e3c9e316-0b5c-4db8-817d-f92df00215ae}`). 바이트 순서가 틀렸으면 이런
모양이 안 나온다. 시험에 못 박아 두었다.

### 전/후

| | 전 | 후 |
|---|---:|---:|
| `parse_errors` | 109 | **0** |
| DEVPROP 키 189건 중 **값이 빈 것** | 110 | **1** |
| `Properties` 밖 값의 변화 | — | **0건** |
| `0x10` FILETIME 41건 | — | **전부 그대로** |

남은 1건은 값이 하나도 없는 키다 — 읽지 못한 것이 아니다.

`0x12` 109건이 값 없음에서 `"USB Input Device"`·`"input.inf"`·
`"HID_Inst.NT"` 로 바뀌었다. **`0x10` 이 안 움직인 것이 가장 중요하다** —
장치의 연결·제거 시각이 그 타입이고, 표준 번호와 우연히 같아 전부터 옳게
읽히고 있었다. 새 경로가 그것을 건드렸다면 USB 타임라인이 통째로 흔들린다.

표기가 바뀐 것 둘도 있다. `0x11` 8건이 `255` → `"ff000000"`, `0x07` 30건이
`[]` → `""` 다. 앞은 추측을 걷어낸 것이고 뒤는 둘 다 빈 값이다.

관통 검산도 지났다 — `inspect_jsonl` (ref 유일 45,349건), `hexdump_record`
(되짚음 100/100), 전체 스위트 1,334 통과. 대조 기록은
`docs/artifact-notes.md` 2026-09-06 절이다.

### 남은 둘은 지금도 참이다

`0x11`(919건)과 `0x82`(4건)는 뜻을 모른다. `docs/limitations.md` 로 갔다.
`0x11` 의 4바이트 폭이 **셀 인라인 저장의 폭**일 수 있다는 실마리가 이번에
생겼는데, 확인하지 않았다.

---

## 2026-09-07 · `$UsnJrnl` v3 를 읽게 했고, 그 자리에서 경계 버그가 나왔다 (**해결됨**)

`work.md` 2번의 앞쪽입니다. 실측과 대조 기록은 `docs/artifact-notes.md` 의
2026-09-07 절이고, 여전히 참인 제약(v4 · v3 미검증)은 `docs/limitations.md`
의 "`$UsnJrnl` v4 를 읽지 못하고, v3 는 실물로 확인한 적이 없다" 절입니다.

### 막고 있던 것은 **NTFS 한정으로는 막힌 것이 아니었다**

적혀 있던 이유는 이랬습니다 — "v3 의 파일 참조는 128비트 파일 ID 라 NTFS 의
(엔트리, 시퀀스) 로 쪼개지지 않는데, 그 두 필드가 USN 레코드를 `$MFT` 와 잇는
유일한 통로다."

**NTFS 에서 `FILE_ID_128` 은 64비트 파일 참조를 하위 8바이트에 담고 상위
8바이트를 0 으로 둡니다.** 그래서 상위가 0 이기만 하면 v2 와 똑같이 쪼개집니다.
상위가 0 이 아니면 그것은 NTFS 참조가 아니므로(ReFS) **레코드를 내지 않고
건너뜁니다** — 하위만 잘라 담으면 없는 MFT 엔트리를 가리키는 레코드가 되고,
06단계가 대조할 수 없는 값을 04단계가 지어내는 셈입니다.

v2 와 v3 의 차이는 그 파일 참조 둘의 폭뿐입니다. 나머지는 이름까지 순서도
뜻도 같고 오프셋만 16 밀립니다.

### 그 자리에서 나온 것 — **"덜 들어온 것"을 손상으로 세고 있었다**

버전마다 헤더 크기가 다르므로(v2 60 · v3 76) 파서가 순회 전에 하던 "60바이트
미만이면 더 읽어 온다" 는 예비 검사가 더는 성립하지 않습니다. 고치려고 열어
보니 **그 자리에 이미 버그가 있었습니다.**

레코드가 버퍼 끝에 걸치면 `unpack` 이 "버퍼 밖으로 벗어남" 을 냈고, 파서는
그것을 **손상과 같은 예외**로 받아 8바이트씩 걸어 들어가며 재동기화했습니다.
그러면 레코드 시작점을 지나쳐 **그 레코드가 통째로 사라지고**, 있지도 않은
손상 구간이 `parse_errors` 로 결산에 오릅니다.

합성 저널로 재현했습니다 — 레코드 1건 유실, `parse_errors` 2구간 216바이트.
회귀 테스트는 `tests/test_usn_parser.py` 의
`test_a_record_straddling_the_boundary_is_not_counted_as_corruption` 입니다.

`IncompleteRecord` 를 손상과 갈랐습니다. 파서는 그것을 받으면 재동기화하지
않고 더 읽어 와서 같은 자리를 다시 보며, 스트림이 끝났으면 잘린 꼬리로 버립니다.

### **실물에서는 안 터지고 있었습니다** — 왜인지도 쟀습니다

키오스크 KAPE 수집 둘(626,600레코드)에서 전/후가 완전히 같습니다. 건수도,
어느 USN 이 나왔는지의 집합도 같습니다.

**`$J` 레코드는 4096바이트 페이지 경계를 넘지 않습니다** — 626,600건 전수에서
0건입니다. 기본 청크 1 MiB 가 4096 의 배수라 버퍼 경계가 항상 페이지 경계와
겹치고, 그래서 걸치는 레코드가 애초에 생기지 않았습니다.

도달하는 조건은 셋입니다 — 청크 크기가 페이지 배수가 아닐 때(파서는 8192
이상이면 받습니다), 카빙·재조립으로 정렬이 깨진 `$J`, 그리고 **v3**(헤더가
76바이트라 옛 예비 검사값 60 을 넘습니다). 세 번째가 이번에 생겼습니다.

**"고쳐서 레코드를 되찾았다" 가 아닙니다.** 이 저널들에서는 잃은 것이
없었다는 사실을 확인한 것입니다.

---

## 2026-09-07 · 소견 18건 중 13건에 사실이 없었다 (**해결됨**)

`K-LIVE-0907-wide` 를 사람이 읽다 나왔습니다. 소견의 문장이
`"file_created, outside_time_range"` — **그 레코드의 플래그를 그대로 옮긴
것**이었고, 13건이 그랬습니다.

### 셋이 동시에 침묵했습니다

**13건 전부 06단계를 통과해** 보고서의 "확인된 사항 15건" 에 들어갔습니다.

| 검사 | 왜 안 걸렸나 |
|---|---|
| `technique_supported` | `technique` 이 `null` 이라 판정할 것이 없다 |
| `statement_grounded` (12-1) | 플래그 이름은 **증거 안에 있는 토큰**이라 뽑을 것이 없다 |
| `value_match` | 조립 경로라 파이썬이 원본에서 옮긴다. 항등식이다 |

빈 문장은 이미 막고 있었는데(`validate_selection`), **글자가 있으면 통과**
했습니다. 빈 것과 무내용은 성질이 같습니다.

### 막는 자리를 05 로 뒀습니다

06 에서 강등하면 수치는 정직해지지만 **소견이 남지 않습니다.** 모델에게 다시
쓰게 하는 것이 답입니다. 어휘는 `mappings/_flags.yaml` 이 원본이고, 판정은
`assembly.is_flag_dump` 입니다.

**판정은 좁게 합니다.** 낱말 하나라도 어휘 밖이면 통과시킵니다 — 정상 문장을
기각하면 증거가 사라지고, 무내용을 놓치면 06 이 한 번 더 봅니다. 그래서
"짧다" 나 "동사가 없다" 로는 재지 않습니다.

곁들여 `SelectionError` 가 **자기 안내문을 들고 오게** 했습니다. 재시도
피드백이 `evidence_fields` 문구로 못박혀 있어, 문장이 무내용인 것에 "필드
이름을 확인하라" 고 말하고 있었습니다 — 모델은 문장을 그대로 두고 필드만
만집니다.

### 되돌려 확인했고, 실물로도 돌렸습니다

옛 산출물 18건에 판정기를 그대로 대면 **무내용 13건을 전부 잡고 정상 문장
5건은 통과**시킵니다.

`K-LIVE-0907-recheck` (같은 증거·같은 모델 `qwen2.5:7b`, 2026-09-07):

| | 전 | 후 |
|---|---:|---:|
| 무내용 소견 | 13/18 | **0/20** |
| 환각률 | 11.8% | 5.0% |
| **사고와 관련된 소견** | 0 | **0** |

**재시도는 한 번도 걸리지 않았습니다.** 프롬프트에 나쁜 예를 하나 넣은 것만으로
모델이 첫 시도에 문장을 썼습니다 — 그래서 **판정기의 실물 재시도 경로는 아직
안 밟아 봤습니다.** 시험(가짜 백엔드)과 옛 산출물 대조로만 확인돼 있습니다.

**고친 것은 형식이지 내용이 아닙니다.** 새 소견 20건 중 9건이
`"2026-08-28T…에 파일이 생성되었지만 분석 기간 외입니다"` 입니다 — 사실이라
통과하는 것이 맞고, **모델이 사고 범위 밖이라고 스스로 말하면서도 그것을
고를 수밖에 없었다**는 뜻입니다. 원인은 배분이고, `work.md` 15번입니다.

---

## 2026-09-07 · 창 밖 신호가 자리를 다 먹고 있었다 (**해결됨**)

`work.md` 15번입니다. 무내용 소견(바로 위 절)을 고친 뒤에도 소견 20건 중
9건이 `"…분석 기간 외입니다"` 였고, **모델이 범위 밖이라고 말하면서 그것을
고른다**는 것이 단서였습니다. 줄 것이 그것뿐이었습니다.

### 원인 — 후보 자격과 순서는 다른 판단이다

`allocation._rank` 의 0순위(신호) 정렬 키가 `(0, moment, ref)` 였습니다.
**오름차순이라 오래된 신호가 먼저 나갑니다.** `outside_time_range` 는
`NON_SIGNAL_FLAGS` 로 이미 "신호 아님" 이었지만 그것은 **후보 자격**이지
**순서**가 아니었습니다. 둘이 다른 판단이라는 것이 이 버그의 전부입니다.

| `K-LIVE-0907-wide` (2026-09-07) | |
|---|---:|
| 모델에게 간 것 | 60건 |
| 그중 창 밖 | **42건** |
| 04 에 있던 창 안 + 신호 | 1,356건 |
| 그중 모델에게 간 것 | **0건** |

그 1,356건에 사고 당일 `08-31 03:20~03:24` 의 `shell_spawned` —
`cmd.exe`·`powershell.exe`·`rundll32.exe` — 가 들어 있었습니다.

### 고친 방식 — 0순위 안에 띠

```
띠 0   창 안 신호
띠 0   창 밖이지만 window_independent 플래그를 든 것
띠 1   그 밖의 창 밖 신호
```

**버리지 않습니다.** 후순위일 뿐이라 자리가 남으면 갑니다 — 시간창을 좁히는
것과 다릅니다. **전부 내리지도 않습니다.** 잠복과 사전 작업이 창 밖에 남고,
이 케이스의 `account_created` 도 사고 5일 전입니다.

어느 이름이 남는지는 `mappings/_flags.yaml` 의 `window_independent` 이고,
가르는 기준은 "상태를 바꾸는 것 · 방어를 끄는 것 · 흔적을 지우는 것" 입니다.
시간에 비례해 쌓이는 사건형(`file_created` 등)은 넣지 않습니다 — 넣으면
후순위로 내린 뜻이 사라집니다.

**모르는 이름은 로드 시점에 거부합니다.** 여기서 오타는 다른 곳보다 나쁩니다 —
배분은 **되기는 하므로** 아무도 멈추지 않고 창 밖 레코드가 조용히 후순위로
내려갑니다.

### 같은 입력으로 다시 잰 값

| | 전 | 후 |
|---|---:|---:|
| 창 밖 전달 | 42/60 | **12/60** |
| 창 안 신호 전달 | 0 | **30** |
| `08-31` `shell_spawned` | 0/29 | **13/29** |
| USB 등록 4건 | 4/4 | 4/4 |
| 창 밖 `account_created` 6건 | 6/6 | **6/6** |

마지막 줄이 이 설계의 시험입니다 — **창 밖인데도 살아남아야 하는 것**이
살아남았습니다.

C-001 픽스처는 전/후가 같습니다(limit 60 에서 정답 3/3). 그 레코드들에는
`outside_time_range` 가 없습니다. **벤치마크 자체는 이 기계에서 못 돌립니다** —
`cases/C-001` 이 없고 데이터셋의 `evidence/` 가 비어 있습니다.

### 실물 관통 — 소견이 사고 당일을 말한다

`K-LIVE-0907-band` (같은 증거·같은 모델 `qwen2.5:7b`, 2026-09-07).

| | 무내용만 고쳤을 때 | 띠까지 |
|---|---|---|
| 소견 | 20건 | 5건 |
| 인용 레코드 | 22건 | 13건 |
| `technique` 이 붙은 소견 | 6/20 | **5/5** |
| 소견이 말하는 시각 | 대부분 `08-26`·`08-28` | **`08-31` 사고 당일** |

소견 다섯 중 넷이 사고 당일이고, 인용한 것이 배분에서 노린 바로 그
레코드들입니다.

```
F4  SYSMON#11521   08-31 03:20:34  shell_spawned   cmd.exe
F5  SYSMON#11606   08-31 03:20:59  shell_spawned   powershell.exe
F2  USN#526638496  08-31 03:21:01  file_created    (외 2건)
F3  EVTX-SEC#2670  08-26 07:04:23  account_created  ← 창 밖인데 살아남은 것
```

**F3 이 이 설계의 증거입니다.** 사고 5일 전의 `testuser` 계정 생성이
`window_independent` 때문에 자리를 지켰고, 모델이 그것을 골랐습니다.

### 그래도 그대로인 것 셋

**① 환각률 0% 는 여전히 인용하면 안 됩니다.** 조립 경로라 `value_match` 가
항등식이고, 판정 대상이 5건입니다.

**② 기법 오배정이 남았습니다.** F2 가 `USN` 의 파일 생성 셋을 `T1091`
(이동식 미디어 전파)로 묶고 *"복제를 위한 매체를 통해 전파되는 것으로 보인다"*
라고 씁니다. 레코드에 없는 추론인데 `statement_grounded` 는 **증거 밖 토큰**만
보지 **증거 밖 추론**은 보지 않고, `technique_supported` 는 T1091 매핑이
`$UsnJrnl` 을 요청하므로 통과시킵니다. `work.md` 12번 계열입니다.

**③ USB 등록 레코드는 여전히 아무 소견도 인용하지 않습니다.** 청크 5에
`REG-SYS#10367724` 등 4건이 실려 갔는데 모델이 고르지 않았습니다. 배분이
아니라 모델입니다 — 그리고 그 레코드들은 `flags` 가 비어 있어(아래) 신호로
보이지도 않습니다.

**`device_connected` 가 레지스트리를 보지 않습니다.** 룰이
`evtx:DriverFrameworks`·`KernelPnP`·`System` 세 채널만 보므로
`Enum\USB` 키에는 붙지 않습니다. 목표 시나리오의 핵심 증거가 신호가 아니라
tier 2 여분으로 실려 가는 셈이고, 그래서 `window_independent` 에 넣은
`device_connected` 는 이 경로에서 아무 일도 하지 않습니다. `work.md` 0번과
같은 자리입니다.

### 이 띠가 못 하는 것

**아티팩트 안에서만 순서를 정합니다.** 자릿수 배분이 먼저 아티팩트별로 자리를
나누고 띠는 그 안에서만 겁니다. 그래서 창 밖 신호밖에 없는 아티팩트도 자기
자리는 그대로 받습니다 — 바닥 한 자리가 그것을 보장하기 때문이고, 창 밖이라고
아티팩트를 통째로 굶기지는 않습니다. 위 표에서 창 밖 12건이 남은 것이 그
성질입니다. 모르고 보면 "띠가 안 걸렸다" 로 읽힙니다.

**`--limit 60` 은 그대로입니다.** 창 안 신호만 1,356건이라 60칸으로는 여전히
일부만 봅니다. 순서와 자릿수는 다른 문제이고, 한 번에 둘을 바꾸면 어느 쪽이
효과인지 가를 수 없어 이번에는 순서만 고쳤습니다(`work.md` 15번의 "정하지
않은 것").

---

## 2026-09-07 · 11개 관문을 전부 통과하고도 정답 하나를 놓쳤다 (**해결됨**)

`518_Test_0907` 수집으로 돌린 실행이 `live_check.py` 판정을 전부 PASS
했는데, 보고서에 **조사 대상인 `518.exe` 가 한 줄도 없었습니다.** 증거에는
있었습니다 — `Windows/Prefetch/518.EXE-71CD1C31.pf` 에 3회 실행, 최근 실행
`01:57:30Z`.

**PASS 가 정상이 아니라는 것을 이 실행이 다시 보였습니다.** 판정은 구조
불변식만 봅니다(ref 유일성, offset, 목업 혼입). 무엇을 봤는지는 판정 대상이
아니라, 셋이 겹쳐 정답을 걸러 내는 동안 화면은 초록색이었습니다.

### 세 개가 사슬로 걸렸다

**① 02 가 서술에 없는 도구를 골랐습니다.** 입력은
*"실행 주체와 실행 방법은 아직 확인되지 않았습니다"* 인데 모델이
`T1059.003`(Windows Command Shell)을 냈습니다. 셸이라는 낱말이 없고
**모른다고 명시된** 자리입니다.

02 의 기법 목록이 03 의 유일한 입력이라, 이 한 줄이 나머지를 끌고 갑니다.
`T1059.003` 매핑은 `prefetch` 를 **tier 2** 로 둡니다 — Tier 2 루프백이
미구현이라 영구 미수집입니다. 그리고 `$MFT`·`$UsnJrnl` 은 아예 요청하지
않습니다.

**② `--artifacts` 는 02 프롬프트 힌트일 뿐이었습니다.** 사람이
`prefetch`·`$MFT`·`$UsnJrnl` 을 적어 줬는데 03 은 그것을 읽지 않았습니다.
"꼭 봐라"고 적은 것이 기법 매핑 하나에 밀려 통째로 빠졌습니다.

**③ 벽시계를 UTC 로 읽었습니다.** *"오전 11시경"* → `11:00:00Z`. 실제
실행은 10:57 KST = `01:57:30Z` 로 아홉 시간 어긋난 범위 밖이었고,
`outside_time_range` 로 밀렸습니다.

### 조치 — 셋 다 02·03 에서 닫는다

**① `grounding.ungrounded_techniques` (02).** **분류기가 아닙니다.**
판정하는 것은 하나뿐입니다 — 기법 이름이 *도구* 를 지목하는데
(`T1059.003` = Windows **Command Shell**, `T1218.011` = **Rundll32**)
서술 어디에도 그 도구가 없는가. `MECHANISM_CUES` 에 적힌 기법만 보고,
적지 않은 기법은 손대지 않습니다. 그래서 목록을 안 넓히면 조용합니다 —
`work.md` 10번의 "02 에 가드를 두면 분류기를 다시 만드는 것" 이라는 판단과
부딪히지 않는 선이 여기입니다.

걸리면 그 기법을 떨구고 `ungrounded_technique` 로 셉니다(`ungrounded_entity`
와 같은 무리 — 실패가 아니라 측정이라 조치가 `record`). 떨궈서 하나도 안
남으면 스키마 위반이므로 **재시도합니다.** 이 자리는 커버리지 검사와 달리
재시도가 값을 합니다 — 지적이 기법 하나로 구체적이기 때문입니다.

**같은 함수가 반대 방향도 합니다.** 서술이 이름을 댄 실행 파일
(`518.exe`)을 `entities.processes` 에 되돌려 놓습니다. 지어내는 것이 아니라
**원문에 글자 그대로 있는 것**이라 `ungrounded_entities` 검사도 그대로
통과합니다.

**② `--force-artifacts` (03).** 사람이 지정한 아티팩트를 기법 매핑과
무관하게 Tier 1 로 올립니다. 세 갈래입니다 — 이미 Tier 1 이면 손대지 않고
(매핑의 범위가 더 구체적입니다), Tier 2 면 **매핑이 적어 둔 `scope_template`
을 그대로 써서** 승격하고, 아무도 요청하지 않았으면 시간 범위만 걸어
추가합니다. `live_check.py` 가 `--artifacts` 를 여기에 그대로 넘기고,
지정한 것이 `selected` 에도 `excluded` 에도 없으면 **판정에 실패합니다.**

**③ `timeband.widen_for_local_time` (02).** 한국어 서술이고 표준시가 적혀
있지 않으면 벽시계 시각을 KST 로 읽어, 그 값 주변 ±2시간을 범위가 덮게
만듭니다.

**옮기지 않고 넓힙니다.** 모델이 변환을 했는지 안 했는지 우리는 모르므로
통째로 −9시간 옮기면 이미 옳게 변환한 경우를 망칩니다. 두 읽기를 다 덮는
쪽이 안전하고, 그것은 프롬프트가 모델에게 하는 말과 같은 방향입니다
("범위가 좁으면 증거를 놓칩니다").

**점이 아니라 띠를 보는 이유는 2차 실행이 가르쳐 줬습니다.** 프롬프트를
고치자 모델이 변환은 옳게 했는데(`02:00:00Z`) 그 시각을 **범위의 시작
경계에** 놓았습니다. 실제 실행은 `01:57:30Z` 로 2분 30초 빨랐습니다 —
점만 봤다면 "이미 덮고 있다"고 판정하고 같은 레코드를 또 놓쳤을 것입니다.
`경`·`쯤` 은 정각이 아니라는 뜻입니다.

### 프롬프트도 함께 고쳤다

가드는 **틀린 것을 잡을 뿐 옳게 만들지 못합니다.** 그래서 셋 다 프롬프트에
지시를 넣고 few-shot 예시를 하나 더 뒀습니다(도구 이름이 없으면 그 도구의
기법을 고르지 말 것 / 모르면 상위 기법 / KST→UTC 변환 / 서술이 댄 파일명은
`processes` 에 남길 것). 실측에서 모델이 지시만으로 셋 다 지켰고
(`T1204.002`, `518.exe`, `02:00Z`), 가드는 마지막 2분 30초만 메꿨습니다.
**둘 다 있어야 합니다** — 지시는 대개 듣지만 늘 듣지는 않고, 가드는 늘
걸리지만 옳은 답을 만들지는 못합니다.

### 그래도 그대로인 것

**강제 선별은 비쌉니다.** 아무도 요청하지 않은 아티팩트를 올리면 범위를
좁힐 근거가 없어 시간 범위만 걸립니다. `$MFT` 를 그렇게 넣으면 볼륨의 모든
파일 레코드가 후보가 됩니다 — 100MB 넘는 아티팩트는 04 가 창 밖을 하드로
잘라 내므로(`DEFAULT_LARGE_ARTIFACT_BYTES`) 터지지는 않지만, 시간 범위가
넓으면 그 방어도 얇아집니다. 지정한 사람이 그러라고 한 것이므로 막지 않고,
`rationale` 에 그렇게 적어 보고서까지 보냅니다.

**묶음 접두어는 채널을 통째로 엽니다.** `--artifacts evtx` 는 카탈로그의
`evtx:*` 열일곱을 전부 Tier 1 로 올립니다. 사람이 손으로 열일곱을 적게
만들지 않으려고 둔 편의이고, 그만큼 04 가 오래 걸립니다.

**오배정은 여전히 못 잡습니다.** 이 가드는 `MECHANISM_CUES` 에 적힌 기법이
근거 없이 나온 것만 봅니다. 절을 옮기긴 했는데 **엉뚱한 기법에 붙인 것**은
그대로입니다(`limitations.md` 의 "02단계가 축을 놓치면" 절).

---

## 2026-09-07 · 창 밖이 자리를 다 먹는 자리가 **두 군데 더** 있었다 (**해결됨**)

같은 날 오전에 0순위(신호)에 띠를 넣었는데, **1·2순위에는 같은 띠가
없었습니다.** 그 둘은 **앵커까지의 거리**로 줄을 세웁니다.

**앵커는 사고와 관계있는 레코드가 아니라 그냥 플래그가 붙은 레코드입니다.**
시끄러운 아티팩트가 하나 있으면 타임라인 전체에 앵커가 깔리고, 그러면
거리가 사실상 무작위가 되어 **창 밖이 창 안을 밀어냅니다.**

실측(`K-TEST-518-VERIFY`, 2026-09-07): 신호가 337,650건 중 **143,877건**
(대부분 `$UsnJrnl`)이라 앵커가 143,261개였습니다. 프리패치 296건이 **전부
1순위**로 들어갔고, 배정된 자리 넷을 2026-05-28 의 창 밖 레코드가 전부
가져갔습니다.

```
PF#3307899125  MSEDGEWEBVIEW2.EXE  2026-05-28  outside_time_range
PF#3307899126  MSEDGEWEBVIEW2.EXE  2026-05-28  outside_time_range
PF#2012747154  TCPVCON64.EXE       2026-05-28  outside_time_range
PF#328281288   LISTDLLS64.EXE      2026-05-28  outside_time_range
```

조사 대상인 `518.EXE` 의 실행 기록(`PF#1909267505`, run_count 3, 최근 실행
`2026-09-07T01:57:30Z`)은 앵커에서 **0.031초** 떨어져 있었는데 **121등**
이었습니다 — 앞의 120건이 그보다 더 가까웠을 뿐입니다. 창 안인데 한 자리도
받지 못했습니다.

**한 번 헛짚었습니다.** 처음에는 2순위에만 띠를 넣었는데 실물에서 아무것도
달라지지 않았습니다 — 프리패치가 전부 1순위였기 때문입니다. 단위 시험은
2순위를 만들어 놓고 재고 있었고, **실물로 다시 돌려 보고서야** 그 사실이
드러났습니다.

**이 실패는 어디에도 안 보입니다.** 03단계는 프리패치를 정확히 골랐고,
04단계는 그 레코드를 정확히 파싱했고, 11개 관문은 전부 PASS 입니다.
04 산출물을 직접 열어야만 보입니다.

### 조치

세 순위가 **같은 띠 값**을 쓰게 했습니다. 거리는 띠 다음입니다.

```
0순위 키:  (0, 창밖인가, 시각, ref)
1순위 키:  (1, 창밖인가, 앵커까지의_거리, 시각, ref)
2순위 키:  (2, 창밖인가, 앵커까지의_거리, 파일순서)
```

식을 하나로 둔 이유가 이번 일 자체입니다 — 갈라 두면 언젠가 한쪽만
고쳐지고, 0순위에만 걸었다가 1순위에서 물린 것이 그 예입니다.

**버리지 않습니다.** 창 밖이 후순위일 뿐이라 자리가 남으면 갑니다.
레지스트리처럼 **전부** 창 밖인 아티팩트는 아무것도 달라지지 않습니다 —
상대 순서라 다 같은 띠면 예전 순서 그대로입니다.

### 이 건은 처음 세 개와 다르다

`work-claude.md` 가 지목한 셋(02 기법 가드·03 강제 선별·시간대)은
**아무것도 안 봤던** 것이고, 이것은 **보고 나서 떨어뜨린** 것입니다.
앞의 셋을 다 고치고 나서야 드러났습니다 — 프리패치가 파싱되기 전에는
떨어뜨릴 레코드 자체가 없었습니다.

---

## 2026-09-07 · 사람이 이름을 댄 파일이 자리를 못 받고 있었다 (**해결됨**)

같은 날 띠를 세 순위에 다 걸어 창 밖 문제를 닫았는데, **찾던 것은 여전히
안 왔습니다.** 조사 대상 `518.exe` 의 프리패치 실행 기록이 창 안 39건 중
**39등**이었습니다 — 띠 안의 순서를 `앵커까지의 거리`가 정하는데, 앵커가
143,261개라 그 거리가 사실상 잡음이었기 때문입니다.

### 자리가 문제가 아니라 순서가 문제였다

`518.exe` 를 이름에 가진 레코드는 23건이고, 그 23건이 이런 이야기를 합니다.

| 시각 | 레코드 | flags |
|---|---|---|
| 09-03 02:34:58 | `$UsnJrnl` 3건 | `file_created`, `outside_time_range` |
| 09-03 02:35:18 | `$UsnJrnl` 2건 | `deleted`, `outside_time_range` |
| 09-07 01:57:12 | `$UsnJrnl` 5건 | `file_created` |
| 09-07 01:57:29~31 | `$UsnJrnl` 11건 | 일부 `file_created` |
| 09-07 01:57:30 | `PF#1909267505` | (없음) |
| — | `MFT#267001` | `timestamp_mismatch` |

**떨어뜨렸다 → 지웠다 → 나흘 뒤 다시 떨어뜨렸다 → 실행했다.** 이 중
모델에 간 것은 `MFT#267001` 하나뿐이었습니다.

### 조치 — 엔티티 매칭을 키의 맨 바깥에 둔다

02단계 `entities.processes` 에 있는 이름을 가진 레코드가 **순위·띠·거리
보다 앞서** 갑니다.

```
(엔티티매칭, 순위, 창밖, 거리, …)
```

**순위 안이 아니라 밖인 이유가 실측에 있습니다.** 23건 중 **8건이 플래그가
하나도 없습니다** — 프리패치 실행 기록이 그중 하나입니다. 순위 안에 넣으면
이 8건은 0순위 신호 수만 건에게 져서 영영 못 옵니다.

**이름 전체로 봅니다.** 조각으로 보면 무관한 것이 쓸려 옵니다 — `518` 로
보면 `$UsnJrnl` 185건이 걸리는데 대부분 `mat-debug-5188.log` 였고,
`518.exe` 로 보면 21건이었습니다.

**`processes` 만 봅니다.** `paths` 를 넣지 않은 이유는 그 첫 항목이
03단계의 `web_root` 로 쓰이는 **디렉터리** 문자열이라, 걸면 그 아래 전부가
매칭되기 때문입니다.

**필드는 `name`·`path` 와 `fields` 의 스칼라 값만 봅니다.** 목록형을
제외한 이유는 프리패치의 `loaded_files` 입니다 — 적재 DLL 목록까지 보면
흔한 이름 하나로 프리패치 전량이 매칭됩니다. 반대로 `fields` 의 스칼라는
evtx 4688 의 `NewProcessName` 처럼 **누가 실행했는지**를 담고 있습니다.

결과(실물, `K-TEST-518-VERIFY`): 프리패치 1등, `$MFT` 1등, `$UsnJrnl`
상위 21건이 전부 `518.exe` 관련으로 바뀌었습니다.

### 05 프롬프트가 flags 를 다루게 했다

**같은 실행에서 `timestamp_mismatch` 는 이미 모델에 가 있었습니다.**
`MFT#267001` 이 그 플래그를 달고 전달됐는데, 모델은 "이 파일이 존재하고
빠른 실행 행위가 있다"고만 썼습니다. **레코드는 갔는데 안 읽은 것**이라
배분으로는 안 고쳐집니다.

그래서 `select_system.txt` 에 넣었습니다 — `flags` 는 **이 레코드가
당신에게 온 이유**이고, 고른 레코드에 플래그가 있으면 그것이 가리키는
것을 반드시 다루되 **이름을 옮겨 적지 말고 어느 값에서 나왔는지**를
쓰라는 규칙입니다.

**공짜가 아닙니다.** 고정분이 2,194 → 2,633자(약 +220토큰)가 되었고,
그만큼 매 질의의 레코드 예산이 줄어듭니다. `tests/test_interpret.py` 의
`--num-ctx 5400` 은 이 고정분에 맞춰 둔 상수였어서 5620 으로 옮겼습니다
(예산 414 → 415자로 같습니다).

### 이 변경이 바꾸는 것 — 수치의 뜻

**배분이 입력이 말한 이름 쪽으로 기웁니다.** 지금까지 이 배분은 "무엇을
찾는지 모르는 채로 고른다"는 성질이 있었고, 그것이 재현율을 모델 성능의
지표로 쓸 수 있게 하는 근거였습니다. 이제 정답 증거가 입력에 이름이 나온
파일이면 재현율이 공짜로 오릅니다. **실행 조건으로 함께 적어야 합니다.**

기울기가 모델의 환각으로 생기지는 않습니다 — `entities` 는 입력 원문에
글자 그대로 있는 값만 남기 때문입니다(`coverage.ungrounded_entities`,
`grounding.restore_named_processes`).

### 아직 안 한 것

**흔한 이름에 대한 상한이 없습니다.** 02가 `explorer.exe` 를 내면 그
아티팩트 자리를 그 이름이 다 가져갑니다. 자리 수 자체는 안 늘어나니 터지지
않지만 진짜 이상한 레코드를 밀어낼 수 있습니다. "엔티티 매칭은 자리의
절반까지" 같은 상한을 얹을 수 있는데, **먼저 재 보고 필요하면** 하기로
했습니다 — 지금은 그런 실측이 없습니다.

**"그 파일이 무엇을 만들었나"는 여전히 못 봅니다.** 그 레코드들은
`518.exe` 를 이름에 갖고 있지 않습니다. `docs/limitations.md` 에 남습니다.


---

## 2026-09-07 · ValleyRAT 실물 관통에서 드러난 배분·컨텍스트 결함 (**해결됨**)

8,192 assemble 창에서 선택 JSON이 세 번 잘리던 문제는 기본 창을 16,384로
올려 닫았다. 대상 파일 이름이 맞는 레코드만 앞세우고 자식 프로세스를 놓치던
문제는 Sysmon EID 1의 ProcessGuid 계보, 제한된 PID/Image 대체 연결, 대상
실행 시점 ±2분 우선순위를 배분 키에 추가해 닫았다. 실물 Sysmon에서 대상
실행 두 건과 `msiexec.exe`, PowerShell, `rundll32.exe`가 작은 쿼터 안에
함께 들어오는 것을 확인했다.

`--artifacts registry`가 세 하이브 전체를 강제해 쿼터를 희석하던 문제는
묶음 요청에만 사건 공통 핵심 키 범위를 적용해 닫았다. 정확한 하이브 이름은
기존 의미를 유지한다.

`$J`의 310,120회 파싱 실패는 파서 결함이 아니라 `$MFT` 조각 오수집이었다.
세 연속 FILE 레코드와 번호를 사전 검사해 한 번의 수집 오류로 바꿨다.
USN 이력을 되찾은 것은 아니므로 재수집 한계는 `limitations.md`에 남겼다.

---

## 2026-09-07 · Defender 트로이목마 탐지·격리를 T1204.002 의 뒷받침 증거로 인정했다 (**해결됨**)

`K-TEST-518-VERIFY` 에서 05단계가 Windows Defender 의 탐지·격리 기록을 정확히 골라 왔는데 06단계가 `technique_unsupported` 로 기각했다. `T1204.002` 매핑에 `evtx:Defender` 가 없었기 때문이다.

    EID 1116  09-03 02:35:03/06  Trojan:Win32/PossibleMalware.A  탐지
    EID 1117  09-03 02:35:18     Trojan:Win32/PossibleMalware.A  격리

격리 시각이 같은 파일의 `$UsnJrnl` deleted(02:35:18)와 초 단위로 맞는 **오기각**이다. 안티바이러스가 그 파일을 악성으로 판정하고 지운 기록은 "사용자가 악성 파일을 실행했다"의 근거가 맞다.

`artifacts` 가 아니라 `corroborates` 에 넣었다(`mappings/windows/T1204.002.yaml`). `artifacts` 는 03단계의 수집 축이라 넓히면 파싱 시간과 토큰 예산이 커지지만, `corroborates` 는 06단계 판정에서만 읽히므로 수집 부담 없이 오기각을 닫는다.

**이 프로젝트에서 `corroborates` 를 쓰는 첫 매핑이다.** 규약대로 실측 기각을 보고 넣었고, 판단 근거를 `benchmark/rejections.yaml` 에 `mapping_narrow` 로 남겼다. 테스트 `test_corroborates_has_rejection_record` 가 대장에 근거 없는 확장을 막는다.

