# 시나리오 기반 sLLM 트리아지 도구 — 작업 지침서

**프로젝트명(가칭)**: dfir-triage
**과정**: K-Shield Jr. 침해사고 대응 및 분석 파트 팀 프로젝트
**문서 목적**: 팀원 전원이 동일한 설계 전제 위에서 병렬 작업하기 위한 기준 문서

---

## 1. 프로젝트 개요

### 1.1 한 줄 정의

침해 시나리오를 입력받아 **분석할 아티팩트를 먼저 선별한 뒤**, 선별된 아티팩트만 파싱해 sLLM으로 해석하고, 그 해석이 실제 증거에 근거하는지 기계적으로 검증하는 온프레미스 트리아지 도구.

### 1.2 기존 도구와의 차이

공개된 AI 기반 DFIR 도구(AIFT, DFIR-Chain 등) 대부분은 **정해진 프로파일대로 전부 파싱한 뒤 LLM에 해석을 맡기는** 구조다. 본 프로젝트는 순서를 뒤집어 **LLM이 무엇을 볼지 먼저 결정**한다.

이 차이가 만드는 것:

| 항목 | 전수 파싱 방식 | 시나리오 기반 선별 |
|---|---|---|
| 컨텍스트 문제 | 수십만 건을 소형 모델 컨텍스트에 넣을 수 없음 | 처음부터 대상이 적어 회피 |
| 분석가 사고 모사 | 낮음 | 실무 분석가의 판단 순서와 일치 |
| 리스크 | 느릴 뿐 | **선별 실패 시 증거를 아예 놓침** |

마지막 행이 이 프로젝트의 핵심 연구 질문으로 이어진다: **시나리오 기반 선별의 재현율은 얼마인가.**

### 1.3 발표에서 제시할 세 가지 수치

1. **재현율** — 정답 케이스에서 실제 증거가 담긴 아티팩트를 몇 % 선별했는가
2. **환각률** — LLM 해석 문장 중 근거 검증에서 기각된 비율
3. **효율** — 전수 분석 대비 소요 시간·토큰 절감 (**아직 산출 경로가 없다** — 8.1)

"빠르다"가 아니라 **"이만큼 빠른데 이만큼만 놓친다"**를 말하는 것이 목표다.

### 1.4 명시적 비목표

다음은 이번 프로젝트 범위에 포함하지 않으며, 최종 발표에서 한계로 명시한다.

- 해석의 **타당성** 자동 검증 (문장이 데이터로부터 타당하게 도출되는지)
- Tier 2 루프백 (Tier 1 결과에 따른 추가 선별)
- 파일시스템 예외 케이스 전반 (압축·희소 파일, ADS, $LogFile)
- 리눅스 아티팩트

---

## 2. 아키텍처

### 2.1 파이프라인

```
[입력] 자연어 서술 또는 EDR/SIEM 알럿
   ↓
[02] 시나리오 정규화        ← sLLM + 고정 스키마 검증
   ↓
[03] 아티팩트 우선순위 선별  ← 결정론적 (매핑 테이블)
   ↓
[04] 결정론적 파싱          ← 결정론적 (바이트 레벨)
   ↓
[05] sLLM 해석              ← sLLM
   ↓
[06] 근거 검증              ← 결정론적 (필터)
   ↓
[07] 결과 보고              ← 결정론적 (템플릿 렌더링, 검증 통과분만 입력)
```

07단계는 초안에서 sLLM으로 적었으나 **의도적으로 템플릿 렌더링만 한다.**
검증을 통과한 문장을 모델이 다시 쓰게 하면 마지막 단계에서 환각이 재유입되어
앞의 검증이 전부 무의미해진다. 근거는 `docs/limitations.md` 3장에 있다.

### 2.2 설계 원칙

**원칙 1 — LLM 구간과 결정론적 구간을 번갈아 배치한다.**
LLM 출력이 나올 때마다 다음 단계에서 기계적으로 검증되므로, 오류가 파이프라인 끝까지 증폭되지 않는다.

**원칙 2 — 파싱은 결정론적 도구가, 해석만 LLM이 담당한다.**
소형 모델에 파싱까지 맡기면 환각이 데이터 계층에서 발생해 검증 자체가 불가능해진다.

**원칙 3 — 단계 간 통신은 파일로만 한다.**
함수 호출로 엮으면 팀원이 서로를 기다려야 한다. 파일 계약만 정해두면 목업 입력으로 독립 개발이 가능하고, 중간 산출물이 남아 디버깅이 쉽다.

**원칙 4 — 모든 분석 문장은 원본 오프셋을 참조한다.**
포렌식에서 근거 없는 문장은 가치가 없다. 참조가 없거나 틀린 문장은 리포트에서 제거한다.

**원칙 5 — 보지 않기로 한 것과 그 이유를 남긴다.**
선별에서 제외한 아티팩트를 최종 보고서까지 전달한다. 이것이 선별 방식의 리스크를 "방법론적 결함"에서 "문서화된 판단"으로 바꾼다.

### 2.3 근거 검증의 층위

| 층위 | 내용 | 이번 범위 |
|---|---|---|
| 1. 참조 존재 | `refs`의 ID가 파싱 결과에 실재하는가 | **구현** |
| 2. 값 일치 | 문장이 언급한 값이 레코드의 실제 값과 같은가 | **구현** |
| 3. 함의 타당성 | 해석이 데이터로부터 타당하게 도출되는가 | 미구현, 분석가 검토 영역 |

3층위를 자동화하려면 또 다른 판단 주체가 필요하고, 그것이 LLM이면 순환 논리가 된다. 명시적 한계로 남긴다.

---

## 3. 구현 방침

### 3.1 파싱: 직접 구현 vs 외부 라이브러리

**가르는 기준은 하나다 — 레코드마다 원본 바이트 오프셋을 주는가.** 주지 않는
라이브러리는 후보에서 빠진다. `offset`은 동결된 `parsed_record`의 필수 필드이고,
그것이 없으면 근거 추적 구조 자체가 성립하지 않는다. 성능은 그다음이다
(`docs/limitations.md` "넣지 않은 것 — 네이티브 evtx 엔진" —
`pyevtx-rs`가 1.9배 빨랐는데도 이 관문에서 빠졌다).

| 방침 | 아티팩트 | 근거 |
|---|---|---|
| **전부 자체 구현** | `$UsnJrnl:$J`, 프리패치, RecentFileCache, SQLite 페이지·셀 엔진 | 구조가 단순하거나(USN), 오프셋을 주는 라이브러리가 없다 |
| **하이브리드** | `$MFT` | 파일 순회·오프셋 계산·fixup·타임스탬프 변환·경로 재구성·`scope` 필터는 우리 코드, **레코드 바이트를 속성으로 해석하는 일만** `third_party/analyzeMFT` |
| **라이브러리 어댑터** | EVTX(python-evtx), 레지스트리·Amcache(python-registry), SRUM(dissect.esedb) | 바이너리 XML + 청크 압축, ESE B-tree — 난이도가 차원이 다르다. 셋 다 오프셋을 얻을 수 있어 고른 것이다 |

**어느 아티팩트가 지금 어느 칸에 있는지는 `docs/limitations.md` 2장이 권위다.**
여기 표는 늘어나므로 목록이 아니라 **분류 기준**으로 읽는다.

직접 구현의 이유는 성능이 아니라 **설계 요구**다. 대부분의 기존 파서는 파싱된
값만 반환하고 원본 오프셋을 주지 않아, 본 프로젝트의 근거 추적 구조가 성립하지
않는다. 또한 "웹루트 하위 .aspx만" 같은 범위 한정 읽기는 직접 구현해야 가능하다.

발표에서는 **"오프셋을 주지 않는 라이브러리는 쓰지 않았고, 쓴 것은 어댑터에서
오프셋을 붙였다"**로 설명한다. 혼용은 판단력의 표현이지 타협이 아니다.
`$MFT`가 순수 자체 구현이 아니라 하이브리드인 것은 숨기지 않는다 — 어디까지가
우리 코드인지는 `third_party/README.md`가 가른다.

**초안은 이 기준을 *"파일시스템 계층은 직접 구현해 오프셋을 보존하고, 로그
계층은 검증된 라이브러리를 사용했다"*로 적었다.** 분류로는 대체로 맞고 지금도
여러 문서가 그 문장을 인용한다. 다만 `$MFT`가 하이브리드가 되면서 어긋났다 —
**가르는 것은 계층이 아니라 오프셋이다.** 계층으로 읽으면 `$MFT`가 어느 칸인지
설명되지 않고, SQLite가 왜 로그도 파일시스템도 아닌데 자체 구현인지도
설명되지 않는다.

### 3.2 직접 구현 파서의 검증 의무

직접 구현의 리스크는 **조용히 틀리는 것**이다. 다음을 필수로 한다.

- 외부 도구와 대조하는 스크립트를 `tools/compare_*.py`로 둔다
- 레코드 수, 경로, 타임스탬프 4종이 전부 일치해야 통과
- 대조 결과는 **수치까지** `docs/artifact-notes.md`에 기록한다

대조는 `tests/`가 아니라 `tools/`에 있다. 증거 이미지가 있어야 돌아가는데
이미지는 gitignore 대상이라 기계마다 없고, `pytest`에 넣으면 대조가 없는
기계에서 조용히 skip 되어 "통과"로 보이기 때문이다.

**대조 상대가 우리 코드면 대조가 아니다.** 같은 오해를 공유하는 두 경로는
서로를 지지할 뿐이다(2026-09-02, 프리패치 대조가 그랬다). 계보가 다른 구현을
고른다 — `$MFT`는 `dissect.ntfs`, 프리패치는 PECmd, Amcache는 AmcacheParser,
SQLite는 `sqlite3`.

`$MFT`는 `third_party/analyzeMFT` 계열이므로 **analyzeMFT는 대조군이 될 수
없다.** 3.1의 하이브리드 결정이 대조 상대를 하나 줄였고, 그래서 계보가 전혀
다른 `dissect.ntfs`를 쓴다.

기존 도구를 **쓰지 않는 것**과 **대조군으로 삼는 것**은 별개다. "우리 파서가
MFTECmd와 1,842건 전부 일치했다"는 발표에서 신뢰도를 크게 올린다.

### 3.3 파싱 범위 한정

완벽주의 방지를 위해 다음을 기준으로 한다.

- **$MFT**: 레코드 헤더, `$STANDARD_INFORMATION`, `$FILE_NAME`, `$DATA` 런리스트
- 상주/비상주 속성 중 상주만 우선, 비상주는 여유 시
- 압축·희소 파일, ADS는 범위 외
- **판정 기준**: "우리가 만든 테스트 이미지에서 동작하면 통과"

처리하지 못한 예외는 `docs/limitations.md`에 명시한다. 이것이 감점 요인이 아니라 성숙도의 지표다.

### 3.4 실패 처리 방침

**폴백은 초기에 구현하지 않는다.** 선형 파이프라인이 안정되기 전에 폴백을 넣으면 "폴백이 잘못 걸린 것인지 원래 로직이 틀린 것인지" 구분이 불가능하다.

대신 **실패 지점을 기록하는 자리는 처음부터 만든다.**

- 모든 단계의 실패를 `errors.jsonl`에 append
- 실패 시 동작은 **명확히 중단하고 사유 출력** (조용히 넘어가지 않음)
- 누적된 실패 유형 데이터를 보고 3주차에 폴백 필요 여부 판단

이 로그 자체가 발표 자료가 된다. "스키마 검증 실패율 8%, 그중 6%는 존재하지 않는 ATT&CK ID 생성" 같은 수치는 sLLM 한계의 정량적 근거다.

---

## 4. 파일 입출력 계약

상세 명세는 `docs/pipeline-io-spec.md` 참조. 여기서는 요약과 필수 준수 사항만 기술한다.

### 4.1 파일 목록

| 파일 | 생성 주체 | 형식 |
|---|---|---|
| `01_input.json` | 사람/수집 스크립트 | JSON |
| `02_scenario.json` | sLLM | JSON |
| `03_selection.json` | 스크립트 | JSON |
| `04_parsed/*.jsonl` | 스크립트 | JSONL |
| `05_findings.json` | sLLM | JSON |
| `06_verified.json` | 스크립트 | JSON |
| `07_report.md` | 스크립트 (Jinja2 템플릿) | Markdown |
| `errors.jsonl` | 전 단계 | JSONL |

대량 레코드가 JSONL인 이유는 스트리밍 처리와 부분 읽기를 위해서다.

### 4.2 필수 준수 사항

**공통 헤더** — 모든 JSON 문서는 `case_id`, `stage`, `schema_version`, `generated_at`, `generator`를 포함한다. `generator`에는 모델명과 양자화 수준까지 기록해 결과 파일만으로 실험 조건을 복원할 수 있게 한다.

**참조자 형식** — `<접두어>#<레코드번호>`. **접두어 목록의 유일한 출처는 `src/common/refs.py`의 `ARTIFACT_PREFIX`다** — 아티팩트가 늘 때마다 늘어나므로 여기 옮겨 적지 않는다. 레코드 번호는 아티팩트 내부 고유 번호를 그대로 쓴다(`$MFT`는 레코드 번호, EVTX는 RecordId, 레지스트리·SQLite처럼 일련번호가 없는 것은 오프셋). 자체 일련번호를 매기면 원본 대조가 불가능해진다. 문자열 조립은 반드시 `refs.py`를 경유한다.

**claims 필드** — 05단계 findings의 각 문장은 `statement`(자연어)와 별도로 `claims`(`ref`/`field`/`value` 삼중항 배열)를 포함해야 한다. 자연어는 기계 검증이 불가능하므로, 문장이 주장하는 사실만 구조화해 검증 대상으로 삼는다.

**input_refs** — 05단계는 LLM에 실제로 전달한 레코드 목록을 기록한다. 06단계에서 "전달하지 않은 레코드를 참조했는가"를 검사하기 위함이며, 이것이 가장 흔한 환각 유형이다.

### 4.3 검증 판정 규칙

| 조건 | 판정 |
|---|---|
| `claims` 전부 일치 | `passed` |
| `claims` 중 하나라도 불일치 | `rejected` (부분 통과 없음) |
| `claims`가 빈 배열 | `unverifiable` |
| `refs`가 `input_refs` 밖 레코드 포함 | `rejected` |

부분 통과를 두지 않는 이유: 하나라도 틀린 문장은 신뢰할 수 없다.

`unverifiable`을 별도로 둔 이유: "전반적으로 침해 정황이 뚜렷하다" 같은 종합 판단은 참조를 달 수 없다. 기각하면 요약이 사라지고 통과시키면 구멍이 생기므로, 제3의 판정으로 두고 보고서에서 시각적으로 구분한다.

### 4.4 비교 규칙

타임스탬프는 `tolerance.timestamp_seconds` 범위 내 오차를 허용한다. 완전 일치를 요구하면 `03:14:22` vs `03:14:22.1234567Z` 같은 표기 차이에서 대량 오탐이 발생한다.

경로는 대소문자 무시 + 구분자 정규화 후 비교한다.

**검증기가 지나치게 엄격해지는 것을 막기 위해**, 사람이 직접 옳다고 판단한 문장 묶음(`benchmark/validator/cases.json`)을 넣어 통과율을 확인하는 절차(`benchmark/validator_check.py`)를 필수로 수행한다. 새 비교 규칙을 넣을 때마다 사례를 추가하므로 건수는 그 파일이 말한다.

---

## 5. 디렉터리 구조

상세는 `docs/project-structure.md` 참조. 여기서는 최상위 구조와 역할만 기술한다.

```
dfir-triage/
├── src/          파이프라인 코드 (단계별 디렉터리)
├── schemas/      단계 간 데이터 계약 (JSON Schema 6개)
├── mappings/     기법→아티팩트 매핑 지식 (YAML)
├── benchmark/    평가 코드 및 정답 데이터셋
├── cases/        실행 산출물 (Git 제외)
├── tests/        단위 테스트
├── docs/         문서
├── tools/        개발용 유틸리티 (외부 도구 대조 스크립트 포함)
└── third_party/  남의 코드 (`src/`와 섞지 않는다)
```

**핵심 구조**: `src/`가 도구를 만들고, `benchmark/`가 그 도구를 채점하고, `cases/`에 결과가 쌓인다.

### 5.1 주의할 분리

- **`parsers/` vs `structs/`** — `structs/`는 온디스크 구조 정의(오프셋·크기·타입), `parsers/`는 그것으로 파일을 순회하는 로직. 구조 정의를 분리해야 NTFS 스펙 문서와 1:1 대조가 가능하다.
- **`checkers/` 개별 파일** — 검증 항목을 켜고 끌 수 있어야 강도별 실험이 가능하다.
- **`mappings/`는 코드가 아닌 데이터** — 여러 명이 동시에 채울 수 있고, 개수를 셀 수 있고, 파이썬을 몰라도 작성 가능하다.
- **`benchmark/`가 `src/` 밖에 있는 이유** — 평가는 파이프라인의 일부가 아니라 파이프라인을 대상으로 하는 별개 작업이다.

### 5.2 CLI 규약

모든 단계 스크립트는 `--in`, `--out`을 받는 독립 CLI로 구현한다. 시작 시 입력을, 종료 시 출력을 스키마로 검증하고, 실패는 `errors.jsonl` 기록 후 비정상 종료한다.

파싱 단계는 `--skip-existing` 옵션을 제공한다. 파싱이 가장 오래 걸리므로 실험 반복 시 필수다.

---

## 6. 팀 분담

| 영역 | 담당 디렉터리 | 선행 조건 |
|---|---|---|
| 파싱 계층 | `src/stage04_parse/`, `src/common/refs.py`, `tests/` | `03_selection.json` 목업 |
| LLM 파이프라인 | `src/stage02_normalize/`, `src/stage05_interpret/`, `src/stage07_report/` | 스키마 확정 |
| 선별·매핑 | `src/stage03_select/`, `mappings/` | 스키마 확정 |
| 검증·평가 | `src/stage06_verify/`, `benchmark/` | 04·05 출력 목업 |

`src/common/`과 `schemas/`는 공용이다. **초기에 한 명이 골격을 잡고 확정한 뒤 동결**하며, 이후 변경은 전체 공지를 거친다.

---

## 7. 착수 순서

다음 순서를 반드시 지킨다.

1. **`schemas/` 6개 파일 작성 및 확정**
2. **`src/common/` 골격 구현** (io, schema, errors, refs)
3. **목업 입출력 파일 한 세트를 손으로 작성** → `benchmark/datasets/C-001-webshell/`
4. 각 담당자가 목업을 입력 삼아 병렬 착수

**3번을 건너뛰면 병렬화가 불가능하다.** 목업 작성에 반나절이면 충분하지만, 없으면 파싱 담당자가 완성될 때까지 나머지 전원이 대기한다.

---

## 8. 평가 계획

### 8.1 지표

| 지표 | 산출 방법 | 데이터 출처 |
|---|---|---|
| 기법 식별 재현율 | `expected_techniques` 중 02단계가 짚은 비율 (정밀도도 함께) | `02_scenario.json` vs `ground_truth.json` |
| 선별 재현율 | `required_artifacts` 중 `selected`에 든 비율 | `03_selection.json` vs `ground_truth.json` |
| 증거 도달률 | `required_refs`가 parsed → delivered → cited → verified 네 칸 중 어디서 사라졌는가 | 04·05·06 산출물 |
| 환각률 | `rejected / (passed + rejected)` | `06_verified.json` |
| 검증 불가율 | `unverifiable / total_findings` | `06_verified.json` |
| 정규화 실패율 | 스키마 위반 발생 케이스 비율 | `errors.jsonl` |
| 소요 시간 | 단계별 실행 시간 | `cases/<id>/live_check.json` |

앞의 여섯은 `benchmark/evaluate.py`가, 소요 시간은 `benchmark/collect.py`가 낸다.

**재현율을 셋으로 가른 이유** — "재현율 60%"만으로는 무엇을 고쳐야 할지 알 수
없다. 기법을 못 짚은 것과, 짚었는데 아티팩트를 안 고른 것과, 골랐는데 그
레코드가 모델에 안 간 것과, 갔는데 문장이 검증에서 기각된 것은 **고칠 자리가
전부 다르다.** 증거 도달률의 네 칸이 그 자리를 가리킨다.

**1.3의 세 번째 수치(효율)는 아직 산출 경로가 없다.** 전수 파싱 기준선을 한
번도 재지 않아 "대비 절감"의 분모가 비어 있다. 단계별 소요 시간은 실행마다
쌓이고 있으므로, 같은 이미지에 기준선을 한 번 재면 채워진다.

### 8.2 실험 축

여유에 따라 다음 순으로 수행한다.

1. **프롬프트 변경에 따른 환각률 변화** (필수) — "참조 형식을 명시했더니 rejected가 18%→6%" 같은 결과
2. **모델별 비교** — Qwen2.5, Llama 3.1, EXAONE 등 동일 벤치마크
3. **양자화 수준별 성능 저하** — Q4 vs Q8
4. **검증 강도별 통과율** — `--checkers` 조합 변경

### 8.3 데이터셋

`ground_truth.json`에는 "이 케이스에서 반드시 찾아야 할 증거 `ref` 목록"을 사람이
미리 기록한다. 형식은 `benchmark/ground_truth_schema.json`이 권위다.

**`provenance`가 그 정답을 무엇으로 만들었는지 가른다.** `derived_from_spec`은
명세 예시에서 뽑아낸 것이라 **자기채점**이고 발표 수치가 될 수 없다. 사람이
실물 증거를 보고 만든 것만 수치가 된다.

**현재 상태(2026-09-02)** — 데이터셋은 `C-001-webshell` 하나뿐이고 그 `provenance`가
`derived_from_spec`이다. 지금 나오는 재현율 100%는 **배선이 맞다는 뜻이지 성능이
아니다**(`benchmark/README.md`가 ⚠️로 표시한다). 공개 이미지(CFReDS·CyberDefenders·
Ali Hadi·DFRWS)는 후보로 남아 있을 뿐 아직 쓰지 않았고, 손에 있는 증거는 자체 랩
이미지뿐이다. 사람이 만든 정답을 세우는 일은 `work.md` 8번이다.

**목표 시나리오는 키오스크다.** `C-001-webshell`은 지금 있는 유일한 데이터셋일 뿐
목표가 아니다 — 데이터셋·매핑·기법 목록을 넓히는 우선순위는 키오스크가 정한다
(`work.md` 0번).

---

## 9. 리스크와 대응

| 리스크 | 징후 | 대응 |
|---|---|---|
| 파싱 완벽주의 | 예외 케이스 처리에 계속 시간 투입 | 3.3의 범위 기준 준수, 나머지는 `limitations.md` |
| EVTX 늪 | 바이너리 XML 직접 구현 시도 | python-evtx 사용 확정 |
| claims 품질 저하 | 7B 모델이 문장과 구조화 출력 중 한쪽을 대충 처리 | 문장 생성과 claims 추출을 2회 호출로 분리 — **프롬프트만 써 두고 배선 안 됨**(`docs/llm-handover.md`). 지금은 1회 호출이다 |
| 스키마 변경 연쇄 | 개발 중 필드가 계속 바뀜 | `schema_version` 불일치 시 즉시 에러, 변경은 전체 공지 |
| 검증기 과엄격 | 정상 문장이 대량 기각 | `validator_check.py` 정기 수행 |
| 발표 자료 부족 | 데모만 있고 수치가 없음 | `errors.jsonl`과 `06_verified.json`을 1주차부터 축적 |

---

## 10. 발표 시 포지셔닝

**"분석가 대체가 아닌 단서 제시"**로 일관되게 프레이밍한다.

상용 벤더(Magnet Forensics 등)도 AI 기능을 "수사상 참고 자료이며 포렌식 감정 결과나 전문가 의견이 아니다"로 명확히 선을 긋고 있다. 동일한 포지셔닝을 취하면 증거능력 관련 질문을 사전에 방어할 수 있다.

또한 상용 도구들이 오프라인·온프레미스 LLM으로 이동하고 있다는 사실은 **본 프로젝트의 sLLM 선택이 타당함을 뒷받침하는 외부 근거**로 인용한다.

### 예상 질문과 답변 방향

**"이미 AIFT 같은 도구가 있는데 왜 만들었나"**
→ 전수 파싱 후 해석하는 기존 구조와 달리, 선별을 먼저 하는 구조를 검증하기 위함. 그리고 그 구조의 리스크(재현율)를 정량 측정한 것이 본 프로젝트의 기여.

**"소형 모델로 되겠나"**
→ 되는 부분과 안 되는 부분의 경계를 실측했다. (측정 데이터 제시)

**"LLM이 틀린 말을 하면"**
→ 근거 검증 계층에서 기계적으로 걸러진다. 기각률 자체가 지표로 관리된다. 다만 해석의 타당성은 검증 범위 밖이며, 이는 명시적 한계다.

---

## 부록 A. 관련 자료

**학술**
- DFIR-Metric (arXiv 2505.19973) — DFIR 도메인 LLM 벤치마크. 평가 설계 참고
- ForensicLLM (DFRWS/FSI Digital Investigation 2025) — LLaMA 3.1-8B + RAG/RAFT, 근거 기반 응답 생성
- Leveraging LLMs for Memory Forensics (ACM DTRAP 3748263) — Volatility3 출력 정규화 후 LLM 전달 파이프라인
- Scanlon et al., ChatGPT 포렌식 평가 — 범용 LLM의 한계 논거

**오픈소스 참고 구현**
- FlipForensics/AIFT — Dissect 파싱 + AI 분석, 리포트 포맷 참고
- DFIR-Chain — Volatility + YARA + LLM 트리아지 아키텍처
- Volatility-MCP-Server, MemoryInvestigator, AutoMem — 소규모 구현, 코드 통독 가능

**교육**
- SANS FOR563 (Applied AI for DFIR: Leveraging Local LLMs) — 커리큘럼 구성이 기능 로드맵으로 참고 가능

## 부록 B. 관련 문서

- `docs/pipeline-io-spec.md` — 단계별 입출력 상세 명세 및 예시
- `docs/project-structure.md` — 디렉터리 구조 및 파일별 역할
- `docs/mapping-guide.md` — 매핑 테이블 작성 규칙
- `docs/llm-handover.md` — 02·05 LLM 연결 인계 문서
- `docs/agent-harness.md` — `.claude/` 에이전트 하네스 안내 (팀원용)
- `docs/artifact-notes.md` — 온디스크 구조 조사 메모 (진행 중 누적)
- `docs/limitations.md` — **지금도 참인** 미지원 범위 및 알려진 한계
- `docs/limitations-log.md` — 해결된 한계의 기록 (`limitations.md`에서 옮겨 온 것)
- `work.md` — 다음에 무엇을 할 차례인가 (작업 큐)
- `.claude/skills/add-parser`, `.claude/skills/add-scenario` — 파서·시나리오를 늘리는 절차
