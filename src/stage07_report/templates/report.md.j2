# 침해사고 분석 보고서 — {{ case_id }}

## 개요

- 대상 호스트: {{ hosts | join(', ') if hosts else '미상' }}
{# 블록 태그({% if %})를 쓰지 않는 것은 trim_blocks 가 뒤의 줄바꿈을 먹기
   때문이다. 줄 끝에서 조건을 걸어야 하는 자리에서는 식으로 쓴다. #}
- 대상 OS: {{ target_os }}{{ " (증거에서 판정: " ~ windows ~ ")" if windows else "" }}
- 분석 기간: {{ period_start }} ~ {{ period_end }}
- 식별 기법: {{ techniques | join(', ') if techniques else '없음' }}
- 검증 결과: 통과 {{ stats.passed }} / 기각 {{ stats.rejected }} / 미검증 {{ stats.unverifiable }}
{% if stats.rejected %}

기각된 {{ stats.rejected }}개 문장은 근거 검증을 통과하지 못해 본 보고서에서 제외되었습니다.
{% endif %}

## 확인된 사항
{% if passed %}
{% for finding in passed %}

### {{ finding.id }} — {{ finding.title }} [{{ finding.severity_label }}]

{{ finding.statement }}
{% if finding.evidence %}

{% for line in finding.evidence %}
> 근거: {{ line }}
{% endfor %}
{% endif %}
{% endfor %}
{% else %}

근거 검증을 통과한 항목이 없습니다.
{% endif %}
{% if timeline %}

## 타임라인

| 시각 | 사건 | 근거 |
|---|---|---|
{% for entry in timeline %}
| {{ entry.ts }} | {{ entry.event }} | {{ entry.refs | join(', ') }} |
{% endfor %}
{% endif %}

## 미검증 항목
{% if unverifiable %}

다음 서술은 특정 증거로 뒷받침되지 않는 종합 판단이며 분석가 검토가 필요합니다.
{% for finding in unverifiable %}
- {{ finding.statement }}
{% endfor %}
{% else %}

없습니다.
{% endif %}

## 분석 범위

### 확인한 아티팩트
{% if examined %}

| 아티팩트 | 레코드 | 비고 |
|---|---|---|
{% for item in examined %}
| {{ item.artifact }} | {{ item.records }} | {{ item.note }} |
{% endfor %}

레코드 0건은 **해당 범위에서 흔적이 확인되지 않았다**는 뜻이며, 아티팩트를
읽지 못한 것과 다릅니다.
{% else %}

확인한 아티팩트를 특정할 수 없습니다. 04단계 산출물(`_manifest.json`)이
보고 단계에 전달되지 않았습니다.
{% endif %}

### 확인하지 못한 아티팩트
{% if limits %}

| 아티팩트 | 사유 |
|---|---|
{% for limit in limits %}
| {{ limit.artifact }} | {{ limit.reason }} |
{% endfor %}
{% else %}

선별된 아티팩트를 모두 확인했습니다. 다만 이는 **선별 범위 안에서**의
진술이며, 선별 자체가 놓친 것은 이 표에 나타나지 않습니다.
{% endif %}

### 입력 서술을 어떤 기법으로 읽었는가
{% if technique_evidence %}

| 기법 | 입력에서 근거로 삼은 구간 |
|---|---|
{% for item in technique_evidence %}
| {{ item.id }} ({{ item.name }}) | {{ item.evidence_text }} |
{% endfor %}

**기법과 근거가 어긋나 보이면 선별이 그만큼 어긋난 것입니다.** 기법이 볼
아티팩트를 정하기 때문입니다. 이 대조는 자동 판정하지 않습니다 — 어느 절이
어느 기법이어야 하는지를 아는 표를 두면 그 표가 곧 분석이 되어 버립니다.
{% else %}

02단계 산출물이 보고 단계에 전달되지 않아 근거 구간을 싣지 못했습니다.
{% endif %}

### 기법으로 옮기지 못한 서술
{% if unmapped_text %}

입력 서술 중 아래 구간은 **어떤 ATT&CK 기법으로도 옮기지 않았습니다.**
기법이 없으면 볼 아티팩트도 정해지지 않으므로, 그 구간이 가리키는 자리는
조사 대상에 들어가지 않았습니다 — 위의 "확인하지 못한 아티팩트" 표에도
나타나지 않습니다.

{% for span in unmapped_text %}
- {{ span }}
{% endfor %}

**"증거가 없었다"는 뜻이 아니라 "보지 않았다"는 뜻입니다.** 이 구간이
사건에 중요하다면 해당 기법을 지정해 다시 돌려야 합니다.
{% else %}

입력 서술이 모두 하나 이상의 기법으로 옮겨졌습니다.
{% endif %}

---

본 보고서는 자동 생성되었으며 수사상 참고 자료입니다. 포렌식 감정 결과나
전문가 의견이 아니며, 해석의 타당성은 분석가 검토가 필요합니다.

생성: {{ generated_at }} / {{ generator }}
