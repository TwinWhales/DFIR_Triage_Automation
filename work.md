# work.md — 다음에 할 일

2026-08-26 실측(K-001 랩 환경 대비 완성도 점검)에서 시작한 작업 큐다.
**설계 근거와 데이터 형식은 여기 옮겨 적지 않는다** — 각 항목에 붙은 문서가
권위다. 여기 있는 것은 "무엇을, 어디서, 어떻게 확인하며" 뿐이다.

끝낸 항목은 지우고, 그 사실은 `docs/limitations-log.md`에 남긴다.
지금도 참인 제약은 `docs/limitations.md` 쪽이다.

## 지금 순서 (2026-09-02)

**번호는 이름이고 순서가 아니다.** 다른 문서가 "`work.md` 0번"으로 가리키므로
번호를 다시 매기지 않는다. 집는 순서는 이렇다.

| 순 | 항목 | 착수 조건 |
|---|---|---|
| 1 | **9번 — 05단계 컨텍스트** (착수함, 2026-09-03) | 없다. 순서가 항목 안에 있다 |
| 2 | **1번 — 윈도우 버전 축** | 없다. 목록을 만드는 데까지는 새 이미지가 필요 없다 |
| 3 | **8번 — 벤치마크 정답 데이터** (팀원에게 넘긴다) | 없다. 막아 둘 것 둘은 2026-09-02 에 닫혔다 |
| 4 | 0번 — 키오스크 스냅샷으로 무엇이 닫혔는지 센다 | 스냅샷 A 가 어디 있는지부터 |
| 5 | 7번 — 02단계 경로 환각 가드 | 없다. 무엇이 환각인지 사람이 먼저 정해야 한다 |
| 이후 | 3번 Wazuh · 5번 GUI · 2번 저널링 · 4번 설계 판단 | |

6번(evtx 엔진)은 큐가 아니라 **닫힌 결정**이라 순서에서 뺐다.

**9번이 앞인 이유.** 2026-09-03 에 **지금 돌아가는 파이프라인의 프롬프트가
잘리고 있다**는 것이 실측으로 드러났다. 상수는 고쳤지만(아래 처리됨), 고치고
나니 창과 커버리지가 맞바꿈이라는 것이 같이 드러났다 — 창을 좁혀야 GPU 에
다 올라가는데, 좁히면 실을 수 있는 레코드가 54건에서 열 건 아래로 떨어진다.
05단계는 이 도구가 "sLLM 으로 해석한다"고 말하는 유일한 자리라, 그 자리가
증거의 얼마를 보고 있는지가 다른 어떤 항목보다 먼저다.

**1번이 그다음인 이유.** 2026-09-01 에 실물 스냅샷 하나에서 **프리패치 축이 통째로
죽어 있었다.** 표에 없는 조합이라 조용히 틀리지 않고 시끄럽게 실패했고 고쳤지만,
그 표 여덟 줄 중 **실측은 셋뿐이다.** 나머지 다섯은 "안 틀린 것"이 아니라
"확인 안 한 것"이고, 이번 실측이 그중 한 줄의 적용 범위를 반증했다 — 명세는
"1903 이상 = 216" 이라 말하는데 19045 는 212 였다.

**8번을 2순위에 두는 이유.** 밖(팀원·수집본)을 기다리는 구간이 있다. 넘기는
문서를 먼저 쓰고 1번을 이어서 잡으면 두 트랙이 겹쳐 돈다. **넘기기 전에 막아
둘 것 둘은 닫혔다** (8번 첫 절) — 지금은 절차를 그대로 건네면 된다.

**처리됨** — 전부 `docs/limitations-log.md` 에 있다. 고친 뒤에도 남은 제약은
`docs/limitations.md` 에 따로 있다.

- **2026-08-26** — `--volume` 통로, 02·05 실패 응답 원문 보존, Sysmon 세 룰의
  EID 제약, 프리패치 장치 경로 두 번째 형태, 05단계 토큰 예산·레코드 다이어트
- **2026-08-27** — 프리패치 적재 경로 변환과 프롬프트 keep 목록, Amcache 숫자
  값 이름(대응표를 공개 자료가 아니라 같은 이미지 안의 조인으로 얻었다)
- **2026-08-28** — SRUM 파서, 04단계 성능 18.7% 단축(산출물 646,879건 바이트
  단위 동일), `$MFT` × `dissect.ntfs` 최초 대조
- **2026-08-30** — `tools/hexdump_record.py`(10종 357건 되짚기, 어긋남 0건),
  `tools/inspect_jsonl.py`(매니페스트를 믿지 않고 파일을 세어 맞춘다)
- **2026-08-31** — `tools/live_check.py`(목업 없이 실물로 관통시키며 11개
  관문을 판정한다), 모델 출력 상한 `num_predict`(타임아웃 3회·906.7초 →
  37.7초), 제약 디코딩(`format`), HID/BadUSB 매핑을 카탈로그·flags·스키마에
  이음, `benchmark/` 레이아웃 재편(`fixtures/` 는 손으로 쓴 것,
  `golden/` 은 재생성 대상)
- **2026-09-01** — 프리패치 `(30, 212)` 실측(0건 → 192건), 파서 실패를
  `errors.jsonl` 에도 남김, 매핑이 모르는 키를 로드 시점에 거부, `$UsnJrnl`
  미지원 레코드 버전을 매니페스트·`errors.jsonl` 결산에 올림 (그 경고가
  레코드마다 찍혀 방금 만든 결산 한 줄을 도로 덮던 것까지)
- **2026-09-02** — SQLite 파서(2-3). 페이지·셀을 직접 걷는 범용 엔진과
  `sqlite3` 대조 도구, 아티팩트 둘(`sqlite:StateRepository`·
  `sqlite:Notifications`), 사용자 프로필 경로(`user_paths`)
- **2026-09-03** — 05단계 토큰 추정 상수(`CHARS_PER_TOKEN` 2.5 → 2.0).
  실측으로 프롬프트가 창을 넘어 앞이 잘리고 있었다. 실제 평가 토큰 수
  (`prompt_eval_count`)를 백엔드가 남기게 해 추정과 나란히 찍는다
- **2026-09-02** — 벤치마크 정답의 자기채점 판정을 `authored_by` 에서 떼어
  `provenance` 로 옮김, `required_refs` 접두어 어휘를 `src/common/refs.py`
  하나에서 봄 (스키마는 모양만 본다)

---

## 먼저 — 이 큐가 어디서 나왔나

알럿 하나를 실물 60GB 이미지에 관통시킨 2026-08-26 실측이다. **결과 전체는
`docs/limitations.md` 의 2026-08-26 절 셋에 있다 — 먼저 읽는다.** 그때 05단계가
3회 재시도 끝에 약 25분 만에 소견 0건으로 멈추던 원인은 2026-08-31 에 닫혔다.

### 지금 다시 돌리는 방법 — 단계를 손으로 잇지 않는다

`cases/` 는 gitignore 라 그때 케이스가 남아 있지 않다. `tools/live_check.py` 가
01→07 을 실물로 관통시키며 단계마다 판정한다. **스텁을 받는 인자가 없다** —
목업이 한 군데라도 섞이면 나머지 판정이 무의미해지므로, 마지막에 산출물의
`generator` 문자열과 매니페스트 시각으로 "정말 안 섞였나"를 다시 확인한다.

```bash
.venv/Scripts/python.exe tools/live_check.py \
  --case-id LC-01 --evidence evidence/win10_sysmon_testimage.001 --volume 1 \
  --model qwen2.5:latest --raw "<알럿 서술>" --force
```

`--volume 1` 이 필수다. 이 이미지는 NTFS 가 둘(0.4GiB 복구 + 59.4GiB 시스템)이라
안 주면 04단계가 **후보를 보여 주고 멈춘다.** 그것이 정상 동작이므로
`live_check` 는 그 자리를 FAIL 로 적고 뒤 단계를 건너뛴다.

판정·시간·측정치는 `cases/<id>/live_check.json` 과 `benchmark/results/` 에 남아
실행끼리 diff 된다. 누적을 표로 보는 것은 `benchmark/collect.py` 다. 단계 하나만
돌리는 명령과 대조 도구는 `CLAUDE.md` 에 있다.

## 0. 키오스크 스냅샷 — 왔는데 무엇이 닫혔는지 세지 않았다

### 키오스크 스냅샷 A 는 이미 있다

2026-08-31 에 키오스크 스냅샷 하나를 KAPE 로 수집했다
(`KAPE_Results/KIOSK_snapshotA_20260831T094536`, Windows 10 Home 빌드
**19045.6466**). 그것으로 닫힌 것은 지금까지 **프리패치 자리 하나**다 —
192건 전량이 `UnknownLayout` 이었고 `(30, 212)` 를 실측으로 채웠다
(`docs/artifact-notes.md` 2026-09-01).

**먼저 할 일은 그 수집본을 찾는 것이다.** 이 기계의 `evidence/` 에 없다
(`week10.E01`·`win10_sysmon_testimage.001` 둘뿐). 다른 기계에서 수집한
것이면 어디 있는지를 이 자리에 적고, 없어졌으면 다시 뜬다 — 아래 확인이
전부 그 수집본 하나로 끝나는 일이다.

### 다섯 채널 — 있었는지 없었는지 아무도 세지 않았다

`AssignedAccess` 3종·`DriverFrameworks`·`RDPConnection` 이 그 수집본에
있었는지가 어디에도 적혀 있지 않다. 파일 목록 한 번이면 시작된다.

```bash
ls "<수집본>/C/Windows/System32/winevt/Logs" | grep -iE "assignedaccess|driverframeworks|rdp|terminal"
```

**다만 수집본에서 안 보이는 것은 답이 아니다.** KAPE 는 자기 타깃 목록으로
거르므로 "그 버전에 없다 / KAPE 가 안 뽑았다 / 우리 경로가 틀렸다" 셋이
구분되지 않는다(1-2 절). 안 보이면 KAPE 타깃 목록을 함께 보거나, **그 VM 의
디스크를 직접 열어** 확인한다.

- 그 다섯의 **파일 경로 문자열**이 맞는가
  (`src/stage04_parse/evidence.py`의 `FILE_LAYOUT`)
- 그 채널들의 **event_id 추정값**이 맞는가 (`mappings/_flags.yaml`)
- `kiosk_restriction_event`가 "로그가 작다"는 전제대로인가 — 수만 건이면
  05단계 쿼터를 혼자 태운다
- `unexpected_parent_process`가 `explorer.exe`를 이상으로 보는 가정 —
  **Home 에디션이면 이것은 스냅샷 A 로도 못 잰다.** Assigned Access 를 실제로
  켠 상태여야 한다. 침해할 필요는 없고 정상 상태로 충분하다

### 2026-08-26 Sysmon 이미지로 닫힌 것

- ~~`evtx:Sysmon` 의 파일 경로 문자열~~ — 맞았다
- ~~`shell_spawned` 목록이 정상 운영 중 몇 건이나 뜨는가~~ — 5.5분에 5건.
  전부 Windows 서비싱(`sedsvc.exe`·`CompatTelRunner.exe`)이다
- ~~`execution_from_unusual_path`~~ — 3건, 전부 `C:\Windows\Temp\{GUID}\DismHost.exe`
- 덤으로 **세 룰이 EID 5 도 잡던 버그**가 드러나 고쳤다

### 같은 수집본으로 함께 닫히는 것 둘

**섀도 카피.** 프리패치 적재 경로 변환이 섀도 카피 참조를 그대로 두는지는
지금 합성 테스트로만 확인돼 있다(디스크의 두 이미지 모두 섀도 참조 0건).
섀도 카피가 있는 스냅샷이면 `tools/scan_prefetch.py` 대조 한 번으로 실물
확인이 된다 — `docs/limitations.md` "프리패치 적재 경로와 프롬프트 keep 목록".

**keep 어휘.** `prompt_keep_paths` 는 정상 운영 이미지의 분포로만 골랐다
(적중 9.81%, `max_items: 12`). 악성 DLL 이 드롭 자리에 실제로 있는 이미지에서
다시 재야 한다.

덤으로 **Stage 0 베이스라인**(4번 절)의 시작이기도 하다.

---

## 1. 윈도우 버전 축 — 표 여덟 줄 중 실측은 셋이다

2026-09-01 에 키오스크 스냅샷의 프리패치 **192건 전량**이 `UnknownLayout` 이었다.
19045.6466 의 파일 정보 블록이 212바이트인데 표에 버전 30 의 216·220·224 만
있었다. **명세를 옮긴 줄이 실물에 반증됐다** — [LIBSCCA] 는 "1903 이상 = 216"
이라 말한다. 전말은 `docs/artifact-notes.md` 2026-09-01 절에 있다.

그래서 이 절의 대상은 프리패치 한 줄이 아니라 **버전에 따라 움직이는 자리
전부**다. 나머지 명세 줄들은 *안 틀린 것*이 아니라 *확인 안 한 것*이다.

| (버전, 블록 크기) | 출처 | 대응 |
|---|---|---|
| (17, 68) | [LIBSCCA] | XP/2003 — **대상 아님** |
| (23, 156) | **실측** `evidence/[root]` 73건 | Win7 |
| (26, 224) | [LIBSCCA] | Win8.1 |
| (30, 224) | [LIBSCCA] | Win10 1809 이하 |
| (30, 220) | **실측** `0824test.001` 137건 | Win10 1703 |
| (30, 216) | [LIBSCCA] | "1903 이상" — **19045 가 212 였으므로 이 줄이 가장 의심스럽다** |
| (30, 212) | **실측** 키오스크 스냅샷 192건 | Win10 19045.6466 |
| (31, 224) | [LIBSCCA] | Win11 |

### 1-1. 버전에 따라 움직이는 자리 목록 — 새 이미지 없이 된다

**이미지를 모으는 것보다 이것이 먼저다.** 목록이 없으면 어느 수집본이 무엇을
닫는지 모르는 채로 용량만 쓴다. 가르는 기준은 하나다 — **어긋났을 때 시끄러운가,
조용한가.** 이 절의 첫 산출물은 이 표를 확인해 채우는 것이다.

| 자리 | 지금 | 어긋나면 |
|---|---|---|
| 프리패치 `FILE_INFORMATION` | 실측 3 / 명세 5 | **시끄럽다** — `UnknownLayout`, `errors.jsonl` 에도 남는다 |
| `$UsnJrnl` 레코드 버전 (`usn_record.py`) | v2 만 지원. v3·v4 는 이름은 알지만 거부 | **시끄럽다** — 단 2026-09-01 부터다. 그전엔 stderr 경고뿐이라 조용한 쪽이었다. 다만 v3 를 실물로 본 적이 없다 |
| `osinfo` 빌드 → `family` | win7·win10 실측. win8·win81·win11 **미확인** | 조용하다 — 판정 실패는 "안 거른다"로 막혀 있으나, **잘못 판정하는** 경우는 안 막힌다 |
| `osinfo.AVAILABILITY` (Amcache·RecentFileCache·SRUM) | 경계 빌드가 9200·7601 | 조용하다 — 잘못 거르면 **있는 증거를 안 읽는다** |
| `evidence.FILE_LAYOUT` 파일 경로 | 채널 다섯 미확인 (0번) | 조용하다 — `artifact_not_found` 로 보여 "수집 누락"과 구분이 안 된다 |
| 매핑의 `event_ids` | **버전 조건이 없다** | **조용하다** — 구버전 0건이 "그 버전엔 없는 이벤트"와 "탐지 실패"로 갈리지 않는다 |
| Amcache 값 이름 대응표 | Win10 이미지 하나의 조인으로 얻음 | 반쯤 정직하다 — 모르는 이름은 숫자로 남는다. 다만 **세대가 다르면 키 구조 자체가 다르다** |
| 레지스트리 바이너리 블롭 (Shimcache·UserAssist·BAM) | 미착수. 매핑에도 없다 | 해당 없음 — 애초에 안 읽는다 |

**함께 고칠 것 하나** — `docs/limitations.md` 의 버전 절이 아직 "섹터 크기가
512로 박혀 있습니다"라고 적고 있다. 2026-08-28 에 고쳤다(`sector_size_of`).

### 1-2. 무엇을 모을 것인가 — 기본은 이미지다

**수집본으로는 못 재는 것이 이 축에 있다.** KAPE 수집본은 KAPE 의 경로 목록으로
걸러진 결과라, *"우리 `FILE_LAYOUT` 경로가 그 버전에서 맞는가"* 를 물으면 셋이
구분되지 않는다.

```
그 버전에 그 파일이 없다 / 있는데 KAPE 타깃이 안 뽑았다 / 뽑혔는데 우리 경로가 틀렸다
```

이미지면 아무 데나 `ls` 할 수 있어 셋이 갈린다. **0번의 다섯 채널이 미검증으로
남아 있는 것이 정확히 이 부류다.** `$UsnJrnl` 의 ADS 이름·`$LogFile`·미할당·VSS
도 수집본에서 뭉개진다 — `VOLUME_PATH_OVERRIDES` 가 존재하는 이유가 그것이다.

**수집본으로 충분한 것은 하나 — 파일 하나하나의 내부 구조.** `(30, 212)` 를
192건 전량 대조로 잡은 것이 그 증거다. **수집본이 유일한 답인 때 둘** — ① 운영
중이라 끌 수 없는 실물 키오스크(3번 절) ② 이미 수집본으로만 받은 증거.

용량은 버전 다섯을 동시에 들지 않는 것으로 푼다 — 하나 붙여 재고 지운다.

**E01 은 열린다(2026-09-01 실측). vmdk 는 아직이다.** `evidence/week10.E01` 이
`EwfContainer` 로 열려 파티션 테이블까지 읽혔다. 다만 그 이미지는 EFI + ext4 라
버전 축의 증거로는 못 쓴다 — 남은 것은 **Windows NTFS 가 담긴 E01 로 04단계를
관통시키는 것**이다. vmdk 는 스냅샷 델타·분할이 걸릴 수 있다. 확인은 한 줄이다.

```bash
.venv/Scripts/python.exe -c "from dissect.target import Target; t=Target.open(r'<경로>'); print(len(list(t.filesystems)))"
```

키오스크 기준 우선순위:

| 순 | 무엇 | 왜 |
|---|---|---|
| 1 | Win10 **LTSC** (17763 / 19044) | 키오스크는 LTSC 로 깔리는 일이 많다. `(30, 224)`·`(30, 220)` 구간을 실물로 가른다 |
| 2 | Win11 (22000+ / 26100) | `(31, 224)` 가 명세뿐. `osinfo` 의 win11 판정도 미확인이다 |
| 3 | Win10 1903~1909 (18362 / 18363) | 명세가 216 이라 말하는 구간. **여기가 가장 의심스럽다** |
| 4 | Win8.1 (9600) | `(26, 224)` 명세뿐. `osinfo` win81 과 Amcache 세대가 함께 걸린다 |
| 5 | Win7 (7601) | 이미 실측. 회귀용으로만 |

**증거마다 적을 것** — 어디서 났는가 · `CurrentBuildNumber` 와 `UBR` · 에디션 ·
이미지인가 수집본인가(수집본이면 도구와 대상, 그리고 **"무엇이 안 뽑혔는지를
모른다"는 사실**). 빌드를 안 적은 증거는 이 축에서 쓸 수 없다.

### 1-3. 문서는 어디에 적나 — 새 문서를 만들지 않는다

| 무엇 | 어디 |
|---|---|
| 값 (오프셋·크기·경계 빌드) | 코드의 표 — `FILE_INFORMATION`·`AVAILABILITY`·`FILE_LAYOUT`. 각 줄에 `source`(실측인가 명세인가)를 단다 |
| **무엇을 어떤 방법으로 확인했나** | `docs/artifact-notes.md` 버전별 절. 대조 명령과 건수까지 |
| 지금 못 보는 것 | `docs/limitations.md` |

**명세를 옮겨 적지 않는다.** `(30, 212)` 를 정한 방법이 기준이다 — 실행 횟수
자리를 "1~7회 실행된 파일에서 유효 시각 개수 == 그 값" 으로 정해 112/112 를
맞췄다. 새 버전도 같은 방법으로 채운다.

### 1-4. 외부 도구가 이 기계에 이미 있다

`C:\Users\user\Desktop\Tools` 에 `PECmd`·`MFTECmd`·`EvtxECmd`·`RECmd`·
`SrumECmd`·`TimelineExplorer` 가 있다. `(30, 212)` 는 `tools/scan_prefetch.py`
로 잡았는데 **그것은 우리 구현과 공통 오해를 공유한다.** PECmd 로 한 번 더
맞추는 것이 이 절에서 가장 싼 작업이다.

대조 도구는 `tools/compare_prefetch.py` 로 만들어 뒀다 — **아직 실물에 돌린
적이 없다.** 짝은 .pf 파일 이름으로 짓고, 실행 횟수·실행 시각(개수와 순서)·
경로 해시·볼륨 0 을 채점한다. 버전은 채점하지 않고 교차표로만 인쇄한다
(PECmd 의 표기가 바뀐 것을 우리 오류로 세지 않으려고). 적재 파일은 개수만
본다 — 경로 문자열은 우리가 드라이브 문자로 바꿔 싣는 쪽이라 내용 대조는
`scan_prefetch.py` 몫이다.

```bash
PECmd.exe -d "<수집본>\C\Windows\Prefetch" --csv C:\temp\pecmd_out
.venv/Scripts/python.exe tools/compare_prefetch.py \
  --ours cases/<케이스>/04_parsed/prefetch.jsonl \
  --pecmd C:\temp\pecmd_out --full
```

`--full` 은 **범위를 비우고 뽑은 04 출력**에만 붙인다. 선별된 출력에 붙이면
"PECmd 에만 있음"이 잔뜩 나오는데 그것은 파서의 오류가 아니다.

### 확인 방법

**도구를 새로 만들기 전에, 수집본을 04단계에 그대로 넣는다.** 표에 없으면
실패하는 성질이 곧 탐지기다.

```bash
.venv/Scripts/python.exe -m src.stage04_parse.parse \
  --in <03 선별 결과> --out cases/<케이스>/04_parsed/ --evidence <수집본>/C
.venv/Scripts/python.exe tools/inspect_jsonl.py --parsed cases/<케이스>/04_parsed
```

보는 것은 셋이다 — `_manifest.json` 의 `parse_errors`, `errors.jsonl` 의
`detail.total_failure`, 매니페스트의 `windows` 판정. 이것으로 "무엇이 표에
없었는지"까지 안 보이면 그때 스윕 도구를 만든다.

**고친 뒤에는 되돌려 확인한다.** 새 줄을 넣었으면 값을 일부러 틀려 시험이
실제로 잡는지 본다 — `(30, 212)` 때 `run_count_offset` 을 `0x7C` 로 바꿔
두 시험이 잡는 것을 확인했다.

## 2. 파서 셋 — 남은 것은 저널링이다

2026-08-27 에 잡은 트랙이다. 절차는 `.claude/skills/add-parser/SKILL.md`
하나가 권위다 — **등록 지점이 다섯이고 그중 셋은 빠뜨려도 조용하다.**

방침은 `work-guide.md` 3.1 이 이미 정해 놨다 — *"파일시스템 계층은 직접
구현해 오프셋을 보존하고, 로그 계층은 검증된 라이브러리를 사용했다."*
셋이 그 선을 각각 어느 쪽에 두는지가 난이도를 갈랐다.

**둘은 닫혔다.** SRUM(옛 2-1)은 로그 계층이라 라이브러리로 갔고,
SQLite(2-3)는 `offset` 규약 때문에 직접 구현으로 갔다 — 표준 라이브러리가
행이 파일 어디에 있었는지를 말해 주지 않는다. 대신 그것을 **대조 상대**로
썼다.

### 2-2. 저널링 — `$UsnJrnl` 보강 + `$LogFile` 신규

**`$UsnJrnl:$J` 는 이미 있다**(직접 구현, `_artifacts.yaml` 등재됨).
보강 범위를 먼저 재고 시작한다.

`$LogFile` 은 카탈로그에 **`supported: false` 로 자리만 있다**
(`_artifacts.yaml`). 이미지에 64MB 로 존재한다. 파일시스템 계층이라
방침대로면 **직접 구현**이고, LSN 레코드와 redo/undo 연산을 해석해야 한다.
설치된 `dissect.ntfs` 도 `$LogFile` 은 안 건드린다.

**셋 중 값 대비 비용이 가장 나쁘다.** 얻는 것이 이미 있는 `$UsnJrnl` 과
상당히 겹친다. 순서를 마지막에 두는 이유다.

### 2-3. SQLite — **끝났다 (2026-09-02)**

범용 엔진 + 프로파일 두 층으로 만들었다. 상세는 `docs/artifact-notes.md`
2026-09-02 두 절, 못 하는 것은 `docs/limitations.md` 같은 날짜다.

보류 사유 둘 중 **대조 상대 없음은 해소됐다** — 이미지를 헤더 매직으로
훑어 SQLite 64개를 찾았고(`0824test.001` 22개 · Win7 42개), 그 전부가
`sqlite3` 와 전수 일치했다(테이블 2,368개, 행 209,393건).

**팀 분담선은 다시 그었다** — 레코드를 오프셋과 함께 뽑아 05단계에 태우는
데까지가 이쪽이고, 거래 데이터 자체의 대사·비교는 담당 6이다.

**POS 거래 DB 는 여전히 대상이 없다.** 다만 이제 파서가 있으므로, 랩이
서서 `pos_db_root` 가 정해지면 `mappings/_artifacts.yaml` 한 줄과
`parsers/sqlite.py` 의 `PROFILES` 몇 줄로 붙는다. 그때 볼 것 둘:

- 그 DB 가 `WITHOUT ROWID` 테이블을 쓰는가. 쓰면 지금 엔진은 못 읽는다
  (실측에서 OneDrive DB 는 21개 중 14개가 그랬다)
- 시각 컬럼의 인코딩. **이름이 아니라 값을 보고 정한다** — 실물
  `StateRepository` 의 `Package._Created` 는 컬럼이 있는데 전부 0 이었다

**남은 것은 삭제 레코드 복구다.** freeblock·unallocated 에 남은 행이
DFIR 에서 SQLite 를 볼 값의 절반인데 1차 범위에서 뺐다. 정상 경로가
`sqlite3` 와 100% 일치하는 것을 먼저 증명해야 복구본이 맞는지도 판정할 수
있어서였고, **그 증명이 끝났으므로 지금 집을 수 있는 상태다.**

---

## 3. Wazuh 알럿을 그대로는 못 받는다

레포 전체에 `wazuh`·`sigma`·`winlogbeat`·`active-response` 참조가 0건이다.
`edr_alert` 경로는 있으나 **자체 형식**을 기대한다. 실측:

```
Wazuh 원본(rule.mitre.id / rule.level / agent.name)  →  AlertAdapterError
평탄화한 자체 형식(mitre / severity / host)          →  정상 변환
```

**할 일 셋.**

- `src/stage02_normalize/alert_adapter.py`에 Wazuh 모양을 평탄화하는 변환
  추가 (약 50줄). `rule.mitre.id`→`mitre`, `rule.level`→`severity`,
  `agent.name`→`host`, `data.win.eventdata.*`→`process.*`
- `tools/make_case.py`에 `--alert` 경로. 지금은 자연어 입력만 만든다
- Wazuh active-response에서 부를 래퍼

**라이브 호스트에서 바로 못 읽는다는 것도 함께 본다.** `open_source()`는
이미지 파일 또는 폴더만 받는다(`\\.\C:` 없음). 알럿이 나면 KAPE가 먼저
돌아 폴더를 만들어야 한다 — 계획서에 KAPE가 있으니 운영으로 메꿔지지만,
그 호출을 감싸는 자리가 지금 없다.

### 다음 세션이 바로 집을 수 있게 — 배선

**먼저 실물 샘플 하나를 `tests/data/` 에 붙이고 시작한다.** 필드 이름을
기억으로 적으면 조용히 틀린다 — Wazuh 는 `alerts.json` 한 줄과 API 응답의
모양이 다르고, `rule.mitre` 는 버전에 따라 `id`/`technique`/`tactic` 이
**전부 배열**이다. 샘플이 붙기 전에는 아래 표를 확정하지 않는다.

**손댈 자리는 하나다.** `convert()` 앞에 평탄화 함수를 세우고, `convert()`
자신은 지금 모양 그대로 둔다.

```
raw(Wazuh)  →  _flatten_wazuh(raw)  →  convert(raw, evidence)  →  시나리오
                     ^ 새로 만드는 것            ^ 안 건드린다
```

`convert()` 를 고쳐 두 모양을 다 받게 하면 **어느 형식이 어느 필드를
채웠는지가 함수 안에서 섞인다.** 지금 `convert()` 는 "기법이 없으면
`AlertAdapterError`" 처럼 실패를 정직하게 내는데, 입력 모양이 둘이 되면 그
메시지가 어느 쪽을 가리키는지 알 수 없게 된다.

**모양을 알아보는 기준**을 먼저 정한다 — `raw` 에 `rule` 이 있고 그것이
객체이면 Wazuh 로 본다. `source_type` 을 새로 만들지 않는다(`edr_alert`
그대로다). 스키마는 동결이고, 이것은 형식 판별이지 새 입력 종류가 아니다.

| Wazuh | 우리 | 주의 |
|---|---|---|
| `rule.mitre.id[]` | `mitre[]` | 배열이다. 하나만 오는 경우도 배열로 온다 |
| `rule.level` (0~15) | `severity` | **숫자→문자열 대응을 정해야 한다.** `SEVERITY_CONFIDENCE` 가 `critical`/`high`/... 를 기대한다 |
| `rule.description` | `rule_name` | |
| `agent.name` | `host` | `agent.ip` 는 `ips[]` 로 |
| `data.win.eventdata.image` | `process.path` | 소문자 키다. Windows 이벤트 경유일 때만 있다 |
| `timestamp` | `detected_at` | Wazuh 는 `+0900` 오프셋을 붙여 보낸다. `_detected_at()` 이 받는지 확인 |

**확인 방법** — 샘플을 넣어 02단계를 돌리고, 지금 자체 형식으로 만든
K-ALERT 시나리오와 `techniques`·`time_range`·`entities` 가 같은지 본다.
같은 사건을 두 입력 형식으로 넣으면 같은 시나리오가 나와야 한다. 다르면
평탄화가 무언가를 흘린 것이다.

**정할 것 하나** — `rule.level` 대응. 12 이상을 `critical` 로 볼지 13
이상으로 볼지에 따라 `overall_confidence` 가 0.9 와 0.95 사이에서 갈리고,
그 값은 보고서에 그대로 실린다. 근거 없이 정하지 말고 Wazuh 문서의 레벨
정의를 인용해 `alert_adapter.py` 주석에 남긴다.

---

## 4. 설계 판단이 필요한 것 둘 — 혼자 정하지 않는다

### 3대 상관분석

한 실행 = 한 볼륨은 `ref` 유일성을 지키려는 **의도된 제약**이다. 깨면
06단계 검증이 흔들린다. K-001 Stage 8(POS→관리서버)처럼 두 호스트에
걸친 사실은 지금 사람이 보고서 3장을 합쳐야 한다.

선택지 셋으로 보인다 — (a) 사람이 합친다 (b) `ref`에 호스트 접두어
(스키마 동결을 건드림) (c) 07단계 위에 병합 리포터를 얹는다.
**어느 쪽인지는 발표에서 무엇을 보여줄 건지에 달렸다.**

### 스냅샷 A/B 베이스라인 비교

계획서의 오프라인 축이 "정상 A와 침해 후 B를 비교"인데 **코드에 개념
자체가 없다.** 키오스크처럼 도는 프로세스가 극히 제한적인 환경에서는
이것이 가장 강한 필터가 된다.

넣지 않은 이유는 `docs/limitations.md`의 "넣지 않은 것 — 화이트리스트
역탐지"에 있다. 필요한 것은 부정 매처 하나와 목록을 04단계에 넘길
통로이고, 둘 다 지금 없다. 범위가 커서 별도 트랙이다.

---

## 5. 분석가용 콘솔 (GUI) — 지금 개념 자체가 없다

레포 전체에 GUI·웹 UI 참조가 0건이다. `work-guide.md` 1.4 비목표에도 없으니
**빼기로 한 것이 아니라 아직 안 만든 것**이다. 덮는 범위는 케이스 생성 →
01~07 실행과 진행 상황 → 07 보고서 열람이다.

### 요구사항 넷 — 나머지는 화면 배치 문제다

`run_pipeline.sh` 를 그대로 버튼 하나로 만들면 이 넷이 깨진다.

- **`--volume` 을 안 주면 04단계가 후보를 보여 주고 멈춘다. 그것이 정상
  동작이다.** GUI 가 실패로 삼켜 재시도하거나 크기로 추측해 고르면 "아티팩트
  없음"이 아니라 **다른 볼륨의 결과가 조용히 나온다.** 볼륨 선택은 화면이
  받아야 하는 입력이지 숨길 단계가 아니다
- **05단계는 오래 걸리고 실패한다.** 요청-응답 하나로 감싸면 화면이 수십 분
  멈췄다가 아무것도 없이 끝난다. 단계별 진행과 중단 사유가 실시간으로 보여야 한다
- **실패를 삼키지 않는다.** `cases/<id>/errors.jsonl` 이 실패의 원본이고, 화면은
  그것을 감추는 자리가 아니라 **가장 잘 보이게 놓는 자리**다
- **04단계는 로컬의 60GB 이미지를 직접 읽는다.** 이미지를 업로드받는 서버
  모델은 이 파이프라인과 맞지 않는다. **분석가의 기계에서 도는 것**을 전제한다

### 정할 것 셋 — 기울어진 쪽과 이유

- **형태 → 로컬 웹**(FastAPI/Flask). 요구사항 넷 중 둘(진행 상황, 중단·재개)이
  서버가 상태를 들고 있을 때만 자연스럽고 스택이 파이썬 안에 남는다. 대가는
  둘이다 — **포트를 연다**(증거 도구라 `127.0.0.1` 바인딩을 못 박는다), 07 출력이
  `.md` 라 **Markdown 렌더 의존성이 하나 는다**. 데스크톱 앱은 창을 닫으면 실행이
  같이 죽고, 정적 뷰어는 애초에 실행을 못 한다
- **단계 호출 → subprocess.** "폴백을 만들지 않는다"와 "실패는 `errors.jsonl` 에
  남긴다"가 성립하는 이유는 단계가 독립 프로세스이기 때문이다. import 로 당기면
  그 규약을 GUI 안에서 다시 만들어야 하고 진실이 둘이 된다
- **위치 → `ui/` 또는 `app/` 최상위.** `src/` 안에 넣으면 "어디까지가 우리
  파이프라인 구현인가"의 경계가 흐려진다(`third_party/README.md` 와 같은 이유)

확정하면 고른 이유를 이 자리에 한 줄로 남긴다.

### 확인 방법

**같은 케이스를 GUI 로 한 번, `run_pipeline.sh` 로 한 번 돌려 `cases/<id>/`
산출물 일곱이 같은지 본다.** 다르면 인자를 흘렸거나 어딘가에 폴백을 만든 것이다.
`--volume` 을 안 준 케이스도 함께 본다 — 04단계의 "후보 보여 주고 멈춤"을 그대로
보여 주는지가 확인점이다.

**착수 조건은 없다.** 다만 **3번보다 먼저 집으면 알럿 입력 화면을 두 번 만들게
된다** — Wazuh 평탄화가 들어오면 입력이 하나 는다. 순서는 3번 뒤가 싸다.

## 6. evtx 파서 엔진 — 안 바꾸기로 했다 (2026-08-28)

**네이티브 엔진 교체는 후보에서 빠졌다.** `pyevtx-rs` 가 레코드의 원본 바이트
오프셋을 주지 않는데, `offset` 은 동결된 `parsed_record` 의 필수 필드이고
"기존 파서를 쓰지 않고 직접 구현하는 이유"로 스키마에 적혀 있다. 즉 이 교체는
발표에서 내세우는 논지를 정면으로 깬다.

**후보를 새로 찾을 때의 첫 관문도 이것이다** — 빠르기보다 먼저, 레코드마다
원본 바이트 오프셋을 주는가.

남은 선택지는 **A. 파이썬 딕셔너리 워커**(1.9배, 약 200줄 재구현)와 **C. 안
한다**(evtx 는 199 rec/s 로 남는다) 둘이고, 2026-08-28 시점에는 **C** 다.

측정치 전부 — 병목 분해, `pyevtx-rs` 정합성 대조, 끝단 이득이 2.75배인 이유,
두 패키지가 Windows 에서 공존 못 하는 것 — 는 `docs/limitations.md` 의
**"넣지 않은 것 — 네이티브 evtx 엔진"** 절에 있다. **다시 재지 않아도 된다.**

## 7. 02단계가 입력에 없는 경로를 지어낸다 — 가드가 없다

**증상** — 프롬프트가 *"입력에 명시된 것만 넣으십시오. 추측하지
마십시오"* 라고 적어 두었는데도 두 번 다 지어냈다
(`docs/limitations.md` 5장 ④).

```
K-001  entities.paths ← C:\Windows\System32          입력 서술에 없음
K-002  entities.paths ← C:\Program Files\BankingApp\  입력 서술에 없음
```

**왜 위험한가** — `entities.paths` 의 **첫 항목이 03단계의 `web_root`
변수**가 된다(`src/stage03_select/scope_resolver.py` 의 `ENTITY_VARIABLES`).
지어낸 경로가 선별 범위를 정하는 자리까지 간다. 그런데 06단계는
`findings` 의 `claims` 만 검증하므로 **이 층은 아무도 보지 않는다.**
잘못된 `web_root` 는 "증거 없음" 이 아니라 **엉뚱한 폴더의 결과**를 낸다.

**설계가 필요한 이유 — 단순 규칙은 정답 데이터도 기각한다.**
목업 `02_scenario.json`(사람이 손으로 쓴 것)의 `entities.paths` 가
`C:\inetpub\wwwroot` 인데, 입력 서술에는 그 경로가 없다. IIS 기본
웹루트라는 **도메인 추론**이고 분석가라면 같은 값을 적을 것이다.
즉 "입력 문자열에 없으면 환각" 으로 막으면 **쓸모 있는 추론까지 잃는다.**
가드의 어려움이 전부 여기 있다.

**선택지 셋으로 보인다.**

- (a) **02단계에서 거부한다** — 입력에 없는 경로를 내면 재시도. 도메인
  추론을 잃고, 웹셸 목업이 지금 값으로는 통과하지 못한다
- (b) **표시하고 03단계에서 가른다** — 어느 값이 입력에서 왔고 어느 것이
  추론인지 남긴 뒤, `web_root` 처럼 **범위를 정하는 자리**에만 입력 유래를
  요구한다. 스키마에 자리가 없다(동결 대상)
- (c) **`web_root` 를 첫 항목으로 보는 가정 자체를 없앤다** — 매핑이
  요구하는 변수를 02단계가 명시적으로 채우게 한다. 범위가 가장 크다

**확인할 자리는 이미 있다** — `tests/test_llm_live.py`. 실제 모델을 부르는
테스트라 "이 모델이 지어내는가" 를 재현할 수 있다. 다만 위 이유로 **지금은
단정하는 검사를 넣지 않았다** — 무엇이 환각인지 사람이 먼저 정해야 한다.

**고칠 자리** — 프롬프트는 `src/stage02_normalize/llm_client.py`, 검증은
`normalize.py`, 소비처는 `src/stage03_select/scope_resolver.py`.

---

## 8. 벤치마크 정답 데이터 — 팀원에게 넘기는 작업

### 왜 필요한가 — 지금 수치는 자기채점이다

하나뿐인 데이터셋(`C-001-webshell`)의 정답이 **스펙 문서 예시에서 역산한 것**
이라 `provenance` 가 `derived_from_spec` 이다. `evaluate.py` 도 결과 끝에
*"정답을 사람이 만들지 않은 케이스가 1건 있습니다"* 를 찍는다. **재현율 100% 는
지금 아무 뜻이 없다.** 이 절이 그것을 뜻 있게 만드는 작업이다.

### 넘기기 전에 막아 둔 것 둘 (2026-09-02 처리됨)

둘 다 팀원이 며칠 작업하고 **마지막에 튕기는** 자리였다.

- **자기채점 판정을 자유 문자열에서 뗐다.** `authored_by` 는 **누가**(이름 그대로),
  `provenance` 는 **어디서 왔나**(`human_analysis` / `derived_from_spec` /
  `derived_from_pipeline`)다. 채점에 쓸 수 있는 것은 앞의 하나뿐이고, **없으면
  사람이 만들지 않은 것으로 본다** — 틀리는 방향이 "자기채점을 발표에 쓴다"가
  되면 안 된다
- **`required_refs` 접두어 목록을 스키마에서 걷어냈다.** 여섯 종만 받아
  프리패치·Sysmon·Amcache 를 적을 자리가 없었다. 스키마는 **모양만** 보고 어휘는
  `evaluate.py` 가 `src/common/refs.py` 의 `ARTIFACT_PREFIX` 로 본다. 모르는
  접두어는 거부된다

### 절차는 노션 페이지에 있다

**팀원에게 그대로 주는 절차**(케이스 선정 → 외부 도구로 사람이 먼저 보기 →
`input.json`·`ground_truth.json` 작성 → 스키마 검사 → 채점 → 완료 기준)는
노션으로 옮겼다. 넘겨받는 사람이 이 파일 전체를 읽을 이유가 없다.

> 노션 링크: (여기에 붙인다)

우리가 알아야 할 것은 셋이다.

- **파이프라인 결과를 보고 정답을 쓰면 채점이 아니라 답안지 베끼기다.** 사람이
  외부 도구로 먼저 보는 순서가 이 작업의 전부다
- **레지스트리·프리패치는 외부 도구가 같은 식별자를 주지 않는다.** 그런
  아티팩트는 `required_refs` 를 **비우고** `required_artifacts` 로만 채점한다.
  비우는 것이 지어내는 것보다 낫다
- **수치가 나쁘게 나오는 것이 정상이다.** 처음부터 100% 면 역산을 의심한다

`evaluate.py` 는 정답 레코드마다 **파싱 → 전달 → 인용 → 검증통과** 넷을 따로
세므로 어디서 끊겼는지가 곧 고칠 곳이다 — 기법 미식별은 02 프롬프트/모델,
미선별은 `mappings/`, 미파싱은 파서나 `scope`, 미인용은 05 프롬프트나
`record_filter` 상한.

### 이 트랙의 하위 목록

`benchmark/README.md` 의 과제 1~10 이 그대로 뒷줄이다. 사람이 증거를 봐야 푸는
것(과제 1·2·5)이 위 절차이고, 나머지(과제 3·4·6~10)는 코드 작업이라 넘기지
않고 우리가 집는다.

## 9. 05단계 컨텍스트 — 증거의 얼마를 모델이 보나 (2026-09-03 착수)

**순서가 이 항목 안에 있다.** 셋을 이 차례로 한다. 바꾸면 손해가 나는데,
그 이유가 각 절에 적혀 있다.

| 순 | 무엇 | 상태 |
|---|---|---|
| 9-0 | 예산 공식을 실측에 맞춘다 | **끝났다** (2026-09-03) |
| 9-1 | 필드 블랙리스트 다이어트 | 다음 |
| 9-2 | Map-Reduce 분할 질의 + 기법–아티팩트 검사기 | 9-1 뒤 |
| 9-3 | 창 크기 최적화 | 9-2 뒤. **먼저 하면 안 된다** |

### 왜 이 순서인가 — 창을 먼저 좁히면 손해다

실측(`docs/limitations.md` "05단계는 창을 좁히지 못한다")이 두 가지를 말한다.

- **창을 좁히면 GPU 점유가 오른다** — 32,768 에서 61%, 8,192 에서 79%.
  다만 이 기계의 천장은 KV 캐시가 아니라 모델 가중치라 100% 는 창으로
  도달하지 못한다. 그리고 8,192 아래로는 벌이가 거의 없다(4,096 이 81%).
- **좁히면 레코드가 급감한다** — 32,768 에서 54건, 8,192 에서 아홉 건,
  4,096 에서 네 건. 4-3 이 "Tier 1 아티팩트당 4건으로 7단계 체인을
  재구성해야 한다"를 미해결로 둔 자리를 더 나쁘게 만든다.

즉 **좁은 창으로 한 번 묻는 것은 커버리지만 잃는다.** 좁은 창은 여러 번
묻는 구조(9-2)와 함께여야 이득이다. 다이어트(9-1)를 먼저 하는 것은 9-2 의
청크 하나에 들어가는 레코드 수를 늘리기 때문이다.

### 9-1. 필드 블랙리스트 다이어트

`Hashes`·`FileVersion`·`Company`·`ProcessGuid` 류를 프롬프트에서 뺀다.
실물(`K-LIVE-0902-wide`)에서 **24%** 줄었다 — 평균 1,057자 → 809자.
같은 창에서 전달 레코드가 54건 → 58건이 됐다. **끝났다** (2026-09-03).

**화이트리스트가 아니라 블랙리스트다.** 남길 것을 적으면 목록에 없는
아티팩트가 조용히 빈 껍데기가 된다. 지금 넣자는 제안에는 `$MFT` 의
`si_*`/`fn_*` 도 `$UsnJrnl` 의 `reason`·`name` 도 없었고, 그러면 그
아티팩트의 판단 근거가 통째로 사라진다. 새 파서가 붙을 때(add-parser)
목록을 안 고쳐도 안전한 쪽이 블랙리스트다.

**목록은 코드가 아니라 어휘에 둔다** — `mappings/_flags.yaml`. 선례가 둘
있다(`privileged_groups`, `prompt_keep_paths`). 규칙은
`docs/mapping-guide.md`.

**뺐다는 사실을 프롬프트가 말해야 한다.** 목록을 자를 때 이미 그렇게 한다
(`llm_client._trim_notice`). 말하지 않으면 모델이 "해시 정보가 없다"를
근거로 쓰고, 그것은 우리가 유발한 환각이다.

확인: 아티팩트별 절감량을 재서 `docs/artifact-notes.md` 에 남긴다. 안
남기면 나중에 "`Company` 를 지워서 놓친 건가"를 가릴 수 없다.

### 9-2. Map-Reduce 분할 질의

아티팩트(또는 토큰) 단위로 나눠 묻고, 각 질의에서 나온 단서를 모아 **한 번
더** 묻는다. 두 번째 질의가 없으면 아티팩트 하나 안에서만 판단하게 되고,
05단계의 값어치인 교차 아티팩트 문장이 원리적으로 안 나온다.

정해 둘 것 넷.

- **분기 임계값은 `allocation.prompt_token_budget()` 이다.** 상수로 따로
  적으면 창이나 예약이 바뀔 때 임계값만 옛날 값으로 남는다 (9-0 에서 이
  함수를 만들어 뒀다).
- **출력 예약은 질의마다 다르다.** 객관식 선별과 문장·타임라인 생성은
  크기가 몇 배 차이다 (`RESERVE_FINDINGS_TOKENS` 의 이름이 그 자리다).
- **청크 하나가 실패하면 조용히 넘어가지 않는다.** 남은 것만으로 완주하면
  그것이 폴백이다. `errors.jsonl` 에 남기고, 부분 결과로 갈 거면 그 사실이
  07 보고서까지 나가야 한다.
- **Reduce 입력도 예산 판정을 받는다.** 단서가 수십 건이면 그것만으로
  창을 넘는다. 넘을 때 무엇을 자르는지가 정해져 있어야 한다.

**검증 겨냥을 함께 옮긴다.** `claims` 를 파이썬이 원본에서 조립하면 06 의
`value_match` 는 항등식이 되어 통과율 100% 가 보장된다. 그 자리를
**기법–아티팩트 정합성** 검사기로 채운다 — 소견의 `technique` 과 `refs` 가
가리키는 아티팩트가 `mappings/windows/T*.yaml` 의 `artifacts:` 에서 만나지
않으면 기각한다. 근거와 변환(`src/common/refs.py` 의 `PREFIX_ARTIFACT`)이
이미 있어 검사기 하나면 된다. 환각률이 무엇을 재고 무엇을 안 재는지는
`docs/limitations.md` 의 유형 표에 있다.

### 9-3. 창 크기 최적화

9-2 가 들어온 뒤에 `stage05_interpret/llm_client.py` 의 `DEFAULT_NUM_CTX`
를 내린다. 지금은 02단계와 공유하던 상수에서 떼어만 놓았고 값은 32,768 이다.

**내리기 전에 재야 할 것**: 자·토큰 비가 케이스마다 다르다. 레코드가 많은
쪽에 맞춰 2.0 으로 잡아 뒀는데, 작은 프롬프트에서는 한글 시스템 프롬프트
비중이 커져 11% 모자랐다. 창에 여유가 있는 동안은 안 드러나지만 좁히고
나면 그 11% 가 곧 잘림이다 (`docs/limitations.md` "남은 것 — 자·토큰 비는
한 값으로 고정되지 않습니다").

### 확인 방법

```bash
# 예산과 실측이 같이 찍힌다. 자·토큰 비가 크게 벗어나면 경고한다
.venv/Scripts/python.exe -m src.stage05_interpret.interpret   --in cases/<케이스>/04_parsed --scenario cases/<케이스>/02_scenario.json   --selection cases/<케이스>/03_selection.json   --out cases/<케이스>/05_findings.json --llm ollama --model qwen2.5:7b
```

### 관련 문서

설계 제안의 원문과 그 검토는 `docs/proposals/stage05_adaptive_diet_architecture.md`.
실측과 어긋난 자리는 그 문서 안에 표시돼 있다.

## 참고 문서

| 주제 | 문서 |
|---|---|
| **지금 안 되는 것과 그 이유** | `docs/limitations.md` |
| 벤치마크 구조·지표·남은 과제 | `benchmark/README.md` |
| 새 파서 추가 절차 | `.claude/skills/add-parser/SKILL.md` |
| 새 시나리오 대응 절차 (관문 넷) | `.claude/skills/add-scenario/SKILL.md` |
| 매핑 작성 규칙, flags 어휘 | `docs/mapping-guide.md` |
| 온디스크 구조, 외부 도구 대조 기록 | `docs/artifact-notes.md` |
| 데이터 형식 | `schemas/*.json` + `schemas/README.md` |
| 설계 근거, 팀 분담, 비목표 | `work-guide.md` |
