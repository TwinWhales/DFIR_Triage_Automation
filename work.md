# work.md — 엔터프라이즈 DFIR 자동화 도약 로드맵 (Next Milestones)

단일 머신 기반의 01~07단계 파이프라인과 신호등 검증 체계(🟢Passed / 🟡Warning / 🔴Rejected)가 성공적으로 안착함에 따라,  
기존의 미시적 튜닝 작업을 마감하고 **실제 엔터프라이즈 침해사고 대응 및 자율 에이전트(Agentic AI) 체계로 도약하기 위한 3대 핵심 과제**에 집중합니다.

---

## 🎯 3대 핵심 과제 요약

| 순번 | 핵심 과제 | 주요 목표 | 기대 효과 |
|:---:|:---|:---|:---|
| **1** | **GUI 기능 보강 및 SIEM 알럿 연동 완성** | FastAPI 웹 대시보드 고도화 + Wazuh 알럿 자동 트리거 파이프라인 구축 | 관제 알럿 발생부터 시각화 리포트까지 원스톱 완전 자동화 (시연 킬러 콘텐츠) |
| **2** | **멀티 노드 상관분석 및 전체 공격 체인 복원** | 키오스크·POS기·관리서버 간 횡적 이동(Lateral Movement) 통합 추적 | 단일 단말 분석을 넘어 전사적 침해 캠페인(End-to-End) 스토리 복원 |
| **3** | **5단계 ➔ 2단계 자율 루프백 로직 구현** | LLM의 추가 조사 요구(`investigation_requests`)에 따른 2차 심층 재수집 루프 | 일방통행 파이프라인을 탈피한 포렌식 에이전틱 AI(Agentic ReAct Loop) 확립 |

---

## 1. GUI 기능 보강 및 SIEM(Wazuh) 알럿 연동 완성 (담당: 규섭님 / 검증: 우리 트랙)

단순 CLI 터미널 환경을 넘어, 보안 관제 센터(SOC) 및 분석관이 즉시 위협을 인지할 수 있는 웹 기반 대시보드를 완성합니다.  
본 영역의 **구체적인 구현 및 인프라 연동은 규섭님 트랙**에서 전담하며, **우리는 파이프라인 인터페이스 지원 및 GUI ➔ CLI 결과 동등성 검증(확인)**을 담당합니다.

### 📋 구체적 구현 요구사항 및 데이터 연동 규격 (규섭님 담당)
1. **Tailscale 기반 KAPE 원격 증거 수집 (`evidence/`)**:
   - 엔드포인트(키오스크, POS기, 관리서버)에 이상 징후 발생 시 Tailscale VPN 망을 통해 KAPE를 원격 트리거하여 타겟 아티팩트를 자동 수집.
   - 수집된 결과물은 파이프라인이 즉시 분석할 수 있도록 `evidence/` 폴더 아래 노드별/타임스탬프 형식으로 입력:
     - 디렉터리 경로 예시: `evidence/<노드명>_<타임스탬프>/C/...` (예: `evidence/KIOSK_snapshotA_20260908T032657/C`)
     - 라이브 디스크 원시 접근(`\\.\C:`) 없이, KAPE 추출 폴더가 `evidence/`에 배치되는 즉시 파이프라인이 자동 감지하여 무중단 분석 착수.
2. **Wazuh Alerts 날짜별 수집 및 적재 파이프라인 (`alerts/`)**:
   - Wazuh Active-Response 또는 Webhook을 통해 실시간 수신되는 침해 의심 알럿 JSON을 `alerts/` 폴더에 **날짜별**로 자동 분류 및 적재:
     - 디렉터리 경로 규격: `alerts/YYYY-MM-DD/<alert_id>.json` (또는 `alerts/YYYY-MM-DD/alerts.json`)
   - 날짜별 알럿 적재 즉시 `tools/make_case.py --alert alerts/YYYY-MM-DD/<alert_file>`을 트리거하여 `01_input.json` 자동 생성 연계.
3. **FastAPI 기반 웹 UI 고도화 (`ui/`)**:
   - **사건 대시보드**: 케이스별 분석 진행 상태(01~07단계 실시간 진행률 바) 표시.
   - **신호등 인터랙티브 뷰어**: 🟢확정 사실, 🟡주의 요망(경고 사유 포함), ⚪미확인 영역 시각화.
   - **타임라인 시각화**: 프로세스 생성, 네트워크 통신, 파일 이벤트의 시간대별 직관적 그래픽 타임라인 제공.
   - **원클릭 증거 추적**: 소견 클릭 시 해당 원본 아티팩트 레코드(오프셋, Hex, JSON 필드) 즉시 팝업 조회.

### 🔍 우리의 역할 (검증 및 대조)
- **GUI vs CLI 결과 일치 검증**: FastAPI 웹 UI 화면이 표출하는 소견, 신호등 판정(🟢/🟡/🔴), 타임라인이 실제 CLI 파이프라인 산출물(`cases/<case_id>/07_report.md` 및 `06_verified.json`)과 100% 동일하게 일치하는지 대조 검증 수행.

---

## 2. 멀티 노드(키오스크, POS기, 관리서버) 상관분석 및 전체 공격 체인 복원

실제 침해사고는 단일 단말에서 끝나지 않고 내부망 전파로 이어집니다. 다중 엔드포인트의 증거를 종합 분석하여 공격자의 전체 침해 시나리오를 구성합니다.

### 주요 구현 내용
1. **다중 노드 케이스 구조화**:
   - 사건 단위를 다중 호스트(`node_kiosk`, `node_pos`, `node_server`)의 묶음 케이스로 수집 및 관리.
2. **노드 간 교차 지표(Cross-Node IOC) 상관분석 엔진**:
   - C2 접속 IP/포트, 도용된 관리자 계정명, 생성된 악성 스크립트 해시(MD5/SHA256)를 축으로 노드 간 행위를 연결.
   - 예: `키오스크의 외부 다운로드(13:37)` ➔ `키오스크-POS기 간 내부 포트 스캔 및 SMB 접속(13:45)` ➔ `관리서버에서의 악성 작업 등록(13:52)`.
3. **캠페인 레벨 통합 보고서 (Global Campaign Report)**:
   - 각 노드의 07단계 신호등 결과를 종합하여, 공격자의 **초기 침투(Initial Access) ➔ 횡적 이동(Lateral Movement) ➔ 목적 달성(Exfiltration/Impact)**으로 이어지는 전사적 공격 체인 다이어그램 자동 렌더링.

---

## 3. 5단계(해석) ➔ 2단계(정규화) 자율 루프백 로직 구현 (Agentic Loop)

일방통행(01 ➔ 07) 분석의 한계를 극복하고, LLM이 포렌식 분석관처럼 "의심스러운 단서를 발견하면 스스로 추가 증거를 재요청하는 피드백 루프"를 구현합니다.

### 주요 구현 내용
1. **05단계 추가 조사 요청 (`investigation_requests`) 인터페이스**:
   - 05단계 모델이 1차 해석 중 미진한 단서(예: 외부 C2 통신 IP 발견, 알 수 없는 프로세스 생성 등)를 발견하면 구조화된 추가 조사 요청 출력:
     ```json
     "investigation_requests": [
       {"type": "expand_time_range", "pivot_time": "2026-09-07T13:37:00Z", "window_hours": 2},
       {"type": "request_technique", "technique_id": "T1041", "rationale": "발견된 C2 IP로의 데이터 유출 여부 확인 필요"}
     ]
     ```
2. **피드백 오케스트레이션 (ReAct Feedback Loop)**:
   - 파이썬 오케스트레이터가 요청을 수신하여 02단계(시간 범위 확장, 기법 추가) ➔ 03단계(추가 아티팩트 선별) ➔ 04단계(2차 타겟 파싱)를 재트리거.
3. **가드레일 및 2차 종합**:
   - 무한 루프 방지를 위해 재질의 횟수를 **최대 1회**로 엄격히 제한.
   - 1차 증거 + 2차 추가 증거를 통합하여 최종 서사와 신호등 소견을 완결.

---

## 📌 팀 트랙 및 문서 관리 원칙

* **팀원 트랙 (규섭님)**:
  - GUI 웹 대시보드 고도화 (`ui/`)
  - Tailscale 기반 KAPE 원격 증거 수집 래퍼 (`evidence/` 저장)
  - Wazuh Alerts 수집 및 날짜별 적재 자동화 (`alerts/YYYY-MM-DD/`)
* **팀원 트랙 (세형님)**:
  - KIOSK 실측 오답 분석(미탐/오탐 원인 규명) 및 벤치마크 정답 데이터(Ground Truth) 작성 진행.
* **코어 엔진 트랙 (우리 트랙)**:
  - 2번 멀티 노드 상관분석 및 전사 캠페인 통합 보고서 엔진 개발
  - 3번 5단계 ➔ 2단계 자율 피드백 루프백 엔진 개발
  - GUI 화면과 CLI 산출물 간 결과 동등성 검증(대조)
* **해결된 이력 및 시스템 제약**:
  - 완료된 엔지니어링 기록은 [docs/limitations-log.md](docs/limitations-log.md) 참조.
  - 현재도 유효한 파서 및 시스템 층위의 제약은 [docs/limitations.md](docs/limitations.md) 참조.
