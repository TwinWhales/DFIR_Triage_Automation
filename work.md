# work.md — 다음에 할 일

**여기에는 "무엇을, 어디서, 어떻게 확인하며" 만 남긴다.** 설계 근거·측정치·
왜 그렇게 정했는지는 각 항목에 붙은 문서가 권위다. 여기 옮겨 적으면 진실이
둘이 되고, 갈라지는 순간 어느 쪽이 맞는지 알 수 없게 된다.

- **끝난 것은 지운다.** 그 사실은 `docs/limitations-log.md` 에 남는다.
- **고친 뒤에도 남은 제약**은 `docs/limitations.md` 다 — 가르는 기준은 날짜가
  아니라 **지금도 참인가**다.
- 실행 명령은 `CLAUDE.md`, 데이터 형식은 `schemas/`.

## 지금 순서 (2026-09-06)

**번호는 이름이고 순서가 아니다.** 다른 문서가 "`work.md` 0번"으로 가리키므로
번호를 다시 매기지 않는다. 집는 순서는 이렇다.

| 순 | 항목 | 착수 조건 |
|---|---|---|
| 1 | **8번 — 벤치마크 정답 데이터** (팀원에게 넘긴다) | 없다. 막아 둘 것 둘은 2026-09-02 에 닫혔다 |
| 2 | **12-2 — claims 가 문장을 다 덮는가** | 없다. 8번과 같은 트랙이다 |
| 3 | 0번 — 키오스크 축 다섯 채널 | Assigned Access 를 켠 스냅샷 |
| 4 | 1번 — 버전 축에 남은 다섯 줄 | 빌드가 다른 이미지 (제안서 1-2 의 우선순위) |
| 이후 | 10번 근거 모으기 · 3번 Wazuh · 5번 GUI 대조 · 2번 저널링 · 4번 설계 판단 | |

**12-1 이 닫혀서 순서가 바뀌었다** (2026-09-06). 문장이 증거 밖의 것을 말하는
것은 이제 06 이 잡는다. 남은 12-2 는 "claims 가 문장을 다 덮는가"이고, 그것을
곧바로 강등 사유로 쓰면 정상 문장이 대량으로 미검증이 된다는 것이 실측으로
드러났다 — 고칠 자리가 06 이 아니라 05 일 수 있어 8번과 나란히 본다.

**1번이 내려갔다** (2026-09-05). 새 이미지 없이 되는 자리를 **다 썼다** —
버전에 따라 움직이는 자리 목록(1-1)이 섰고, PECmd 교차 대조(1-4)에서
`(30, 212)` 가 192건 전건 일치했다. 남은 다섯 줄은 빌드가 다른 이미지가
있어야 재므로 0번과 같은 자리로 내려간다. **표 여덟 줄 중 실측이 셋이라는
사실은 그대로다** — 나머지 다섯은 "안 틀린 것"이 아니라 "확인 안 한 것"이다.

**7번이 큐에서 내려갔다** (2026-09-06). 7-1(입력에 없는 값을 지어낸다)은
2026-09-05 에 닫혔고, 남은 7-2(오배정)는 **02 에서 고치지 않기로 결론이 난
것**이라 할 일이 아니다 — 지금도 참인 한계이므로 `docs/limitations.md` 에만
둔다. 완화는 10번이 맡는다.

**8번·12번을 묶는 이유.** 둘 다 "수치가 무엇을 뜻하는가"다. 8번이 없으면
재현율이 자기채점이고, 12번이 없으면 환각률이 `--mode assemble` 에서
항등식이다. 발표에서 수치를 인용하기 전에 둘 다 필요하다.

---

## 0. 키오스크 축 — 다섯 채널이 어느 증거에도 없다

`AssignedAccess` 3종 · `DriverFrameworks` · `RDPConnection` 이 **손에 있는
어느 증거에도 없다.** KAPE 수집(`evidence/KAPE_Results/snapshot1/C`)도 같은
다섯이 비어 있는 것을 2026-09-04 에 `FILE_LAYOUT` 으로 대조해 확인했다.
그래서 아래 넷을 아직 못 쟀다.

- 그 다섯의 **파일 경로 문자열**이 맞는가 (`src/stage04_parse/evidence.py` 의
  `FILE_LAYOUT`)
- 그 채널들의 **event_id 추정값**이 맞는가 (`mappings/_flags.yaml`)
- `kiosk_restriction_event` 가 "로그가 작다"는 전제대로인가 — 수만 건이면
  05단계 쿼터를 혼자 태운다
- `unexpected_parent_process` 가 `explorer.exe` 를 이상으로 보는 가정 —
  **Home 에디션으로는 못 잰다.** Assigned Access 를 실제로 켠 상태여야 한다.
  침해할 필요는 없고 정상 상태로 충분하다

**수집본에서 안 보이는 것은 답이 아니다.** KAPE 는 자기 타깃 목록으로 거르므로
"그 버전에 없다 / KAPE 가 안 뽑았다 / 우리 경로가 틀렸다" 셋이 구분되지 않는다.
안 보이면 KAPE 타깃 목록을 함께 보거나 **그 VM 의 디스크를 직접 연다.**

**지금 잴 수 있는 것 둘.** `RDPSession` · `KernelPnP` 는 이 수집에 **있다.**
기법 매핑이 요청하기만 하면 실물로 잴 수 있다.

**같은 수집본으로 함께 닫히는 것 둘.**

- **섀도 카피** — 프리패치 적재 경로 변환이 섀도 참조를 그대로 두는지는 지금
  합성 테스트로만 확인돼 있다. 섀도 카피가 있는 스냅샷이면
  `tools/scan_prefetch.py` 대조 한 번으로 실물 확인이 된다.
- **keep 어휘** — `prompt_keep_paths` 는 정상 운영 이미지의 분포로만 골랐다
  (적중 9.81%). 악성 DLL 이 드롭 자리에 실제로 있는 이미지에서 다시 재야 한다.

덤으로 **Stage 0 베이스라인**(4번)의 시작이기도 하다.

## 1. 윈도우 버전 축 — 표 여덟 줄 중 실측은 셋이다

2026-09-01 에 키오스크 스냅샷의 프리패치 **192건 전량**이 `UnknownLayout`
이었다. 19045.6466 의 파일 정보 블록이 212바이트인데 표에 버전 30 의
216·220·224 만 있었다. **명세를 옮긴 줄이 실물에 반증됐다.**

**새 이미지 없이 되는 데까지 갔다 (2026-09-05).**

- **1-1 목록** — 버전에 따라 움직이는 자리 여덟이 표로 섰다. 그중 다섯 줄을
  코드와 대조해 표가 실제와 맞는 것을 확인했다(프리패치 `FILE_INFORMATION` ·
  `$UsnJrnl` 레코드 버전 · `osinfo` 의 `FAMILIES` 와 `AVAILABILITY` · 매핑의
  `event_ids` 에 버전 조건이 없다는 것). 나머지 셋은 이미지가 있어야 하거나
  (`FILE_LAYOUT` 다섯 채널은 0번) 애초에 미착수다(레지스트리 바이너리 블롭).
- **1-4 외부 도구 대조** — PECmd 를 돌려 `(30, 212)` 가 **192건 전건 일치**.
  이 레이아웃을 **우리 코드 밖에서** 확인한 첫 기록이다. 첫 판에 나온 불일치
  1건은 파서가 아니라 대조 도구였다. 기록은 `docs/artifact-notes.md` 의
  2026-09-05 절이고, 되돌려 확인한 표도 거기 있다.

**남은 것은 1-2 뿐이고 이미지가 있어야 한다.** 다섯 줄이 여전히 명세다 —
`(17,68)`·`(26,224)`·`(30,224)`·**`(30,216)`**·`(31,224)`. 그중 `(30,216)` 이
가장 의심스럽다. "1903 이상 = 216" 이라는 그 주장이 19045 에서 이미 한 번
반증됐기 때문이다.

무엇을 어느 순서로 모으는지, 수집 방법, 이 기계에 이미 있는 외부 도구,
확인 방법은 **`docs/proposals/windows-version-axis.md`** 에 있다.

## 2. 저널링 파서 — `$UsnJrnl` 보강 + `$LogFile` 신규

`$UsnJrnl` 은 v2 만 읽는다. v3/v4 는 미지원 레코드로 세어 매니페스트와
`errors.jsonl` 결산에 올린다(2026-09-01 처리). **읽게 만드는 것이 남았다.**

`$LogFile` 은 파서 자체가 없다. 트랜잭션 로그라 삭제·이름 변경의 **되돌림
기록**이 남아, `$MFT`·`$UsnJrnl` 이 놓친 자리를 메운다.

제약은 `docs/limitations.md` 의 "`$UsnJrnl` v3/v4 를 읽지 못한다" 절,
추가 절차는 `.claude/skills/add-parser/SKILL.md`.

## 3. Wazuh 알럿을 그대로는 못 받는다

`alert_adapter` 는 평탄한 EDR 알럿을 받는데 Wazuh 는 중첩 JSON 이다.
그대로 넣으면 필드를 못 찾는다.

배선 설계는 **`docs/proposals/wazuh-alert-intake.md`** 에 있다.

## 4. 설계 판단이 필요한 것 둘 — 혼자 정하지 않는다

### 3대 상관분석

`$MFT` · `$UsnJrnl` · evtx 를 시간축으로 엮어 "파일 생성 → 실행 → 지속성"
같은 사슬을 만드는 것. **지금은 05단계가 문장으로만 잇는다.** 결정론적으로
엮으면 06 이 검증할 수 있는 대상이 늘지만, 무엇을 사슬로 볼지가 곧 탐지
룰이라 혼자 정할 것이 아니다.

### 스냅샷 A/B 베이스라인 비교

정상 상태(A)와 침해 후(B)를 떠서 **차이만** 보는 것. 오탐이 크게 줄지만
"A 를 언제 뜨는가"가 운영 전제를 바꾼다. 0번의 수집본이 A 의 시작이다.

## 5. GUI — 만들었고, 스크립트와 같은 결과인지 아직 안 봤다

`ui/` (FastAPI)가 2026-09-04 에 이 기계에서 구동 확인됐다. 형태·호출 방식·
위치는 그때 정해졌고 코드가 그 기록이다.

**남은 것은 대조다.** 같은 케이스를 GUI 로 한 번, `run_pipeline.sh` 로 한 번
돌려 `cases/<id>/` 산출물 일곱이 같은지 본다. 다르면 인자를 흘렸거나 어딘가에
폴백을 만든 것이다. `--volume` 을 안 준 케이스도 함께 본다 — 04단계의
"후보 보여 주고 멈춤"을 그대로 보여 주는지가 확인점이다.

지금도 참인 제약(폴더 선택창, 테스트 부재, 진행 상태, `start.bat` 관문)은
`docs/limitations.md` 의 "2026-09-04 · 웹 UI 에서 지금도 참인 것".

## 8. 벤치마크 정답 데이터 — 팀원에게 넘기는 작업

**지금 수치는 자기채점이다.** 정답을 우리가 만들었으므로 재현율은 "우리가 볼
것이라고 적은 것을 우리가 봤다"는 뜻이다. `provenance` 가 그 사실을 산출물에
남기지만, 그것이 수치를 참으로 만들지는 않는다.

사람이 만든 정답 데이터가 있어야 재현율이 뜻을 갖는다. 절차는 노션 페이지에
있고, **넘기기 전에 막아 둘 것 둘은 2026-09-02 에 닫혔다** — 지금은 절차를
그대로 건네면 된다.

수치가 어디서 나오는지는 `benchmark/README.md`.

## 10. `corroborates` 를 채울 근거 모으기

기계는 2026-09-04 에 됐다 — `corroborates:` 키를 매핑에 둘 수 있고 06 만
읽는다. **아직 아무 매핑도 쓰지 않는다.** 넓힐 근거가 없기 때문이다.

**여기가 오배정(옛 7-2)의 완화 자리이기도 하다.** 02 가 절을 엉뚱한 기법에
붙이는 것은 고치지 않기로 했다(닫힌 판단, `docs/limitations.md`). 그때 실제로
아픈 것은 기법 이름이 아니라 `evtx:Security` 같은 아티팩트를 안 여는 것이고,
근거 축을 넓히면 그 통증이 줄어든다.

```bash
.venv/Scripts/python.exe benchmark/collect.py --rejections
```

기각 하나는 두 원인을 섞는다(모델이 틀렸나, 매핑이 좁나). 기계가 못 가르므로
사람이 가르고, 가른 결과가 `benchmark/rejections.yaml` 에 남는다.

원자료는 **06단계가 실행마다 덧붙인다**(`stage06_verify/runlog.py` →
`benchmark/results/rejections.jsonl`). 기각을 만드는 단계 자신이 쓰므로
`run_pipeline.sh`·웹 UI·`live_check` 어느 입구로 돌려도 남는다.

| `verdict` | 뜻 | 무엇을 하나 |
|---|---|---|
| `model_wrong` | 모델이 기법을 잘못 붙였다 | **매핑을 고치지 않는다.** 기각이 옳았다 |
| `mapping_narrow` | 증거는 맞는데 매핑이 좁다 | 그 기법의 `corroborates:` 에 넣는다 |

**병목은 케이스 수다.** 지금까지 나온 (기법, 인용 아티팩트) 쌍 여섯 중 기각은
하나이고 그마저 정탐이었다. 8번과 같은 자리에서 막혀 있다.

**두 번째 경로가 열려 있다.** `T1059` 가 맞게 나와도 그 매핑은
`evtx:Security` 를 **tier 2** 로 요청하고, Tier 2 루프백이 미구현이라
(`src/stage07_report/report.py`) 영원히 `deferred` 다.

**곁가지** — `T1091.yaml` 이 `evtx:Sysmon` 을 **수집 대상으로도** 안 갖는다.
USB 에서 실행된 파일은 Sysmon EID 1 의 `Image` 가 이동식 드라이브를 가리키므로
직접 증거에 가깝고, 그렇다면 `corroborates` 가 아니라 `artifacts` 다. 실물로
확인한 뒤의 일이다(0번).

설계와 실측은 `docs/limitations-log.md` 의 2026-09-04 절 둘.

## 12. 문장이 claims 를 뒷받침하는지 아무도 보지 않는다

`--mode assemble` 에서 06 이 대조하는 것은 `claims` 뿐이고 그 값은 파이썬이
원본에서 조립한다. `value_match` 는 항등식이다. **환각률 0% 를 품질로 인용하지
않는다.**

세 가지를 갈랐고 **첫째(12-1, 증거에 없는 것을 말하는가)는 2026-09-06 에
닫혔다** — `checkers/statement_grounded.py`. 전말은 `docs/limitations-log.md`
2026-09-06 절이다. 둘이 남았다.

### 12-2. claims 가 문장을 다 덮는가 — 남아 있다

12-1 이 보는 것은 "증거 어딘가에 있는가"다. **레코드에는 있는데 claims 에는
없는** 것을 말한 문장은 그대로 통과한다 — 실측 소견 "MMC.EXE 와
EXPLORER.EXE 가 KIOSK 사용자 폴더에서 실행되어 …"(claims 는
`loaded_file_count` 둘뿐)가 그것이다.

**곧바로 강등 사유로 쓰면 안 된다.** claims 만 대조 상대로 두고 돌리니 손으로
옳다고 판단해 둔 41건 중 8건이 강등됐고, 걸린 것이 전부 문장의 **주어**였다
(2026-09-06 실측). 고칠 자리는 06 이 아니라 05 일 수 있다 —
`assembly.claim_for` 가 문장이 기대는 필드를 claims 에 싣게 하는 쪽
(`evidence_fields`)이 먼저다.

### 12-3. 인용한 것이 정상 항목인가 — 남아 있다

알려진 정상 서비스·자동실행 목록(베이스라인)이 있어야 한다. 4번과 같은
문제이므로 그쪽과 묶는다.

**주의** — `--mode model`(모델이 문장을 직접 쓴다)로 한 번 더 돌려 나란히
놓는 것이 지금 할 수 있는 가장 싼 대조다.

**고칠 자리** — `src/stage06_verify/checkers/`, 지표는 `benchmark/collect.py`
와 `tools/live_check.py` 의 측정 절.

증상과 지금도 참인 것은 `docs/limitations.md` 의 "`--mode assemble` 에서
환각률 0% 는 품질을 뜻하지 않습니다".

---

## 큐에서 내린 것 — 어디로 갔나

| 항목 | 왜 | 어디에 |
|---|---|---|
| 6번 evtx 파서 엔진 | **닫힌 결정** (2026-08-28). 교체 안 한다 | `docs/limitations.md` "넣지 않은 것 — 네이티브 evtx 엔진" |
| 9번 05단계 컨텍스트 | 끝났다 (2026-09-03) | `docs/limitations.md` "05단계 레코드 배분 (**구현됨**)" |
| SQLite 파서 (2-3) | 끝났다 (2026-09-02) | `docs/limitations.md` 의 SQLite 절 둘 |
| 11번 02 기법 누락 | 1차 처리됨 (2026-09-04). 남은 절은 7번과 한 자리였다 | `docs/limitations-log.md` 2026-09-04 |
| **7번 02 출력 검증** | 7-1 은 끝났다 (2026-09-05). 7-2(오배정)는 **닫힌 결정** — 02 에 가드를 두지 않는다 | 끝난 것은 `docs/limitations-log.md`, 지금도 참인 것은 `docs/limitations.md` "02단계가 축을 놓치면". 완화는 **10번** |
| **12-1 증거 밖 표현** | 끝났다 (2026-09-06) | `docs/limitations-log.md` 2026-09-06 |
| GUI 형태·호출·위치 결정 | 정했고 만들었다 | `ui/` 코드와 `docs/limitations.md` UI 절 |
| 1번·3번의 상세 | 계획 자료라 분량이 컸다 | `docs/proposals/` 의 두 문서 |
