jsonschema>=4.21
pyyaml>=6.0
jinja2>=3.1
python-evtx>=0.7.4
python-registry>=1.3
construct>=2.10
requests>=2.31
pytest>=8.0

# 트랙 A(VolumeSource) — raw dd/E01 이미지를 추출 없이 직접 읽는다.
# src/stage04_parse/evidence.py 의 VolumeSource 참조.
dissect.target>=3.25
